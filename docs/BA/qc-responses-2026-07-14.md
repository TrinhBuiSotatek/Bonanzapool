---
type: qc-response
module: exbot
status: draft
created: 2026-07-05
updated: 2026-07-05
owner: "@hienduong"
---

# QC Responses — ExBot Module (2026-07-05)

> File này chỉ lưu ở local — KHÔNG commit, KHÔNG push.

---

## UC-EXBOT-bot-safe-close — Open Questions (v2, 2026-07-06)

| ID | Ưu tiên | Ref | Question | Evidence in Docs | Why It Matters | Owner | Status | Ans |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Q4 | Medium | UC §1, SRS flows.md F-05, FR-EXBOT-073 | UC §1 liệt kê "ExBot System Operator (Close Worker)" là Primary Actor. Tuy nhiên, từ UC §3 và FR-EXBOT-073, Close Worker không tự quyết định trigger — nó chỉ thực thi flow khi được gọi. 5 trigger conditions thực tế đến từ: (1) deep-audit worker, (2) hedge-sync worker, (3) partial_repair worker, (4) light-check/hedge-stopped, HOẶC (5) admin qua `POST /api/exbot/close`. Không có dedicated `bot_safe_close` queue trong 11 queues (FR-EXBOT-010). Vậy "Close Worker" trong UC §1 là **architecture component (ExBot Lambda)** hay **actor**? BA xác nhận lại role của Close Worker trong UC. | UC §1: "Primary: ExBot System Operator (Close Worker)" UC §3 Step 1: "Close Worker: acquire User Lock..." FR-EXBOT-073: 5 trigger conditions FR-EXBOT-010: 11 queues — không có bot_safe_close queue | Nếu Close Worker là architecture chứ không phải actor, thì UC §1 Actor list cần được cập nhật để phản ánh đúng. Trigger mechanism cần được ghi rõ trong UC. | BA | ⏳ Pending | **Confirmed.** "Close Worker" trong UC §1 là label cho ExBot Lambda — component thực thi flow, không phải independent actor. Trigger mechanism đã được cập nhật: Trigger section nay liệt kê đủ 5 điều kiện từ FR-EXBOT-072 (circuit breaker exhausted / margin critical / 3 stops in 7 days / partial repair exhausted / admin force-close). Step 2 đổi "Close Worker" → "ExBot Lambda" cho nhất quán với AWS architecture. Actor list `ExBot System Operator (Close Worker)` giữ nguyên — đúng convention các UC khác trong module. |

---

## UC-EXBOT-bot-start — Open Questions

| ID | Priority | Ref | Question | Why It Matters | Status | Ans |
| --- | --- | --- | --- | --- | --- | --- |
| I-02 | High | UC §4 A11 vs FR-EXBOT-025, FR-EXBOT-036, OQ-EXBOT-11 | UC mô tả A11 (reconcile mismatch) là "actual size deviates > threshold" nhưng không định nghĩa threshold cụ thể là bao nhiêu. FR-EXBOT-025 cũng không nêu threshold. FR-EXBOT-036 có nhắc `drift_threshold` và OQ-EXBOT-11 đề cập `lpValueUsd × 3%` nhưng đây là ứng viên chưa được zen xác nhận (OQ-EXBOT-11 vẫn Open). Tester cần biết ngưỡng deviation cụ thể để thiết kế test case kiểm tra boundary. | Blocker cho test reconcile mismatch. (Ghi chú 2026-07-02: BA xác nhận formula `drift_threshold = lpValueUsd × 3%` và behavior logic của A11 đã confirmed; tuy nhiên zen chưa confirm OQ-EXBOT-011 — boundary test vẫn bị block cho đến khi OQ-EXBOT-011 Closed.) | Open | CÂU này cần cf với khách |
| I-11 | High | UC §4 A7, UC §4 A10 vs `srs/states.md` (dòng `hedge_pre_open → safe_mode`) | UC §4 A7 mô tả "HL unreachable tại bước hedge → enter `error` state". UC §4 A10 mô tả "HL reject IOC → enter `error` state". Nhưng `srs/states.md` chỉ định nghĩa transition hợp lệ từ `hedge_pre_open` là `hedge_pre_open → safe_mode` khi "HL order fails" — không có transition `hedge_pre_open → error`. UC đã được cập nhật 2026-07-04 (arc-migration) nhưng conflict vẫn còn. BA vui lòng xác nhận: khi HL unreachable hoặc IOC bị reject tại bước hedge open, trạng thái đích chính xác là `error` hay `safe_mode`? Nếu là `safe_mode`, cần cập nhật UC §4 A7/A10 cho nhất quán với state machine. | Blocker cho AC-07 và AC-09. Tester không thể viết expected result cho 2 exception path quan trọng nhất của hedge open. | Open | **I-11 Resolved.** Confirmed: khi HL unreachable (A7) hoặc HL reject IOC (A10) tại bước hedge open, trạng thái đích là `safe_mode` — nhất quán với `states.md` (`hedge_pre_open → safe_mode: HL order fails`) và FR-EXBOT-050 (HL API unreachable → SAFE_MODE entry condition). UC A7 và A10 đã được cập nhật từ `enter error state` → `enter safe_mode` với auto-recovery path per FR-EXBOT-050. |
| I-12 | Medium | UC §4 A10; `02_backbone/message-list.md` | Sau khi I-11 được giải quyết, nếu destination state khi IOC bị HL reject là `safe_mode`: E-EXBOT-008 ("Hyperliquid API is currently unreachable. Bot entered Safe Mode.") có đúng không? Hay cần error code riêng cho "HL reject IOC order"? Hiện tại `message-list.md` có E-EXBOT-007 là "Hedge adjustment rejected: insufficient margin" nhưng không có code riêng cho IOC reject tại bot-start. | Tester không thể verify đúng message code cho A10 cho đến khi I-11 và I-12 đều được giải quyết. | Open | **I-12 Resolved.** E-EXBOT-008 không phù hợp cho A10 vì HL vẫn reachable, chỉ reject lệnh. Đã đăng ký E-EXBOT-026 riêng cho "HL rejects IOC order at bot-start": `"Hedge order rejected by Hyperliquid. Bot entered Safe Mode."` — internal alert, nhất quán với pattern E-EXBOT-011/018. UC A10 đã update để reference E-EXBOT-026. |
| I-13 | Low | UC §7 FR Trace; `srs/spec.md` | UC §7 FR Trace chỉ liệt kê FR-EXBOT-001, 002, 004, 020, 030, 031. Các FR sau không được trace mặc dù trực tiếp liên quan đến bot-start: (1) FR-EXBOT-003 — state tracking; (2) FR-EXBOT-011 — queue consumer idempotency; (3) FR-EXBOT-081 — KMS key provisioning; (4) FR-EXBOT-091 — HL rate limiter. BA có thể bổ sung FR trace vào UC §7 không? | Không block test design nhưng làm tăng rủi ro tester bỏ sót coverage cho idempotency, KMS error path, và rate limit. | Open | **I-13 Resolved.** Đã bổ sung FR-EXBOT-003 (state tracking), FR-EXBOT-011 (queue consumer idempotency), FR-EXBOT-081 (KMS key provisioning), FR-EXBOT-091 (HL rate limiter) vào UC §7 FR Trace. |
| I-14 | Low | `srs/erd.md` `hl_agent_keys` table | ERD `hl_agent_keys` sau arc-migration annotate 9 trường là RETIRED nhưng `key_status` — trường quan trọng nhất (3-state: `active`/`superseded`/`revoked`) — không được liệt kê rõ với type/constraint. Note cuối ERD chỉ nói "Table holds per-user key metadata and `key_status` only" mà không khai báo type/constraint. | Thiếu khai báo type/constraint cho `key_status` làm tester không thể verify schema constraint trong test case. | Open | **I-14 Resolved.** Confirmed: `key_status` là `TEXT NOT NULL`, 3 values hợp lệ: `active / superseded / revoked`, constraint UNIQUE active per user enforce ở DB level per BR-EXBOT-012. Đã bổ sung khai báo đầy đủ vào ERD `hl_agent_keys` table definition. |
| I-15 | Low | `srs/spec.md §FR-EXBOT-081` | FR-EXBOT-081 mô tả "KMS failure → abort, retry max 3, admin alert" nhưng không định nghĩa: (1) Nội dung message thông báo admin? (2) Alert kênh nào? (3) Sau max retry, bot-start tự retry khi KMS recover hay user phải nạp tiền lại? | Ảnh hưởng test case cho negative path KMS failure. | Open | I-15 Resolved. Message: E-EXBOT-027 đã đăng ký: "Key-provision failed for user {wallet_address} after 3 retries. Manual re-trigger required via admin panel." Kênh: notification queue — nhất quán với các internal alert khác. Recovery: User KHÔNG cần deposit lại — tiền vẫn còn trong BnzaExVault. Admin re-trigger key-provision job thủ công qua admin panel sau khi KMS/HL recover. |
| I-16 | Low | `userstories/us-001.md` Notes vs `srs/states.md` | US-EXBOT-001 Notes ghi: "`cooldown` and `parked` lifecycle states map to `status='active'`". Nhưng `srs/states.md` (updated 2026-06-18) đã remove hoàn toàn `cooldown` và `parked` states. US-001 Notes chưa được cập nhật. | Có thể gây hiểu nhầm cho tester mới về scope of one-bot policy. | Open | **I-16 Resolved.** `cooldown` và `parked` states đã bị remove từ 2026-06-18. Notes section của US-001 đã được cleanup — bỏ dòng tham chiếu 2 states này. One-bot policy vẫn đúng: `status IN ('active','paused','closing','safe_mode','error')`. |
| I-17 | Minor | `spec.md` FR-EXBOT-080 KMS failure path vs `states.md` agent key states | `spec.md` FR-EXBOT-080 đề cập `key_status='provisioning'` trong KMS failure path. Nhưng `states.md` chỉ định nghĩa 3 states chính thức: `active / superseded / revoked` — không có `provisioning`. Không rõ `provisioning` là transient in-progress description hay DB state chính thức. | Tester không biết có cần test case cho `key_status='provisioning'` không. | Open | **I-17 Resolved.** Confirmed: `provisioning` là DB state chính thức. Row được tạo với `key_status='provisioning'` sau khi KMS generate keys thành công (trước khi gọi HL approveAgent). Đã update: `states.md` thêm `provisioning` vào state machine (4 states); `erd.md` update `key_status` constraint; `spec.md` FR-EXBOT-080 clarify flow; `spec.md` BR-EXBOT-012 confirm `provisioning` không vi phạm UNIQUE active constraint. |
| I-18 | Major | `frd.md` FR-EXBOT-001 (5 preflight checks) vs `spec.md` FR-EXBOT-002 (6 preflight checks) | `frd.md` FR-EXBOT-001 liệt kê 5 preflight checks — không có vault balance check. `spec.md` FR-EXBOT-002 (canonical) liệt kê 6 checks, vault balance là step 2 (E-EXBOT-025). FRD chưa được update sau khi UC được update ngày 2026-07-07. | Major: Tester thiết kế test dựa vào FRD sẽ bỏ sót hoàn toàn vault balance check và E-EXBOT-025 test path. | Open | **I-18 Resolved.** FRD FR-EXBOT-001 đã được sync với spec.md FR-EXBOT-002 — thêm vault balance check (E-EXBOT-025) là check #2, renumber các checks tiếp theo từ #3-6. Thứ tự đúng: (1) one-bot → (2) vault balance → (3) HL margin → (4) key_status → (5) builder fee → (6) LP simulation. Nhất quán với F-03b flows.md. |

---

## UC-EXBOT-deep-audit — Open Questions

| ID | Priority | Ref | Question | Why It Matters | Status | Ans |
| --- | --- | --- | --- | --- | --- | --- |
| Q-DA-03 | Medium | UC Step 1; NFR-EXBOT-012 | **Shard iteration strategy cho AWS Lambda deep-audit worker:** UC Step 1 nói Lambda đọc bots từ Aurora PostgreSQL nhưng không specify Lambda đọc tất cả shards một lần rồi xử lý tuần tự, hay có một EventBridge Scheduler trigger per shard. | Multi-shard test design cho Phase B (4 shards) phụ thuộc vào Lambda worker shard iteration strategy. Phase A test không bị ảnh hưởng. | Open | Deep-audit Lambda không tự iterate shards. Kiến trúc v1: EventBridge trigger bot-scan (`rate: 1 minute`) → bot-scan query `bots WHERE status='active'` → fan-out per-bot SQS message → deep-audit Lambda xử lý 1 `botId` duy nhất per invocation. Shard iteration là concern của bot-scan layer, không phải deep-audit. Lưu ý: trong v1 hiện tại, deep-audit queue đã deploy nhưng không nhận message tự động — `safe_mode_tier` column chưa có trong schema. Periodic 6h cadence (`next_deep_audit_at`) đã có trong DB schema nhưng chưa được wired. Cả 2 path này sẽ active ở v1.1. **Phase A test cho deep-audit defer sang v1.1.** |
| Q-DA-04 | Low | UC A1; erd.md | **Cadence switching mechanism cho AWS Lambda deep-audit worker:** UC A1 nói "update cadence to high-risk interval (1 hour)" khi HL unreachable nhưng không rõ Lambda worker có update `bots.next_deep_audit_at` field không, hay EventBridge Scheduler tự switch schedule dựa trên state query. | Tester cần verify cadence switching mechanism để design test đúng behavior. | Open | Cadence switching dùng **Cách A — Lambda ghi `bots.next_deep_audit_at`** (per-bot): Normal completion → `now + 6h`; HL unreachable (A1) → `now + 1h`; High-risk condition (`circuit_breakers.state != 'closed'` hoặc `margin_status IN ('warning','critical')`) → `now + 1h`. Bot-scan query `WHERE status='active' AND next_deep_audit_at <= now` mỗi phút (via `idx_bots_due_audit`) → fan-out per-bot SQS. EventBridge **không** tự switch schedule. Field `next_deep_audit_at` và index `idx_bots_due_audit` đã có sẵn trong v1 schema. Mechanism này là v1.1 implementation, không cần thêm infrastructure mới. |
| Q-DA-06 | Medium | UC Step 2; FR-EXBOT-091; OQ-EXBOT-15 | **MỚI (2026-07-06 Re-audit):** Deep-audit Step 2 gọi HL qua HL Rate Limiter (ElastiCache Redis) với weight=2. OQ-EXBOT-15 (vẫn Open trong spec.md §9) xác định Tech Lead cần xác định: HL Rate Limiter weight consumption nên được thực hiện **trước** hay **sau** khi acquire Redis Redlock? Deep-audit không sử dụng Redis Redlock (không có hedge mutation). | Tech Lead decision về rate limiter ordering ảnh hưởng đến system design tổng thể. Phase A deep-audit test không bị ảnh hưởng trực tiếp. | Open | Q-DA-06 không applicable cho v1. Deep-audit v1 không gọi HL thật — `observeDeepAudit()` là stub, không có rate limiter hay Redlock implementation. Với v1.1: deep-audit không dùng Redlock (không có hedge mutation) nên không có ordering question — chỉ cần consume weight trước khi gọi HL. OQ-EXBOT-15 chỉ ảnh hưởng hedge-sync, không ảnh hưởng deep-audit. |
| Q-DA-07 | Low | UC Step 3; spec.md FR-EXBOT-016 | **MỚI (2026-07-06 Re-audit):** UC Step 3 verify actual short size từ HL matches `last_known_hl_short_size`. Khi mismatch được phát hiện (→ SAFE_MODE), UC không mô tả rõ liệu `last_known_hl_short_size` có được update thành actual size từ HL không, hay để nguyên giá trị mismatch. Nếu không update, subsequent deep-audit sẽ tiếp tục phát hiện mismatch → potential loop. | Tester cần biết state của `last_known_hl_short_size` sau mismatch detection để design recovery test đúng. | Open | Có. Worker update `bot_runtime_state.last_known_hl_short_size = actual HL size` và `last_hl_reconcile_at = now` **bất kể match hay mismatch** — trước khi trigger SAFE_MODE. UC đã được update (Step 3 + A3). Behavior này là v1.1 planned, consistent với pattern `§13.3`. **Recovery test:** Verify `bot_runtime_state.last_known_hl_short_size = actual HL size` trong DB ngay sau mismatch detection, trước khi test recovery path. |

---

## UC-EXBOT-hedge-sync — Open Questions

| ID | Priority | Ref | Question | Why It Matters | Status | Ans |
| --- | --- | --- | --- | --- | --- | --- |
| Q1 | High | UC §3 step 5 note; OQ-EXBOT-013 (SRS §9) | Khi delta tính bằng 0, Worker có nên bỏ qua lệnh HL và đi thẳng vào INV-STOP stop replacement hay nên abort toàn bộ hedge-sync? UC step 5 note ghi "Behavior pending OQ-EXBOT-013". Nếu skip stop replacement thì stop có thể bị stale khi giá ETH đóng hoặc mở/đóng range. | delta=0 có thể xảy ra khi target và actual khớp nhưng stop cần cập nhật. Việc bỏ qua stop replacement trong luồng A6 sẽ là bug. Tester chưa thể viết expected result cho AC-11 một cách hoàn chỉnh. | Open — ⏳ Pending zen — OQ-EXBOT-013 | Pending bởi khách |
| Q2 | High | UC §3 step 8-9; FR-EXBOT-035; SPEC §19.5 | INV-STOP protocol (§19.5) được mô tả là "protected cancel→place", nhưng chi tiết cụ thể là: (a) place trước rồi cancel sau, hay (b) cancel trước rồi place sau? OQ-EXBOT-002 trong SRS note "Does HL support place-before-cancel stop replacement?". | Không biết path (a) hay (b) → không thể viết test case cho cancel/place sequence. | Open — ⏳ Pending zen — OQ-EXBOT-002 | Pending bởi khách |
| Q3 | High | UC §3 step 4-6; FR-EXBOT-060; OQ-EXBOT-014 | Worker có phải fetch HL marginSummary TRƯỚC hay SAU khi giành Redlock không? OQ-EXBOT-014 trong SRS: "marginSummary fetch ordering in hedge-sync preflight: does the Worker fetch marginSummary before or after acquiring the lock?" | Nếu fetch sau lock mà mất nhiều thời gian, heartbeat có thể bị bỏ lỡ → lock tự giải phóng → race condition. | Open — ⏳ Pending Tech Lead — OQ-EXBOT-014 |  |
| Q6 | Medium | FR-EXBOT-036; OQ-EXBOT-011 | `partial_repair` dùng drift_threshold = `deltaErrorUsd > max($25, lpValueUsd × 3%)`. `lpValueUsd` trong công thức được tính như thế nào? OQ-EXBOT-011 (SRS §9) chưa xác nhận formula. | Nếu `lpValueUsd` chưa có formula xác nhận, expected result của test reconcile partial fill không thể tính ra con số cụ thể. | Open — ⏳ Pending zen — OQ-EXBOT-011 | Pending bởi khách |
| Q7 | Medium | UC §3 step 12-13; FR-EXBOT-030 | stop_trigger_px được tính lại sau reconcile, nhưng nếu delta=0 thì `entry_price` và `liq_price` có được update hay vẫn giữ giá trị cũ? | Nếu `entry_price` không đổi, việc tính lại `stop_trigger_px` cũng cho kết quả cũ → stop replacement trong luồng A6 có thể là no-op. | Open — ⏳ Blocked bởi Q1 / OQ-EXBOT-013 | Phụ thuộc vào câu Q1. Pending bởi khách |
| Q-N1 | Low | UC §7 FR Trace; spec.md FR-EXBOT-092 | UC §7 FR Trace liệt kê `FR-EXBOT-026` (User Lock conceptual) nhưng chưa có `FR-EXBOT-092` (Redlock interface chi tiết: acquire/extend/release, TTL=90s, idempotencyKey pattern, Lua script ownership check). FR-092 được thêm vào SRS trong arc-migration pass 2026-07-03. | Không block test design — FR-092 đã được cite trong audit report §F.1 F1-03. Chỉ là gap về traceability. | Open — Low priority | FR-EXBOT-092 (Redlock interface: acquire/extend/release, TTL=90s, idempotencyKey pattern) đã được bổ sung vào UC §7 FR Trace. Gap do FR-092 được thêm vào SRS trong arc-migration pass 2026-07-03 nhưng UC chưa được cập nhật theo. |
| Q11 | Low | UC §3 step 1-3; FR-EXBOT-011/026/027 | UC mô tả rất chi tiết thứ tự: (1) insert idempotency → (2) check stateVersion → (3) acquire lock. Nhưng UC không giải thích lý do kinh doanh tại sao phải theo đúng thứ tự này. | Nếu thứ tự thay đổi trong implementation, UC sẽ "sai" dù logic nghiệp vụ không thay đổi. | Open — Không block test design | **Q11 — Acknowledged, no change needed.** Thứ tự (1) idempotency → (2) stateVersion → (3) lock có business rationale rõ ràng: Step 1 trước — FR-EXBOT-011: chặn duplicate delivery ngay từ đầu. Step 2 trước lock — FR-EXBOT-027: check stateVersion trước lock để discard message stale mà không tốn TTL của Redlock. Step 3 sau cùng — FR-EXBOT-092: acquire lock muộn nhất có thể để minimize thời gian giữ lock → giảm rủi ro timeout và contention. |
| Q12 | Low | flows.md F-02; UC diagram | UC có Mermaid placeholder generic chưa được điền. Diagram module-level có trong flows.md F-02 đủ để tester suy ra luồng. | Không block test design — flows.md F-02 đủ. | Open — Không block | Mermaid placeholder generic trong UC đã được thay bằng reference rõ ràng đến **flows.md F-02: Hedge-Sync Execution (Delta-Only)**. F-02 cover luồng chính: hedge-sync queue → Hedge-Sync Worker → Redis Redlock acquire/release → Hyperliquid clearinghouseState + adjustShortDelta → reconcile queue → Aurora PostgreSQL update hedge_legs. |

---

## UC-EXBOT-light-check — Open Questions

| ID | Priority | Ref | Question | Why It Matters | Status | Ans |
| --- | --- | --- | --- | --- | --- | --- |
| I-10 | Low | UC §3 step 5; SRS ERD | UC mô tả idempotency insert nhưng không đề cập `expires_at` field trong `queue_idempotency`. SRS ERD có `expires_at` — Light-Check Worker có set `expires_at` khi insert hay để NULL? Nếu để NULL, table sẽ tích lũy vô hạn theo thời gian. | Ảnh hưởng đến long-running test scenarios và môi trường test — table bloat có thể gây slow query trong CI. | Open | `expires_at` luôn được set (TTL = 1 phút) khi insert vào `queue_idempotency`. Gap cleanup đã được xử lý: một EventBridge cron job (mỗi 1 giờ) sẽ thực thi `DELETE FROM queue_idempotency WHERE expires_at < now`. Decision này đã được cập nhật vào FR-EXBOT-011 AC trong spec.md và UC step 5. |
| v3-I-01 | Low | UC §7 FR Trace; SRS FR-EXBOT-012; FR-EXBOT-040 | UC §7 FR Trace liệt kê FR-EXBOT-013 → 033 nhưng thiếu FR-EXBOT-012 (FR chính của light-check) và FR-EXBOT-040 (Circuit Breaker State Machine). | Tester thiếu traceability từ test cases đến FR-012 và FR-040 trong test report. | Open | v3-I-01 Resolved. Đã bổ sung FR-EXBOT-012 (light-check worker logic chính: zero HL calls invariant, circuit breaker suppression, lp_rebalancing skip) và FR-EXBOT-040 (Circuit Breaker State Machine) vào UC §7 FR Trace. |
| v3-I-02 | High | US-EXBOT-007 AC-007-1; UC step 10; SRS FR-EXBOT-012 | US-EXBOT-007 AC-007-1 viết: "the system sets lifecycle_state='lp_rebalancing'" — ngụ ý light-check set field này. Nhưng UC step 10 và SRS FR-EXBOT-012 nói rõ: light-check KHÔNG set `lifecycle_state='lp_rebalancing'`, đó là trách nhiệm của hedge-sync handler downstream. | Tester dùng US-007 AC-1 sẽ viết expected result sai cho lp_rebalancing test cases, dẫn đến false-fail. | Open | **v3-I-02 Resolved.** AC-007-1 `When` clause đã được sửa. `lifecycle_state='lp_rebalancing'` không phải do light-check set — light-check chỉ detect `range_out` và enqueue message vào hedge-sync queue. Hedge-sync worker là người set `lifecycle_state='lp_rebalancing'` khi bắt đầu xử lý LP rebalance. |
| v3-I-03 | High | UC step 9; SRS OQ-EXBOT-12; SRS erd.md | UC step 9 tham chiếu `fundingApr7dPct` từ bảng `funding_rolling_metrics`, nhưng bảng này không có trong ERD (`srs/erd.md`). ERD chỉ có `funding_daily_metrics`. (1) `funding_rolling_metrics` là bảng, view, hay materialized view? (2) Khi nào được cập nhật? (3) Schema gồm những field gì? Ngoài ra OQ-EXBOT-12 (7d APR aggregation formula) vẫn Open. | Test case cho `funding_alert` (AC-LC-09) không thể thiết kế pre-condition đúng. Đây là Hard Blocker cho test design. | Open | **v3-I-03 Partially resolved.** `funding_rolling_metrics` đã được bổ sung vào ERD. Loại: bảng thật (`pgTable`), không phải view hay materialized view. Schema: `id` (uuid PK), `bot_id` (uuid UNIQUE FK), `funding_rate_1h_pct`, `funding_rate_24h_pct`, `funding_apr_7d_pct`, `updated_at`. Update bởi: `bnza-market-cron` worker — **chưa implement ở v1**. Bảng có thể trống; strategy engine fallback sang `fundingRate × 8760` khi không có data. |
| v3-I-04 | Low | UC step 7; SRS FR-EXBOT-093; SRS OQ-EXBOT-09 | UC step 7 dùng threshold cố định "stale > 5min". SRS FR-EXBOT-093 dùng "stale > 2× refresh interval". Nếu refresh interval = 2.5min thì hai policy tương đương — nhưng OQ-EXBOT-09 (interval chưa được xác định) vẫn Open. | Tester phải biết threshold chính xác để thiết kế boundary test case. | Open | Khi biết refresh interval thực tế, BA sẽ update UC step 7 cho khớp với FR-093. |
| v3-I-06 | Low | SRS §9 OQ-EXBOT-11; UC step 9; AC-LC-07 | SRS §9 OQ-EXBOT-11 (`lpValueUsd` formula) vẫn đánh dấu **Open**, nhưng UC step 9 đã ghi công thức: `lpValueUsd = max(0, lpEthAmount × currentPriceUsd)`, và audit v2 đã tạo AC-LC-07 dựa trên công thức này. Nếu zen close OQ-11 với công thức khác, AC-LC-07 cần update. | Tránh tình huống BA/zen close OQ-11 với delta mà QC không biết phải update test cases. | Open | OQ-EXBOT-11 vẫn Open, pending zen. Công thức hiện tại trong UC step 9 (`max(0, lpEthAmount × currentPriceUsd)`) là working assumption. Khi OQ-11 được close, nếu công thức thay đổi thì UC step 9 và AC-LC-07 sẽ được update theo. |
| v3-I-07 | Low | SRS flows.md F-01; UC step 11 | F-01 flow diagram trong `srs/flows.md` không có bước nào hiển thị: đọc HL Mark Price Cache tại step 11, enqueue `price-near-stop-audit`, hay logic freeze hedge-sync khi stale. Toàn bộ stop monitoring sub-flow vắng mặt trong diagram. | Tester dùng F-01 làm tài liệu tham chiếu sẽ bỏ sót stop trigger path khi design test coverage. | Open | Confirmed. F-01 has been updated: added HL Mark Price Cache participant, added stop monitoring sub-flow (step 11) — reads markPriceUsd, stale check > 120s with band widening + freeze logic, stop trigger guard with PSAQ enqueue. Fixed alt block — circuit half_open branch added. Added step 12 overrun check block — atomic safe_mode SET + partial-repair enqueue. |
| v3-I-08 | Low | userstories/us-008.md; SRS FR-EXBOT-040 | US-EXBOT-008 (Circuit Breaker) không có changelog entry cho arc-migration 2026-07-04. Nội dung circuit breaker là platform-agnostic, tuy nhiên chưa được BA xác nhận rõ ràng là "không có thay đổi cần thiết cho migration". | Tester cần sự xác nhận chính thức để yên tâm dùng US-008. | Open | US-EXBOT-008 content is platform-agnostic — all ACs reference only `circuit_breakers` table columns and business logic with no Cloudflare or AWS-specific terms. No arc-migration changes were required; absence of a 2026-07-04 changelog entry is intentional. |

---

## UC-EXBOT-monitor-status — Open Questions

| ID | Priority | Ref | Question | Why It Matters | Status | Ans |
| --- | --- | --- | --- | --- | --- | --- |
| I-003 | H | UC §3 step 3 và step 6 vs SRS ERD bảng `bot_runtime_state` + SRS FR-EXBOT-021 | UC step 6 tính drift % nhưng chỉ liệt kê một trong hai giá trị đầu vào cần thiết ở step 3. `last_known_hl_short_size` đã có trong danh sách D1 reads, nhưng `target_short_size` hoàn toàn không được đề cập. Theo SRS FR-EXBOT-021, `targetShortEth = lpEthAmount × hedgeRatio` — tức là giá trị này được tính lại từ công thức, không phải đọc thẳng từ D1. Chưa rõ nguồn dữ liệu chính xác và behavior khi mẫu số bằng 0. | Nếu Worker dùng sai nguồn, drift % trên màn hình sẽ hiển thị sai. Tester không biết phải kiểm tra giá trị nào là đúng để viết assertion, và không thể thiết kế test case cho trường hợp chia cho 0. | Open | `targetShortEth` is computed at runtime as `lp_eth_amount × (target_ratio_bps / 10000)` per FR-EXBOT-021 — not read from a stored column (`target_short_size` does not exist in v1 schema). UC step 3 updated to include `bot_runtime_state.lp_eth_amount` and `hedge_legs.target_ratio` in the reads list. UC step 6 updated to clarify the formula and define `drift_pct = null` when `lp_eth_amount = 0` or `null`. |
| N-001 | M | UC §3 bước 7 JSON schema | UC schema response chia fields thành "Implemented fields" và "Pending implementation (BA-defined)". Nhãn "Pending" có thể khiến tester bỏ qua assertion cho các trường đó hoàn toàn, thay vì kiểm chứng rằng chúng trả về `null`. | Tester có thể bỏ sót assertion cho các trường "Pending". Trường trả về `null` đúng so với trường vắng mặt hoàn toàn trong JSON là hai hành vi khác nhau. | Open | Updated step 7 null-handling note: pending fields must be **present** in the JSON response with value `null` — not omitted. Added explicit tester assertion note: verify each pending field returns `null`, not absent. |
| N-002 | M | UC §2 Preconditions; UC §4 Alternate flows | UC liệt kê ví Investor không khớp → 403 trong phần Preconditions nhưng không định nghĩa nó như một alternate flow có tên. Tester đọc chỉ phần Alternate Flows sẽ không tìm thấy đường dẫn này. | Đường dẫn 403 là vấn đề bảo mật — đọc trái phép bot của người dùng khác. Không có alternate flow tường minh thì tester thiếu cơ sở để thiết kế scenario kiểm tra. | Open | Added A9 (wallet mismatch) to Alternate Flows: Investor `wallet_address` does not match `bot.user_wallet_address` → 403 (E-EXBOT-030). Registered E-EXBOT-030 in spec.md §5 Error Matrix. |
| N-003 | L | UC §6 FR Trace; `srs/spec.md` | FR Trace trong UC hiện chỉ liệt kê FR-EXBOT-002, FR-EXBOT-003, FR-EXBOT-090. Thiếu ba FR liên quan trực tiếp: FR-EXBOT-060 (margin_status), FR-EXBOT-093 (Pool Slot0 Cache currentTick), FR-EXBOT-050 (điều kiện vào SAFE_MODE). | Test cases trace đến FR cho báo cáo coverage; FRs bị thiếu sẽ xuất hiện là không được UC này cover. | Open | Added FR-EXBOT-050 (SAFE_MODE entry conditions — A1), FR-EXBOT-060 (margin_status thresholds — step 3/7), FR-EXBOT-093 (Pool Slot0 Cache currentTick — step 4/5) to §6 FR Trace. |

---

## UC-EXBOT-pause-resume — Open Questions

| ID | Priority | Ref | Question | Why It Matters | Status | Ans |
| --- | --- | --- | --- | --- | --- | --- |
| Q14 | Minor | UC §1; SRS spec.md §1 | **Naming inconsistency sau ARC migration (2026-07-04):** UC §1 Actor list viết "ExBot System Operator (status update worker)" nhưng SRS spec.md §1 giới thiệu "ExBot Lambda". Sau ARC migration, tên chuẩn là "ExBot Lambda". UC cần cập nhật để nhất quán. | Nhất quán về naming — không ảnh hưởng đến test design nhưng gây nhầm lẫn khi đọc tài liệu. | Open | Q14 already resolved by arc-migration 2026-07-04 — UC §1 Actors currently lists "ExBot Lambda" (not "status update worker"). No further action needed. |
| Q15 | Minor | UC §1; UC Diagram | **UC Diagram (Mermaid) vẫn dùng "Status Worker" thay vì "ExBot Lambda" sau ARC migration.** UC §1 Actor list và Mermaid diagram cần được cập nhật thành "ExBot Lambda" để nhất quán với SRS spec.md. | Nhất quán về naming — không ảnh hưởng đến test design nhưng gây nhầm lẫn. | Open | Already fixed in arc-migration 2026-07-04. Mermaid diagram currently uses `participant ExBotLambda as ExBot Lambda`. |

---

## UC-EXBOT-user-redeem — Open Questions

| ID | Priority | Ref | Question | Why It Matters | Status | Ans |
| --- | --- | --- | --- | --- | --- | --- |
| I-02 | High | uc-user-redeem.md §3 step 13; flows.md F-04 | UC mô tả Worker "gửi HL-portion USDC về nhà đầu tư qua RedemptionQueue ledger" nhưng không định nghĩa: (a) HL-portion được tính như thế nào (tổng số tiền từ HL closing position minus fees? minus funding?); (b) ai thực sự là người thực hiện on-chain transfer USDC (Worker gọi trực tiếp hay qua Operator Facade?); (c) transaction hash của HL-portion transfer có được lưu vào `close_operations.hedge_close_tx` không? | Không có công thức hoặc mô tả source of truth cho HL-portion amount → tester không thể verify số tiền nhà đầu tư nhận đúng không. | Outdated vs AWS arc — HL-portion transfer mechanism changed. Pending Tech Lead for updated flow details. | Pending, cần phải cf với team và check lại flow cho câu hỏi này. |
| I-03 | Medium | uc-user-redeem.md §3 step 8; FR-EXBOT-026; FR-EXBOT-092; flows.md F-04 | **Partially Resolved (2026-07-04):** UC step 8 đã cập nhật đúng terminology: "User Lock (Redis Redlock via ElastiCache)". FR-EXBOT-026 xác nhận: acquired=false → re-queue with delay. **Remaining gap:** flows.md F-04 sequence diagram không có participant Redlock/UserLock (trong khi F-02 hedge-sync hiển thị rõ lock acquire/release). Tester đọc F-04 không thể trace scenario lock contention trong user_redeem. | Diagram gap không block happy-path design nhưng cần để thiết kế test case lock contention chính xác. | Partially Resolved — behavior confirmed via FR-EXBOT-026; F-04 diagram missing Redlock participant | F-04 updated: added `User Lock (Redis Redlock)` participant. Lock is acquired after `close_operations` row is created (idempotency first), before any HL operations. `acquired=false` → re-queue with delay; SLA clock continues running — total delay > 5 min triggers A1 SLA breach alert. Lock released after reconcile confirms size=0, or on hedge close failure before transitioning to `residual_hl_liability`. `close_operations` notation also corrected to explicit 3-step transitions: `requested → lp_closed → funds_returned`. |
| I-N1 | Medium | uc-user-redeem.md §2 Preconditions; states.md State Registry | UC §2 mở rộng precondition: "Bot status='active' (or paused/safe_mode — user may redeem from any non-closed state)". states.md State Registry vẫn không liệt kê các trạng thái nào được phép khởi tạo close request. Cần xác nhận: (a) Bot ở `hedge_stopped_cooldown` có thể được redeem không? (b) Bot ở `lp_rebalancing` có thể được redeem không? (c) Bot ở `error` có thể được redeem không? | Tester cần biết chính xác các trạng thái bắt đầu hợp lệ để thiết kế pre-condition cho test case. | Open — cần BA confirm và update states.md | **(a)** `hedge_stopped_cooldown` — **Allowed.** LP NFT still held by BnzaExVault; on-chain contract does not check `lifecycle_state`. **(b)** `lp_rebalancing` — **Allowed.** `RedeemStrategyV1` only validates tokenId ownership. If user calls `redeem(oldTokenId)` mid-rebalance, tx reverts safely; `redeem(newTokenId)` succeeds after rebalance completes. **(c)** `error` — **Allowed.** LP NFT still in vault; user retains right to redeem regardless of HL-side error state. All 3 states allowed. UC Preconditions updated to enumerate all 6 allowed `lifecycle_state` values. `states.md` State Registry updated with `user_redeem` column. Verified against `RedeemStrategyV1.sol` and `BnzaExVaultImpl.sol` — no `lifecycle_state` gate exists on-chain. |
| I-N2 | Low | uc-user-redeem.md §3 step 9 | UC step 9 chỉ nói "retries up to 3 times on reject/timeout" nhưng không mô tả: (a) Retry strategy: trong cùng Worker invocation hay re-queue message? (b) Có backoff delay giữa các lần retry không? Nếu re-queue với delay, tổng thời gian 3 retries có thể vượt SLA 5 phút. | Ảnh hưởng thiết kế test case simulate HL partial failure và timing. | Open — Minor | **(a)** In-invocation — 3 retries occur within the same Lambda invocation, inside the Redlock-acquired block (TTL=90s). No SQS re-queue per retry attempt. **(b)** Backoff between individual attempts is an implementation detail not specified at BA level; SLA impact is negligible since all 3 retries complete within seconds inside the same invocation. UC step 9 updated to clarify. |
| I-N3 | Medium | uc-user-redeem.md §3 step 7–12; states.md close_operations table | states.md close_operations table xác nhận `hedge_close_pending` là state hợp lệ cho user_redeem. UC main flow steps 7–12 không bao gồm transition tới `hedge_close_pending` — nhảy thẳng từ `funds_returned` (step 7) sang `hedge_closed` (step 12). State tồn tại trong canonical state table nhưng vắng mặt trong UC scenario description. | Tester không thể thiết kế test case cho trạng thái `hedge_close_pending` trong user_redeem. | Open | `hedge_close_pending` is now added as an explicit transition in UC step 9 (after lock acquired, before `closeShortReduceOnlyIoc` call) and in F-04 diagram. Full transition: `funds_returned → hedge_close_pending → hedge_closed`. This state serves as a recovery checkpoint — if Lambda crashes after lock acquisition, the recovery worker can identify that hedge close was in-progress and resume correctly. |
| I-N4 | Low | srs/spec.md §FR-EXBOT-092; frd.md §FR-EXBOT-091 | spec.md đánh số **FR-EXBOT-092** cho `User Lock (Redis Redlock via ElastiCache)`; frd.md đánh số **FR-EXBOT-091** cho cùng tính năng. UC file §7 FR Trace và v3/v4 audit report đều tham chiếu `FR-EXBOT-092` (theo spec.md). Behaviour của Redlock mô tả nhất quán ở cả hai nguồn — chỉ số FR là khác. | Minor traceability risk: tester tra cứu FR-EXBOT-091 trong frd.md và FR-EXBOT-092 trong spec.md có thể nhầm lẫn nếu không biết đây là cùng một requirement. | Open | Numbering mismatch confirmed — frd.md had an off-by-one shift caused by spec.md inserting FR-EXBOT-091 (HL Rate Limiter) which cascaded subsequent numbers. Fixed: frd.md FR-EXBOT-091 (User Lock) → FR-EXBOT-092; FR-EXBOT-092 (Pool Slot0 Cache) → FR-EXBOT-093. Behavior description is identical in both files — no content conflict, numbering only. |
