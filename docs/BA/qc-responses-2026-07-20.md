---
type: qc-response
module: exbot
status: draft
created: 2026-07-20
updated: 2026-07-20
owner: "@hienduong"
---

# QC Responses — ExBot Module (2026-07-20)

> File này chỉ lưu ở local — KHÔNG commit, KHÔNG push.

---

## UC-EXBOT-bot-safe-close — Open Questions

| ID | Ưu tiên | Ref | Question | Evidence in Docs | Why It Matters | Owner | Status | Ans |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Q9 | Medium | FR-EXBOT-072, UC §3 | FR-EXBOT-072 nói "idempotency_key UNIQUE enforced" và "trigger_reason populated" nhưng không có document nào định nghĩa format/value cụ thể. | FR-EXBOT-072: "idempotency_key UNIQUE enforced", "trigger_reason populated" UC §3: Preconditions SRS erd.md: close_operations table | Nếu không biết format, không thể verify UNIQUE constraint hoạt động đúng trong test. BA cần bổ sung: (1) format/value của `idempotency_key` (ví dụ: `{botId}:{kind}:{trigger_timestamp}`), (2) format/value của `trigger_reason` (ví dụ: enum values: `circuit_breaker_exhausted`, `margin_critical`, `3_stops_7d`, `partial_repair_exhausted`, `admin_force_close`). | BA | ⏳ Pending | **Q9 — `idempotency_key` format và `trigger_reason`:** `idempotency_key` format: `bot-safe-close:{botId}:{sessionId}` — confirmed từ `buildIdempotencyKey()` trong develop branch. UNIQUE enforced ở DB level. `trigger_reason` **không tồn tại** trong bảng `close_operations`. Field tương đương là `reason` trong SQS message payload (enum: `l2_evacuation \| parked_escalation \| admin`), được log ra CloudWatch dưới event `bot_safe_close_requested` — không persist vào DB. Spec đã được cập nhật (FR-EXBOT-072): xóa claim `trigger_reason` là DB column, thay bằng mô tả đúng. **Cách verify cho tester:** kiểm tra CloudWatch log event `bot_safe_close_requested`, không verify trong DB. |

---

## UC-EXBOT-bot-start — Open Questions

| ID | Priority | Ref | Question | Why It Matters | Status | Ans |
| --- | --- | --- | --- | --- | --- | --- |
| I-02 | High | UC §4 A11 vs FR-EXBOT-025 | UC mô tả A11 (reconcile mismatch) là "actual size deviates > threshold" nhưng không định nghĩa threshold cụ thể là bao nhiêu. FR-EXBOT-025 cũng không nêu threshold. FR-EXBOT-036 có nhắc drift_threshold và OQ-EXBOT-11 đề cập lpValueUsd × 3% nhưng đây là ứng viên chưa được zen xác nhận (OQ-EXBOT-11 vẫn Open). Tester cần biết ngưỡng deviation cụ thể để thiết kế test case kiểm tra boundary. | Blocker cho test reconcile mismatch. (Ghi chú 2026-07-02: BA xác nhận formula drift_threshold = lpValueUsd × 3% và behavior logic của A11 đã confirmed; tuy nhiên zen chưa confirm OQ-EXBOT-011 — boundary test vẫn bị block cho đến khi OQ-EXBOT-011 Closed.) | Open | Threshold là **exact fill** — actual HL position size phải bằng đúng target size đã đặt lệnh. Không có vùng chấp nhận phần trăm (không phải ±3% hay bất kỳ % nào). Ví dụ: target = 100 ETH → actual phải = 100 ETH. Nếu HL chỉ fill được 99.9 ETH → A11 triggered → `reconcile_partial` → `partial_repair` được enqueue. **Lưu ý cho tester:** `drift_threshold = max($25, lpValueUsd × 3%)` là threshold của light-check (trigger hedge-sync theo thời gian), **không phải** threshold của bước reconcile này. |

---

## UC-EXBOT-hedge-sync — Open Questions

| ID | Priority | Ref | Question | Why It Matters | Status | ANS |
| --- | --- | --- | --- | --- | --- | --- |
| Q2 | High | UC §3 step 8-9; FR-EXBOT-035; SPEC §19.5 | INV-STOP protocol (§19.5) được mô tả là "protected cancel→place", nhưng chi tiết cụ thể là: (a) place trước rồi cancel sau, hay (b) cancel trước rồi place sau? OQ-EXBOT-002 trong SRS note "Does HL support place-before-cancel stop replacement?". Kết quả quyết định nội dung test case cho bước F1-09 và F1-10. nhưng | Không biết path (a) hay (b) → không thể viết test case cho cancel/place sequence và dự kiến vùng thời gian 0-stop tồn tại. | Open — ⏳ Pending zen — OQ-EXBOT-002 | **Place-before-cancel** là path chính thức theo zen (OQ-EXBOT-02 đã Closed). Sequence: Place stop mới → Verify stop mới active (`verifyStopPlaced`) → Cancel stop cũ — chỉ sau khi bước 2 thành công. **Không có vùng 0-stop tại bất kỳ thời điểm nào.** Direct cancel-then-place là forbidden. **Failure case:** Nếu place stop mới thất bại → stop cũ vẫn còn active → `partial_repair` được enqueue. Không vào SAFE_MODE ở bước này. **Test case F1-09/F1-10:** Verify sequence place → verify → cancel. Mock place failure → verify stop cũ vẫn còn active và partial_repair được enqueue. |
| Q6 | Medium | FR-EXBOT-036; OQ-EXBOT-011 | `partial_repair` dùng drift_threshold = `deltaErrorUsd > max($25, lpValueUsd × 3%)`. `lpValueUsd` trong công thức được tính như thế nào? OQ-EXBOT-011 (SRS §9) chưa xác nhận formula. | Nếu `lpValueUsd` chưa có formula xác nhận, expected result của test reconcile partial fill không thể tính ra con số cụ thể. | Open — ⏳ Pending zen — OQ-EXBOT-011 | OQ-EXBOT-011 đã Closed — zen confirmed: `lpValueUsd = (lpEthAmount × uniPoolPrice) + lpUsdcAmount` (principal only, exclude tokensOwed; price = Uniswap pool slot0). `drift_threshold = max($25, lpValueUsd × 3%)`. |
| Q7 | Medium | UC §3 step 12-13; FR-EXBOT-030 | stop_trigger_px được tính lại sau reconcile, nhưng nếu delta=0 (không có lệnh HL) thì `entry_price` và `liq_price` có được update hay vẫn giữ giá trị cũ? UC step 12-13 ngầm định reconcile luôn trả về giá trị mới, nhưng nếu delta=0 thì `clearinghouseState` có cho `entry_price` mới không? | Nếu `entry_price` không đổi, việc tính lại `stop_trigger_px` cũng cho kết quả cũ → stop replacement trong luồng A6 có thể là no-op. Expected result của AC-11 bị mơ hồ. | Open — ⏳ Pending zen (Q1 đã được trả lời: stop replacement vẫn chạy khi delta=0; Q7 hỏi riêng về việc entry_price/liq_price có được cập nhật từ clearinghouseState hay không) | Khi delta = 0 (hoặc abs < 0.000001): hedge-sync trả về `status: no_op_dust` ngay lập tức — không gọi HL order, không reconcile, không update `entry_price`/`liq_price`, không replace stop. Tất cả giữ nguyên giá trị cũ. `stop_trigger_px` không được tính lại. Confirmed từ develop branch `rebalance.ts` line 83–84 và `handler-impl.ts` (không có stop replacement nào chạy sau `dispatchAction`). |

---

## UC-EXBOT-light-check — Open Questions

| ID | Priority | Ref | Question | Why It Matters | Status | Ans |
| --- | --- | --- | --- | --- | --- | --- |
| v3-I-03 | High | UC step 9; SRS OQ-EXBOT-12; SRS erd.md | UC step 9 tham chiếu `fundingApr7dPct` từ bảng `funding_rolling_metrics`, nhưng bảng này không có trong ERD (`srs/erd.md`). ERD chỉ có `funding_daily_metrics`. (1) `funding_rolling_metrics` là bảng, view, hay materialized view? (2) Khi nào được cập nhật? (3) Schema gồm những field gì? Ngoài ra OQ-EXBOT-12 (7d APR aggregation formula) vẫn Open trong SRS §9. **Partially resolved 2026-07-14:** `funding_rolling_metrics` đã được bổ sung vào ERD (bảng thật, schema đã biết). Tuy nhiên bảng được cập nhật bởi `bnza-market-cron` worker — **chưa implement ở v1**. Bảng có thể trống; strategy engine fallback sang `fundingRate × 8760` khi không có data. OQ-EXBOT-12 vẫn chưa chính thức closed. | Test case cho `funding_alert` (AC-LC-09) vẫn bị block do `bnza-market-cron` chưa implement ở v1 và OQ-EXBOT-12 chưa closed chính thức. | Partially Resolved — Pending OQ-EXBOT-12 close | (1) `funding_rolling_metrics` là **bảng thật**, 1 row/bot (UNIQUE bot_id). (2) Được cập nhật bởi `bnza-market-cron` worker — **chưa deploy ở v1**, bảng trống. Strategy engine fallback sang `fundingRate × 8760` khi không có data. (3) Schema: `id`, `bot_id`, `funding_rate_1h_pct`, `funding_rate_24h_pct`, `funding_apr_7d_pct`, `updated_at` — đã có trong ERD từ 2026-07-14. **OQ-EXBOT-12 Closed (zen confirmed):** Formula = `( Σ funding_net_usd 7 rows gần nhất / lp_value_usd ) × (365/7) × 100`. Alert khi < −15%. Nếu < 7 rows → `× 365/n`. |
| v3-I-04 | Low | UC step 7; SRS FR-EXBOT-093; SRS OQ-EXBOT-09 | UC step 7 dùng threshold cố định "stale > 5min". SRS FR-EXBOT-093 dùng "stale > 2× refresh interval". Nếu refresh interval = 2.5min thì hai policy tương đương nhau — nhưng OQ-EXBOT-09 (interval chưa được xác định) vẫn Open. BA xác nhận: khi biết refresh interval thực tế, UC step 7 sẽ được update cho khớp với FR-093. | Tester phải biết threshold chính xác để thiết kế boundary test case. Pending zen close OQ-EXBOT-09. | Open — Pending OQ-EXBOT-09 | OQ-EXBOT-09 Closed (zen confirmed): refresh interval = **60s** (provisional, config-driven). Stale threshold theo FR-EXBOT-093 = `2 × 60s = 120s`. UC step 7 sẽ được update từ `> 5 min` → `> 2× refresh interval (120s)` để khớp FR-093. Tester dùng **120s** làm boundary cho test case AC-LC-07. Giá trị có thể thay đổi sau Phase 0 NV-12 RPC verification. |
| v3-I-06 | Low | SRS §9 OQ-EXBOT-11; UC step 9; AC-LC-07 | SRS §9 OQ-EXBOT-11 (`lpValueUsd` formula) vẫn đánh dấu **Open**, nhưng UC step 9 đã ghi công thức: `lpValueUsd = max(0, lpEthAmount × currentPriceUsd)`, và audit v2 đã tạo AC-LC-07 dựa trên công thức này. Nếu zen close OQ-11 với công thức khác, AC-LC-07 và test cases tương ứng cần update. | Tránh tình huống BA/zen close OQ-11 với delta mà QC không biết phải update test cases. | Open — Pending OQ-EXBOT-11 (zen) | OQ-EXBOT-11 đã Closed trong spec.md (zen confirmed). Công thức đúng: `lp_value_usd = (lpEthAmount × uniPoolPrice) + lpUsdcAmount` — principal only, exclude tokensOwed, price = Uniswap pool slot0. UC step 9 hiện ghi sai (`max(0, lpEthAmount × currentPriceUsd)` — thiếu `lpUsdcAmount`). Sẽ update UC step 9 cho khớp. AC-LC-07 và test cases dùng công thức zen — tester cần update boundary test để include cả USDC component. |

---

## UC-EXBOT-monitor-status — Open Questions

| ID | Priority | Ref | Question | Answer | Answered By | Date | Status |
| --- | --- | --- | --- | --- | --- | --- | --- |
| V-001 | H | UC §3 step 7 JSON schema vs `srs/erd.md` bảng `bot_runtime_state` | UC response schema dùng tên trường `runtime_health_status` nhưng `srs/erd.md` định nghĩa cột là `health_status` (không có tiền tố `runtime_`). Tester không thể viết assertion chính xác: nếu dùng `runtime_health_status` mà thực tế API trả về `health_status` → test sẽ miss field. BA/Tech Lead vui lòng xác nhận: (a) tên trường chính thức trong JSON response là gì; (b) nếu khác với ERD thì cột ERD cần được cập nhật, hoặc ngược lại. | UC response schema dùng tên trường `runtime_health_status` nhưng `srs/erd.md` định nghĩa cột là `health_status` (không có tiền tố `runtime_`). Tester không thể viết assertion chính xác: nếu dùng `runtime_health_status` mà thực tế API trả về `health_status` → test sẽ miss field. BA/Tech Lead vui lòng xác nhận: (a) tên trường chính thức trong JSON response là gì; (b) nếu khác với ERD thì cột ERD cần được cập nhật, hoặc ngược lại. |  |  | Open |
| V-002 | H | UC §3 step 7 JSON schema vs `srs/erd.md` bảng `bot_runtime_state` | UC response schema dùng tên trường `last_reconcile_at` nhưng `srs/erd.md` định nghĩa cột là `last_hl_reconcile_at` (có tiền tố `hl_`). Tương tự V-001 — tester không thể viết assertion chính xác vì không biết tên trường thực tế trong JSON response. BA/Tech Lead vui lòng xác nhận tên trường chính thức và cập nhật tài liệu nguồn tương ứng. | Hai tên là intentional — không phải inconsistency: **DB column (ERD):** `last_hl_reconcile_at` — đúng **API JSON response (UC):** `last_reconcile_at` — đúng, prefix `hl_` được bỏ khi expose ra API. Tester dùng **`last_reconcile_at`** để viết assertion. Lưu ý thêm: trong v1, field này luôn trả về `null` (stub chưa implement mapping từ DB). Unblock khi v1.1 implement `getRuntimeState` đầy đủ. |  |  | Open |
| V-003 | H | UC §3 step 7 JSON schema vs `srs/erd.md` | Trường `safe_mode_tier` xuất hiện trong JSON response schema của UC nhưng không có cột tương ứng trong bất kỳ bảng nào của `srs/erd.md`. Không rõ: (a) nguồn dữ liệu (bảng nào, cột nào?); (b) kiểu dữ liệu và các giá trị hợp lệ (enum?); (c) điều kiện phân biệt các tier; (d) ý nghĩa từng tier ảnh hưởng đến hành vi bot như thế nào. Thiếu thông tin này làm tester không thể thiết kế scenario phân nhánh theo `safe_mode_tier`. BA vui lòng khai báo cột ERD và bổ sung định nghĩa vào `common-rules.md`. | **(a) Nguồn:** `bot_runtime_state.safe_mode_tier` — **v1 gap**, column chưa tồn tại trong v1 DB, thêm vào v1.1. Trong v1 API luôn trả về `null`. ERD đã được bổ sung. **(b) Kiểu dữ liệu:** TEXT nullable. Enum: `warning` \| `restricted` \| `frozen`. `null` = bot hoạt động bình thường. **(c) Điều kiện:** `restricted` — set bởi `enterSafeMode()` khi stop missing/invalid, stop filled + position still open, stop_replacing_overrun, margin_critical. `frozen` — escalation từ `restricted` (type defined, chưa có trigger trong v1). `warning` — type defined, chưa có trigger trong v1. **(d) Ảnh hưởng:** `null`/`normal` → light-check bình thường. `restricted` hoặc `frozen` → bot-scan block khỏi light-check, route sang deep-audit. **Tester lưu ý:** Trong v1, `safe_mode_tier` luôn `null` — scenario phân nhánh theo tier chỉ test được từ v1.1. |  |  | Open |
| V-004 | H | UC §3 step 7 JSON schema vs `srs/erd.md` | Trường `dry_run` xuất hiện trong JSON response schema của UC nhưng không có cột tương ứng trong `srs/erd.md`. Không rõ: (a) nguồn dữ liệu; (b) kiểu dữ liệu (boolean?); (c) ý nghĩa — khi `dry_run = true` thì bot hoạt động khác gì so với `dry_run = false`; (d) ảnh hưởng đến các trường khác trong response. BA/Tech Lead vui lòng khai báo cột ERD và mô tả ngữ nghĩa. | **(a) Nguồn:** `dry_run` **không phải DB column** — đọc trực tiếp từ env var `EXBOT_DRY_RUN=true` tại runtime. Không cần thêm vào ERD. **(b) Kiểu:** Boolean. **(c) Nghĩa:** Khi `dry_run = true`, mọi HL order call (adjustShortDelta, placeStop, closeShort, cancelByCloid) đều short-circuit — return `submitted: true` mà **không gửi lệnh thật đến Hyperliquid**. Bot chạy toàn bộ logic bình thường nhưng không có side-effect thật trên HL. Dùng cho staging/testing environment. **(d) Ảnh hưởng response:** Không ảnh hưởng các field khác — chỉ là indicator cho UI/tester biết deployment đang ở chế độ dry run. |  |  | Open |
| V-005 | M | UC §3 step 7 JSON schema vs `srs/erd.md` bảng `bot_runtime_state` | Trường `safe_mode_reason` và `cooldown_end_at` trong JSON response được liệt kê nhưng không có liên kết tường minh đến cột ERD cụ thể trong danh sách reads (step 3). Có thể hai trường này đọc từ `bot_runtime_state` nhưng chưa được xác nhận. BA vui lòng bổ sung tên cột chính xác vào danh sách reads ở step 3 hoặc thêm ghi chú nguồn vào schema ở step 7. | Hai field này đã được phân loại đúng trong UC step 7 — nằm trong **"Pending implementation"**, không phải "Implemented fields". Cả hai sẽ trả về `null` trong v1. `safe_mode_reason`: nguồn dự kiến = `bot_runtime_state.safe_mode_reason` (v1 gap, chưa có column, thêm v1.1). `cooldown_end_at`: nguồn dự kiến = `bot_runtime_state.cooldown_end_at` (v1 gap, chưa có column, thêm v1.1). **Tester lưu ý (đã có trong doc step 7):** các pending field phải **present với value `null`** trong response — không được absent. Assertion phải check `field === null`, không phải `field === undefined`. |  |  | Open |
| V-006 | L | UC §3 step 7 JSON schema — trường `bot_id` | UC response schema có trường `bot_id` nhưng không khai báo tường minh đây là alias của `bots.id`. Nếu tester xem xét tên trường một cách máy móc có thể tìm kiếm cột tên `bot_id` trong DB (không tồn tại). BA vui lòng bổ sung ghi chú vào step 7: `bot_id` trong response = `bots.id` trong Aurora PG. | `bot_id` trong response = `bots.id` trong Aurora PG (primary key của bảng `bots`). Không có cột riêng tên `bot_id` trong DB. |  |  | Open |
| V-007 | M | UC §3 step 3 danh sách reads vs `srs/erd.md` bảng nguồn | Trường `last_error_code` xuất hiện trong JSON response schema (step 7) nhưng không được liệt kê trong danh sách reads ở step 3. Tester không thể xác định bảng nguồn (`bots`, `bot_runtime_state`, hay bảng khác). BA vui lòng bổ sung `last_error_code` vào danh sách reads ở step 3 với tên bảng và cột chính xác. | `last_error_code` nguồn từ bảng `bot_runtime_state`, cột `last_error_code` (TEXT). **V1 gap** — cột chưa tồn tại trong schema, code hiện tại hardcode trả về `null`. Response sẽ luôn là `null` cho đến v1.1 khi cột được thêm vào. |  |  | Open |
| V-008 | M | UC §4 Alternate Flows vs `srs/states.md` State Registry | Trạng thái `paused` có mặt trong State Registry của `srs/states.md` nhưng không có alternate flow tương ứng trong UC. Khi bot ở trạng thái `paused`, `GET /api/exbot/status` trả về gì (HTTP code, `status` value, thông báo)? Thiếu định nghĩa này làm tester không thể thiết kế scenario cho trạng thái `paused`. BA vui lòng bổ sung alternate flow A10 (hoặc tên phù hợp) cho trạng thái `paused`. | Khi `status='paused'`, API trả về **HTTP 200**, `"status": "paused"`, `"lifecycle_state"` giữ nguyên giá trị trước khi pause (thường `"active"`). Tất cả mutation buttons bị disable; user_redeem vẫn allowed. **v1 gap:** Lambda hiện có `isAdmin` guard cứng — Investor nhận 403. A10 chỉ testable sau khi guard được remove. |  |  | Open |

---

## UC-EXBOT-user-redeem — Open Questions

| ID | Priority | Ref | Question | Why It Matters | Status | Ans |
| --- | --- | --- | --- | --- | --- | --- |
| I-02a | High | uc-user-redeem.md §3 step 13; flows.md F-04 | **(a) HL-portion được tính như thế nào?** Tổng số tiền từ HL closing position là minus fees? minus funding? Công thức cụ thể chưa được định nghĩa trong bất kỳ tài liệu nào. | Không có công thức hoặc mô tả source of truth cho HL-portion amount → tester không thể verify số tiền nhà đầu tư nhận đúng không. | Open — ⏳ Pending BA/team confirm | HL-portion investor nhận về được tính qua 3 bước trong `hl-withdraw` worker: (1) đọc `clearinghouseState.withdrawable` từ HL API trước khi rút (initial), (2) poll lại sau khi rút settled (final), (3) `principal_amount = initial − final`. Con số này đã net PnL short, funding, và trading fees vì HL API tự tính vào balance — không có breakdown riêng từng loại. Kết quả ghi vào `redemption_requests.principal_amount` và đây là số investor nhận on-chain qua `RedemptionQueue.fulfillRequest`. Lưu ý: `principal_amount` ≠ `hl_portion_usdc` (số USDC ban đầu bridge sang HL lúc mở bot = `ceil(totalUsdc / 2)`). Cả user_redeem lẫn bot_safe_close đều đi qua cùng chain: `lp_leg_exec → hedge_sync → hl_withdraw → hl_fulfill`. URL Doc Dev gửi cho flow withdraw: https://github.com/sotatek-dev/bonanzapool/blob/develop/contracts/bnza-exbot/docs/flow-withdraw-close-settlement-ba.md |
