---
title: Question Backlog — UC-EXBOT-light-check
date: 2026-06-18
author: QC Q&A Agent
version: v1
---

# Question Backlog — UC-EXBOT-light-check

> Generated: 2026-06-18 (updated: 2026-06-18 re-audit v3 -- added I-015, I-016, I-017)
> Source files: docs/qc/uc-read/UC-EXBOT-light-check/UC-EXBOT-light-check_light-check_audited_20260618_v3.md

---

## Open Questions

| ID | Priority | Ref | Question | Why It Matters | Status |
|----|----------|-----|----------|----------------|--------|
| I-001 | High | FR-EXBOT-033; FR-EXBOT-012 SRS | The `stop_replacing_started_at` overrun detection path is completely absent from UC §3 main flow. SRS FR-EXBOT-033 (primary detection ≤5 min) and FR-EXBOT-012 explicitly require each light-check pass to evaluate `stop_replacing_started_at IS NOT NULL AND age > 60s` → enqueue `partial_repair(reason='stop_replacing_overrun')` + SAFE_MODE. BA/Tech Lead: add this step to UC §3. | Tester cannot design a test case for the stop_replacing overrun → SAFE_MODE path because the UC does not describe the trigger, the condition check, or the expected system response. | Open |
| I-002 | High | UC §4 A4; FR-EXBOT-023 | A4 says `range_boundary_near` 90%: "Enqueue price-near-stop-audit even before price reaches stop" but FR-EXBOT-023 lists `range_boundary_near` as a `RebalanceReason[]` (which normally routes to `hedge-sync`). BA: clarify whether `range_boundary_near` routes to (a) `hedge-sync`, (b) `price-near-stop-audit` as implied by A4, or (c) both. Fix the UC to be explicit. | Tester cannot determine the correct routing for `range_boundary_near`. Incorrect routing test would verify wrong behavior. | Open |
| I-003 | High | UC §3 step 11; FR-EXBOT-012 SRS 3-way price split | UC step 11 says "If `markPrice >= stop_price`" without specifying the source of `markPrice`. FR-EXBOT-012 states `hlMarkPrice` is used for stop trigger detection. However, the UC has HL weight = 0. How is `hlMarkPrice` obtained without an HL call? Is it `bot_runtime_state.eth_price_usd`? Is it cached in MarketDataDO or D1? BA/Tech Lead: specify the exact data source for `hlMarkPrice` in the light-check path. | Stop trigger detection test cases cannot be written. If `hlMarkPrice` requires an HL call, this is a BR-EXBOT-003 architectural violation. | Open |
| I-004 | High | SRS spec §9 OQ-EXBOT-10; FR-EXBOT-023 | `range_boundary_near` evaluation formula is unresolved: "is '90% to upper/lower' measured in tick distance or price distance?" (OQ-EXBOT-10). zen/SOTATEK: resolve and update FR-EXBOT-012 or FRD with the explicit formula (e.g., tick-based: `currentTick >= tickUpper - 0.10 × (tickUpper - tickLower)` vs price-based: `sqrtPrice >= sqrtUpper × 0.90`). | Tester cannot define test inputs (currentTick vs tick range boundary) without the formula. | Open |
| I-005 | High | SRS states.md (State Registry table); UC §2 | `hedge_stopped_cooldown` state: SRS State Registry says light-check RUNS ("run (no hedge-sync)") but UC §2 preconditions and skip logic do not mention this state. BA: add `hedge_stopped_cooldown` to UC §2 or §3 with explicit statement: "Light-check runs but hedge-sync enqueue is suppressed for this state; stop monitoring continues." | Testers will miss the `hedge_stopped_cooldown` scenario. Stop monitoring behavior during this state would not be verified. | Open |
| I-006 | Medium | FR-EXBOT-023; frd.md | `time_fallback` reason: condition is "4h since last adjustment" but neither the UC nor FRD specifies which D1 field represents "last adjustment time." Candidates: `bot_runtime_state.last_hl_reconcile_at`, `hedge_legs.updated_at`, or the timestamp of the last `rebalance_attempts` row. BA/Tech Lead: specify the exact D1 field used as "last adjustment time" for the `time_fallback` reason. | Tester cannot set up correct preconditions for `time_fallback` test cases without knowing which timestamp field to set. | Open |
| I-007 | Medium | UC §3 step 7; FR-EXBOT-093 | UC does not describe the exception path when `MarketDataDO` is unavailable or its cache is stale beyond 2× refresh interval. FR-EXBOT-093 states DO forces a refresh on staleness, but does not say what the light-check worker should do if the DO is unreachable (skip bot? enter SAFE_MODE? re-queue?). BA: add an exception path for MarketDataDO unavailability in UC §3 or §4. | Tester cannot design a test case for MarketDataDO failure mode in the context of light-check. | Open |
| I-008 | Medium | SRS spec §9 OQ-EXBOT-11; FR-EXBOT-023 | `drift_threshold` formula requires `lpValueUsd` but OQ-EXBOT-11 states: "how is `bot_runtime_state.lp_value_usd` computed and updated?" is unresolved. The candidate formula is `(lpEthAmount × uniPoolPrice) + lpUsdcAmount` but not confirmed by zen. zen/SOTATEK: resolve OQ-EXBOT-11 and update FR-EXBOT-011 with the confirmed `lpValueUsd` formula. | Tester cannot compute the expected `drift_threshold` trigger point. The "$25 floor vs 3% of LP value" threshold crossover cannot be tested without the formula. | Open |
| I-009 | Medium | SRS spec §9 OQ-EXBOT-12; FR-EXBOT-023 | `funding_alert` reason requires "7d APR < −15%" but the aggregation formula from `funding_daily_metrics` is unresolved (OQ-EXBOT-12). Candidate formula is sum of latest 7 rows `funding_net_usd` annualized relative to LP capital, but exact annualization is not specified. zen: resolve OQ-EXBOT-12 and update SRS with the confirmed 7-day APR formula. | Tester cannot set up `funding_daily_metrics` test data to trigger/not trigger `funding_alert` without the confirmed formula. | Open |
| I-010 | Low | UC §3 step 10; FR-EXBOT-027 | The UC mentions enqueuing `hedge-sync` with `{botId, reasons, stateVersion}` but does not specify the source of `stateVersion`. FR-EXBOT-027 clarifies it is `bot_runtime_state.state_version`. Suggestion: UC step 10 should add "(stateVersion from bot_runtime_state.state_version)" for traceability. | Minor: tester may not know to verify `stateVersion` field in the enqueued message without reading FR-EXBOT-027 separately. | Open |
| I-011 | Low | UC §3 step 3 | UC text names `chunkSendBatch` (an internal helper function) in the main flow. The business-relevant constraint is "send in batches of ≤100 per CF limit." Suggestion: rephrase step 3 to describe the batch-size constraint rather than the helper function name. | Minor: does not block test design. | Open |
| I-012 | High | `frd.md FR-EXBOT-032` vs `srs/spec.md FR-EXBOT-033` | `frd.md FR-EXBOT-032` states "Overrun (>60s) **detected by audit** → SAFE_MODE." `srs/spec.md FR-EXBOT-033` states light-check is the **primary detector (≤5 min)** for `stop_replacing_started_at` overrun; deep-audit is only the secondary backstop. Both docs declare "SPEC v5.2.6 prevails" — srs/spec.md is canonical. BA/Tech Lead: update `frd.md FR-EXBOT-032` to state light-check is primary detector (≤5 min) and deep-audit is secondary, matching srs/spec.md FR-EXBOT-033. | A developer reading only frd.md would implement overrun detection solely in deep-audit (6h lag), missing the ≤5-minute primary detection requirement. Tester writing test cases against frd.md alone would test the wrong detection window. | Open |
| I-013 | Medium | UC §7 FR Trace; `usecases/index.md`; UC frontmatter `linked_stories` | Three inconsistencies across artefact metadata: (1) UC §7 FR Trace omits **FR-012** — the core "zero HL calls" rule that is the defining constraint of this UC. `usecases/index.md` correctly lists FR-012. (2) `usecases/index.md` omits **FR-023** (canonical RebalanceReason enum), which is in UC §7. (3) UC frontmatter `linked_stories: [US-EXBOT-005, US-EXBOT-006]` but `usecases/index.md` only lists US-EXBOT-005. BA: (1) Add FR-012 to UC §7. (2) Add FR-023 to index. (3) Reconcile linked_stories between UC and index. | A tester using UC §7 to scope review would miss the HL-weight=0 requirement (FR-012) — the primary constraint of this UC. | Open |
| I-014 | Low | `frd.md FR-EXBOT-060`; US-EXBOT-010 AC-1; UC §3 step 9 | UC reads `hedge_legs.margin_status` from D1 and evaluates `margin_warning` as a RebalanceReason, but does not trace the downstream constraint: when `margin_warning` is in the hedge-sync payload, the hedge-sync worker must block size-increase adjustments (FR-EXBOT-060). This cross-UC behavioral constraint is not visible in UC §7 FR Trace. Suggestion: UC §7 or §3 routing table should note that `margin_warning` reason constrains hedge-sync behavior per FR-EXBOT-060. | Tester may not verify that a hedge-sync payload containing `margin_warning` correctly blocks size-increase in the downstream worker. | Open |

---

| I-015 | High | US-EXBOT-007 AC-EXBOT-007-1 vs FR-EXBOT-015 (SRS spec.md) | US-007 AC-EXBOT-007-1 references `BnzaExVault.vaultRebalance()` and `VaultRebalanced` event. FR-EXBOT-015 (SRS spec, canonical) describes the LP rebalance as `BnzaExVault.redeem(tokenId)` (close LP NFT) + compute new tick range + mint new NFT. The function name `vaultRebalance()` and event `VaultRebalanced` do not appear in FR-EXBOT-015 or anywhere else in SRS or FRD. BA/Tech Lead: reconcile US-007 with FR-EXBOT-015. Either (a) update US-007 to use `redeem(tokenId)` + mint, or (b) confirm `vaultRebalance()` is a new function to add to BnzaExVault and update FR-EXBOT-015 accordingly. Blocked until zen confirms Vault ABI. | Developer reading US-007 would implement `vaultRebalance()`. Developer reading FR-EXBOT-015 would implement `redeem() + mint`. These are architecturally different (one call vs two calls, different events emitted). Tester cannot define the correct call sequence or expected events for integration tests without resolution. | Open |
| I-016 | High | FR-EXBOT-015 (SRS spec); UC section 3 main flow; UC section 7 FR Trace | The LP range rebalance trigger path (`range_out` to `lp_rebalancing` transition + `partial_repair` enqueue) is entirely absent from UC section 3 main flow, section 4 alternate flows, and section 7 FR Trace. Despite US-007 being added to `linked_stories` on 2026-06-18, the UC body was not updated to describe: (a) the `range_out` detection step, (b) the `lifecycle_state` transition to `lp_rebalancing`, (c) the `partial_repair` queue enqueue, (d) the hedge suppression rule during `lp_rebalancing`, (e) the crash-recovery path. BA: update UC section 3 main flow and section 7 FR Trace (add FR-015) to cover this path. | A tester reading UC section 3 has no indication that `range_out` should trigger a `lp_rebalancing` transition. All LP rebalance behavior is invisible, making it impossible to design test cases for this P1 feature. | Open |
| I-017 | Major | `srs/flows.md` F-05 (2026-06-12, NOT updated) vs `srs/spec.md` + `srs/states.md` + `frd.md` (all updated 2026-06-18) | `srs/flows.md` F-05 was NOT updated on 2026-06-18. It still contains the entire park/re-entry loop dropped by HLD-2026-06-18: `vaultClose(tokenId, dest=UNINVESTED)`, `FundsParked` event, `lifecycle_state=cooldown (60 min)`, `uninvestedBalanceOf(user, botId)`, `parked` state, and `REENTRY` worker loop. All contradict spec.md HLD-2026-06-18 note, states.md (cooldown/parked removed), and frd.md FR-EXBOT-002/070/073. BA action required: update `flows.md` F-05 to reflect `bot_safe_close` transitioning directly to `closed` (no re-entry worker, no cooldown/parked state, no uninvestedBalanceOf). | Developer or tester reading flows.md F-05 gets a contradictory picture of post-`bot_safe_close` lifecycle behavior. A developer implementing light-check state-skip logic based on flows.md would add `cooldown` and `parked` to the skip list (non-existent states after HLD-2026-06-18). A tester creating test data with `lifecycle_state='cooldown'` would be testing behavior that cannot occur. | Open |

## Answered Questions

| ID | Priority | Ref | Question | Answer | Answered By | Date | Status |
|----|----------|-----|----------|--------|-------------|------|--------|

