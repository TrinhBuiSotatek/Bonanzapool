---
title: Danh sách câu hỏi tồn đọng — UC-EXBOT-bot-safe-close
date: 2026-06-18
author: qc-uc-read-exbot
version: v2
---

# Danh sách câu hỏi tồn đọng — UC-EXBOT-bot-safe-close

> Tạo ngày: 2026-06-18
> Nguồn: docs/qc/uc-read/UC-EXBOT-bot-safe-close/UC-EXBOT-bot-safe-close_bot-safe-close_audited_20260618_v1.md
> Bản dịch tiếng Việt từ: UC-EXBOT-bot-safe-close_bot-safe-close_questions_20260618_v1.md

---

## Câu hỏi đang mở

| ID | Ưu tiên | Tham chiếu | Nội dung câu hỏi | Vì sao quan trọng | Trạng thái |
|----|---------|------------|------------------|-------------------|------------|
| I-001 | High | UC §3 bước 2-3 vs FR-EXBOT-073 bước 2; states.md bảng close_operations | Trạng thái `close_operations.state='hedge_close_pending'` được định nghĩa trong cả states.md lẫn FR-EXBOT-073 là trạng thái trong khi đóng vị thế short trên HL, nhưng UC §3 không đề cập trạng thái này — UC nhảy thẳng từ bước 1 (tạo close_operations với state='requested') sang bước 2 (lấy lease + đóng HL). BA/Tech Lead cần bổ sung một bước rõ ràng vào UC §3, giữa bước 1 và bước 2, ghi nhận việc đặt `close_operations.state='hedge_close_pending'`, đúng với FR-EXBOT-073. | Tester không thể xác định thời điểm `hedge_close_pending` được ghi, và không thể kiểm tra trạng thái này trong integration test. Chuỗi chuyển trạng thái của `close_operations` trong test sẽ thiếu một bước chuyển. | Mở |
| I-002 | High | US-EXBOT-012 AC-012-1 vs FR-EXBOT-070 (spec.md + frd.md, cập nhật 2026-06-18) | US-012 AC-012-1 (happy path admin force-close) tham chiếu `BnzaExVault.vaultClose` và `uninvested_balances` — cả hai đã bị loại bỏ theo HLD-2026-06-18. FR-EXBOT-070 (nguồn chuẩn) quy định "Không dùng `uninvested_balances` hay `uninvestedBalanceOf` API" và mô tả `executeStrategy(RedeemStrategyV1)` là phương thức đóng LP. BA cần cập nhật US-012 AC-012-1 để thay thế `vaultClose`/`uninvested_balances` bằng luồng `executeStrategy(RedeemStrategyV1)` + `RedemptionQueue`, thống nhất với AC-012-3 và FR-EXBOT-073. | Developer triển khai admin force-close theo US-012 sẽ tạo lệnh gọi `vaultClose()` và bảng `uninvested_balances` — cả hai đều không còn tồn tại trong kiến trúc HLD-2026-06-18. Integration test cho admin force-close sẽ kiểm tra lệnh gọi on-chain sai. | Mở |
| I-003 | High | usecases/index.md dòng UC-EXBOT-bot-safe-close vs nội dung UC (cập nhật 2026-06-18); FR-EXBOT-070 | Mô tả trong index.md vẫn ghi "System hedge-first close + USDC park + automatic re-entry closed loop" — đây là mô tả trước khi HLD-2026-06-18 loại bỏ park/re-entry. Nội dung UC đã được cập nhật 2026-06-18 để bỏ phần này. Ngoài ra, index.md có FR-071 trong FR Trace nhưng UC §7 FR Trace không có. BA cần: (1) cập nhật mô tả index.md cho phù hợp với luồng hiện tại; (2) xác nhận FR-EXBOT-071 có áp dụng cho UC này không, hay chỉ áp dụng cho user_redeem. | Người đọc dùng index.md để tham khảo nhanh sẽ hiểu sai phạm vi UC. Bất nhất về FR-071 gây ra phạm vi test không đầy đủ. | Mở |
| I-004 | High | srs/flows.md F-05 (cập nhật lần cuối 2026-06-12) vs srs/spec.md FR-EXBOT-070/073 + srs/states.md + frd.md (tất cả cập nhật 2026-06-18) | flows.md F-05 vẫn còn toàn bộ vòng lặp park/re-entry: `vaultClose(tokenId, dest=UNINVESTED)`, sự kiện `FundsParked`, `lifecycle_state=cooldown` (60 phút), `uninvestedBalanceOf(user, botId)`, trạng thái `parked`, và vòng lặp REENTRY worker. Ngoài ra, frd.md FR-EXBOT-031 vẫn tham chiếu "§16.7 cooldown closed loop". Tất cả mâu thuẫn với spec.md FR-EXBOT-070/073 và ghi chú HLD-2026-06-18 trong states.md. BA cần: (1) cập nhật flows.md F-05 thay thế luồng park/re-entry bằng `executeStrategy(RedeemStrategyV1)` → `RedemptionQueue` FIFO; (2) cập nhật frd.md FR-EXBOT-031 xóa tham chiếu §16.7. | Developer hoặc tester đọc flows.md F-05 sẽ xây dựng/kiểm tra một re-entry worker và trạng thái cooldown không còn tồn tại. Dữ liệu test tạo với `lifecycle_state='cooldown'` hoặc `lifecycle_state='parked'` sẽ kiểm tra hành vi không thể xảy ra. | Mở |
| I-005 | High | FR-EXBOT-072; UC §2 preconditions | UC §2 liệt kê 4 trigger nhưng thiếu trigger thứ 5 theo FR-EXBOT-072: `partial_repair` bị kiệt sức (consumer của partial_repair thất bại 3 lần liên tiếp cho cùng một sự kiện trigger). Ràng buộc `UNIQUE` trên `idempotency_key` (ngăn tạo thêm hàng `close_operations` thứ hai khi đã có close đang thực hiện) cũng không được đề cập trong preconditions. BA cần: thêm partial_repair exhausted làm trigger thứ 5 vào UC §2; bổ sung ghi chú về kiểm tra `idempotency_key`. | Tester không thể thiết kế test case cho trigger T4 (partial_repair exhausted). Precondition về idempotency không được mô tả trong UC khiến kịch bản trigger trùng lặp không có cơ sở UC. | Mở |
| I-006 | High | UC §3 bước 5-6; FR-EXBOT-073 | UC §3 bước 5 gọi `vault.executeStrategy(RedeemStrategyV1, user, botId, params)` nhưng hợp đồng tham số `params` chưa được định nghĩa. Bước 6 đề cập "optional convertPrincipalToUsdc" nhưng không nêu rõ khi nào flag này là true/false, ai đặt giá trị này, hoặc giá trị mặc định là gì. FR-EXBOT-073 (SRS) cũng không định nghĩa cấu trúc `params`. | Tester không thể viết integration test cho bước đóng LP mà không biết schema của `params`. Test case kiểm tra "phí được trả bằng đồng tiền trong cặp hay USDC" cần biết tham số nào điều khiển hành vi này. | Mở |
| I-007 | High | UC §4 A1 vs US-009 AC-009-2 vs FR-EXBOT-073 | Trạng thái được giữ khi đóng HL thất bại được mô tả không nhất quán giữa các nguồn: UC §4 A1 nêu `close_operations.state='residual_hl_liability'`; US-009 AC-009-2 nêu `close_operations.state` ở mức `'hedge_close_pending'`; FR-EXBOT-073 nêu "thử lại tối đa 3 lần trước khi báo cáo admin và giữ ở `hedge_close_pending`". Ranh giới giữa `hedge_close_pending` (trong khi đang thử lại) và `residual_hl_liability` (sau khi tất cả các lần thử đều thất bại) không được xác định trong UC. BA cần làm rõ trong UC §4 A1: `hedge_close_pending` được giữ trong suốt 3 lần thử lại, sau đó chuyển sang `residual_hl_liability` khi tất cả thất bại. Cập nhật US-009 AC-009-2 cho nhất quán. | Tester không thể xác định phải kiểm tra trạng thái nào tại thời điểm nào trong luồng lỗi. Integration test có thể kiểm tra sai trạng thái. | Mở |
| I-008 | Medium | FR-EXBOT-034; FR-EXBOT-036; UC §7 FR Trace; index.md FR Trace | UC §7 FR Trace (FR-070, FR-072, FR-073) thiếu FR-EXBOT-034 (Stop Cooldown — định nghĩa trigger T3: "3 lần bot_safe_close trong 7 ngày") và FR-EXBOT-036 (Partial Repair — định nghĩa trigger T4: partial_repair exhausted). index.md còn bao gồm FR-071 không có trong UC §7. BA cần: thêm FR-034 và FR-036 vào UC §7 FR Trace; làm rõ bất nhất về FR-071. | Tester dùng UC §7 làm ranh giới phạm vi test sẽ bỏ sót hoàn toàn hai luồng trigger T3 và T4. | Mở |
| I-009 | Medium | FR-EXBOT-072; FR-EXBOT-040; erd.md circuit_breakers | Trigger T1 (circuit breaker kiệt sức) được định nghĩa trong FR-EXBOT-072 là "reset_at được mở rộng 3 lần trở lên mà không có probe thành công". Tuy nhiên, bảng `circuit_breakers` trong erd.md không có column nào theo dõi số lần mở rộng (không có `extension_count` hay tương đương). State machine của circuit breaker (FR-EXBOT-040) cũng không mô tả cách đếm số lần mở rộng. BA/Tech Lead cần xác định: `extension_count` có được thêm vào bảng `circuit_breakers` không? Hoặc điều kiện "3 lần mở rộng" được đánh giá như thế nào từ dữ liệu hiện có? | Tester không thể mô phỏng điều kiện trigger T1 trong môi trường test mà không biết mô hình dữ liệu để theo dõi số lần mở rộng. | Mở |
| I-010 | Medium | UC §3 bước 2; FR-EXBOT-092; FR-EXBOT-026 | UC §3 bước 2 nêu "lấy lease UserLockDO" nhưng không mô tả hành vi khi việc lấy lease thất bại (acquired=false). FR-EXBOT-026 định nghĩa hành vi re-queue khi hedge-sync thất bại lấy lock, nhưng không có quy tắc tương đương nào cho bot_safe_close. Trên thực tế, một hedge-sync đang chạy cho cùng user có thể đang giữ lock khi bot_safe_close kích hoạt. BA cần bổ sung alternate flow vào UC §4 cho trường hợp UserLockDO không thể lấy được trong ngữ cảnh bot_safe_close: re-queue, chờ, hay báo cáo admin? | Tester không thể thiết kế test cho tình huống tranh chấp lock trong bot_safe_close. Nếu close worker không lấy được lock, hành vi hệ thống không được xác định. | Mở |
| I-011 | Medium | FR-EXBOT-072; erd.md bảng close_operations | FR-EXBOT-072 nêu "Tất cả năm luồng trigger đều tạo hàng close_operations với `trigger_reason` được điền". Tuy nhiên, bảng `close_operations` trong erd.md KHÔNG có column `trigger_reason`. Column này xuất hiện trong yêu cầu SRS nhưng vắng mặt trong schema. BA cần: thêm column `trigger_reason` vào bảng `close_operations` trong erd.md, hoặc tài liệu hóa cách tiếp cận thay thế. | Developer viết migration D1 sẽ không biết cần thêm column `trigger_reason`. Tester kiểm tra việc ghi `trigger_reason` không có cơ sở schema. | Mở |
| I-012 | Low | Tiêu đề file UC vs changelog UC | Tiêu đề UC vẫn ghi "UC-EXBOT-bot-safe-close: System-Initiated Safe Close + Auto Re-Entry" — phần "Auto Re-Entry" đã bị loại bỏ theo changelog 2026-06-18. | Gây nhầm lẫn nhỏ cho người đọc khi xem tiêu đề UC. | Mở |
| I-013 | High | UC §3 bước 6; FR-EXBOT-073; LpFeeOps | UC §3 bước 6 nêu phí được định tuyến qua LpFeeOps (phí vận hành + phí hiệu suất, tính bằng pair currency) trước khi trả vốn gốc, nhưng cả UC lẫn FR-EXBOT-073 đều không định nghĩa: (1) công thức tính phí vận hành (tỷ lệ cố định? phần trăm vốn gốc? cấu hình theo từng bot?); (2) công thức tính phí hiệu suất (phần trăm lợi nhuận? mô hình high-water mark? tỷ lệ cố định?); (3) giá trị gốc dùng để tính mỗi loại phí là gì; (4) ai cấu hình mức phí và lưu ở đâu (bảng D1? cấu hình on-chain?). BA/Tech Lead cần định nghĩa công thức tính phí, các tham số đầu vào, và nguồn cấu hình cho cả hai loại phí. | Tester không thể kiểm chứng số tiền khấu trừ đúng trong integration test cho bot-safe-close nếu không biết công thức tính phí. Bất kỳ assertion nào về số tiền vốn gốc trả về đều sẽ không chính xác nếu thiếu định nghĩa này. | Mở |

---

## Câu hỏi đã được trả lời

| ID | Ưu tiên | Tham chiếu | Nội dung câu hỏi | Câu trả lời | Người trả lời | Ngày | Trạng thái |
|----|---------|------------|------------------|-------------|---------------|------|------------|

---

## Câu hỏi tạm hoãn

| ID | Ưu tiên | Tham chiếu | Nội dung câu hỏi | Lý do hoãn | Trạng thái |
|----|---------|------------|------------------|------------|------------|
