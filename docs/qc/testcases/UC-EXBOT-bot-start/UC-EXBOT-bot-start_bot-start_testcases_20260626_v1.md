# Test Cases — UC-EXBOT-bot-start (Bot Start)

**Document title:** UC-EXBOT-bot-start — Bot Start — Test Cases (Logic-only)
**Date created:** 2026-06-26
**Author/Agent:** qc-func-tc-design-exbot
**Version:** v1
**UC source:** uc-bot-start.md (updated 2026-06-20)
**UC audit source:** UC-EXBOT-bot-start_bot-start_audited_20260626_v4.md (57/100, NOT READY)
**SRS baseline:** srs/spec.md, srs/states.md, srs/flows.md, srs/erd.md
**Variant:** logic (no UI)

> **Design note — Blocker I-01:** UC alternate flows A7, A8, A10, A11 state `lifecycle_state='error'`. SRS `states.md` maps all four to `lifecycle_state='safe_mode'`. All TCs use SRS as source of truth. Affected TCs are marked `[Note: I-01]`.

## I. Operation: Bot start (preflight checks)

### I.1. Functional verification — Operation: Bot start (preflight checks)

| TC ID | Title | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|
| TC_001 | Verify the one-bot policy allows a start when the user has no active bot | The user has no `bots` row, or all existing rows have a status that is not one of `active`, `paused`, `closing`, `safe_mode`, `error`. | Submit a start request (`POST /api/exbot/start`) with valid user credentials and valid start parameters. | 1. The one-bot policy check passes and the Worker proceeds to the margin check; no `E-EXBOT-001` is returned. (Reference: BR-EXBOT-001, FR-EXBOT-001.) | P1 |
| TC_002 | Verify the one-bot policy blocks a start when the user already has a running bot | The user already has a running bot (existing `bots` row with `lifecycle_state='active'`). | Submit a start request for the same user. | 1. The start is blocked (`E-EXBOT-001`, HTTP 409); no new `bots` row is created. (Reference: BR-EXBOT-001.) | P0 |
| TC_003 | Verify the one-bot policy blocks a start when the user's bot is paused | The user has an existing `bots` row with `lifecycle_state='paused'`. | Submit a start request for the same user. | 1. The start is blocked (`E-EXBOT-001`); no new `bots` row is created. (Reference: BR-EXBOT-001.) | P0 |
| TC_004 | Verify the one-bot policy blocks a start when the user's bot is in safe mode | The user has an existing `bots` row with `lifecycle_state='safe_mode'`. | Submit a start request for the same user. | 1. The start is blocked (`E-EXBOT-001`); no new `bots` row is created. (Reference: BR-EXBOT-001.) | P0 |
| TC_005 | Verify the one-bot policy blocks a start when the user's bot is in error state | The user has an existing `bots` row with `lifecycle_state='error'`. | Submit a start request for the same user. | 1. The start is blocked (`E-EXBOT-001`); no new `bots` row is created. (Reference: BR-EXBOT-001.) | P0 |
| TC_006 | Verify the one-bot policy blocks a start when the user's bot is being closed | The user has an existing `bots` row with `lifecycle_state='closing'`. | Submit a start request for the same user. | 1. The start is blocked (`E-EXBOT-001`); no new `bots` row is created. The `closing` state is in the blocking list per BR-EXBOT-001. | P0 |
| TC_007 | Verify the one-bot policy allows a start when the user's only prior bot is closed | The user has an existing `bots` row with `lifecycle_state='closed'` (terminal state, not in the blocking list). | Submit a start request for the same user. | 1. The one-bot policy check passes; the Worker proceeds to the margin check; no `E-EXBOT-001` is returned. (Reference: BR-EXBOT-001.) | P1 |
| TC_008 | Verify the start is blocked when the user's HL margin is below the required margin (BVA — just below threshold) | 1. The one-bot policy check passes. 2. The user's HL isolated margin balance is just below the required margin including the 2.0x safety buffer — by the smallest representable unit. (Example: required × 2.0 = threshold; balance = threshold − 1 unit.) | Submit a start request when the user's balance is one unit below the required threshold. | 1. The start is blocked (`E-EXBOT-002`, HTTP 400); no `bots` row is created. The message tells the user how much more to deposit. (Reference: FR-EXBOT-061.) | P0 |
| TC_009 | Verify the start is allowed when the user's HL margin equals exactly the required threshold (BVA — exact boundary) | 1. The one-bot policy check passes. 2. The user's HL isolated margin balance equals exactly the required margin including the 2.0x safety buffer. (Example: balance = required × 2.0 exactly.) | Submit a start request when the user's balance is at the exact boundary. | 1. The margin check passes; no `E-EXBOT-002` is returned; the Worker proceeds to the agent key check. (Reference: FR-EXBOT-061.) | P0 |
| TC_010 | Verify the margin comparison uses BigDecimal arithmetic, not floating-point, to avoid rounding errors | 1. The one-bot policy check passes. 2. The required margin value is chosen so that float(required × 2.0) differs from BigDecimal(required × 2.0) due to binary representation error. (Example: a value with many significant decimal digits.) | Submit a start request with this margin value and observe the Worker's comparison outcome. | 1. The Worker uses BigDecimal for the margin comparison; the pass/fail decision matches the mathematically exact result. A float-based comparison would produce a different (wrong) outcome, but BigDecimal does not. (Reference: NFR-EXBOT-008.) | P1 |
| TC_011 | Verify the start is allowed when the user's HL agent key is approved and not expired | 1. The one-bot policy and margin checks pass. 2. The user's HL agent key is approved and not expired (`hl_agent_keys.status='approved'` and `expiry_at` is in the future). | Submit a start request with an approved, valid agent key. | 1. The agent key check passes; no `E-EXBOT-003` or `E-EXBOT-004` is returned; the Worker proceeds to the builder fee check. (Reference: FR-EXBOT-081.) | P1 |
| TC_012 | Verify the start is blocked when the agent key is still waiting for approval | 1. The one-bot policy and margin checks pass. 2. The user's HL agent key is still waiting for approval (`hl_agent_keys.status='pending'`). | Submit a start request while the agent key is pending. | 1. The start is blocked (`E-EXBOT-003`, message "Agent key is awaiting approval. Please complete the approval process before starting."); no `bots` row is created. Note [Issue I-02]: UC A9 maps E-EXBOT-003 to expired key incorrectly. (Reference: FR-EXBOT-081, srs/spec.md §5.) | P0 |
| TC_013 | Verify the start is blocked when the agent key has expired | 1. The one-bot policy and margin checks pass. 2. The user's HL agent key has expired (`hl_agent_keys.status='expired'` or `expiry_at < now`). | Submit a start request with an expired agent key. | 1. The start is blocked (`E-EXBOT-004`); no `bots` row is created. Note [Issue I-02]: UC A9 incorrectly cites E-EXBOT-003 for this path. (Reference: FR-EXBOT-081, srs/spec.md §5.) | P0 |
| TC_014 | Verify the start is blocked when the agent key has been revoked | 1. The one-bot policy and margin checks pass. 2. The user's HL agent key has been revoked (`hl_agent_keys.status='revoked'`). | Submit a start request with a revoked agent key. | 1. The start is blocked with an appropriate error code (exact code pending BA/Dev confirmation — spec §5 does not define a separate `revoked` error); no `bots` row is created. | P0 |
| TC_015 | Verify the start is blocked when no agent key exists for the user | 1. The one-bot policy and margin checks pass. 2. No `hl_agent_keys` row exists for the user. | Submit a start request with no agent key registered. | 1. The start is blocked with an error indicating no agent key is registered; no `bots` row is created. | P1 |
| TC_016 | Verify the start is blocked when the HL builder fee (5bps) is not confirmed | 1. The one-bot policy, margin, and agent key checks pass. 2. The user's HL account does not have the 5bps builder fee confirmed. | Submit a start request without the builder fee confirmed. | 1. The start is blocked (`E-EXBOT-005`, message "HL builder fee (5bps) approval required before starting ExBot."); no `bots` row is created. Note [Issue I-04]: UC A5 uses different text. (Reference: srs/spec.md §5.) | P0 |
| TC_017 | Verify the start is allowed when the HL builder fee (5bps) is confirmed | 1. The one-bot policy, margin, and agent key checks pass. 2. The user's HL account has the 5bps builder fee confirmed. | Submit a start request with the builder fee confirmed. | 1. The builder fee check passes; no `E-EXBOT-005` is returned; the Worker proceeds to the LP mint simulation. (Reference: FR-EXBOT-002.) | P1 |
| TC_018 | Verify the start is blocked when the LP mint simulation fails | 1. All four prior preflight checks (one-bot, margin, agent key, builder fee) pass. 2. The LP mint simulation inputs will cause the simulation to fail (e.g. the tick range is outside the valid Uniswap V3 range). | Submit a start request with LP parameters that cause the simulation to fail. | 1. The simulation fails and the start is blocked (`E-EXBOT-006` or equivalent); no `bots` row is created; no on-chain transaction is issued during this step (simulation only). (Reference: FR-EXBOT-002 step 5.) | P0 |
| TC_019 | Verify all five preflight checks pass and the bot record is created at the preflight stage | All five preflight conditions are met: no active bot, sufficient HL margin, agent key approved, builder fee confirmed, LP mint simulation passes. | 1. Submit a valid start request with all conditions met. 2. Read the `bots` row after the Worker completes all preflight checks. | 1. All five checks pass; no preflight error code is returned. 2. A `bots` row is created with `lifecycle_state='preflight'`; no on-chain transaction has been issued yet. (Reference: FR-EXBOT-001, FR-EXBOT-002.) | P0 |

### I.2. Integration & State verification — Operation: Bot start (preflight checks)

| TC ID | Title | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|
| TC_020 | Verify the preflight checks run in the declared order and stop at the first failure | 1. The margin check is set to fail (margin below threshold). 2. The agent key is pending (would also fail if reached). | Submit a start request and observe which error code is returned. | 1. `E-EXBOT-002` (margin) is returned; `E-EXBOT-003` (agent key) is NOT returned. This confirms the checks run in order (one-bot → margin → agent key → builder fee → LP sim) and stop at the first failure. (Reference: FR-EXBOT-002.) | P0 |

### I.3. Non-functional (logic) verification — Operation: Bot start (preflight checks)

| TC ID | Title | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|
| TC_021 | Verify the five preflight checks complete within the latency budget under normal conditions | 1. All five preflight conditions are set to pass. 2. HL API is responding within normal latency. | Submit a start request and measure the time from request receipt to the `bots` row being written with `lifecycle_state='preflight'`. | 1. All five checks complete and the `bots` row is persisted within the latency budget defined in NFR-EXBOT-002. (Reference: NFR-EXBOT-002.) | P1 |

## II. Operation: Bot initialization sequence (state machine)

### II.1. Functional verification — Operation: Bot initialization sequence (state machine)

| TC ID | Title | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|
| TC_022 | Verify the LP mint state is written to D1 before the on-chain transaction is sent | The `bots` row is at `lifecycle_state='preflight'`; all preflight checks have passed. | 1. Observe the sequence of D1 writes vs. on-chain transaction submissions during the LP mint step. 2. Read the D1 `bots.lifecycle_state` immediately before `vaultMint` is submitted. | 2. `bots.lifecycle_state='lp_opening'` is persisted to D1 before the on-chain `vaultMint` transaction is submitted (write-before-call ordering). (Reference: FR-EXBOT-003.) | P0 |
| TC_023 | Verify a successful on-chain LP mint produces the correct on-chain event and D1 records | The `bots` row is at `lifecycle_state='lp_opening'`; the `BnzaExVault.vaultMint` call is configured to succeed on the test chain. | 1. Submit the `vaultMint` transaction on-chain. 2. Confirm the `VaultMinted` event is emitted; let the Worker process the event and read D1. | 1. On-chain: `VaultMinted` event is emitted with the correct `botId`, `tokenId`, and minted token amounts. 2. Off-chain: `bots.lifecycle_state='lp_opened'`; a `positions` row is inserted with the correct `tokenId`, `bot_id`, and amounts; the `positions.bot_id` UNIQUE constraint holds. (Reference: FR-EXBOT-004, FR-EXBOT-020.) | P0 |
| TC_024 | Verify an on-chain LP mint revert moves the bot to the error state | The `bots` row is at `lifecycle_state='lp_opening'`; the `vaultMint` transaction is configured to revert (e.g. insufficient ERC-20 allowance). | 1. Submit the `vaultMint` transaction; it reverts. 2. Observe the Worker's state transition. | 1. On-chain: the transaction is reverted; no `VaultMinted` event is emitted. 2. Off-chain: `bots.lifecycle_state='error'`; no `positions` row is created. (Reference: FR-EXBOT-004.) | P0 |
| TC_025 | Verify the hedge-open state is written to D1 before calling Hyperliquid | The `bots` row is at `lifecycle_state='lp_opened'`. | 1. Observe the sequence of D1 writes vs. HL API calls during the hedge open step. 2. Read `bots.lifecycle_state` immediately before the outbound HL IOC order call is issued. | 2. `bots.lifecycle_state='hedge_pre_open'` is persisted to D1 before the outbound HL API call is made (write-before-call ordering). (Reference: FR-EXBOT-003, srs/states.md.) | P0 |
| TC_026 | Verify a successful HL IOC fill within tolerance produces the hedge-confirmed state and records | The `bots` row is at `lifecycle_state='hedge_pre_open'`; HL API is available; the IOC order will fill fully; the reconcile result is within the allowed tolerance. | 1. Let the Worker submit the short IOC order to HL. 2. Let the Worker read the fill result, reconcile `actual_short_size` vs `targetShortEth`, and write D1. | 1. `bots.lifecycle_state='hedge_post_confirmed'`; a `hedge_legs` row is created with the correct `short_size`, `entry_price`, and `bot_id`; the `hedge_legs.bot_id` UNIQUE constraint holds; `actual_short_size` is within tolerance of `targetShortEth`. (Reference: FR-EXBOT-030.) | P0 |
| TC_027 | Verify an HL IOC rejection moves the bot to safe mode, not to error | The `bots` row is at `lifecycle_state='hedge_pre_open'`; HL will reject the IOC order (e.g. price slipped beyond limit). | 1. Let the Worker submit the IOC; HL returns a rejection. 2. Observe the Worker's state transition. | 1. `bots.lifecycle_state='safe_mode'`; no confirmed `hedge_legs` row is created. Note [I-01 — SRS overrides UC]: UC A10 states `error`; SRS maps this to `safe_mode`. (Reference: srs/states.md, FR-EXBOT-050.) | P0 |
| TC_028 | Verify an HL API outage during hedge open moves the bot to safe mode, not to error | The `bots` row is at `lifecycle_state='hedge_pre_open'`; HL API is unreachable (simulated timeout or HTTP 503). | 1. Let the Worker attempt the IOC order; the HL call times out or returns 503. 2. Observe the Worker's state transition. | 1. `bots.lifecycle_state='safe_mode'`; no partial `hedge_legs` row is created. Note [I-01 — SRS overrides UC]: UC A7 states `error`; SRS maps this to `safe_mode`. (Reference: srs/states.md, FR-EXBOT-050.) | P0 |
| TC_029 | Verify a reconcile mismatch beyond tolerance after hedge open moves the bot to safe mode | The `bots` row is at `hedge_pre_open`; the IOC order partially fills; `actual_short_size` deviates from `targetShortEth` beyond the allowed tolerance threshold. | 1. Let the Worker read the fill result; `actual_short_size` is outside the tolerance band. 2. Observe the Worker's state transition. | 1. `bots.lifecycle_state='safe_mode'`; no confirmed `hedge_legs` size is recorded. Note [I-01 — SRS overrides UC]: UC A11 states `error`; SRS maps this to `safe_mode`. (Reference: srs/states.md, FR-EXBOT-050.) | P0 |
| TC_030 | Verify the hedge short size uses hedgeRatio = 0.70 (Phase A) and BigDecimal arithmetic | The LP ETH exposure for the position is known; Phase A `hedgeRatio` is configured as 0.70 per SRS §3.7. | 1. Verify `targetShortEth = LP_ETH_exposure × 0.70`. 2. Confirm the value stored in D1 and passed to HL is computed with BigDecimal, not float. | 1. `targetShortEth` equals `LP_ETH_exposure × 0.70` with BigDecimal precision; the stored value uses the correct decimal representation; no float arithmetic is used. (Reference: NFR-EXBOT-008, SRS §3.7.) | P0 |
| TC_031 | Verify the stop-placement state and cloid are written to D1 before calling Hyperliquid | The `bots` row is at `lifecycle_state='hedge_post_confirmed'`; the `cloid` has been computed. | 1. Observe the sequence of D1 writes vs. HL API calls during the stop placement step. 2. Read `bots.lifecycle_state` and the stored `cloid` immediately before `placeReduceOnlyStopMarket` is called. | 2. `bots.lifecycle_state='stop_placing'` and the `cloid` are persisted to D1 before the outbound `placeReduceOnlyStopMarket` call is made. (Reference: FR-EXBOT-031, FR-EXBOT-003.) | P0 |
| TC_032 | Verify the stop-order cloid matches the deterministic keccak256 formula | The `botId`, `attemptId`, `stage`, and `version` values used for stop placement are known. | 1. Read the `cloid` stored in D1 after the stop placement step. 2. Independently compute `first128BitsHex(keccak256("bnza:{botId}:{attemptId}:{stage}:{version}"))` with the same inputs. | 1. The stored `cloid` equals the independently computed hash; the cloid is deterministic (same inputs always produce the same output); no random generation is used. (Reference: FR-EXBOT-031.) | P0 |
| TC_033 | Verify a confirmed HL stop order moves the bot to the stop-verified state | The `bots` row is at `lifecycle_state='stop_placing'`; HL confirms the `placeReduceOnlyStopMarket` order. | 1. Let the Worker submit `placeReduceOnlyStopMarket` with the computed `cloid` and stop price. HL confirms the order. 2. Read D1. | 1. `bots.lifecycle_state='stop_verified'`; stop order details (order ID, stop price, `cloid`) are persisted in D1. (Reference: FR-EXBOT-031.) | P0 |
| TC_034 | Verify a failed HL stop placement moves the bot to safe mode, not to error | The `bots` row is at `lifecycle_state='stop_placing'`; HL rejects or does not respond to `placeReduceOnlyStopMarket`. | 1. Let the Worker submit `placeReduceOnlyStopMarket`; HL rejects the call or the call times out. 2. Observe the Worker's state transition. | 1. `bots.lifecycle_state='safe_mode'`; the stop order is not confirmed. Note [I-01 — SRS overrides UC]: UC A8 states `error`; SRS maps this to `safe_mode`. (Reference: srs/states.md, FR-EXBOT-050.) | P0 |
| TC_035 | Verify the stop price uses stopSafetyFactor = 0.70 (Phase A) and BigDecimal arithmetic | The `entry_price` from the hedge fill is known; Phase A `stopSafetyFactor` is configured as 0.70 per SRS §3.10. | 1. Verify `stop_price = entry_price × (1 − 0.70) = entry_price × 0.30`. 2. Confirm the value submitted to HL and stored in D1 is computed with BigDecimal. | 1. `stop_price` equals `entry_price × 0.30` with BigDecimal precision; no float arithmetic is used; the value submitted to HL matches the D1-stored value. (Reference: NFR-EXBOT-008, SRS §3.10.) | P0 |
| TC_036 | Verify the bot moves from stop-verified to the active state | The `bots` row is at `lifecycle_state='stop_verified'`. | Let the Worker process the stop verification and write the final state. | 1. `bots.lifecycle_state='active'`; the transition is persisted to D1. (Reference: FR-EXBOT-003.) | P0 |
| TC_037 | Verify the complete eight-stage initialization sequence runs in the correct order and ends active | A fresh user with no existing bot; all five preflight conditions pass; on-chain `vaultMint` succeeds; the HL IOC fills fully within tolerance; the stop placement is confirmed. | 1. Submit a valid start request and let the whole initialization sequence run. 2. Read `bots.lifecycle_state` after each D1 write to capture the full sequence. | 1. States are written in this exact order: `preflight → lp_opening → lp_opened → hedge_pre_open → hedge_post_confirmed → stop_placing → stop_verified → active`. The final `lifecycle_state='active'`. On-chain: `VaultMinted` emitted; HL short and stop confirmed. Off-chain: `positions` and `hedge_legs` rows created with correct values. (Reference: FR-EXBOT-003, srs/states.md.) | P0 |

### II.2. Integration & State verification — Operation: Bot initialization sequence (state machine)

| TC ID | Title | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|
| TC_038 | Verify a duplicate start request during initialization is rejected by the one-bot policy | A bot is already at `lifecycle_state='lp_opening'` (initialization is in progress for the user). | Submit another start request for the same user while the first initialization is still running. | 1. The second request is rejected (`E-EXBOT-001`); no duplicate `bots` row is created; no duplicate on-chain `vaultMint` call is triggered. (Reference: BR-EXBOT-001.) | P0 |
| TC_039 | Verify a retried stop placement with the same cloid does not create a duplicate stop order on Hyperliquid | The first `placeReduceOnlyStopMarket` call succeeded on HL; the Worker retries because of timeout uncertainty. | 1. The Worker sends a second `placeReduceOnlyStopMarket` call with the same `cloid`. 2. Observe the HL response and the final D1 state. | 1. HL deduplicates on `cloid`; no duplicate stop order is created on HL; `bots.lifecycle_state='stop_verified'` is consistent; the `queue_idempotency` record prevents double-processing. (Reference: FR-EXBOT-031.) | P0 |
| TC_040 | Verify HL API calls during initialization are routed through HLRateLimitDO and stay within the rate limit | Multiple concurrent start flows are active for different users; `HLRateLimitDO` monitoring is enabled. | 1. Trigger multiple concurrent start requests for different users. 2. Monitor the aggregate HL outbound call weight through `HLRateLimitDO`. | 1. Aggregate HL API call weight stays at or below 800 weight/min; calls queue or throttle via `HLRateLimitDO` when the limit is approached; no HTTP 429 errors are returned by HL due to a rate-limit breach. (Reference: NFR-EXBOT-005.) | P1 |

### II.3. Non-functional (logic) verification — Operation: Bot initialization sequence (state machine)

| TC ID | Title | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|
| TC_041 | Verify the full start-to-active initialization completes within the latency target | All external calls succeed; network conditions are normal; HL API responds within normal latency. | Submit a valid start request and measure the elapsed time from request receipt to `lifecycle_state='active'` being written. | 1. The complete initialization (from preflight to `active`) finishes within the latency budget defined in NFR-EXBOT-002. (Reference: NFR-EXBOT-002.) | P1 |

## III. Operation: Post-active initialization

### III.1. Functional verification — Operation: Post-active initialization

| TC ID | Title | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|
| TC_042 | Verify the light-check schedule is created with the correct time window after the bot goes active | The `bots` row has just transitioned to `lifecycle_state='active'` for the bot. | 1. Read the `bot_runtime_state` row for the bot immediately after activation. 2. Compute the expected window: `[activation_time + 4min15s, activation_time + 5min45s]`. | 1. A `bot_runtime_state` row exists; `next_light_check_at` falls within the expected window (5 min + jitter ± 45s); the value is stored as an integer timestamp (not float). (Reference: FR-EXBOT-013.) | P0 |
| TC_043 | Verify the last-known HL short size is saved from the reconcile result when the bot goes active | The `hedge_post_confirmed` reconcile step confirmed the actual short size; `lifecycle_state='active'`. | Read `bot_runtime_state.last_known_hl_short_size` for the bot. | 1. `last_known_hl_short_size` equals the reconciled actual short size from `hedge_post_confirmed`; the value is not null and matches the `hedge_legs` entry. (Reference: FR-EXBOT-013.) | P0 |
| TC_044 | Verify the circuit breaker is created in the closed state when the bot goes active | The `bots` row has just transitioned to `lifecycle_state='active'`. | Read the `circuit_breakers` row for the bot immediately after activation. | 1. A `circuit_breakers` row exists; `state='closed'`; all counter fields are at initial values defined in SRS. (Reference: FR-EXBOT-040.) | P0 |
| TC_045 | Verify a bot with no runtime state row is not scheduled for light-check monitoring | Simulated failure: the `bot_runtime_state` row was not written after the bot reached `lifecycle_state='active'` (e.g. a D1 write error). | 1. Allow the light-check cron to run. 2. Observe whether the bot is picked up by the scheduler. | 1. The light-check cron does not find the bot in the schedule (no `next_light_check_at` entry); bot monitoring does not start. This confirms `bot_runtime_state` is a hard prerequisite for scheduling light-check. (Reference: FR-EXBOT-013.) | P1 |

### III.2. Integration & State verification — Operation: Post-active initialization

| TC ID | Title | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|
| TC_046 | Verify the user lock is released after the bot reaches the active state | The `UserLockDO` lease was acquired at the start of initialization (90-second lease); the bot has reached `lifecycle_state='active'`. | 1. Verify the `UserLockDO` lease is released after `lifecycle_state='active'` is written. 2. Submit another start request for the same user (expected to be blocked by one-bot policy, not by a lock timeout). | 1. The `UserLockDO` lease is released; the second request is evaluated against the one-bot policy (`E-EXBOT-001`) and is not blocked by a lingering lock. (Reference: FR-EXBOT-002.) | P1 |

## IV. Operation: Agent key security

### IV.1. Functional verification — Operation: Agent key security

| TC ID | Title | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|
| TC_047 | Verify the agent private key is decrypted in memory at hedge open and never stored as plaintext | The `hl_agent_keys` row contains the encrypted key fields (`encrypted_secret`, `wrapped_dek`, `secret_iv`, `secret_auth_tag`, `dek_iv`); the Worker is about to execute the hedge open step. | Let the hedge open step run; inspect the `hl_agent_keys` table and D1 before and after signing. | 1. The Worker decrypts the key using AES-256-GCM envelope scheme at the moment of signing; the plaintext key is used only in memory; it is not persisted to D1, not written to any log, and not returned in any response. (Reference: FR-EXBOT-080, AC-12.) | P0 |
| TC_048 | Verify the agent key plaintext does not appear in any log or API response across all exit paths | Full log capture is enabled; the test covers the happy path, one `safe_mode` path (HL unreachable), and one `error` path (on-chain revert). | 1. Execute the happy path end-to-end. 2. Execute the safe mode path (A7 — HL unreachable → `safe_mode`). 3. Execute the error path (`vaultMint` revert → `error`). 4. Collect all Worker log output and all HTTP response bodies. | 1–3. No log line or response body contains the plaintext agent private key, any partial key material, the `encrypted_secret` value, or the `wrapped_dek` value. Encrypted field names may appear in log context, but their values must not. (Reference: FR-EXBOT-080, AC-12.) | P0 |

### IV.2. Integration & State verification — Operation: Agent key security

| TC ID | Title | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|
| TC_049 | Verify HL agent key sensitive fields are not included in any API response payload | The standard bot-start happy path is configured to run. | Capture all HTTP responses from the ExBot Worker during the full start sequence. | 1. No response payload includes the fields `encrypted_secret`, `wrapped_dek`, `secret_iv`, `secret_auth_tag`, or `dek_iv`; these fields remain internal to the Worker. (Reference: FR-EXBOT-080.) | P0 |

## V. Operation: Data integrity

### V.1. Functional verification — Operation: Data integrity

| TC ID | Title | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|
| TC_050 | Verify a duplicate LP position insert for the same bot is blocked by the UNIQUE constraint | A `positions` row already exists for the bot (created after a successful LP mint). | Simulate a retry or duplicate event that causes the Worker to attempt a second `INSERT` into `positions` with the same `bot_id`. | 1. The D1 UNIQUE constraint on `positions.bot_id` raises a violation; no duplicate `positions` row is created; the Worker handles the constraint error without silently continuing with corrupted state. (Reference: srs/erd.md.) | P0 |
| TC_051 | Verify a duplicate hedge-legs insert for the same bot is blocked by the UNIQUE constraint | A `hedge_legs` row already exists for the bot (created after `hedge_post_confirmed`). | Simulate a retry that causes the Worker to attempt a second `INSERT` into `hedge_legs` with the same `bot_id`. | 1. The D1 UNIQUE constraint on `hedge_legs.bot_id` raises a violation; no duplicate `hedge_legs` row is created; the Worker handles the error correctly. (Reference: srs/erd.md.) | P0 |
| TC_052 | Verify no floating-point values are stored in D1 financial fields after a full start sequence | A full happy-path run has completed up to `lifecycle_state='active'`; all D1 writes for the bot have been made. | 1. Read all financial fields from the `positions`, `hedge_legs`, `bot_runtime_state`, and `bots` rows for the bot. 2. Inspect the stored column types and values. | 1. All financial fields (amounts, prices, sizes, ratios, thresholds) are stored as `TEXT` (BigDecimal string) or `INTEGER` (scaled integer); no `REAL`/`FLOAT` column type is used for any financial data. (Reference: NFR-EXBOT-008.) | P0 |

### V.2. Integration & State verification — Operation: Data integrity

| TC ID | Title | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|
| TC_053 | Verify the idempotency record is created before stop placement and prevents double-submission | The bot is at the stop placement step; the `cloid` has been computed. | 1. Verify that a `queue_idempotency` row is created with the `cloid` as the key before the outbound HL call. 2. Simulate a re-submission of the same stop placement request (same `cloid`). 3. Observe the outcome. | 1. The `queue_idempotency` row exists with the `cloid` key before the HL call. 2–3. The re-submission returns the cached result from the prior call; no second `placeReduceOnlyStopMarket` call is issued to HL. (Reference: FR-EXBOT-031.) | P0 |

## VI. Operation: Business rules and access control

### VI.1. Functional verification — Operation: Business rules and access control

| TC ID | Title | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|
| TC_054 | Verify the ExBot Worker is deployed as a separate Worker from the OPERATOR Facade | Deployment configuration for the ExBot Worker and OPERATOR Facade is accessible. | Inspect the deployment configuration and verify that the ExBot Worker is a separate Cloudflare Worker from OPERATOR; confirm it does not expose public HTTP routes. | 1. The ExBot Worker and OPERATOR Facade are separate deployed Workers; ExBot does not have public HTTP routes; all requests arrive via service binding from OPERATOR. (Reference: BR-EXBOT-010.) | P1 |

### VI.2. Integration & State verification — Operation: Business rules and access control

| TC ID | Title | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|
| TC_055 | Verify a direct HTTP call to the ExBot Worker that bypasses the OPERATOR binding is rejected | A direct HTTP access path to the ExBot Worker (bypassing the OPERATOR service binding) is available for testing. | Send a `POST` directly to the ExBot Worker endpoint without a valid OPERATOR service binding token. | 1. The request is rejected with an authentication or binding failure; the ExBot Worker does not process bot-start logic; no `bots` row is created. (Reference: BR-EXBOT-010.) | P0 |

## VII. Operation: Non-functional requirements (system-level)

### VII.1. Non-functional (logic) verification — Operation: Non-functional requirements (system-level)

| TC ID | Title | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|
| TC_056 | Verify concurrent start requests for different users are processed without data corruption | Multiple concurrent `POST /api/exbot/start` requests are ready for different users; the target concurrency level matches NFR-EXBOT-001. | 1. Submit all requests simultaneously. 2. Monitor D1 writes for all bots. | 1. All requests are processed correctly; no `bots.bot_id` collision occurs; `positions.bot_id` and `hedge_legs.bot_id` UNIQUE constraints hold under concurrency; no cross-contamination of state between concurrent bots. (Reference: NFR-EXBOT-001.) | P1 |
| TC_057 | Verify the bot's D1 state survives a Cloudflare Worker restart mid-initialization | The bot is at `lifecycle_state='hedge_pre_open'` (state persisted to D1); a Worker instance restart occurs. | 1. Confirm `lifecycle_state='hedge_pre_open'` is in D1 before the restart. 2. Restart the Worker instance (or switch to a new instance). 3. Let the new Worker instance read D1. | 1. The D1-persisted `lifecycle_state='hedge_pre_open'` is readable by the new Worker instance; the Worker can resume from the correct state, or the light-check cron can detect the stuck intermediate state; no state corruption occurs. | P1 |
| TC_058 | Verify no sensitive data (agent key, DEK) appears in Cloudflare Worker structured logs | Structured log capture is enabled; the test covers a happy path run and one `safe_mode` path (HL unreachable). | 1. Execute the full happy path. 2. Execute the A7 path (HL unreachable → `safe_mode`). 3. Collect all Cloudflare Worker structured log output. | 1–2. Logs contain only operational data (`botId`, `lifecycle_state`, timestamps, error codes, step identifiers); no key material, `wrapped_dek` values, or `encrypted_secret` values appear in any log field or message. (Reference: FR-EXBOT-080, AC-12.) | P0 |

