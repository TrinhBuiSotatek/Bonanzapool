# Test Cases — UC-EXBOT-bot-start Start ExBot

**Document Title:** Test Cases — UC-EXBOT-bot-start Start ExBot (v7)
**Date Created:** 2026-07-09
**Author / Agent:** qc-func-tc-design-exbot
**Version:** v7

---

**Total test cases:** 58 (FUNC: 42, INTG: 9, NFR: 7)
**Scope:** Logic-only (backend / API / bot — no UI)
**Update trigger:** REQUIREMENT_DELTA (UC updated 2026-07-09: v5 audit — IS-01/IS-03 resolved, E-EXBOT-017 message updated, AC-06 through AC-15 added, V5-05 last_known_hl_short_size gap)
**Source UC:** UC-EXBOT-bot-start_bot-start_audited_20260709_v5.md
**Source scenarios (if any):** UC-EXBOT-bot-start_bot-start_scenarios_20260709_v5.md
**Output language:** English

#### Requirement Traceability Matrix

| AC ID | Acceptance Criteria | Linked Test Cases | Status |
|---|---|---|---|
| AC-01 | Happy path — all 9 lifecycle states in sequence; hedge + stop populated; last_known_hl_short_size non-NULL | TC_001, TC_002, TC_024, TC_025, TC_032, TC_039, TC_041, TC_042, TC_057 | Covered |
| AC-02 | One-bot policy blocks start when active | TC_008, TC_009 | Covered |
| AC-03 | One-bot policy blocks when safe_mode | TC_009 | Covered |
| AC-04 | Vault balance = 0 — E-EXBOT-025 blocks start | TC_011, TC_012 | Covered |
| AC-05 | Margin insufficient — exact shortfall in message | TC_014, TC_015 | Covered |
| AC-06 | key_status='provisioning' blocks start — E-EXBOT-017 | TC_017, TC_049 | Covered |
| AC-07 | Margin 2.0x threshold exact boundary | TC_013, TC_014, TC_015 | Covered |
| AC-08 | KMS key — private key never in DB or logs | TC_007, TC_047 | Covered |
| AC-09 | LP mint revert/timeout → lifecycle_state='error'; E-EXBOT-028; no funds moved | TC_026, TC_050, TC_051 | Covered |
| AC-10 | HL unreachable at hedge → safe_mode + auto-recovery; E-EXBOT-008 | TC_030, TC_052, TC_054 | Covered |
| AC-11 | HL reject IOC → safe_mode + E-EXBOT-026 | TC_031, TC_053, TC_054 | Covered |
| AC-12 | Stop fail → safe_mode + E-EXBOT-009 | TC_040, TC_055 | Covered |
| AC-13 | Preflight sequential — vault fail blocks key check | TC_021, TC_056 | Covered |
| AC-14 | No partial bot record on any preflight fail | TC_021 | Covered |
| AC-15 | stop_price BigDecimal safetyFactor=0.70; stop < liq_price | TC_037, TC_042, TC_058 | Covered |

---


## I. Operation: Key Provision (chain indexer to KMS to HL approveAgent)


### I.1. Functional verification — Operation: Key Provision

| TC ID | Test Title/Summary of test cases | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|
| TC_001 | Verify key-provision creates master key and agent key in KMS with only public addresses stored | A new on-chain deposit event has been detected by Chain Indexer for a user with no existing `hl_agent_keys` row.\nAWS KMS is available and responsive. | Process the `key-provision` queue message for the user; verify both KMS `GenerateKeyPair` calls complete successfully. | KMS returns two public addresses (`hl_user_address` for master key, `agent_address` for agent key). A row is inserted into `hl_agent_keys` containing only public addresses and metadata — no private key material is present in any DB field, response payload, or application log. (Reference: FR-EXBOT-080, NFR-EXBOT-006.) | P0 |
| TC_002 | Verify key_status transitions from provisioning to active only after HL approveAgent confirms the agent registration | KMS has generated both key pairs successfully; a row exists in `hl_agent_keys` with `key_status='provisioning'`.\nHL `approveAgent` call is in progress (pending HL confirmation). | Check `hl_agent_keys.key_status` before HL confirmation arrives; then let HL confirm `approveAgent` and check again. | Before HL confirmation: `key_status='provisioning'` (the recognized 4th state — BA confirmed I-17; all 4 states: provisioning / active / superseded / revoked). After HL confirms `approveAgent`: `key_status='active'` is written atomically to Aurora PostgreSQL and a `bot-start` job is enqueued. (Reference: FR-EXBOT-080, SRS F-03a, I-17 resolved.) | P0 |
| TC_003 | Verify KMS failure during key generation retries up to the limit and sends an admin alert after final failure | KMS is mocked to return `ThrottlingException` (or equivalent error) on every `GenerateKeyPair` call. | Trigger key-provision processing; count the number of KMS retry attempts before the worker gives up. | The worker retries up to the configured maximum (not fewer, not more). After the final failure: no `hl_agent_keys` row with `key_status='active'` is created; an admin alert is triggered; no `bot-start` job is enqueued. (Example: max retries = 3.) (Reference: FR-EXBOT-080, IC-EXBOT-005.) | P0 |
| TC_004 | Verify HL approveAgent failure keeps key_status as provisioning and triggers retry | KMS has generated both key pairs; `hl_user_address` and `agent_address` are available; `key_status='provisioning'` is set in `hl_agent_keys`.\nHL `approveAgent` is mocked to return an error. | Let Key-Provision Worker call HL `approveAgent` while it is set to fail; then check the DB row and the queue. | `hl_agent_keys.key_status` remains `'provisioning'` (does not become `active`); the worker re-enqueues a retry via the `key-provision` queue; no `bot-start` job is enqueued until `key_status='active'` is confirmed. An admin alert is sent when the SLA wait time is exceeded. `provisioning` is the official 4th state (BA confirmed I-17 — all 4 states: provisioning / active / superseded / revoked). (Reference: FR-EXBOT-080, I-17 resolved.) | P0 |
| TC_005 | Verify duplicate key-provision queue message does not provision a second key pair | A `key-provision` message has already been processed once and `key_status='active'` is set.\nThe same message is redelivered (SQS at-least-once semantics). | Deliver the same `key-provision` message a second time with the same `message_id`. | The second delivery encounters a UNIQUE constraint conflict on `queue_idempotency.message_id` and exits immediately without calling KMS again; only one `hl_agent_keys` row with `key_status='active'` exists; no duplicate `bot-start` job is enqueued. (Reference: FR-EXBOT-011, BR-EXBOT-012.) | P0 |

### I.2. Integration & State verification — Operation: Key Provision

| TC ID | Test Title/Summary of test cases | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|
| TC_006 | Verify only one active agent key row exists per user at any time | A user already has one `hl_agent_keys` row with `key_status='active'`. | Attempt to insert a second `hl_agent_keys` row with `key_status='active'` for the same user (simulating a race condition or misconfigured re-provisioning). | The DB-level constraint rejects the second insert (BR-EXBOT-012: only one `active` row per user at any time); the existing active row remains intact; no new row with `active` status is committed. (Reference: BR-EXBOT-012, FR-EXBOT-080.) | P0 |

### I.3. Non-functional (logic) verification — Operation: Key Provision

| TC ID | Test Title/Summary of test cases | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|
| TC_007 | Verify private key material never appears in DB, logs, or response payloads after key provisioning | Key-provision flow has completed successfully; `hl_agent_keys.key_status='active'`. | Dump the `hl_agent_keys` table from Aurora PostgreSQL; search application logs for the time window of the provisioning run; inspect any API response bodies from Key-Provision Worker. | No hex-encoded or base64-encoded 256-bit/512-bit private key pattern appears in any Aurora PostgreSQL field, log line, or response payload. Only `hl_user_address`, `agent_address`, and metadata (status, timestamps) are stored in plaintext. Private keys remain exclusively in AWS KMS HSM. (Reference: FR-EXBOT-080, NFR-EXBOT-006.) | P0 |

## II. Operation: Bot Start Preflight


### II.1. Functional verification — Operation: Bot Start Preflight

| TC ID | Test Title/Summary of test cases | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|
| TC_008 | Verify the one-bot policy rejects a second start when the user already has a running bot | The user already has an ExBot with `status='active'` in `bot_registry`. | Process a `bot-start` job for the same user while the existing bot is still active. | The start is rejected with `E-EXBOT-001` ("You already have an active ExBot. Close or wait for the existing bot to finish.", HTTP 409). No new row is added to `bot_registry` or `bots`. `bot_registry` count for the user remains 1. (Reference: FR-EXBOT-001, BR-EXBOT-001.) | P0 |
| TC_009 | Verify the one-bot policy counts paused, closing, safe_mode, and error statuses toward the limit | The user has one existing bot — this test is run four times with the bot set to each disqualifying status in turn: `paused`, `closing`, `safe_mode`, `error`. | Process a `bot-start` job for the user each time the existing bot is in one of the four statuses above. | In each of the four cases the start is rejected with `E-EXBOT-001` (HTTP 409) and no new bot record is created. All four statuses count toward the one-bot limit per BR-EXBOT-001. (Reference: FR-EXBOT-001, BR-EXBOT-001.) | P0 |
| TC_010 | Verify a new start is allowed when the user's only existing bot has a closed status | The user has one existing bot with `status='closed'`. | Process a `bot-start` job for the user. | Preflight step 1 passes (the count of disqualifying statuses = 0); the flow proceeds to preflight step 2 (vault balance check). A `closed` bot does not block a new start per BR-EXBOT-001. (Reference: FR-EXBOT-001, BR-EXBOT-001.) | P0 |
| TC_011 | Verify the start is blocked when the user has no confirmed on-chain deposit in the vault | Preflight step 1 (one-bot policy) passes — the user has no active bot.\nThe user's `BnzaExVault` on-chain balance is zero (no deposit has been confirmed on-chain for this user). | Process the `bot-start` preflight for the user while the vault balance is zero. | Preflight step 2 fails with `E-EXBOT-025` ("No confirmed deposit found. Please complete an on-chain deposit before starting the bot.", HTTP 400). No bot record is created in Aurora PostgreSQL. The flow does not proceed to step 3 (margin check). (Reference: FR-EXBOT-002 step 2, E-EXBOT-025.) | P0 |
| TC_012 | Verify the vault balance check passes and the flow proceeds to the margin check when a deposit is confirmed | Preflight step 1 passes.\nThe user has a confirmed on-chain deposit: `BnzaExVault` balance is greater than zero. | Process the `bot-start` preflight with a non-zero vault balance. | Preflight step 2 passes; the flow proceeds to step 3 (HL margin check). No error is returned for this step. (Reference: FR-EXBOT-002 step 2.) | P0 |
| TC_013 | Verify the margin check passes at exactly the minimum required margin including the 2.0x buffer | Preflight steps 1 and 2 (one-bot policy, vault balance) pass.\nThe user's HL isolated margin balance equals exactly the required margin multiplied by 2.0 (right at the threshold, no extra). (Example: required base approx 350; balance approx 700, which is exactly 2.0x.) | Process the `bot-start` preflight with the user's HL margin set to exactly the 2.0x threshold. | Preflight step 3 passes and the flow proceeds to step 4. The formula uses `hlOraclePrice` (not HL mark price or Uniswap pool price) per FR-EXBOT-061. (Reference: FR-EXBOT-002 step 3, FR-EXBOT-061.) | P0 |
| TC_014 | Verify the margin check fails when the user's balance is just below the 2.0x threshold | Preflight steps 1 and 2 pass.\nThe user's HL margin is just below the required margin multiplied by 2.0. (Example: required 2.0x approx 700; balance approx 699.) | Process the `bot-start` preflight with the user's margin set just below the minimum threshold. | Preflight step 3 fails with `E-EXBOT-002` (HTTP 400). The message includes the required amount, current amount, and exact shortfall. No bot record is created in Aurora PostgreSQL. (Reference: FR-EXBOT-002 step 3, FR-EXBOT-061.) | P0 |
| TC_015 | Verify the margin error message shows the exact shortfall without rounding down | Preflight steps 1 and 2 pass.\nThe user's HL margin is below the required 2.0x threshold by an amount that is not a round number. (Example: balance approx 300, required approx 700 — shortfall approx 400.) | Process the `bot-start` preflight with the gap between current and required margin being a non-round amount. | The start is blocked (`E-EXBOT-002`, HTTP 400). The message shows the exact amount the user still needs to deposit — the shortfall is not rounded down to a coarser unit, so depositing exactly that amount would be sufficient. (Example: if shortfall = 400.75, the message shows 400.75, not 400.) (Reference: FR-EXBOT-061 AC.) | P0 |
| TC_016 | Verify the preflight is blocked when HL is unreachable during the margin check step | Preflight steps 1 and 2 pass.\nHL API (mocked) is set to time out or return an error when `marginSummary` is requested. | Process the `bot-start` preflight while HL is unreachable during step 3 (margin check). | The start fails immediately with `E-EXBOT-008`; no bot record is created; the preflight does not proceed to step 4. (Per I-03 confirmed answer: HL timeout at preflight = immediate fail, no retry loop.) (Reference: UC step 3, E-EXBOT-008.) | P0 |
| TC_017 | Verify the start is blocked when the agent key is not yet provisioned (key_status absent or non-active, non-provisioning) | Preflight steps 1, 2, and 3 (one-bot policy, vault balance, margin) pass.\nThe user has no `hl_agent_keys` row at all (key-provision has never run for this user). | Process the `bot-start` preflight while the agent key row is absent. | Preflight step 4 fails with `E-EXBOT-017` ("Bot cannot start: agent key not yet provisioned. Please complete an on-chain deposit to trigger automatic setup.", HTTP 400). No bot record is created. (Reference: FR-EXBOT-002 step 4, E-EXBOT-017 updated message v5.) | P0 |
| TC_018 | Verify the start is blocked when builder fee (5bps) has not been confirmed on HL | Preflight steps 1, 2, 3, and 4 pass.\nBuilder fee (5bps) has not been confirmed on HL for this user. | Process the `bot-start` preflight at step 5 while builder fee is not confirmed. | Preflight step 5 fails with `E-EXBOT-005` ("HL builder fee (5bps) approval required before starting ExBot.", HTTP 400). No bot record is created. (Note: the exact HL endpoint for the fee check is pending OQ-EXBOT-05 — this TC verifies the gate behavior.) (Reference: FR-EXBOT-002 step 5, E-EXBOT-005.) | P0 |
| TC_019 | Verify a previously confirmed builder fee from a prior session still passes the check at start | Preflight steps 1, 2, 3, and 4 pass.\nBuilder fee was confirmed in a previous session (not the current start attempt). | Process the `bot-start` preflight at step 5 using a pre-confirmed builder fee. | Preflight step 5 passes — the system accepts the previously confirmed builder fee without requiring a new on-chain or API confirmation per start attempt. The flow proceeds to step 6 (LP mint simulation). (Per I-10 confirmed answer: builder fee check is a one-time check at start.) (Reference: FR-EXBOT-002 step 5.) | P1 |
| TC_020 | Verify LP mint simulation failure blocks the start without making any on-chain call | All preflight steps 1 through 5 pass.\nLP mint simulation (mocked) is set to fail (e.g., pool liquidity too low or slippage exceeds tolerance). | Process the `bot-start` preflight at step 6 while LP mint simulation is set to fail. | Preflight step 6 fails with `E-EXBOT-006` ("LP mint simulation failed. Check pool liquidity or adjust deposit amount.", HTTP 400). No bot record is created; no on-chain `vaultMint` call is made (no gas consumed). (Reference: FR-EXBOT-002 step 6, E-EXBOT-006.) | P0 |
| TC_021 | Verify no partial bot record is left after any one of the six preflight steps fails | This test is run six times — once for each preflight step — with each step configured to fail while all prior steps pass. | Fail preflight at step 1 (one-bot policy); query Aurora PostgreSQL for new bot records. Repeat for steps 2 (vault balance), 3 (margin), 4 (key_status), 5 (builder fee), and 6 (LP simulation). | After each preflight failure, `bot_registry` and `bots` contain zero new rows for the user. The bot record is only created AFTER all 6 preflight checks pass (FR-EXBOT-002 AC). No partial record exists under any failure scenario. (Reference: FR-EXBOT-002, FR-EXBOT-003.) | P0 |
| TC_022 | Verify the HL rate-limit weight is consumed at step 3 even when a later preflight step fails | Preflight steps 1 and 2 (one-bot policy, vault balance) pass.\nThe margin check at step 3 succeeds (HL API is called and responds successfully).\nThe key_status check at step 4 is configured to fail. | Process the `bot-start` preflight; allow step 3 to complete against HL successfully, then observe failure at step 4. | One HL `marginSummary` API call is made (rate-limit weight consumed) at step 3; the flow then fails at step 4 with `E-EXBOT-017`. The consumed HL rate-limit weight is NOT reversed. No bot record is created. (Reference: FR-EXBOT-002 step 3, FR-EXBOT-061.) | P1 |
| TC_023 | Verify the ExBot Lambda returns 403 when accessed directly without going through API Gateway | An attempt is made to call the ExBot Lambda ARN directly, bypassing API Gateway (e.g., via AWS SDK invoke or a crafted direct invocation without the API Gateway event wrapper). | Send a direct invocation to the ExBot Lambda endpoint that does not include the expected API Gateway headers/context. | The Lambda returns HTTP 403 (or an authorization error equivalent). No `bot-start` processing is triggered. This verifies that the Lambda is not publicly accessible except through API Gateway. (Reference: NFR-EXBOT-003.) | P0 |
| TC_049 | Verify start is blocked when agent key is in provisioning state (E-EXBOT-017 with updated message) | Preflight steps 1, 2, and 3 (one-bot policy, vault balance, margin) pass.\nThe user has an `hl_agent_keys` row with `key_status='provisioning'` (KMS generated keys but HL `approveAgent` has not yet confirmed — key-provision still in progress). | Process the `bot-start` preflight for the user while `key_status='provisioning'`. | Preflight step 4 fails with `E-EXBOT-017` ("Bot cannot start: agent key not yet provisioned. Please complete an on-chain deposit to trigger automatic setup.", HTTP 400). No bot record is created. The system recognizes `provisioning` as a known 4th state (not an error/unknown state) and correctly blocks the start. (Reference: AC-06, E-EXBOT-017, I-17 resolved.) | P0 |

### II.2. Integration & State verification — Operation: Bot Start Preflight

| TC ID | Test Title/Summary of test cases | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|

_No additional integration TCs beyond TC_022 — the preflight integration points are fully covered by the functional TCs above which assert both API call behavior and DB state._

---


## III. Operation: LP Mint & Hedge Open


### III.1. Functional verification — Operation: LP Mint & Hedge Open

| TC ID | Test Title/Summary of test cases | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|
| TC_024 | Verify the bot record is created with lifecycle_state=preflight after all six preflight checks pass | All six preflight checks pass successfully. | Process the `bot-start` job through all preflight steps; query `bots` table immediately after the last preflight step passes. | A new row is created in `bots` and `bot_registry` with `lifecycle_state='preflight'`. The record is created only after ALL 6 preflight checks pass — not before. (Reference: FR-EXBOT-002, FR-EXBOT-003.) | P0 |
| TC_025 | Verify LP mint produces a VaultMinted event and the positions row is updated with tokenId and tick ranges | Bot is in `lifecycle_state='preflight'`; all preflight checks have passed.\nUniswap V3 pool and BnzaExVault are responsive. | Process the LP opening phase — trigger `vaultMint` on-chain; wait for the transaction to be confirmed on-chain. | A `VaultMinted` event is emitted on-chain. The `positions` row in Aurora PostgreSQL is updated with the correct `tokenId`, `tickLower`, `tickUpper`, and liquidity amounts. `lifecycle_state` transitions from `preflight` to `lp_opening`, then to `lp_opened` after confirmation. (Reference: FR-EXBOT-010, FR-EXBOT-003.) | P0 |
| TC_026 | Verify on-chain vaultMint failure transitions the bot to the error state | Bot is in `lifecycle_state='preflight'`.\nOn-chain `vaultMint` call is mocked to revert (e.g., insufficient liquidity, slippage exceeded on-chain). | Process the LP opening phase with `vaultMint` set to revert; wait for the failure to propagate. | `lifecycle_state` transitions to `error` (not left in `lp_opening`). An operator alert is sent. No partial LP position is recorded in `positions`. (Reference: FR-EXBOT-010, FR-EXBOT-003.) | P0 |
| TC_027 | Verify weth_index is stored from per-chain config and not hardcoded after LP mint on Base chain | Bot is executing LP mint on Base chain.\nPer-chain config has a `weth_index` value defined for Base. | Process LP mint on Base chain; inspect the stored `weth_index` value in Aurora PostgreSQL after the LP position is confirmed. | The stored `weth_index` matches the value from the per-chain config (not a hardcoded constant). If the config value is updated and the test is re-run, the stored value reflects the new config. (Reference: OQ-EXBOT-03 pending; TC blocked by open question but written for when it is resolved.) | P1 |
| TC_028 | Verify targetShortEth is computed using BigDecimal arithmetic and matches the expected formula output | LP position has been confirmed on-chain; `lp_opened` state is reached.\nKnown inputs: `lp_eth_value`, `hedge_ratio`, `basis_adjustment` — all in BigDecimal-compatible format. | Trigger hedge-open phase; capture the `targetShortEth` value sent to Signing Lambda. | `targetShortEth` equals `lp_eth_value × hedge_ratio × basis_adjustment` computed with BigDecimal (no floating-point rounding). Example: if lp_eth_value = 1.123456789012345678 ETH, the value is preserved without truncation. No `Number()` or `parseFloat()` is used in the computation path. (Reference: FR-EXBOT-061, AC-11.) | P0 |
| TC_029 | Verify Signing Lambda uses the KMS agent key to sign the short IOC order before submitting to HL | Bot is in `hedge_pre_open` state; `targetShortEth` is computed.\nSigning Lambda has IAM role with `kms:Sign` permission for the agent key. | Allow the hedge-open phase to proceed; intercept the KMS `Sign` call and the subsequent HL order submission. | The KMS `Sign` API is called with the agent key ARN before the HL order is submitted. The HL order payload carries the signature produced by KMS. The private key material never leaves KMS HSM — only the signature is returned. (Reference: FR-EXBOT-080, NFR-EXBOT-006.) | P0 |
| TC_030 | Verify HL unreachable during hedge open transitions the bot to safe_mode (NOT error) with auto-recovery enqueued | Bot is in `hedge_pre_open` state.\nHL API (mocked) is set to time out or return 5xx when the IOC order is submitted. | Allow the hedge-open phase to attempt HL order submission while HL is unreachable. | `lifecycle_state` transitions from `hedge_pre_open` to `'safe_mode'` — NOT to `'error'` (BA confirmed I-11; resolved in v5 audit). E-EXBOT-008 ("Hyperliquid API is currently unreachable. Bot entered Safe Mode. Retrying automatically.") is logged as an operator alert. Auto-recovery per FR-EXBOT-050 is enqueued. The bot is NOT left stuck in `hedge_pre_open`. (Reference: AC-10, E-EXBOT-008, FR-EXBOT-050, I-11 resolved.) | P0 |
| TC_031 | Verify HL IOC order rejection transitions the bot to safe_mode with E-EXBOT-026 (NOT error, NOT E-EXBOT-008) | Bot is in `hedge_pre_open` state.\nHL API (mocked) is set to return an IOC rejection response (order rejected, not filled; HL is still reachable). | Allow the hedge-open phase to submit the IOC order; receive the HL rejection response. | `lifecycle_state` transitions from `hedge_pre_open` to `'safe_mode'` — NOT to `'error'` (BA confirmed I-11 and I-12; resolved in v5 audit). E-EXBOT-026 ("Hedge order rejected by Hyperliquid. Bot entered Safe Mode.") is logged — NOT E-EXBOT-008 (HL is reachable, so E-EXBOT-008 does not apply). Auto-recovery per FR-EXBOT-050 is enqueued. (Reference: AC-11, E-EXBOT-026, I-11 resolved, I-12 resolved.) | P0 |
| TC_032 | Verify post-hedge reconcile updates hedge_legs and transitions lifecycle to hedge_post_confirmed | Bot is in `hedge_pre_open` state; HL IOC order has been submitted successfully. | Simulate HL confirming the IOC order fill; trigger the post-hedge reconcile step. | `hedge_legs` row in Aurora PostgreSQL is updated with the confirmed fill size, entry price, and HL order ID. `lifecycle_state` transitions from `hedge_pre_open` to `hedge_post_confirmed`. (Reference: FR-EXBOT-003, FR-EXBOT-062, AC-01.) | P0 |
| TC_033 | Verify reconcile mismatch beyond the tolerance threshold enqueues a partial_repair job and sends an operator alert | Post-hedge reconcile runs; the confirmed fill size from HL deviates from `targetShortEth` by more than the defined tolerance threshold. | Mock HL to report a confirmed fill size that exceeds the mismatch tolerance (e.g., fill = 0.5 ETH when target = 1.0 ETH). | A `partial_repair` job is enqueued in SQS; an operator alert is sent with the mismatch details (expected vs. actual size). `lifecycle_state` is NOT advanced to `hedge_post_confirmed` until reconciliation is resolved. (Reference: FR-EXBOT-062, AC-07.) | P0 |
| TC_034 | Verify the tokenId stored in positions matches the on-chain LP NFT held by BnzaExVault | LP mint has completed; `lp_opened` state reached; `positions.tokenId` is stored in Aurora PostgreSQL. | Query the on-chain `BnzaExVault` to get the LP NFT token ID it holds; compare to `positions.tokenId` in Aurora PostgreSQL. | `positions.tokenId` in Aurora PostgreSQL exactly matches the LP NFT token ID held by `BnzaExVault` on-chain. No mismatch is allowed. (Reference: FR-EXBOT-010, AC-01.) | P0 |
| TC_050 | Verify LP mint on-chain revert transitions bot to error state with E-EXBOT-028 and no funds moved | All 6 preflight checks pass; bot record created with `lifecycle_state='preflight'`.\nOn-chain `BnzaExVault.vaultMint()` is mocked to revert (e.g., slippage exceeded, contract revert). | Allow `vaultMint()` to be called on-chain; mock the transaction to revert; wait for ExBot Lambda to detect the failure. | `lifecycle_state` transitions to `'error'` (NOT `'safe_mode'` — LP mint failure does not involve the hedge layer). HTTP 502 with E-EXBOT-028 ("Bot startup failed: LP mint transaction did not complete. No funds were moved. Please try again or contact support."). No rows created in `positions` or `hedge_legs`. `BnzaExVault` balance of the user is unchanged. (Reference: AC-09, E-EXBOT-028, states.md `lp_opening → error`.) | P0 |
| TC_051 | Verify LP mint timeout transitions bot to error state (not safe_mode) with E-EXBOT-028 | All 6 preflight checks pass; bot in `lifecycle_state='lp_opening'`.\n`VaultMinted` event does not arrive within the max wait window (timeout scenario — not an on-chain revert). | Allow `vaultMint()` to be submitted on-chain; suppress the `VaultMinted` event until the timeout threshold is reached. | `lifecycle_state` transitions to `'error'` (same as revert — LP mint timeout does NOT produce `safe_mode`). E-EXBOT-028 logged. No partial `positions` row exists. (Reference: AC-09, E-EXBOT-028, states.md `lp_opening → error`.) | P0 |
| TC_052 | Verify HL unreachable during hedge open transitions bot to safe_mode (NOT error) with auto-recovery enqueued | LP mint succeeded; bot in `lifecycle_state='lp_opened'`.\nHL API (mocked) is set to be unreachable (network timeout / 5xx) when the IOC short order is submitted. | Allow hedge-open phase to attempt HL order submission while HL is unreachable. | `lifecycle_state` transitions from `hedge_pre_open` to `'safe_mode'` (NOT `'error'`). E-EXBOT-008 ("Hyperliquid API is currently unreachable. Bot entered Safe Mode. Retrying automatically.") is logged as an operator alert. Auto-recovery message per FR-EXBOT-050 is enqueued. (Reference: AC-10, E-EXBOT-008, FR-EXBOT-050, I-11 resolved.) | P0 |
| TC_053 | Verify HL IOC order rejection transitions bot to safe_mode with E-EXBOT-026 (NOT error, NOT E-EXBOT-008) | LP mint succeeded; bot in `hedge_pre_open`.\nHL API is reachable but returns an IOC rejection response (order rejected / not filled). | Allow hedge-open to submit the IOC order to HL; receive HL's rejection response. | `lifecycle_state` transitions from `hedge_pre_open` to `'safe_mode'` (NOT `'error'`). E-EXBOT-026 ("Hedge order rejected by Hyperliquid. Bot entered Safe Mode.") is logged — NOT E-EXBOT-008 (HL is still reachable). Auto-recovery per FR-EXBOT-050 is enqueued. (Reference: AC-11, E-EXBOT-026, I-12 resolved, I-11 resolved.) | P0 |
| TC_054 | Verify no code path transitions bot from hedge_pre_open to error (negative test — invalid transition) | Bot is in `lifecycle_state='hedge_pre_open'`; any failure condition applies (HL unreachable OR IOC rejected). | Trigger each failure at the hedge-open step in turn: (1) HL unreachable, (2) IOC rejected; observe the resulting `lifecycle_state` in each case. | In BOTH failure scenarios, `lifecycle_state` transitions to `'safe_mode'` — NEVER to `'error'`. If any code path produces `lifecycle_state='error'` from `hedge_pre_open`, that is a defect. (Reference: states.md `hedge_pre_open → safe_mode ONLY`, I-11 resolved.) | P0 |

### III.2. Integration & State verification — Operation: LP Mint & Hedge Open

| TC ID | Test Title/Summary of test cases | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|
| TC_035 | Verify the deterministic cloid for the short IOC order prevents duplicate order submission on retry | A `bot-start` job for bot ID `botId=1001` with `attemptId=1` is processed; the hedge IOC order submission fails transiently after the order is sent but before confirmation is received.\nThe same job is retried with identical `botId` and `attemptId`. | On retry, allow the hedge-open phase to recompute the cloid and attempt to resubmit the IOC order to HL. | The recomputed cloid equals `first128BitsHex(keccak256("bnza:1001:1:hedge:v1"))` — identical to the first attempt. HL deduplicates on cloid and does NOT open a second position. The reconcile step detects the existing fill and advances to `hedge_post_confirmed` without double-sending. (Reference: FR-EXBOT-011, FR-EXBOT-062.) | P0 |
| TC_036 | Verify the Signing Lambda is the only IAM principal allowed to call kms:Sign during hedge open | IAM policies are in place; hedge-open phase is in progress. | Attempt to call `kms:Sign` on the agent key ARN from a principal other than the Signing Lambda IAM role (e.g., the ExBot Lambda role, a developer IAM user, or the Key-Provision Worker role). | All calls to `kms:Sign` from non-Signing-Lambda principals are rejected by IAM with `AccessDeniedException`. Only the Signing Lambda IAM role succeeds. The agent key cannot be used for signing from any other context. (Reference: NFR-EXBOT-006, FR-EXBOT-080.) | P0 |

## IV. Operation: Stop Placement & Bot Activation


### IV.1. Functional verification — Operation: Stop Placement & Bot Activation

| TC ID | Test Title/Summary of test cases | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|
| TC_037 | Verify stop_trigger_px is computed using BigDecimal with stopSafetyFactor=0.70 | Bot has reached `hedge_post_confirmed`; `entry_price` and `liq_distance_pct` are known. | Trigger stop placement; capture the `stop_trigger_px` value sent to HL. | `stop_trigger_px = entry_price × (1 + liq_distance_pct × 0.70)` — computed with BigDecimal (no floating-point rounding). Example: if entry_price = 3500.123456789 and liq_distance_pct = 0.10, stop_trigger_px = 3500.123456789 × (1 + 0.07) = 3745.132098... — precision is preserved. (Reference: FR-EXBOT-050, AC-11.) | P0 |
| TC_038 | Verify stop price computation falls back to the 1/effective_leverage formula when liq_distance_pct is unavailable | Bot has reached `hedge_post_confirmed`; `liq_distance_pct` is not available (e.g., HL did not return a liquidation distance in the position response). | Trigger stop placement while `liq_distance_pct` is absent; observe the fallback formula used. | `liq_distance_pct` falls back to `1 / effective_leverage`; `stop_trigger_px` is then computed as `entry_price × (1 + (1/effective_leverage) × 0.70)`. The fallback is used only when the primary value is unavailable — not as the default. (Reference: FR-EXBOT-050.) | P0 |
| TC_039 | Verify reduce-only stop market placement confirms on HL and lifecycle transitions to stop_verified then active | Bot is in `hedge_post_confirmed` state; `stop_trigger_px` has been computed. | Allow stop placement to submit the reduce-only stop market order to HL; wait for HL to confirm the order. | HL confirms the stop market order; `lifecycle_state` transitions from `hedge_post_confirmed` to `stop_placing`, then to `stop_verified`, then to `active` — in that strict order. No state is skipped. `bot_registry.status` is set to `active`. Monitoring jobs (light-check, deep-audit) are scheduled. (Reference: FR-EXBOT-050, FR-EXBOT-003, AC-01.) | P0 |
| TC_040 | Verify stop placement failure prevents the bot from reaching active and triggers an operator alert | Bot is in `stop_placing` state; HL is mocked to reject or not respond to the stop market order. | Allow stop placement to fail (HL rejects or times out); observe the resulting lifecycle state. | `lifecycle_state` does NOT advance to `active`. The bot transitions to `safe_mode` (per INV-STOP invariant: a bot cannot be active without a confirmed stop). An operator alert is sent with the failure reason. Light-check and deep-audit are NOT scheduled. (Reference: FR-EXBOT-050, INV-STOP, AC-06.) | P0 |
| TC_041 | Verify the full bot-start sequence traverses all 9 lifecycle states in strict order | All external dependencies (KMS, HL, Uniswap V3, Aurora PostgreSQL, SQS) are available and responding normally.\nAll preflight conditions pass (no active bot, vault balance > 0, margin ≥ 2.0x required, key active, builder fee confirmed, LP sim passes). | Process a complete `bot-start` job from initial queue message to final `active` state; record each `lifecycle_state` value at each transition point. | The `lifecycle_state` sequence is exactly: `idle → preflight → lp_opening → lp_opened → hedge_pre_open → hedge_post_confirmed → stop_placing → stop_verified → active`. All 9 states appear in that order; no state is skipped or repeated. (Reference: FR-EXBOT-003, AC-01.) | P0 |
| TC_042 | Verify stop_price is less than liquidation_price and maintains at least a 30% safety buffer | Bot is in `stop_placing` state; `liquidation_price` and `stop_trigger_px` are both computed. | Compare `stop_trigger_px` to `liquidation_price` after stop placement completes; verify the safety margin. | `stop_trigger_px < liquidation_price`. The distance between `stop_trigger_px` and `liquidation_price` is at least 30% of `liq_distance_pct` (stopSafetyFactor = 0.70 means the stop uses only 70% of the liquidation distance, leaving a 30% buffer). Computed with BigDecimal. (Reference: FR-EXBOT-050, AC-11.) | P0 |
| TC_043 | Verify light-check and deep-audit monitoring are scheduled after the bot reaches active state | Bot has reached `lifecycle_state='active'`. | Query the scheduling system (e.g., SQS or cron registry) immediately after `active` state is set. | A light-check job is scheduled per the configured interval (e.g., every N minutes). A deep-audit job is scheduled per its configured interval. Neither job is scheduled before `active` is reached. (Reference: FR-EXBOT-003.) | P0 |
| TC_044 | Verify the status API returns active state and hedge summary after the bot reaches active | Bot has reached `lifecycle_state='active'`; `hedge_legs` and `positions` rows are fully populated. | Call the bot status API endpoint for the active bot. | The API response includes `lifecycle_state='active'`, the LP position summary (tokenId, tick ranges, liquidity), and the hedge summary (short size, entry price, stop price). HTTP 200. No sensitive key material appears in the response. (Reference: FR-EXBOT-003, NFR-EXBOT-006.) | P0 |
| TC_055 | Verify stop placement failure transitions bot to safe_mode with E-EXBOT-009 (NOT active, NOT error) | LP mint and hedge open both succeeded; bot in `lifecycle_state='stop_placing'`.\nHL is mocked to reject or not respond to the reduce-only stop order. | Allow stop placement to attempt submission to HL while HL rejects or times out. | `lifecycle_state` transitions from `stop_placing` to `'safe_mode'` (NOT `'active'`, NOT `'error'`). E-EXBOT-009 ("Failed to place native stop on Hyperliquid. Bot cannot activate without a stop.") is logged as an internal alert. Auto-recovery per FR-EXBOT-050 begins. The bot is NOT marked active. (Reference: AC-12, E-EXBOT-009, states.md `stop_placing → safe_mode`.) | P0 |
| TC_056 | Verify preflight is sequential: vault balance failure (step 2) stops flow before reaching key_status check (step 4) | One-bot policy (step 1) passes.\nVault balance = 0 (step 2 will fail).\nAgent key is present with `key_status='active'` (step 4 would pass if reached). | Process `bot-start` preflight with vault balance = 0; observe which step fails and whether steps 3–6 are reached. | Preflight fails at step 2 with E-EXBOT-025 ("No confirmed deposit found. Please complete an on-chain deposit before starting the bot.", HTTP 400). Steps 3 (margin), 4 (key_status), 5 (builder fee), and 6 (LP sim) are NOT executed — no HL margin API call is made, no `hl_agent_keys` lookup occurs. (Reference: AC-13, FR-EXBOT-002.) | P0 |

### IV.2. Integration & State verification — Operation: Stop Placement & Bot Activation

| TC ID | Test Title/Summary of test cases | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|
| TC_045 | Verify the bot-start queue message is processed exactly once even when redelivered by SQS | A `bot-start` SQS message for a given user is delivered and processed successfully to `active` state.\nSQS redelivers the same message (same `message_id`) due to at-least-once delivery semantics. | Deliver the same `bot-start` message a second time after the bot is already `active`. | The second delivery hits the UNIQUE constraint on `queue_idempotency.message_id` and exits immediately. No second bot record is created. The existing `active` bot is not affected. (Reference: FR-EXBOT-011, BR-EXBOT-001.) | P0 |
| TC_046 | Verify two simultaneous bot-start messages for the same user result in exactly one bot being created | Two `bot-start` messages for the same user with different `message_id` values are delivered concurrently to ExBot Lambda. | Allow both messages to be processed simultaneously; race them through the one-bot policy check. | The DB-level insert or lock ensures exactly one bot record is created. The second concurrent attempt hits the one-bot policy (`E-EXBOT-001`) or a DB uniqueness constraint and is rejected. No duplicate bot is created. (Reference: FR-EXBOT-001, BR-EXBOT-001.) | P0 |
| TC_057 | Verify bot_runtime_state.last_known_hl_short_size is populated after post-hedge reconcile and non-NULL when bot active | Hedge open succeeded; post-hedge reconcile has completed successfully; bot has transitioned to `hedge_post_confirmed`. | Query `bot_runtime_state.last_known_hl_short_size` for this bot after reconcile completes; then verify again after bot reaches `lifecycle_state='active'`. | `bot_runtime_state.last_known_hl_short_size` is NOT NULL; value equals the actual short size confirmed by HL `clearinghouseState`. When bot reaches `lifecycle_state='active'`, this field remains populated. (Light-check and hedge-sync workers read this cached value instead of re-querying HL API.) (Reference: V5-05, FR-EXBOT-025, spec.md §bot_runtime_state.) | P0 |

### IV.3. Non-functional (logic) verification — Operation: Stop Placement & Bot Activation

| TC ID | Test Title/Summary of test cases | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|
| TC_047 | Verify the agent key is never written to logs while it is decrypted for signing during bot start | Bot-start is in the hedge-open phase; Signing Lambda is decrypting the agent key for signing. | Capture all application logs, CloudWatch logs, and structured logging output during the Signing Lambda execution window. | No hex-encoded or base64-encoded private key material appears in any log output, error message, or trace during the signing window. Only the agent address (public) and signing metadata are logged. (Reference: NFR-EXBOT-006, AC-08.) | P0 |
| TC_048 | Verify all financial values in Aurora PostgreSQL after bot activation are stored as BigDecimal TEXT strings | Bot has reached `lifecycle_state='active'`; all financial fields are written: entry_price, stop_trigger_px, targetShortEth, liquidity amounts, tick ranges. | Query the relevant Aurora PostgreSQL columns (entry_price, stop_trigger_px, lp_liquidity, etc.) and inspect the stored data type and value format. | All financial columns are stored as TEXT or NUMERIC with full precision — no floating-point IEEE 754 approximation. Example: entry_price stored as "3500.123456789012345678" (not 3500.1234567890124). No `Number()`, `parseFloat()`, or JavaScript float representation appears in any stored value. (Reference: FR-EXBOT-061, AC-15.) | P0 |
| TC_058 | Verify hedge_legs.stop_price is computed with BigDecimal safetyFactor=0.70 and stored without float error | Bot-start completed successfully; bot reached `lifecycle_state='active'`. Known: `entry_price` and `liq_price` from HL `clearinghouseState` after hedge. | Query `hedge_legs.stop_price` for the active bot; recompute expected value using `entry_price × (1 + liq_distance_pct × 0.70)` with BigDecimal math; compare to stored value. | `hedge_legs.stop_price` matches the BigDecimal result exactly (no floating-point rounding error). Example: `entry_price = 3000`, `liq_price = 3300` → `liq_distance_pct = 0.10` → `stop_price = 3000 × 1.07 = 3210.00` exactly (not 3209.9999... or 3210.00001...). `stop_price < liq_price` (stop fires before liquidation). No `Number()` or `parseFloat()` in the computation chain. (Reference: AC-15, NFR-EXBOT-008, FR-EXBOT-030.) | P0 |
