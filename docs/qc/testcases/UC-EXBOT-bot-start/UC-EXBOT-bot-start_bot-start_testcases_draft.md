# Test Cases — UC-EXBOT-bot-start bot-start

**Total test cases:** 32 (FUNC: 18, INTG: 10, NFR: 4)
**Scope:** Logic-only (backend / API / bot — no UI)
**Source UC:** UC-EXBOT-bot-start_bot-start_audited_20260617_v1.md
**Source scenarios (if any):** none
**Output language:** English

#### Requirement Traceability Matrix

| AC ID | Acceptance Criteria | Linked Test Cases | Status |
|---|---|---|---|
| AC-01 | Happy path — preflight pass, bot transitions through 8 lifecycle states to `active`. | TC_001, TC_020 | Covered |
| AC-02 | One-bot policy block — user already has one active ExBot. | TC_002 | Covered |
| AC-03 | Insufficient HL margin — block at preflight check 2. | TC_003, TC_004, TC_005 | Covered |
| AC-04 | Agent key pending — block at preflight check 3. | TC_006 | Covered |
| AC-05 | Agent key expired — block at preflight check 3. | TC_007 | Covered |
| AC-06 | Builder fee not approved — block at preflight check 4. | TC_009 | Covered |
| AC-07 | LP mint simulation fails — block at preflight check 5. | TC_010 | Covered |
| AC-08 | LP mint actual fails — bot transitions to `error`. | TC_013 | Covered |
| AC-09 | HL hedge open IOC reject — bot to `safe_mode`. | TC_014 | Covered |
| AC-10 | HL API unreachable during hedge open — bot to `safe_mode`. | TC_015 | Covered |
| AC-11 | Stop placement fail — bot to `safe_mode`. | TC_016 | Covered |
| AC-12 | Reconcile mismatch after hedge open — bot to `safe_mode`. | TC_018 | Covered |
| AC-13 | Direct internet access to ExBot Worker rejected. | TC_026 | Covered |
| AC-14 | Double-submit produces exactly one bot. | TC_025 | Covered |
| AC-15 | Base bot + Optimism bot operate independently. | TC_023 | Covered |
| AC (BR-EXBOT-001 boundary) | Closed bot does not count toward one-bot limit. | TC_011 | Covered |
| AC (FR-EXBOT-003 sequence) | Lifecycle steps execute in fixed order, none skipped. | TC_019, TC_020 | Covered |
| AC (FR-EXBOT-024 cloid) | Deterministic cloid used for HL orders. | TC_021 | Covered |
| AC (FR-EXBOT-013 jitter) | `next_light_check_at` initialized with 5-min jitter window. | TC_022 | Covered |
| AC (FR-EXBOT-091 rate limit) | `HLRateLimitDO` mediates all HL calls. | TC_024 | Covered |
| AC (FR-EXBOT-080 + NFR-EXBOT-006) | Agent key never written to logs. | TC_029, TC_030 | Covered |
| AC (audit / logging) | Start writes audit record with actor / action / timestamp / result. | TC_031 | Covered |
| AC (NFR-EXBOT-002 SLA) | Start completes within expected latency budget. | TC_032 | Covered |
| AC (input validation) | Required fields missing in request payload are rejected. | TC_008, TC_017 | Covered |
| AC (D1 atomic) | Bot record created only after preflight passes; no partial rows. | TC_012, TC_027 | Covered |
| AC (rotation) | Revoked / superseded agent keys do not pass preflight. | TC_028 | Covered |

---

## I. Operation: Bot start (preflight + LP mint + hedge open + stop place)

### I.1. Functional verification — Operation: Bot start (preflight + LP mint + hedge open + stop place)

| TC ID | Title | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|
| TC_001 | Verify the start succeeds end-to-end when every preflight check passes | 1. The user has no running ExBot (status is not one of `active`, `paused`, `closing`, `safe_mode`, `error`). <br>2. The user's HL margin is at or above the required margin with the safety buffer applied (`marginBalanceUsd ≥ marginRequiredUsd × 2.0`). <br>3. The user's HL agent key is approved and not expired (`hl_agent_keys.approval_status='approved'` and `expires_at` is in the future). <br>4. The HL builder fee (5bps) is confirmed for the user. <br>5. The LP mint simulation is set to pass. <br>6. The Hyperliquid and BnzaExVault responses (mocked) are set to succeed. | 1. Start a new bot for the user through the Operator Facade and let the whole start process run to the end. | 1. The start succeeds: the bot is created and ends in the running state (`bots.status='active'`, `bots.lifecycle_state='active'`), and the API returns `{status:active, botId}`. On-chain, the LP is minted (`VaultMinted` is emitted) and the HL short plus a reduce-only stop are placed. Off-chain, the `positions` and `hedge_legs` rows are filled in (`token_id`, `entry_price`, `liquidation_price`, `effective_leverage`, `stop_price`, `stop_cloid` populated). (Reference: FR-EXBOT-003, flow F-03; AC-01.) | P0 |
| TC_002 | Verify the one-bot policy blocks a second start while a bot is still running | 1. The user already has a running ExBot whose status counts toward the limit (`status='active'`). | 1. Start a new bot for the same user while the first bot is still running. | 1. The start is rejected and no new bot is created (error code `E-EXBOT-001`, message "You already have an active ExBot. Close or wait for the existing bot to finish.", HTTP 409). No new row is added to `bot_registry` / `bots`. (Reference: FR-EXBOT-001, BR-EXBOT-001; AC-02.) | P0 |
| TC_003 | Verify the start is blocked when the user does not have enough HL margin | 1. The one-bot policy check passes for the user. <br>2. The user's HL margin is below the required margin including its safety buffer. (Example: the rule asks for the required margin plus a 100% buffer; the user has only about half of the required + buffer.) | 1. Start a bot when the user's HL margin is below the required-margin-plus-buffer threshold. | 1. The start is blocked and no bot is created (error code `E-EXBOT-002`, HTTP 400). The message tells the user how much more to deposit to HL. No partial bot row is left in D1. (Reference: FR-EXBOT-002 step 2, FR-EXBOT-061; AC-03.) | P0 |
| TC_004 | Verify the boundary just at the required margin with no buffer is still blocked | 1. The one-bot policy passes. <br>2. The user's HL margin is exactly at the required margin (no buffer applied). (Example: balance equals the required margin, so the 2.0x rule is not satisfied.) | 1. Start a bot when the user's HL margin is exactly at the required margin and not above. | 1. The start is blocked (`E-EXBOT-002`, HTTP 400) because the 2.0x buffer rule is not met at the boundary. No bot record is created. (Reference: FR-EXBOT-061 AC boundary.) | P0 |
| TC_005 | Verify the boundary at exactly the 2.0x buffer passes preflight check 2 | 1. The one-bot policy passes. <br>2. The user's HL margin is exactly at the required margin times the 2.0x buffer (the boundary is just met). | 1. Start a bot when the user's HL margin sits exactly on the 2.0x buffer threshold. | 1. Preflight check 2 passes at the boundary and the start continues to preflight check 3 (no `E-EXBOT-002`). The bot proceeds to record creation. (Reference: FR-EXBOT-061 AC boundary inclusive.) | P1 |
| TC_006 | Verify the start is blocked when the agent key is still waiting for approval | 1. The one-bot policy and margin checks pass. <br>2. The user's HL agent key is still waiting for approval (`approval_status='pending'`). | 1. Start a bot while the agent key is still waiting for approval. | 1. The start is blocked and no bot is created (error code `E-EXBOT-003`, message "Agent key is awaiting approval. Please complete the approval process before starting.", HTTP 400). (Reference: FR-EXBOT-002 step 3, FR-EXBOT-081; AC-04.) | P0 |
| TC_007 | Verify the start is blocked when the agent key has expired | 1. The one-bot policy and margin checks pass. <br>2. The user's HL agent key was approved but has now expired (`approval_status='approved'` and `expires_at` is in the past). | 1. Start a bot while the agent key is expired. | 1. The start is blocked and no bot is created (error code `E-EXBOT-004`, message "Your HL agent key has expired. Please submit a new one.", HTTP 400). (Reference: FR-EXBOT-083; AC-05.) | P0 |
| TC_008 | Verify the start is blocked when the request payload is missing a required field | 1. The user has no running ExBot. <br>2. The `POST /api/exbot/start` payload is missing a required field (for example, the chain selector is absent). | 1. Submit the start request with the incomplete payload. | 1. The start is rejected at input validation with a clear error explaining which field is missing (HTTP 400). No bot record is created and no preflight is run. (Reference: FR-EXBOT-090; UC-bot-start gap I-12 — payload contract still pending.) | P1 |
| TC_009 | Verify the start is blocked when the HL builder fee (5bps) is not approved | 1. The one-bot policy, margin, and agent key checks pass. <br>2. The HL builder fee (5bps) is not yet approved for the user. | 1. Start a bot while the builder fee is not approved. | 1. The start is blocked and no bot is created (error code `E-EXBOT-005`, message "HL builder fee (5bps) approval required before starting ExBot.", HTTP 400). (Reference: FR-EXBOT-002 step 4; AC-06; mechanism pending OQ-EXBOT-05.) | P0 |
| TC_010 | Verify the start is blocked when the LP mint simulation fails | 1. Preflight checks 1 through 4 pass. <br>2. The `BnzaExVault.vaultMint` simulation is set to fail (for example, slippage is outside the tolerance). | 1. Start a bot while the LP mint simulation fails. | 1. The start is blocked and no bot is created (error code `E-EXBOT-006`, message "LP mint simulation failed. Check pool liquidity or adjust deposit amount.", HTTP 400). No on-chain transaction is submitted. (Reference: FR-EXBOT-002 step 5; AC-07.) | P0 |
| TC_011 | Verify a closed bot does not count toward the one-bot limit | 1. The user has one previous ExBot that is fully closed (`status='closed'`, the status is NOT in the counted set `active`, `paused`, `closing`, `safe_mode`, `error`). | 1. Start a new bot for the same user. | 1. The one-bot policy check passes (the closed bot is excluded from the count) and the start continues to the next preflight check. A new bot is created normally. (Reference: BR-EXBOT-001 boundary: only the five counted statuses block a new start.) | P1 |
| TC_012 | Verify preflight short-circuits on the first failed check and leaves no partial state | 1. The one-bot policy fails for the user (a running ExBot already exists). <br>2. The agent key for the user is also pending and the HL margin is also insufficient (multiple checks would fail). | 1. Start a new bot for the same user. | 1. Preflight returns immediately at the first failed check (`E-EXBOT-001` for the one-bot policy) and does NOT attempt any later checks. No bot record is created in `bots` / `bot_registry`. (Reference: FR-EXBOT-002 sequence guarantee, FR-EXBOT-001 AC.) | P1 |
| TC_013 | Verify the bot moves into the error state when the actual LP mint transaction reverts on-chain | 1. All five preflight checks pass. <br>2. The bot record has been created (`lifecycle_state='preflight'`). <br>3. The `BnzaExVault.vaultMint` call is set to revert on-chain. | 1. Let the start submit the LP mint transaction while the contract is set to revert. | 1. On-chain: the `vaultMint` transaction reverts, no `VaultMinted` event is emitted, and the user's USDC is not transferred to the vault. Off-chain: the bot transitions to the error state (`bots.lifecycle_state='error'`, `bots.status='error'`) and waits for admin resolution. (Reference: states.md `lp_opening → error`; UC-bot-start gap I-09 — refund mechanism pending; AC-08.) | P0 |
| TC_014 | Verify the bot moves into safe mode when the HL short order is rejected | 1. The bot has minted the LP and is about to open the hedge (`lifecycle_state='lp_opened'`). <br>2. The HL short order (mocked `openShortIoc`) is set to be rejected by Hyperliquid. | 1. Let the start try to open the HL short while the HL order is rejected. | 1. The hedge open fails and the bot switches to safe mode (`lifecycle_state` goes from `hedge_pre_open` to `safe_mode`, `bots.status='safe_mode'`). No stop is placed and the bot does not become active. An admin alert is raised with `E-EXBOT-007`. (Reference: states.md `hedge_pre_open → safe_mode`, FR-EXBOT-050; AC-09.) | P0 |
| TC_015 | Verify the bot moves into safe mode when the HL API is unreachable during hedge open | 1. The bot has minted the LP (`lifecycle_state='lp_opened'`). <br>2. The HL API is set to be unreachable for longer than the SAFE_MODE entry threshold. (Example: HL down for more than 5 minutes.) | 1. Let the start try to open the HL short while HL is unreachable. | 1. The hedge open call cannot complete, the bot switches to safe mode (`lifecycle_state='safe_mode'`, `bots.status='safe_mode'`), and an alert with `E-EXBOT-008` is sent. The bot retries automatically when HL becomes responsive. (Reference: FR-EXBOT-050 SAFE_MODE trigger; AC-10.) | P0 |
| TC_016 | Verify the bot moves into safe mode when the stop placement fails | 1. The bot has reached `hedge_post_confirmed` (LP minted, hedge open confirmed by reconcile). <br>2. The HL `placeReduceOnlyStopMarket` call is set to fail. | 1. Let the start try to place the reduce-only stop while HL rejects the stop. | 1. The stop placement fails and the bot switches to safe mode (`lifecycle_state` goes from `stop_placing` to `safe_mode`, `bots.status='safe_mode'`). The bot does NOT become active (no `active` state is reached without a confirmed stop). The alert carries `E-EXBOT-009`. (Reference: states.md `stop_placing → safe_mode`, FR-EXBOT-031; AC-11.) | P0 |
| TC_017 | Verify the start is rejected when the request payload selects an unsupported chain | 1. The user has no running ExBot. <br>2. The `POST /api/exbot/start` payload selects a chain that is not in the supported set (only Base and Optimism are supported in Phase 1). | 1. Submit the start request with the unsupported chain identifier. | 1. The start is rejected at input validation (HTTP 400) with a clear message naming the supported chains. No preflight check is run and no bot record is created. (Reference: NFR-EXBOT-009 dual-chain; UC gap I-12 — payload contract pending.) | P1 |
| TC_018 | Verify the bot moves into safe mode when the post-order reconcile detects a size mismatch | 1. The bot has reached `hedge_pre_open` (LP minted, HL short IOC acknowledged). <br>2. The HL `clearinghouseState` (mocked) reports an actual short size different from the target size. | 1. Let the start run the post-order reconcile while the actual HL position size does not match the target. | 1. The reconcile detects the mismatch and the bot switches to safe mode (`lifecycle_state` goes from `hedge_pre_open` to `safe_mode`, `bots.status='safe_mode'`). No `rebalance_attempts` row is written with `status='success'`. The alert carries `E-EXBOT-011`. (Reference: FR-EXBOT-025 reconcile pattern, FR-EXBOT-050 SAFE_MODE trigger; AC-12.) | P0 |

### I.2. Integration & State verification — Operation: Bot start (preflight + LP mint + hedge open + stop place)

| TC ID | Title | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|
| TC_019 | Verify the bot moves through every start step in the fixed order with none skipped | 1. All five preflight checks pass. <br>2. The Hyperliquid and BnzaExVault responses (mocked) are set to succeed. | 1. Start a bot and let the whole start process run to the end. <br>2. Read the bot's stage (`bots.lifecycle_state`) after each saved step. | 2. The bot moves through every start step in the fixed order with none skipped, and each step is saved before the next one begins, ending in the running state (`bots.status='active'`). On-chain: the LP is minted (`VaultMinted` is emitted) and the HL short plus a reduce-only stop are placed. Off-chain: the `positions` and `hedge_legs` rows are filled in. (Example order: `idle -> preflight -> lp_opening -> lp_opened -> hedge_pre_open -> hedge_post_confirmed -> stop_placing -> stop_verified -> active`.) (Reference: FR-EXBOT-003, flow F-03.) | P0 |
| TC_020 | Verify each lifecycle transition is saved to D1 before the next step begins | 1. All five preflight checks pass. <br>2. The Hyperliquid and BnzaExVault responses (mocked) are set to succeed. | 1. Start a bot. <br>2. Read `bots.lifecycle_state` from D1 between every transition (between `preflight` and `lp_opening`, between `lp_opened` and `hedge_pre_open`, and so on). | 2. The current state is fully persisted to D1 before the next transition starts; a worker crash between any two states would leave the bot in the last persisted state, never in an in-memory-only state. (Reference: FR-EXBOT-003 atomic D1 write AC.) | P0 |
| TC_021 | Verify deterministic cloid is used for the hedge open and stop placement | 1. All five preflight checks pass and the LP is minted. <br>2. The HL responses (mocked) are set to succeed. | 1. Start a bot and capture the cloid sent for `openShortIoc` and for `placeReduceOnlyStopMarket`. <br>2. Retry the same start internally (same `{botId, attemptId, stage, version}`). | 2. The cloids are deterministic — the second internal retry computes the same cloid for the same `{botId, attemptId, stage, version}` tuple, so the second submission is detected by HL as a duplicate and the actual HL state is reconciled before any resubmit. (Reference: FR-EXBOT-024.) | P0 |
| TC_022 | Verify the bot runtime state initializes `next_light_check_at` with the correct jitter window when the bot becomes active | 1. The start completes end-to-end and the bot reaches `active`. | 1. After the start finishes, read the `bot_runtime_state` row for the new bot. | 1. The `bot_runtime_state` row exists and `next_light_check_at` is set to roughly 5 minutes from now plus a random jitter inside the documented window. (Example: jitter = ±45 seconds.) `circuit_breakers.state` is initialized to `closed` for the new bot. (Reference: FR-EXBOT-013, FR-EXBOT-040.) | P1 |
| TC_023 | Verify a Base bot and an Optimism bot start independently with the correct per-chain `wethIndex` | 1. User A starts a bot on Base. <br>2. User B starts a bot on Optimism. <br>3. The pool address and `wethIndex` per chain are valid (pending OQ-EXBOT-03; use mocked values that match the chain). | 1. Submit two start requests, one per user, in parallel. | 1. Both bots reach `active` independently. The `positions.weth_index` field is populated correctly per chain (not hardcoded), and there is no cross-bleed between chains (a Base bot only writes Base-side state, an Optimism bot only writes Optimism-side state). (Reference: FR-EXBOT-004, NFR-EXBOT-009; AC-15. Dual-chain pool address still pending OQ-EXBOT-03.) | P1 |
| TC_024 | Verify every HL call during start is mediated by HLRateLimitDO | 1. All five preflight checks pass. <br>2. `HLRateLimitDO` is set to allow the calls (budget not exhausted). | 1. Start a bot and trace all outbound HL calls made during the run (`marginSummary`, `openShortIoc`, `clearinghouseState`, `placeReduceOnlyStopMarket`). | 1. Every HL call asks `HLRateLimitDO` for permission before going out, and the total weight used stays under the per-minute budget. If `HLRateLimitDO` returns `{allowed:false, retryAfterMs}` the worker waits and retries instead of calling HL directly. (Reference: FR-EXBOT-091, NFR-EXBOT-004; UC gap I-17.) | P0 |
| TC_025 | Verify a double-submit of the start endpoint produces exactly one bot | 1. The user has no running ExBot. <br>2. Two start requests are submitted in parallel for the same user. | 1. Submit two `POST /api/exbot/start` requests for the same user at the same time. | 1. Exactly one bot is created. The second request is rejected — either by the D1 UNIQUE constraint on `bot_registry` (returns `E-EXBOT-001` after the first commits) or by a lease that prevents the second worker from creating a row. No partial second bot exists. (Reference: BR-EXBOT-001, FR-EXBOT-092 lease; AC-14; mechanism still pending in UC gap I-14.) | P0 |
| TC_026 | Verify a direct internet request to the ExBot Worker is rejected | 1. The ExBot Worker is deployed separately from the Operator Facade. <br>2. The caller is reaching the ExBot Worker directly over the public internet (no service binding, no internal token). | 1. Send a `POST /api/exbot/start` request directly to the ExBot Worker URL. | 1. The request is rejected with HTTP 403 and no business logic runs. The Operator service binding log shows no event for this request. (Reference: FR-EXBOT-090, BR-EXBOT-010; AC-13.) | P0 |
| TC_027 | Verify a worker crash mid-flow leaves the bot at the last persisted state and not in an inconsistent in-memory state | 1. The bot is mid-start at one of the transitional states (e.g. `hedge_pre_open`). <br>2. The worker is forced to crash before completing the next D1 write. | 1. Crash the worker mid-flow and then read the bot row from D1. | 1. The bot row reflects the last fully persisted `lifecycle_state` (the in-memory next state is NOT applied). The next deep-audit / reconcile picks up the bot and routes it appropriately (recovery path per spec). No `active` state is reached without a confirmed stop. (Reference: FR-EXBOT-003 atomic write, UC gap I-04 — recovery path pending.) | P1 |
| TC_028 | Verify a revoked or superseded agent key is rejected at preflight and never used for signing | 1. The user has an agent key whose status is `revoked` (or `superseded`). <br>2. The one-bot policy and margin checks pass. | 1. Start a bot while the agent key is in a non-approved status. | 1. The start is blocked at preflight check 3 (`E-EXBOT-003` for non-approved key; the key material is NOT decrypted). No HL call is made. (Reference: FR-EXBOT-082, FR-EXBOT-083 rotation; UC gap I-19.) | P0 |

### I.3. Non-functional (logic) verification — Operation: Bot start (preflight + LP mint + hedge open + stop place)

| TC ID | Title | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|
| TC_029 | Verify the agent key is never written to logs while it is being used during start | 1. The user's agent key is approved and not expired. | 1. Run the start so the agent key is decrypted for signing, then read the worker logs and any error output. | 1. The plain agent key and the plain data-encryption key (DEK) do not appear anywhere in the logs or error output; they are used only inside the function and dropped right after. A dump of the `hl_agent_keys` table shows encrypted values only. (Reference: FR-EXBOT-080, NFR-EXBOT-006.) | P0 |
| TC_030 | Verify a database dump shows only encrypted blobs for the agent key columns | 1. The user has an approved agent key stored in `hl_agent_keys`. | 1. Dump the `hl_agent_keys` table and inspect the sensitive columns (`encrypted_secret`, `wrapped_dek`, `secret_iv`, `secret_auth_tag`, `dek_iv`). | 1. Every sensitive column contains ciphertext only; no plain key, no plain DEK, no recognizable secret material is visible. (Reference: FR-EXBOT-080, NFR-EXBOT-006.) | P0 |
| TC_031 | Verify the start writes an audit record with actor, action, timestamp, and result | 1. The user is allowed to start a bot. | 1. Trigger the start (either a successful run or a preflight-blocked run) and read the audit log. | 1. An audit record is written with the actor (user id), the action (`/api/exbot/start`), the timestamp, the target (bot id if created, otherwise the attempt id), and the result (success / error code). The record does NOT contain the plain agent key or any PII beyond what is required for audit. (Reference: NFR §3 audit/logging; UC gap I-21 — audit policy still pending.) | P1 |
| TC_032 | Verify the start completes within the expected SLA when all dependencies respond normally | 1. All five preflight checks pass. <br>2. The Hyperliquid and BnzaExVault responses (mocked) are set to succeed with normal latency. | 1. Start a bot and measure the time from the Operator Facade receiving the request to the bot reaching `active`. | 1. The total time stays within the expected SLA budget for the start path (preflight + LP mint confirmation + hedge open + reconcile + stop place). Going past the budget raises a performance warning. (Example: budget for end-to-end start is on the order of a single hedge-sync SLA, which is under 30 seconds for the HL portion.) (Reference: NFR-EXBOT-002; UC gap I-18 — total SLA pending.) | P1 |
