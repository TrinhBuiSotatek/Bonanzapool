# Test Cases — UC-EXBOT-monitor-status Monitor Bot Status

**Total test cases:** 32 (FUNC: 21, INTG: 8, NFR: 3)
**Scope:** Logic-only (backend / API / bot — no UI)
**Source UC:** uc-monitor-status.md (draft 2026-06-18)
**Source scenarios (if any):** UC-EXBOT-monitor-status_monitor-status_scenarios_20260702_v2.md
**Output language:** English

#### Requirement Traceability Matrix

| AC ID | Acceptance Criteria | Linked Test Cases | Status |
|---|---|---|---|
| AC-01 | Active bot: all required fields present (status, lifecycle_state, LP range, currentTick, rangeState, hedgeSize, targetShortEth, driftPct, marginStatus, light-check timestamp) | TC_001, TC_002, TC_019, TC_020 | Covered |
| AC-02 | SAFE_MODE: status='safe_mode', safe_mode_reason present; not terminal (BR-EXBOT-007) | TC_006, TC_013, TC_014 | Covered (safe_mode_reason field name pending I-006) |
| AC-03 | hedge_stopped_cooldown: cooldown end timestamp present | TC_007, TC_015 | Covered (field name pending I-006) |
| AC-04 | No active bot: HTTP 404 | TC_008, TC_016 | Covered |
| AC-05 | rangeState='in' when tickLower <= currentTick < tickUpper | TC_002, TC_003, TC_004 | Covered |
| AC-06 | rangeState='out' when currentTick < tickLower or currentTick >= tickUpper | TC_003, TC_004, TC_005 | Covered |
| AC-07 | drift % = abs(actual - target) / target x 100 via BigDecimal | TC_009, TC_010, TC_011 | Covered (targetShortEth source pending I-003) |
| AC-08 | Operator Facade unavailable: HTTP 503 | TC_017 | Covered |
| FR-EXBOT-090 | ExBot Worker 403 to direct internet; Facade forwards unchanged | TC_012, TC_022, TC_030 | Covered |

---

## I. Operation: Monitor Bot Status (GET /api/exbot/status)

### I.1. Functional verification — Operation: Monitor Bot Status (GET /api/exbot/status)

| TC ID | Title | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|
| TC_001 | Verify the full status response is returned with all required fields for a normally running bot | 1. Bot is running normally (`bots.lifecycle_state='active'`, `bots.status='active'`).\n2. LP position is in range (`tickLower <= currentTick < tickUpper`) and margin is healthy (`hedge_legs.margin_status='ok'`).\n3. `MarketDataDO` has a valid, fresh tick cache for the bot's pool.\n4. All D1 tables (`bots`, `positions`, `bot_runtime_state`, `hedge_legs`) have fully populated rows. | 1. Call `GET /api/exbot/status` via the Operator Facade for the running bot. | 1. Response is HTTP 200 with a JSON body containing all fields documented in UC §3 step 7: `status='active'`, `lifecycle_state='active'`, `tickLower`, `tickUpper`, `currentTick`, `rangeState='in'`, the actual hedge size (from `bot_runtime_state.last_known_hl_short_size`), the target hedge size (source pending I-003), `driftPct`, `marginStatus='ok'`, and the light-check timestamp field (field name pending I-004). No mandatory field is null without justification. (Ref: UC §3 Main Flow, AC-01, FR-EXBOT-090.) | P0 |
| TC_002 | Verify rangeState is 'in' when the current tick equals the lower boundary of the LP range | 1. Bot is running normally (`bots.lifecycle_state='active'`).\n2. Current tick in `MarketDataDO` equals `tickLower` exactly (lower boundary, inclusive). | 1. Call `GET /api/exbot/status` while `currentTick == tickLower`. | 1. Response returns `rangeState='in'` because the lower bound is inclusive in the half-open interval `[tickLower, tickUpper)`. The `currentTick` value in the response equals `tickLower`. (Ref: UC §3 step 5, AC-05, FR-EXBOT-020.) | P0 |
| TC_003 | Verify rangeState is 'out' when the current tick equals the upper boundary of the LP range | 1. Bot is running normally (`bots.lifecycle_state='active'`).\n2. Current tick in `MarketDataDO` equals `tickUpper` exactly (upper boundary, exclusive). | 1. Call `GET /api/exbot/status` while `currentTick == tickUpper`. | 1. Response returns `rangeState='out'` because `tickUpper` is excluded from the interval `[tickLower, tickUpper)`. (Example: tickLower=100, tickUpper=200, currentTick=200 → rangeState='out'.) (Ref: UC §3 step 5, AC-06, FR-EXBOT-020.) | P0 |
| TC_004 | Verify rangeState is 'in' when the current tick is one unit below the upper boundary | 1. Bot is running normally (`bots.lifecycle_state='active'`).\n2. Current tick in `MarketDataDO` is exactly one unit below `tickUpper` (`currentTick = tickUpper - 1`), the last in-range tick. | 1. Call `GET /api/exbot/status` while `currentTick == tickUpper - 1`. | 1. Response returns `rangeState='in'` — this is the last tick value considered in-range under the half-open interval. (Example: tickUpper=200, currentTick=199 → rangeState='in'.) (Ref: UC §3 step 5, AC-05, FR-EXBOT-020.) | P0 |
| TC_005 | Verify rangeState is 'out' when the current tick is below the lower boundary of the LP range | 1. Bot is running normally (`bots.lifecycle_state='active'`).\n2. Current tick in `MarketDataDO` is less than `tickLower` (below the lower bound). | 1. Call `GET /api/exbot/status` while `currentTick < tickLower`. (Example: tickLower=100, currentTick=99.) | 1. Response returns `rangeState='out'`. The `currentTick`, `tickLower`, and `tickUpper` values are all present in the response so the caller can determine the magnitude of the out-of-range move. (Ref: UC §3 step 5, AC-06.) | P0 |
| TC_006 | Verify the response includes safe_mode_reason when the bot is in SAFE_MODE | 1. Bot has entered SAFE_MODE (`bots.status='safe_mode'`, `bots.lifecycle_state='safe_mode'`).\n2. A `safe_mode_reason` value is persisted in D1 (exact column name pending I-006). | 1. Call `GET /api/exbot/status` for the bot in SAFE_MODE. | 1. Response is HTTP 200 with `status='safe_mode'`, `lifecycle_state='safe_mode'`, and a `safe_mode_reason` field containing the cause of SAFE_MODE entry. Response does not contain any value implying the bot is permanently closed — SAFE_MODE is a recoverable state per BR-EXBOT-007. (Ref: UC §4 A1, AC-02, FR-EXBOT-050, BR-EXBOT-007.) | P0 |
| TC_007 | Verify the response includes a cooldown end timestamp when the bot is in hedge_stopped_cooldown state | 1. Bot is in the stop-fired cooldown period (`bots.lifecycle_state='hedge_stopped_cooldown'`, `bots.status='active'`).\n2. A cooldown end timestamp is persisted in D1 (exact column name pending I-006). | 1. Call `GET /api/exbot/status` for a bot in `hedge_stopped_cooldown`. | 1. Response is HTTP 200 with `lifecycle_state='hedge_stopped_cooldown'` and a cooldown end timestamp accurate enough for the caller to compute remaining duration. Response also conveys that automatic re-hedging will be attempted after the cooldown ends (FR-EXBOT-034). (Ref: UC §4 A2, AC-03, FR-EXBOT-034.) | P0 |
| TC_008 | Verify the response is HTTP 404 when the authenticated user has no active bot | 1. Authenticated user has no bot record in D1, or the existing bot has `status='closed'`. | 1. Call `GET /api/exbot/status` for a user with no active bot. | 1. Response is HTTP 404 with a body containing a "No active bot" message (or equivalent per spec). No bot data fields are present. Response is not HTTP 200 with empty data, and not HTTP 500. (Ref: UC §4 A4, AC-04, NFR-EXBOT-015.) | P0 |
| TC_009 | Verify drift percentage is computed correctly using the formula when actual and target hedge sizes differ | 1. Bot is running normally (`bots.lifecycle_state='active'`).\n2. Actual hedge size (`bot_runtime_state.last_known_hl_short_size`) differs from the target hedge size by a measurable amount. (Example: actual = 1.0 ETH, target = 0.9 ETH, expected drift ≈ 11.11%.) | 1. Call `GET /api/exbot/status` and compare the returned `driftPct` against the manually computed value: `abs(actualShortEth - targetShortEth) / targetShortEth x 100`. | 1. `driftPct` in the response matches the formula result computed via BigDecimal arithmetic — no floating-point rounding error. Result must not show JS float artifacts (e.g., must not return 11.100000000000001 for 11.11%). (Note: targetShortEth source pending I-003 — this case assumes `bot_runtime_state.target_short_size`.) (Ref: UC §3 step 6, AC-07, NFR-EXBOT-008.) | P0 |
| TC_010 | Verify drift percentage is exactly zero when the actual hedge size equals the target | 1. Bot is running normally (`bots.lifecycle_state='active'`).\n2. Actual hedge size equals the target exactly (`last_known_hl_short_size == target_short_size`). | 1. Call `GET /api/exbot/status` when the hedge is perfectly balanced. | 1. Response returns `driftPct = 0` (exactly zero, not a floating-point near-zero). BigDecimal arithmetic must yield precise equality. (Ref: UC §3 step 6, NFR-EXBOT-008.) | P0 |
| TC_011 | Verify drift percentage precision is preserved for large hedge sizes with many decimal places | 1. Bot is running normally (`bots.lifecycle_state='active'`).\n2. Both actual and target hedge sizes are large values with many decimal places. (Example: actual = 9999999.123456789 ETH, target = 9999998.987654321 ETH.) | 1. Call `GET /api/exbot/status` with the large-valued hedge sizes. | 1. Response `driftPct` preserves full decimal precision without truncation or floating-point rounding artifacts, as required by NFR-EXBOT-008 (BigDecimal arithmetic). (Ref: NFR-EXBOT-008.) | P1 |
| TC_012 | Verify the response returns margin_status 'warning' when the margin is in the warning band | 1. Bot is running normally (`bots.lifecycle_state='active'`).\n2. `hedge_legs.margin_status='warning'` is persisted in D1 (marginUsage was in the warning band when hedge-sync last updated it). | 1. Call `GET /api/exbot/status` for a bot with margin in the warning band. | 1. Response returns `marginStatus='warning'`. This value is distinct from `ok` and `critical`. (Ref: UC §3 step 3, FR-EXBOT-060.) | P0 |
| TC_013 | Verify the response returns margin_status 'critical' when the margin is in the critical band | 1. Bot is running normally (`bots.lifecycle_state='active'`).\n2. `hedge_legs.margin_status='critical'` is persisted in D1 (marginUsage was at or above the critical threshold when hedge-sync last updated it). | 1. Call `GET /api/exbot/status` for a bot with margin in the critical band. | 1. Response returns `marginStatus='critical'`. This value is distinct from `ok` and `warning`. Note: reaching critical twice consecutively triggers SAFE_MODE — this case tests read-only reporting of the current critical state, not the SAFE_MODE transition itself. (Ref: UC §3 step 3, FR-EXBOT-060.) | P0 |
| TC_014 | Verify that SAFE_MODE is not presented as a terminal state in the status response | 1. Bot has `lifecycle_state='safe_mode'` and `bots.status='safe_mode'`. | 1. Call `GET /api/exbot/status` for the bot in SAFE_MODE. | 1. Response contains `status='safe_mode'` and does not include any field or value that implies the bot is permanently closed or terminated. SAFE_MODE must remain distinguishable from `lifecycle_state='closed'`. (Ref: BR-EXBOT-007, FR-EXBOT-050, AC-02.) | P0 |
| TC_015 | Verify the coarse status is 'active' while lifecycle_state is hedge_stopped_cooldown | 1. Bot has `lifecycle_state='hedge_stopped_cooldown'` and `bots.status='active'` (per State Registry). | 1. Call `GET /api/exbot/status` while the bot is in `hedge_stopped_cooldown`. | 1. Response returns `status='active'` and `lifecycle_state='hedge_stopped_cooldown'`. The caller can distinguish this from fully active (`lifecycle_state='active'`) using the fine-grained state. Response conveys that hedge-sync is suppressed during this period. (Ref: UC §4 A2, FR-EXBOT-034, states.md State Registry.) | P1 |
| TC_016 | Verify the response correctly reflects the lp_rebalancing transient state | 1. Bot has `lifecycle_state='lp_rebalancing'` and `bots.status='active'` (LP range rebalance is in progress). | 1. Call `GET /api/exbot/status` while the bot is in `lp_rebalancing`. | 1. Response returns `lifecycle_state='lp_rebalancing'` and `status='active'`. State is not presented as stopped, closed, or error. Caller can distinguish this transient state from fully active. (Ref: states.md State Registry, I-005 — derived from State Registry; UC has no alternate flow for this state.) | P1 |
| TC_017 | Verify the response is HTTP 503 when the Operator Facade cannot reach the ExBot Worker | 1. Operator Facade is running but the CF service binding to the ExBot Worker is broken or ExBot Worker is not deployed. | 1. Call `GET /api/exbot/status` via the Operator Facade while the service binding is unavailable. | 1. Response is HTTP 503 with body "Status service temporarily unavailable". No bot data fields are present. Response does not expose a stack trace or internal error details. (Ref: UC §4 A5, AC-08.) | P0 |
| TC_018 | Verify the response reflects margin_status at the exact lower boundary of the warning band | 1. Bot is running normally (`bots.lifecycle_state='active'`).\n2. `hedge_legs.margin_status` was written when marginUsage was exactly at the ok/warning threshold (0.55). | 1. Call `GET /api/exbot/status` with the boundary margin_status from D1. | 1. Response returns the same `marginStatus` value as stored in D1. The boundary assignment (whether 0.55 is 'ok' or 'warning') must be consistent with FR-EXBOT-060. (Ref: FR-EXBOT-060.) | P1 |
| TC_019 | Verify the response reflects the error state and distinguishes it from SAFE_MODE | 1. Bot has `lifecycle_state='error'` and `bots.status='error'` (admin intervention required). | 1. Call `GET /api/exbot/status` for the bot in the error state. | 1. Response returns `status='error'` and `lifecycle_state='error'`. This state is distinct from `safe_mode` — no auto-recovery without admin action. Caller can clearly differentiate `error` from `safe_mode`. (Ref: states.md State Registry, I-005 — derived from State Registry.) | P1 |
| TC_020 | Verify the response for a closed bot is distinguishable from the no-bot-record case | 1. A bot record exists in D1 with `lifecycle_state='closed'` and `bots.status='closed'` (the bot was previously active and is fully closed). | 1. Call `GET /api/exbot/status` for the user whose only bot is in the `closed` state. | 1. Response communicates that there is no active bot. If spec routes this to HTTP 404, the response body must be distinguishable from the "no bot record at all" case so caller can differentiate a never-created bot from a closed one. No live hedge or LP metrics required. (Ref: UC §4 A4, states.md State Registry, I-005.) | P1 |
| TC_021 | Verify two back-to-back status calls return identical data when no state change occurs between them | 1. Bot is running normally (`bots.lifecycle_state='active'`).\n2. No other operation modifies the bot's D1 state or `MarketDataDO` cache between the two calls. | 1. Call `GET /api/exbot/status` twice in quick succession with no intervening state change. | 1. Both responses return HTTP 200 with identical values for all fields. The UC is read-only — it produces no side effects in D1 and does not enqueue any message. (Ref: UC §3 Main Flow, NFR-EXBOT-011.) | P1 |

### I.2. Integration & State verification — Operation: Monitor Bot Status (GET /api/exbot/status)

| TC ID | Title | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|
| TC_022 | Verify hedgeSize and marginStatus fields reflect the D1 values written by the most recent hedge-sync | 1. Bot is running normally.\n2. A hedge-sync has just completed and written updated `bot_runtime_state.last_known_hl_short_size` and `hedge_legs.margin_status` to D1. | 1. Call `GET /api/exbot/status` immediately after the hedge-sync D1 write completes. | 1. Response fields `hedgeSize` and `marginStatus` reflect the values written by the most recent hedge-sync. Because D1 in CF Workers has eventual consistency, a value written within a few seconds may briefly lag — a retry within a few seconds must return the latest value. (Ref: UC §3 step 3, FR-EXBOT-025, §F.4.) | P0 |
| TC_023 | Verify the Operator Facade forwards the ExBot Worker response to the caller without modifying any field | 1. Bot is running normally.\n2. Both Operator Facade and ExBot Worker are available. | 1. Call `GET /api/exbot/status` via the Operator Facade and compare the response with the data ExBot Worker assembled from D1 and MarketDataDO. | 1. Operator Facade forwards the ExBot Worker JSON response to the caller unchanged — no field is added, removed, renamed, or modified in value. OPERATOR owns no ExBot business logic and performs no transformation. (Ref: UC §3 steps 2 and 8, FR-EXBOT-090.) | P0 |
| TC_024 | Verify the currentTick in the response is read from MarketDataDO and not from a direct RPC call | 1. Bot is running normally.\n2. `MarketDataDO` cache contains a known `currentTick` value.\n3. Worker-level tracing or logs are available to distinguish DO reads from outbound RPC calls. | 1. Call `GET /api/exbot/status` and inspect the worker execution trace for outbound on-chain RPC calls during request processing. | 1. `currentTick` value in the response matches the cached value in `MarketDataDO`. Worker trace shows no outbound on-chain RPC call to the Uniswap V3 pool during this request — tick is sourced exclusively from the DO cache per FR-EXBOT-093 and BR-EXBOT-003. (Ref: UC §3 step 4, FR-EXBOT-093.) | P0 |
| TC_025 | Verify the status response for a bot on Base chain uses the Base pool's MarketDataDO currentTick | 1. Bot is deployed on Base chain (chainId 8453) and is running normally.\n2. The Base pool's `MarketDataDO` slot contains a known `currentTick`. | 1. Call `GET /api/exbot/status` for the Base-chain bot. | 1. `currentTick` in the response matches the Base pool's cached tick, not the Optimism pool's tick. `rangeState` reflects the Base pool tick relative to the bot's LP range. (Ref: UC §3 step 4, FR-EXBOT-093, project-context-master §3.3 Dual-chain constraint.) | P0 |
| TC_026 | Verify the status response immediately after a SAFE_MODE transition reflects the new state | 1. Bot has just transitioned from `lifecycle_state='active'` to `lifecycle_state='safe_mode'` due to a SAFE_MODE trigger condition (e.g., two consecutive critical margin readings per FR-EXBOT-050). | 1. Call `GET /api/exbot/status` immediately after the `lifecycle_state` transition is persisted to D1. | 1. Response returns `status='safe_mode'` and `lifecycle_state='safe_mode'` reflecting the current D1 state. Response must not return the previous `active` state due to a D1 consistency lag. If an eventual-consistency delay occurs briefly, a retry within a few seconds must return `safe_mode`. (Ref: UC §4 A1, FR-EXBOT-050.) | P0 |
| TC_027 | Verify ten concurrent status requests for the same bot return consistent data and produce no errors | 1. Bot is running normally.\n2. No state-changing operation runs concurrently during the test window. | 1. Send ten `GET /api/exbot/status` requests concurrently (within a 1-second window) for the same bot. | 1. All ten responses return HTTP 200 with consistent field values drawn from the same D1 snapshot and the same `MarketDataDO` cache. No request returns HTTP 500 or times out abnormally. Because this UC is read-only, no locking or mutual exclusion is required and no race condition is expected. (Ref: FR-EXBOT-090, FR-EXBOT-093.) | P1 |
| TC_028 | Verify the full pipeline from Operator Facade through ExBot Worker, D1, and MarketDataDO in a single request cycle | 1. Bot is running normally with LP in range and margin ok.\n2. All components (Operator Facade, ExBot Worker, D1, MarketDataDO) are available. | 1. Call `GET /api/exbot/status` and verify each stage end-to-end: Facade proxies to ExBot Worker → Worker reads all required D1 fields → queries `currentTick` from `MarketDataDO` → computes `rangeState` and `driftPct` → assembles response → Facade forwards it. | 1. Response contains all fields with values consistent with the D1 data and the `MarketDataDO` cache. `rangeState` and `driftPct` values match the expected computation. Round-trip completes without error. (Ref: UC §3 Full Main Flow, FR-EXBOT-090, FR-EXBOT-093.) | P0 |
| TC_029 | Verify the first status call after bot-start completes returns all fields populated and non-null | 1. UC-EXBOT-bot-start has completed successfully and the bot has reached `lifecycle_state='active'`. | 1. Call `GET /api/exbot/status` immediately after bot-start completes. | 1. All response fields (`tickLower`, `tickUpper`, `last_known_hl_short_size`, `marginStatus`, `driftPct`, `rangeState`) are non-null, confirming bot-start has fully initialized all D1 tables the status endpoint depends on. (Ref: UC §2 Preconditions, §F.4.) | P0 |

### I.3. Non-functional (logic) verification — Operation: Monitor Bot Status (GET /api/exbot/status)

| TC ID | Title | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|
| TC_030 | Verify the ExBot Worker returns HTTP 403 when called directly from the internet, bypassing the Operator Facade | 1. ExBot Worker is deployed and accessible via CF service binding internally. | 1. Send a direct HTTP request to the ExBot Worker endpoint on any path, without going through the Operator Facade (bypassing the CF service binding and internal token). | 1. ExBot Worker returns HTTP 403 Forbidden. No bot data is returned. ExBot Worker is not internet-accessible; it only accepts requests via the CF service binding with a valid internal token. (Ref: FR-EXBOT-090, BR-EXBOT-010.) | P0 |
| TC_031 | Verify the ExBot Worker rejects a request from the service binding path when the internal auth token is missing or invalid | 1. ExBot Worker is deployed and reachable via CF service binding. | 1. Send a request to the ExBot Worker via the CF service binding path but with a missing or invalid internal auth token. | 1. ExBot Worker rejects the request with HTTP 403. No bot data is returned. This ensures that a misconfigured Facade or test harness cannot access ExBot Worker data without the correct internal token. (Ref: FR-EXBOT-090, BR-EXBOT-010.) | P0 |
| TC_032 | Verify no sensitive data appears in the status response body or worker logs | 1. Bot is running normally.\n2. Worker request/response logs are accessible for inspection. | 1. Call `GET /api/exbot/status` and inspect the full response body and the associated worker log output. | 1. Response body contains only the documented status fields. No field contains raw agent key material, raw DEK, HL account credentials, or any other sensitive secret. Worker logs for this request contain no raw key or credential values at INFO level. (Ref: NFR-EXBOT-010, FR-EXBOT-080.) | P0 |
