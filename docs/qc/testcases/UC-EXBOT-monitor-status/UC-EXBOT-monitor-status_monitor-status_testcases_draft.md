# Test Cases — UC-EXBOT-monitor-status Monitor Bot Status (v5)

**Document title:** Test Cases — UC-EXBOT-monitor-status Monitor Bot Status
**Date created:** 2026-07-23
**Author/Agent:** qc-func-tc-design-exbot
**Version:** v5 (draft)

**Total test cases:** 71 (Delta vs v4: 0 content change — IDs renumbered sequentially 1–71, no gaps/reordering) (FUNC: 44, INTG: 18, NFR: 9)
**Scope:** Logic-only (backend / API / Lambda — no UI)
**Update trigger:** ID_RENUMBER
**Source UC:** UC-EXBOT-monitor-status_monitor-status_audited_20260722_v6.md
**Source scenarios:** UC-EXBOT-monitor-status_monitor-status_scenarios_20260723_v5.md
**Output language:** English
**Version note:** v4 — update from v3 (69 TCs → 71 TCs). Trigger: REQUIREMENT_DELTA (audited UC v5→v6, scenarios v4→v5).
Changes: the audited UC v6 revealed a v1 gap — ExBot Lambda applies a hard `isAdmin` guard (step 2b) that rejects any
non-Admin caller (Investor, the UC's Primary actor) with HTTP 403 before any business logic runs. 67 existing TCs updated:
most gained an explicit Admin actor precondition (CH-01); all TCs referencing the endpoint were reworded from
`GET /api/exbot/status` to `GET /api/exbot/status/{botId}` (CH-02); TC_001/012/013/018/034/047/056 had v1-gap null wording
firmed up (CH-04); TC_049 gained an E-EXBOT-031 message assertion now that Alternate Flow A10 (`paused`) is formalized (CH-03).
TC_025 and TC_048 were retired — their expected results (Investor reaching the ownership check, or getting HTTP 200 on a
wallet match) are now factually contradicted by the isAdmin guard. 4 new TCs added (TC_070–073) covering the guard's own
behavior: wallet-matching Investor still blocked (AC-ms-01b), wallet-mismatched Investor blocked the same way (AC-ms-05),
a 4-row decision table proving guard-before-ownership-check order, and a guard-only status-code assertion pending BA
confirmation of the response message body (V-012).
v5 — housekeeping pass, requested by the user, ID_RENUMBER only, no content change. v4's TC IDs were stable-but-non-sequential:
new TCs were inserted mid-file at the position dictated by business-flow reading order (e.g. TC_070–073 landed between
old TC_026 and old TC_027), while retired TCs (old TC_025, old TC_048) left gaps in the numbering. v5 renumbers all 71 TCs
sequentially TC_001→TC_071 in file reading order (top to bottom) for readability, and updates the RTM's `Linked Test Cases`
references to match. No TC's title, pre-conditions, steps, expected result, priority, or annotation text changed — only the
ID numbers and their RTM cross-references changed. This intentionally breaks TC-ID traceability to v3/v4 (for example, old
TC_047 is now TC_050): anyone tracking defects or execution history by the old TC ID must re-map using the old→new table
recorded in the delivery report for this pass.

**Open issues carried from v6 audit (non-blocking):**
- V-001: `runtime_health_status` vs `health_status` column name — formally still Open (BA answer repeats the question); TCs use the UC-stated name; strong indirect technical evidence (ERD changelog) supports it, but no TC change is made until BA gives a real confirmation.
- V-009 (Major, Open): the isAdmin guard (UC, source of truth for v1) directly conflicts with `us-002.md` AC-EXBOT-002-1/2/3, which still describe an Investor happy path returning HTTP 200. Every TC in this file that requires Admin reflects the UC (current v1 behavior); if `us-002.md` is updated instead to match the UC, no TC change is needed — if the guard itself is removed in v1.1, this whole update batch will need to be reverted for the Investor actor.
- V-010 (Minor, Open): `message-list.md` mirror table has not been synced with `E-EXBOT-030`/`E-EXBOT-031` yet — does not block testing since `srs/spec.md` §5 is the canonical source and already contains both messages verbatim.
- V-011 (Minor, Open): `us-002.md` has not yet been updated to reflect the `/{botId}` path parameter or the isAdmin guard v1 gap.
- V-012 (Minor, Open): no message body / `E-EXBOT-*` code is defined yet for the isAdmin guard's own 403 rejection — TC_073 intentionally asserts only the HTTP status code until BA confirms.

#### Requirement Traceability Matrix

| AC ID | Acceptance Criteria | Linked Test Cases | Status |
|---|---|---|---|
| AC-01 | Active bot: 8 Implemented fields non-null + 11 Pending fields as null | TC_001, TC_050, TC_058 | Covered |
| AC-02 | SAFE_MODE: status='safe_mode'; not terminal (BR-EXBOT-007) | TC_006, TC_014, TC_063, TC_064 | Covered |
| AC-03 | hedge_stopped_cooldown: cooldown_end_at present (Pending field) | TC_007, TC_015 | Covered |
| AC-04 | lp_closing: HTTP 200 + E-EXBOT-021 | TC_022 | Covered |
| AC-05 | closed bot: HTTP 200 + E-EXBOT-022; record retained | TC_020, TC_023 | Covered |
| AC-06 | No bot record: HTTP 404 + E-EXBOT-023 | TC_008 | Covered |
| AC-07 | Pool Slot0 Cache unavailable: current_tick=null, range_state=null; HTTP 200 | TC_043, TC_044, TC_045 | Covered |
| AC-08 | lp_rebalancing: HTTP 200 with lifecycle_state='lp_rebalancing' | TC_016 | Covered |
| AC-09 | error state: HTTP 200 with status='error' + E-EXBOT-029 | TC_019 | Covered |
| AC-10 | Investor wallet mismatch: HTTP 403 from the isAdmin guard, not E-EXBOT-030 (v1 gap — the ownership check is unreachable in v1) | TC_027 | Covered [UPDATE] |
| AC-11 | Admin bypass ownership check: HTTP 200 | TC_025 | Covered |
| AC-12 | Missing X-Wallet-Address: HTTP 401 | TC_024 | Covered |
| AC-13 | HMAC invalid (Facade→Lambda): HTTP 401 | TC_040, TC_042 | Covered |
| AC-14 | drift% BigDecimal precision; formula per FR-EXBOT-021 | TC_009, TC_010, TC_011, TC_056 | Covered |
| AC-15 | Pending implementation fields return null (not absent) — exact 11 fields | TC_038 | Covered |
| FR-EXBOT-090 | ExBot Lambda 403 to direct internet; Facade forwards unchanged | TC_031, TC_039 | Covered |
| AC-ms-01 | Active bot: 8 Implemented non-null + 11 Pending null; range_state='in'; drift_pct calculated | TC_001, TC_050, TC_058 | Covered |
| AC-ms-02 | safe_mode: status='safe_mode'; safe_mode_reason=null (Pending) | TC_006, TC_014 | Covered |
| AC-ms-03 | hedge_stopped_cooldown: cooldown_end_at=null (Pending) | TC_007, TC_015 | Covered |
| AC-ms-04 | Pool Slot0 Cache unavailable: current_tick=null, range_state=null | TC_043, TC_044, TC_045 | Covered |
| AC-ms-05 | Wallet mismatch → 403 from the isAdmin guard, not E-EXBOT-030 (v1 gap — ownership check never reached in v1) | TC_027, TC_028 | Covered [UPDATE] |
| AC-ms-06 | No bot record → 404 E-EXBOT-023 | TC_008 | Covered |
| AC-ms-07 | lp_eth_amount=0 → drift_pct=null | TC_054 | Covered |
| AC-ms-08 | paused: HTTP 200; status='paused'; message E-EXBOT-031 (A10 formalized) | TC_051 | Covered |
| AC-ms-09 | Admin bypass: HTTP 200 | TC_025 | Covered |
| AC-ms-10 | closed bot: HTTP 200 + E-EXBOT-022 | TC_020, TC_023 | Covered |
| AC-ms-01b | Investor whose wallet matches the bot owner still receives HTTP 403 from the isAdmin guard, not HTTP 200 | TC_026 | Covered [NEW] |

---

## I. Operation: Monitor Bot Status (GET /api/exbot/status/{botId})

### I.1. Functional verification — Monitor Bot Status

| TC ID | Title | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|
| TC_001 | Verify the full status response is returned with 8 Implemented fields non-null and 11 Pending fields as null for a normally running bot [UPDATED — Reason: CH-01 (added Admin actor precondition), CH-02 (endpoint reworded), CH-04 (firmed up v1-gap null wording)] | 1. The caller has Admin privileges, so the request passes the `isAdmin` guard before any business logic runs (ExBot Lambda step 2b).\n2. Bot is running normally (`bots.lifecycle_state='active'`, `bots.status='active'`).\n3. Pool Slot0 Cache has a valid fresh tick for the bot's pool.\n4. All Aurora PostgreSQL tables (`bots`, `positions`, `bot_runtime_state`, `hedge_legs`) have fully populated rows.\n5. `lp_eth_amount > 0` in `bot_runtime_state`. | 1. Call `GET /api/exbot/status/{botId}` via the Operator Facade for the running bot. | 1. Response is HTTP 200 with a JSON body.\n2. Of the 8 Implemented fields, 5 carry real non-null values for a running bot: `bot_id`, `status='active'`, `lifecycle_state='active'`, `runtime_health_status`, `dry_run`.\n3. The remaining 3 Implemented fields always return `null` in v1 — this is a v1 gap, not a defect: `safe_mode_tier`, `last_reconcile_at`, `last_error_code`.\n4. The 11 Pending fields are all present in the JSON body with value `null`: `tick_lower`, `tick_upper`, `current_tick`, `range_state`, `actual_short_eth`, `target_short_eth`, `drift_pct`, `margin_status`, `last_light_check_at`, `safe_mode_reason`, `cooldown_end_at`.\n5. No field is absent from the JSON object — every one of the 19 fields is present, either with its real value or with `null`. (Ref: UC §3 step 7, AC-01, AC-ms-01, N-001.) | P0 |
| TC_002 | Verify range_state and current_tick are Pending fields — they return null in the current implementation even when the bot is in-range [UPDATED — Reason: CH-01 (added Admin actor precondition), CH-02 (endpoint reworded)] | 1. The caller has Admin privileges, so the request passes the `isAdmin` guard before any business logic runs (ExBot Lambda step 2b).\n2. Bot is running normally (`bots.lifecycle_state='active'`).\n3. Pool Slot0 Cache has a valid tick within `[tick_lower, tick_upper)`. | 1. Call `GET /api/exbot/status/{botId}`. | 1. Response is HTTP 200.\n2. `range_state` is `null` — this is a Pending field; the BVA logic (tickLower <= currentTick < tickUpper → 'in') applies only when the field is implemented.\n3. `current_tick` is `null` — also a Pending field.\n4. All 8 Implemented fields are non-null. (Ref: UC §3 step 7, v5 §D field inventory rows 11–12; applies when Pending fields are implemented.) | P0 |
| TC_003 | Verify range_state='out' behavior is covered when Pending fields are implemented — currentTick equals tick_upper (exclusive boundary) [UPDATED — Reason: CH-01 (added Admin actor precondition), CH-02 (endpoint reworded)] | 1. The caller has Admin privileges, so the request passes the `isAdmin` guard before any business logic runs (ExBot Lambda step 2b).\n2. [Applies when Pending fields are implemented.] Bot is running normally. `current_tick` in Pool Slot0 Cache equals `tick_upper` exactly (upper boundary, exclusive). | 1. Call `GET /api/exbot/status/{botId}` while `current_tick == tick_upper`. | 1. Response returns `range_state='out'` — `tick_upper` is excluded from `[tick_lower, tick_upper)`.\n2. `current_tick` in response equals `tick_upper`. (Ref: UC §3 step 5; applies when Pending fields implemented.) | P0 |
| TC_004 | Verify range_state='in' when Pending fields are implemented — currentTick is one unit below tick_upper (last in-range value) [UPDATED — Reason: CH-01 (added Admin actor precondition), CH-02 (endpoint reworded)] | 1. The caller has Admin privileges, so the request passes the `isAdmin` guard before any business logic runs (ExBot Lambda step 2b).\n2. [Applies when Pending fields are implemented.] `current_tick = tick_upper - 1`. | 1. Call `GET /api/exbot/status/{botId}` while `current_tick == tick_upper - 1`. | 1. Response returns `range_state='in'` — last tick value in-range under the half-open interval. (Ref: UC §3 step 5.) | P0 |
| TC_005 | Verify range_state='out' when Pending fields are implemented — currentTick is below tick_lower [UPDATED — Reason: CH-01 (added Admin actor precondition), CH-02 (endpoint reworded)] | 1. The caller has Admin privileges, so the request passes the `isAdmin` guard before any business logic runs (ExBot Lambda step 2b).\n2. [Applies when Pending fields are implemented.] `current_tick < tick_lower`. | 1. Call `GET /api/exbot/status/{botId}` while `current_tick < tick_lower`. | 1. Response returns `range_state='out'`.\n2. `current_tick`, `tick_lower`, `tick_upper` are all present so the caller can determine the magnitude of the out-of-range move. (Ref: UC §3 step 5.) | P0 |
| TC_006 | Verify the response reflects safe_mode status correctly — safe_mode_reason is a Pending field and returns null [UPDATED — Reason: CH-01 (added Admin actor precondition), CH-02 (endpoint reworded)] | 1. The caller has Admin privileges, so the request passes the `isAdmin` guard before any business logic runs (ExBot Lambda step 2b).\n2. Bot has `bots.status='safe_mode'`, `bots.lifecycle_state='safe_mode'`.\n3. `safe_mode_reason` is not yet implemented (Pending field). | 1. Call `GET /api/exbot/status/{botId}` for the bot in SAFE_MODE. | 1. Response is HTTP 200 with `status='safe_mode'`, `lifecycle_state='safe_mode'`.\n2. `safe_mode_reason` is present in the JSON body with value `null` (Pending field — not yet implemented; when implemented, must contain the cause of SAFE_MODE entry).\n3. Response does not imply the bot is permanently closed — SAFE_MODE is recoverable per BR-EXBOT-007. (Ref: UC §4 A1, AC-02, BR-EXBOT-007.) | P0 |
| TC_007 | Verify the response reflects hedge_stopped_cooldown state — cooldown_end_at is a Pending field and returns null [UPDATED — Reason: CH-01 (added Admin actor precondition), CH-02 (endpoint reworded)] | 1. The caller has Admin privileges, so the request passes the `isAdmin` guard before any business logic runs (ExBot Lambda step 2b).\n2. Bot has `bots.lifecycle_state='hedge_stopped_cooldown'`, `bots.status='active'`.\n3. `cooldown_end_at` is not yet implemented (Pending field). | 1. Call `GET /api/exbot/status/{botId}` for a bot in `hedge_stopped_cooldown`. | 1. Response is HTTP 200 with `lifecycle_state='hedge_stopped_cooldown'`.\n2. `cooldown_end_at` is present in the JSON body with value `null` (Pending field; when implemented, must contain the timestamp for remaining cooldown duration).\n3. `status='active'` per states.md State Registry. (Ref: UC §4 A2, AC-03.) | P0 |
| TC_008 | Verify the response is HTTP 404 with E-EXBOT-023 when there is no bot record for the user [UPDATED — Reason: CH-01 (added Admin actor precondition), CH-02 (endpoint reworded)] | 1. The caller has Admin privileges, so the request passes the `isAdmin` guard before any business logic runs (ExBot Lambda step 2b).\n2. No bot record exists in Aurora PostgreSQL for the authenticated user's `botId`.\n3. This is distinct from a `lifecycle_state='closed'` bot — a closed bot record still exists and returns HTTP 200. | 1. Call `GET /api/exbot/status/{botId}` for a user with no bot record in Aurora PostgreSQL. | 1. Response is HTTP 404 with message body containing E-EXBOT-023 "No active bot found for this account."\n2. No bot data fields are present. Response is not HTTP 200 with empty data. (Ref: UC §4 A4, AC-06, AC-ms-06.) | P0 |
| TC_009 | Verify drift_pct formula is implemented with BigDecimal and normalizeTargetRatioBps() — Pending field assertion applies when implemented [UPDATED — Reason: CH-01 (added Admin actor precondition), CH-02 (endpoint reworded)] | 1. The caller has Admin privileges, so the request passes the `isAdmin` guard before any business logic runs (ExBot Lambda step 2b).\n2. [Applies when Pending fields are implemented.] Bot is running normally.\n3. `bot_runtime_state.lp_eth_amount > 0`; `hedge_legs.target_ratio` is set (e.g. "0.70" representing 70%).\n4. `bot_runtime_state.last_known_hl_short_size` differs from the expected target. | 1. Call `GET /api/exbot/status/{botId}`.\n2. Compute expected `targetShortEth = lp_eth_amount × normalizeTargetRatioBps(target_ratio) / 10000`.\n3. Compute expected `drift_pct = abs(last_known_hl_short_size - targetShortEth) / targetShortEth × 100` using BigDecimal.\n4. Compare returned `drift_pct` against manually computed value. | 1. `drift_pct` matches the BigDecimal formula result — no floating-point rounding error.\n2. `target_ratio` = "0.70" must be converted to `7000n` via `normalizeTargetRatioBps()` before arithmetic — using `Number("0.70") × 10000` is a violation of NFR-EXBOT-008.\n3. Currently `drift_pct` is `null` (Pending field); this assertion activates when implemented. (Ref: UC §3 step 6, FR-EXBOT-021, NFR-EXBOT-008, I-003 resolved.) | P0 |
| TC_010 | Verify drift_pct is exactly zero when actual hedge size equals target — Pending field, applies when implemented [UPDATED — Reason: CH-01 (added Admin actor precondition), CH-02 (endpoint reworded)] | 1. The caller has Admin privileges, so the request passes the `isAdmin` guard before any business logic runs (ExBot Lambda step 2b).\n2. [Applies when Pending fields are implemented.] `last_known_hl_short_size == targetShortEth` exactly. | 1. Call `GET /api/exbot/status/{botId}`. | 1. `drift_pct = 0` (exactly zero, not a float near-zero). BigDecimal arithmetic must yield precise equality. (Ref: UC §3 step 6, NFR-EXBOT-008.) | P0 |
| TC_011 | Verify drift_pct precision is preserved for large hedge values — Pending field, applies when implemented [UPDATED — Reason: CH-01 (added Admin actor precondition), CH-02 (endpoint reworded)] | 1. The caller has Admin privileges, so the request passes the `isAdmin` guard before any business logic runs (ExBot Lambda step 2b).\n2. [Applies when Pending fields are implemented.] `last_known_hl_short_size` and `lp_eth_amount` are large values with many decimal places. | 1. Call `GET /api/exbot/status/{botId}` with large-valued hedge sizes in Aurora PostgreSQL. | 1. `drift_pct` preserves full decimal precision without truncation or floating-point artifacts, as required by NFR-EXBOT-008. (Ref: NFR-EXBOT-008.) | P1 |
| TC_012 | Verify margin_status='warning' — Pending field, value 'warning' applies when implemented [UPDATED — Reason: CH-01 (added Admin actor precondition), CH-02 (endpoint reworded), CH-04 (firmed up v1-gap null wording)] | 1. The caller has Admin privileges, so the request passes the `isAdmin` guard before any business logic runs (ExBot Lambda step 2b).\n2. [Applies when Pending fields are implemented.] `hedge_legs.margin_status='warning'` is persisted in Aurora PostgreSQL (marginUsage was in the warning band when hedge-sync last updated it). | 1. Call `GET /api/exbot/status/{botId}`. | 1. `margin_status` always returns `null` in v1 — it is a Pending field with no implementation yet, not merely an untested value.\n2. Once implemented: response returns `margin_status='warning'`, distinct from `normal` and `critical`. (Ref: FR-EXBOT-060, UC §D row 16.) | P0 |
| TC_013 | Verify margin_status='critical' — Pending field, value 'critical' applies when implemented [UPDATED — Reason: CH-01 (added Admin actor precondition), CH-02 (endpoint reworded), CH-04 (firmed up v1-gap null wording)] | 1. The caller has Admin privileges, so the request passes the `isAdmin` guard before any business logic runs (ExBot Lambda step 2b).\n2. [Applies when Pending fields are implemented.] `hedge_legs.margin_status='critical'` in Aurora PostgreSQL. | 1. Call `GET /api/exbot/status/{botId}`. | 1. `margin_status` always returns `null` in v1 — it is a Pending field with no implementation yet, not merely an untested value.\n2. Once implemented: returns `margin_status='critical'`, distinct from `warning`. Reaching critical twice consecutively triggers SAFE_MODE — this case tests read-only reporting, not the transition. (Ref: FR-EXBOT-060.) | P0 |
| TC_014 | Verify SAFE_MODE is not presented as a terminal state [UPDATED — Reason: CH-01 (added Admin actor precondition), CH-02 (endpoint reworded)] | 1. The caller has Admin privileges, so the request passes the `isAdmin` guard before any business logic runs (ExBot Lambda step 2b).\n2. Bot has `lifecycle_state='safe_mode'`, `bots.status='safe_mode'`. | 1. Call `GET /api/exbot/status/{botId}`. | 1. Response contains `status='safe_mode'` and does not include any field or value implying permanent closure. SAFE_MODE must be distinguishable from `lifecycle_state='closed'`. (Ref: BR-EXBOT-007, AC-02.) | P0 |
| TC_015 | Verify coarse status is 'active' while lifecycle_state is hedge_stopped_cooldown [UPDATED — Reason: CH-01 (added Admin actor precondition), CH-02 (endpoint reworded)] | 1. The caller has Admin privileges, so the request passes the `isAdmin` guard before any business logic runs (ExBot Lambda step 2b).\n2. Bot has `lifecycle_state='hedge_stopped_cooldown'`, `bots.status='active'`. | 1. Call `GET /api/exbot/status/{botId}`. | 1. Response returns `status='active'` and `lifecycle_state='hedge_stopped_cooldown'`. Caller can distinguish from fully active. (Ref: UC §4 A2, states.md.) | P1 |
| TC_016 | Verify lp_rebalancing transient state is reflected correctly [UPDATED — Reason: CH-01 (added Admin actor precondition), CH-02 (endpoint reworded)] | 1. The caller has Admin privileges, so the request passes the `isAdmin` guard before any business logic runs (ExBot Lambda step 2b).\n2. Bot has `lifecycle_state='lp_rebalancing'`, `bots.status='active'`. | 1. Call `GET /api/exbot/status/{botId}`. | 1. Response returns `lifecycle_state='lp_rebalancing'`, `status='active'`. Not presented as stopped, closed, or error. (Ref: UC §4 A6, AC-08.) | P1 |
| TC_017 | Verify HTTP 503 when Operator Facade cannot reach ExBot Lambda [UPDATED — Reason: CH-02 (endpoint reworded to `/{botId}`)] | 1. ExBot Lambda is unavailable (API Gateway returns 503). | 1. Simulate ExBot Lambda unavailability and call `GET /api/exbot/status/{botId}`. | 1. Response is HTTP 503. No bot data fields present. No stack trace or internal error details exposed. No E-EXBOT-* code (infra-level response). (Ref: UC §4 A5.) | P0 |
| TC_018 | Verify margin_status boundary at ok/warning threshold — Pending field, applies when implemented [UPDATED — Reason: CH-01 (added Admin actor precondition), CH-02 (endpoint reworded), CH-04 (firmed up v1-gap null wording)] | 1. The caller has Admin privileges, so the request passes the `isAdmin` guard before any business logic runs (ExBot Lambda step 2b).\n2. [Applies when Pending fields are implemented.] `hedge_legs.margin_status` was written when marginUsage was exactly at the ok/warning threshold (0.55 per FR-EXBOT-060). | 1. Call `GET /api/exbot/status/{botId}`. | 1. `margin_status` always returns `null` in v1 — it is a Pending field with no implementation yet, not merely an untested value.\n2. Once implemented: boundary assignment (whether 0.55 is 'normal' or 'warning') must be consistent with FR-EXBOT-060. (Ref: FR-EXBOT-060.) | P1 |
| TC_019 | Verify error state is reflected correctly and distinguished from SAFE_MODE [UPDATED — Reason: CH-01 (added Admin actor precondition), CH-02 (endpoint reworded)] | 1. The caller has Admin privileges, so the request passes the `isAdmin` guard before any business logic runs (ExBot Lambda step 2b).\n2. Bot has `lifecycle_state='error'`, `bots.status='error'`. | 1. Call `GET /api/exbot/status/{botId}`. | 1. Response returns `status='error'`, `lifecycle_state='error'`, and message E-EXBOT-029. State is distinct from `safe_mode` — no auto-recovery without admin action. (Ref: UC §4 A7, AC-09.) | P1 |
| TC_020 | Verify closed bot returns HTTP 200 (not 404) with lifecycle_state='closed' [UPDATED — Reason: CH-01 (added Admin actor precondition), CH-02 (endpoint reworded)] | 1. The caller has Admin privileges, so the request passes the `isAdmin` guard before any business logic runs (ExBot Lambda step 2b).\n2. A bot record exists in Aurora PostgreSQL with `lifecycle_state='closed'`, `bots.status='closed'` (bot previously active, then safely closed).\n3. Record is retained in Aurora PostgreSQL per UC §4 A3b. | 1. Call `GET /api/exbot/status/{botId}` for the user whose bot has `lifecycle_state='closed'`. | 1. Response is HTTP 200 (NOT 404) with `lifecycle_state='closed'`. Distinguishes closed bot (record retained) from no-record case (404). (Ref: UC §4 A3b, AC-05.) | P0 |
| TC_021 | Verify two back-to-back status calls return identical data when no state change occurs [UPDATED — Reason: CH-01 (added Admin actor precondition), CH-02 (endpoint reworded)] | 1. The caller has Admin privileges, so the request passes the `isAdmin` guard before any business logic runs (ExBot Lambda step 2b).\n2. Bot running normally. No other operation modifies Aurora PostgreSQL or Pool Slot0 Cache between calls. | 1. Call `GET /api/exbot/status/{botId}` twice in quick succession with no intervening state change. | 1. Both responses return HTTP 200 with identical values for all fields. UC is read-only — no side effects in Aurora PostgreSQL, no message enqueued. (Ref: UC §3 Main Flow.) | P1 |
| TC_022 | Verify lp_closing state returns HTTP 200 with E-EXBOT-021 message [UPDATED — Reason: CH-01 (added Admin actor precondition), CH-02 (endpoint reworded)] | 1. The caller has Admin privileges, so the request passes the `isAdmin` guard before any business logic runs (ExBot Lambda step 2b).\n2. Bot has `lifecycle_state='lp_closing'` in Aurora PostgreSQL. | 1. Call `GET /api/exbot/status/{botId}` while bot is in `lp_closing`. | 1. Response is HTTP 200 with `lifecycle_state='lp_closing'` and body containing E-EXBOT-021 "Bot close is in progress. Please wait." Not HTTP 404 — bot record exists. (Ref: UC §4 A3a, AC-04.) | P0 |
| TC_023 | Verify closed bot returns HTTP 200 with E-EXBOT-022 message [UPDATED — Reason: CH-01 (added Admin actor precondition), CH-02 (endpoint reworded)] | 1. The caller has Admin privileges, so the request passes the `isAdmin` guard before any business logic runs (ExBot Lambda step 2b).\n2. Bot has `lifecycle_state='closed'` in Aurora PostgreSQL. | 1. Call `GET /api/exbot/status/{botId}` for the user whose bot is `closed`. | 1. Response is HTTP 200. Body contains E-EXBOT-022 "Bot safely closed. Funds have been returned to your wallet." (Ref: UC §4 A3b, AC-05.) | P0 |
| TC_024 | Verify HTTP 401 when X-Wallet-Address header is missing [UPDATED — Reason: CH-02 (endpoint reworded to `/{botId}`)] | 1. Operator Facade running, ExBot Lambda available.\n2. No `X-Wallet-Address` header in the request. | 1. Call `GET /api/exbot/status/{botId}` without the `X-Wallet-Address` header. | 1. Response is HTTP 401 from Operator Facade. No bot data returned. Facade rejects before reaching ExBot Lambda. (Ref: UC §2, AC-12.) | P0 |
| TC_025 | Verify Admin can query any bot regardless of wallet ownership [UPDATED — Reason: CH-02 (endpoint reworded to `/{botId}`)] | 1. Bot exists with `bot.user_wallet_address = '0xABC...'`.\n2. Admin actor sends `X-Wallet-Address` of a different wallet. | 1. Admin calls `GET /api/exbot/status/{botId}` for a bot owned by a different user. | 1. Response is HTTP 200 with full bot status. Admin bypasses ownership check — `accessControl` middleware grants Admin access to any bot regardless of `bot.user_wallet_address`. (Ref: UC §2, AC-11.) | P0 |
| TC_026 | Verify an Investor whose wallet matches the bot owner is still blocked with HTTP 403 by the isAdmin guard [NEW — AC-ms-01b] | 1. A bot exists and its owner wallet is on file (`bot.user_wallet_address`).\n2. The caller is an Investor (not Admin) whose `X-Wallet-Address` header matches the bot's owner wallet exactly. | 1. Call `GET /api/exbot/status/{botId}` as the Investor whose wallet matches the bot owner. | 1. Response is HTTP 403 Forbidden — the `isAdmin` guard (ExBot Lambda step 2b) rejects the caller before the wallet-ownership check ever runs, and before any Aurora PostgreSQL read happens.\n2. Response is NOT HTTP 200, even though the wallet matches the bot owner — a wallet match no longer grants access in v1 because only Admin passes the guard.\n3. No bot data fields are present in the response. (Ref: UC §6.1.A step 2b, AC-ms-01b, V-009.) | P0 |
| TC_027 | Verify an Investor whose wallet does NOT match the bot owner receives the same HTTP 403 from the isAdmin guard, not the ownership-check error [NEW — AC-ms-05] | 1. A bot exists and its owner wallet is on file (`bot.user_wallet_address`).\n2. The caller is an Investor (not Admin) whose `X-Wallet-Address` header does not match the bot's owner wallet. | 1. Call `GET /api/exbot/status/{botId}` as the Investor whose wallet does not match the bot owner. | 1. Response is HTTP 403 Forbidden — the `isAdmin` guard rejects the caller at step 2b, before the wallet-ownership check (step 3 / A9) is ever evaluated.\n2. Response body does NOT contain the ownership-mismatch message `E-EXBOT-030` — that check is never reached in v1; only the guard fires.\n3. No bot data fields are present in the response. (Ref: UC §6.1.A step 2b, §4 A9, AC-ms-05, V-009.) | P0 |
| TC_028 | Verify the isAdmin guard is evaluated before the wallet-ownership check across every combination of actor and wallet match [NEW — AC-ms-05] | 1. A bot exists and its owner wallet is on file (`bot.user_wallet_address`). | 1. Call `GET /api/exbot/status/{botId}` four times against the same bot, varying only actor role and wallet match: (a) Admin querying a bot owned by a different wallet; (b) Admin querying a bot owned by their own wallet; (c) Investor whose wallet matches the bot owner; (d) Investor whose wallet does not match the bot owner. | 1. Case (a): HTTP 200 with full bot status — Admin bypasses both the guard and the ownership check.\n2. Case (b): HTTP 200 with full bot status — same as (a); wallet match is irrelevant for Admin.\n3. Case (c): HTTP 403 from the `isAdmin` guard — wallet match does not help an Investor.\n4. Case (d): HTTP 403 from the `isAdmin` guard — identical result to (c); the two Investor cases are indistinguishable from the caller's point of view, confirming the guard runs before wallet-ownership is ever checked. (Ref: UC §6.1.A step 2b, AC-ms-05, V-009.) | P0 |
| TC_029 | Verify the isAdmin guard's 403 response is asserted only by HTTP status code, since no message body is defined yet [NEW — V-012] | 1. The caller is an Investor (not Admin), so the request is blocked by the `isAdmin` guard regardless of wallet match. | 1. Call `GET /api/exbot/status/{botId}` as the blocked Investor and inspect the full response body. | 1. Response is HTTP 403 Forbidden.\n2. The test asserts ONLY the HTTP status code for this rejection — it must NOT assert any specific message text or `E-EXBOT-*` code, because the UC does not yet define a message body for this guard rejection (unlike every other 403 in this UC, which carries a defined error code).\n3. This gap is intentional and tracked; once BA confirms the response body content, this test case must be revised to add the message-text assertion. (Ref: UC §6.1.C Error/messages table, V-012.) | P1 |

---

### I.2. Integration & State verification — Monitor Bot Status

| TC ID | Title | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|
| TC_030 | Verify hedge size and margin_status fields in response reflect the Aurora PostgreSQL values written by the most recent hedge-sync [UPDATED — Reason: CH-01 (added Admin actor precondition), CH-02 (endpoint reworded)] | 1. The caller has Admin privileges, so the request passes the `isAdmin` guard before any business logic runs (ExBot Lambda step 2b).\n2. Bot running normally.\n3. A hedge-sync has just completed and written updated `bot_runtime_state.last_known_hl_short_size` and `hedge_legs.margin_status` to Aurora PostgreSQL. | 1. Call `GET /api/exbot/status/{botId}` immediately after the hedge-sync Aurora PostgreSQL write completes. | 1. The `actual_short_eth` (Pending — currently null) when implemented must reflect `last_known_hl_short_size` from the most recent hedge-sync write.\n2. `margin_status` (Pending — currently null) when implemented must reflect `hedge_legs.margin_status` written by the latest hedge-sync.\n3. Aurora Serverless v2 provides read consistency — latest written values must be immediately visible on the next read. (Ref: UC §3 step 3, FR-EXBOT-025.) | P0 |
| TC_031 | Verify Operator Facade forwards ExBot Lambda response unchanged [UPDATED — Reason: CH-01 (added Admin actor precondition), CH-02 (endpoint reworded)] | 1. The caller has Admin privileges, so the request passes the `isAdmin` guard before any business logic runs (ExBot Lambda step 2b).\n2. Bot running normally. Both Facade and Lambda available. | 1. Call `GET /api/exbot/status/{botId}` via Operator Facade and compare response with what ExBot Lambda assembled from Aurora PostgreSQL and Pool Slot0 Cache. | 1. Facade forwards ExBot Lambda JSON response unchanged — no field added, removed, renamed, or modified in value. Facade owns no ExBot business logic. (Ref: UC §3 steps 2 and 8, FR-EXBOT-090.) | P0 |
| TC_032 | Verify current_tick in the response is read from Pool Slot0 Cache only — no direct on-chain RPC call [UPDATED — Reason: CH-01 (added Admin actor precondition), CH-02 (endpoint reworded)] | 1. The caller has Admin privileges, so the request passes the `isAdmin` guard before any business logic runs (ExBot Lambda step 2b).\n2. [Applies when Pending fields are implemented.] Bot running normally. Pool Slot0 Cache contains a known `current_tick`.\n3. Lambda execution trace or logs available. | 1. Call `GET /api/exbot/status/{botId}` and inspect Lambda execution trace for outbound on-chain RPC calls. | 1. `current_tick` in response matches the cached value in Pool Slot0 Cache (ElastiCache Redis).\n2. Lambda trace shows no outbound on-chain RPC call — tick is sourced exclusively from the cache per FR-EXBOT-093 and BR-EXBOT-003.\n3. Currently `current_tick` is `null` (Pending field). (Ref: UC §3 step 4, FR-EXBOT-093.) | P0 |
| TC_033 | Verify Base chain bot uses the Base pool's Pool Slot0 Cache currentTick [UPDATED — Reason: CH-01 (added Admin actor precondition), CH-02 (endpoint reworded)] | 1. The caller has Admin privileges, so the request passes the `isAdmin` guard before any business logic runs (ExBot Lambda step 2b).\n2. [Applies when Pending fields are implemented.] Bot is deployed on Base chain (chainId 8453) and running normally.\n3. Base pool's Pool Slot0 Cache slot contains a known `current_tick`. | 1. Call `GET /api/exbot/status/{botId}` for the Base-chain bot. | 1. `current_tick` in response matches the Base pool's cached tick, not the Optimism pool's tick.\n2. `range_state` reflects Base pool tick relative to the bot's LP range.\n3. Currently both are `null` (Pending fields). (Ref: UC §3 step 4, FR-EXBOT-093.) | P0 |
| TC_034 | Verify SAFE_MODE transition is immediately visible in the next status call [UPDATED — Reason: CH-01 (added Admin actor precondition), CH-02 (endpoint reworded)] | 1. The caller has Admin privileges, so the request passes the `isAdmin` guard before any business logic runs (ExBot Lambda step 2b).\n2. Bot has just transitioned from `lifecycle_state='active'` to `lifecycle_state='safe_mode'` (e.g., two consecutive critical margin readings per FR-EXBOT-050). | 1. Call `GET /api/exbot/status/{botId}` immediately after the `lifecycle_state` transition is persisted to Aurora PostgreSQL. | 1. Response returns `status='safe_mode'` and `lifecycle_state='safe_mode'` — must not return the previous `active` state. Aurora PostgreSQL consistency ensures the new state is immediately visible. (Ref: UC §4 A1, FR-EXBOT-050.) | P0 |
| TC_035 | Verify ten concurrent status requests return consistent data [UPDATED — Reason: CH-01 (added Admin actor precondition), CH-02 (endpoint reworded)] | 1. The caller has Admin privileges, so the request passes the `isAdmin` guard before any business logic runs (ExBot Lambda step 2b).\n2. Bot running normally. No state-changing operation during the test window. | 1. Send ten `GET /api/exbot/status/{botId}` requests concurrently (within a 1-second window) for the same bot. | 1. All ten responses return HTTP 200 with consistent field values from the same Aurora PostgreSQL snapshot and Pool Slot0 Cache value.\n2. No request returns HTTP 500 or times out abnormally. Read-only endpoint — no locking required, no race condition expected. (Ref: FR-EXBOT-090.) | P1 |
| TC_036 | Verify full pipeline E2E — Facade through API Gateway, Lambda, Aurora PostgreSQL, and Pool Slot0 Cache [UPDATED — Reason: CH-01 (added Admin actor precondition), CH-02 (endpoint reworded)] | 1. The caller has Admin privileges, so the request passes the `isAdmin` guard before any business logic runs (ExBot Lambda step 2b).\n2. Bot running normally with LP in range, margin ok. All components available. | 1. Call `GET /api/exbot/status/{botId}` and verify each stage: Operator Facade → API Gateway → HMAC Lambda Authorizer → ExBot Lambda reads Aurora PostgreSQL (4 tables) → queries Pool Slot0 Cache → computes derived values → assembles response → Facade forwards to caller. | 1. Response contains all 8 Implemented fields with values consistent with Aurora PostgreSQL data, plus 11 Pending fields as `null`.\n2. Round-trip completes without error. (Ref: UC §3 Full Main Flow.) | P0 |
| TC_037 | Verify first status call after bot-start returns 8 Implemented fields populated and non-null [UPDATED — Reason: CH-01 (added Admin actor precondition), CH-02 (endpoint reworded), CH-04 (firmed up v1-gap null wording)] | 1. The caller has Admin privileges, so the request passes the `isAdmin` guard before any business logic runs (ExBot Lambda step 2b).\n2. UC-EXBOT-bot-start has completed successfully; bot reached `lifecycle_state='active'`. | 1. Call `GET /api/exbot/status/{botId}` immediately after bot-start completes. | 1. Of the 8 Implemented fields, `bot_id`, `status`, `lifecycle_state`, `runtime_health_status`, and `dry_run` are non-null, confirming bot-start fully initialized the Aurora PostgreSQL tables the status endpoint depends on.\n2. `safe_mode_tier`, `last_reconcile_at`, and `last_error_code` are `null` — this is the v1 gap for these 3 fields, not a sign that bot-start failed to initialize them.\n3. All 11 Pending fields are `null`. (Ref: UC §2 Preconditions.) | P0 |
| TC_038 | Verify exactly 11 Pending fields are present as null in the JSON response — none absent [UPDATED — Reason: CH-01 (added Admin actor precondition), CH-02 (endpoint reworded)] | 1. The caller has Admin privileges, so the request passes the `isAdmin` guard before any business logic runs (ExBot Lambda step 2b).\n2. Bot running normally (`bots.lifecycle_state='active'`).\n3. None of the Pending fields have been implemented. | 1. Call `GET /api/exbot/status/{botId}` and inspect the full JSON response for all Pending field keys and their values. | 1. All 11 Pending fields are present as keys in the JSON body with value `null`: `tick_lower`, `tick_upper`, `current_tick`, `range_state`, `actual_short_eth`, `target_short_eth`, `drift_pct`, `margin_status`, `last_light_check_at`, `safe_mode_reason`, `cooldown_end_at`.\n2. No field is absent — absent ≠ null, two distinct API contract states.\n3. Exactly 11 Pending fields (not more, not fewer). (Ref: UC §3 step 7, AC-15, N-001.) | P0 |

---

### I.3. Non-functional (logic) verification — Monitor Bot Status

| TC ID | Title | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|
| TC_039 | Verify ExBot Lambda returns HTTP 403 when called directly from the internet without API Gateway | 1. ExBot Lambda deployed and accessible via API Gateway + HMAC Lambda Authorizer internally. | 1. Attempt a direct HTTP request to the ExBot Lambda function URL (bypassing API Gateway), without HMAC authorization. | 1. ExBot Lambda returns HTTP 403. No bot data returned. Lambda not internet-accessible directly — only accepts requests via API Gateway with valid HMAC authorization. (Ref: FR-EXBOT-090.) | P0 |
| TC_040 | Verify HMAC Lambda Authorizer rejects request when HMAC signature is missing or invalid [UPDATED — Reason: CH-02 (endpoint reworded to `/{botId}`)] | 1. ExBot Lambda deployed, API Gateway configured.\n2. HMAC Lambda Authorizer configured with known signing secret. | 1. Send a `GET /api/exbot/status/{botId}` request to ExBot Lambda via API Gateway with a missing or invalid HMAC signature. | 1. HMAC Lambda Authorizer rejects with HTTP 401. ExBot Lambda is never invoked. No bot data returned. (Ref: UC §2, AC-13.) | P0 |
| TC_041 | Verify no sensitive data appears in the status response body or Lambda logs [UPDATED — Reason: CH-01 (added Admin actor precondition), CH-02 (endpoint reworded)] | 1. The caller has Admin privileges, so the request passes the `isAdmin` guard before any business logic runs (ExBot Lambda step 2b).\n2. Bot running normally. Lambda execution logs accessible. | 1. Call `GET /api/exbot/status/{botId}` and inspect full response body and associated Lambda log output. | 1. Response body contains only documented status fields. No field contains raw agent key, DEK, HL credentials, or any sensitive secret. Lambda logs contain no raw key or credential values at INFO level. (Ref: NFR-EXBOT-010.) | P0 |
| TC_042 | Verify HTTP 401 when Operator Facade forwards an invalid HMAC signature to API Gateway [UPDATED — Reason: CH-02 (endpoint reworded to `/{botId}`)] | 1. Operator Facade running but misconfigured — sends invalid HMAC signature (wrong key or tampered payload). | 1. Trigger a `GET /api/exbot/status/{botId}` request where the Facade sends a wrong HMAC signature. | 1. HMAC Lambda Authorizer returns HTTP 401. ExBot Lambda is not invoked. No bot data returned. End-to-end HMAC enforcement on the Facade→Lambda path confirmed. (Ref: UC §2, AC-13.) | P0 |

---

## II. QC Review — Gap Fill (2026-07-08)

### II.1. Functional verification — Gap fill

| TC ID | Title | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|
| TC_043 | Verify Pool Slot0 Cache unavailable returns HTTP 200 with current_tick=null and range_state=null [UPDATED — Reason: CH-01 (added Admin actor precondition), CH-02 (endpoint reworded)] | 1. The caller has Admin privileges, so the request passes the `isAdmin` guard before any business logic runs (ExBot Lambda step 2b).\n2. Bot running normally (`bots.lifecycle_state='active'`, `bots.status='active'`).\n3. Pool Slot0 Cache (ElastiCache Redis) is unreachable.\n4. All Aurora PostgreSQL tables have fully populated rows. | 1. Call `GET /api/exbot/status/{botId}` while Pool Slot0 Cache is unreachable. | 1. Response is HTTP 200 — cache unavailability does NOT produce 503 or 500.\n2. `current_tick` = `null`.\n3. `range_state` = `null` — computation skipped when `currentTick` unavailable.\n4. All Aurora PostgreSQL-sourced Implemented fields are populated normally.\n5. No retry to Pool Slot0 Cache. (Ref: UC §4 A8, FR-EXBOT-093, AC-07.) | P0 |
| TC_044 | Verify Pool Slot0 Cache stale snapshot is treated identically to unavailable [UPDATED — Reason: CH-01 (added Admin actor precondition), CH-02 (endpoint reworded)] | 1. The caller has Admin privileges, so the request passes the `isAdmin` guard before any business logic runs (ExBot Lambda step 2b).\n2. Bot running normally. Pool Slot0 Cache has data but snapshot is stale (blockNumber has not advanced beyond threshold). | 1. Call `GET /api/exbot/status/{botId}` while Pool Slot0 Cache has stale snapshot. | 1. Response is HTTP 200 — stale treated identically to unavailable.\n2. `current_tick: null`, `range_state: null`.\n3. All Aurora PostgreSQL-sourced Implemented fields populated normally. (Ref: UC §4 A8, FR-EXBOT-093.) | P1 |
| TC_045 | Verify range_state is null and no computation occurs when current_tick is null [UPDATED — Reason: CH-01 (added Admin actor precondition), CH-02 (endpoint reworded)] | 1. The caller has Admin privileges, so the request passes the `isAdmin` guard before any business logic runs (ExBot Lambda step 2b).\n2. Pool Slot0 Cache returns no `currentTick` (key absent or returns null). | 1. Call `GET /api/exbot/status/{botId}`. Inspect both `current_tick` and `range_state`. | 1. `current_tick: null`.\n2. `range_state: null` — formula `tickLower <= currentTick < tickUpper` is NOT evaluated when `currentTick` is null.\n3. Response does not return `"in"` or `"out"` based on assumption.\n4. HTTP 200 — no error thrown. (Ref: UC §3 step 5.) | P0 |
| TC_046 | BVA rangeState — currentTick = tickLower - 1 (one step below lower bound, must be 'out') when Pending fields implemented [UPDATED — Reason: CH-01 (added Admin actor precondition), CH-02 (endpoint reworded)] | 1. The caller has Admin privileges, so the request passes the `isAdmin` guard before any business logic runs (ExBot Lambda step 2b).\n2. [Applies when Pending fields are implemented.] Bot running normally. `current_tick = tick_lower - 1`. | 1. Call `GET /api/exbot/status/{botId}` while `current_tick = tick_lower - 1`. | 1. `range_state='out'` — `currentTick < tickLower` falls outside `[tickLower, tickUpper)`.\n2. `current_tick` in response equals `tick_lower - 1`. (Ref: UC §3 step 5.) | P0 |
| TC_047 | BVA margin_status — margin_status is 'normal' when marginUsage is below 0.55, when Pending field is implemented [UPDATED — Reason: CH-01 (added Admin actor precondition), CH-02 (endpoint reworded)] | 1. The caller has Admin privileges, so the request passes the `isAdmin` guard before any business logic runs (ExBot Lambda step 2b).\n2. [Applies when Pending fields are implemented.] `hedge_legs.margin_status='normal'` in Aurora PostgreSQL (marginUsage < 0.55 when hedge-sync last updated it). | 1. Call `GET /api/exbot/status/{botId}`. | 1. Currently `margin_status` is `null` (Pending field).\n2. When implemented: response returns `margin_status='normal'` — lowest margin risk band. Value matches Aurora PostgreSQL — status endpoint reads it, does not recompute. (Ref: UC §D row 16, scenarios v4 TS_017; note: value name changed from `ok` to `normal`.) | P1 |
| TC_048 | BVA margin_status — margin_status is 'critical' when marginUsage is at or above 0.75, when Pending field is implemented [UPDATED — Reason: CH-01 (added Admin actor precondition), CH-02 (endpoint reworded)] | 1. The caller has Admin privileges, so the request passes the `isAdmin` guard before any business logic runs (ExBot Lambda step 2b).\n2. [Applies when Pending fields are implemented.] `hedge_legs.margin_status='critical'` in Aurora PostgreSQL. | 1. Call `GET /api/exbot/status/{botId}`. | 1. Currently `margin_status` is `null` (Pending field).\n2. When implemented: response returns `margin_status='critical'` — highest margin risk band. Value matches Aurora PostgreSQL. (Ref: FR-EXBOT-060.) | P0 |
| TC_049 | Verify HTTP 403 when caller's wallet is blocked by Operator Facade [UPDATED — Reason: CH-02 (endpoint reworded to `/{botId}`)] | 1. Caller's wallet address is on the Operator Facade block list.\n2. Valid `X-Wallet-Address` header is present. | 1. Call `GET /api/exbot/status/{botId}` with a blocked wallet address. | 1. Response is HTTP 403 from Operator Facade — not from ExBot Lambda.\n2. Request NOT forwarded to ExBot Lambda. No bot data returned. (Ref: UC §2 Preconditions.) | P0 |
| TC_050 | Verify all 8 Implemented fields are always present (not absent) in the JSON response when a bot exists [UPDATED — Reason: CH-01 (added Admin actor precondition), CH-02 (endpoint reworded), CH-04 (firmed up v1-gap null wording)] | 1. The caller has Admin privileges, so the request passes the `isAdmin` guard before any business logic runs (ExBot Lambda step 2b).\n2. Bot exists in Aurora PostgreSQL with `lifecycle_state='active'`.\n3. The 8 Implemented fields (`bot_id`, `status`, `lifecycle_state`, `safe_mode_tier`, `runtime_health_status`, `last_reconcile_at`, `last_error_code`, `dry_run`) are resolvable from Aurora PostgreSQL. | 1. Call `GET /api/exbot/status/{botId}` and inspect full JSON response keys. | 1. All 8 Implemented fields are present as keys in the JSON body — none is absent.\n2. `safe_mode_tier`, `last_reconcile_at`, and `last_error_code` always appear with value `null` — this is a confirmed v1 gap (CH-04), not an open question; these 3 fields have no live data source in v1 and will carry real values only in v1.1.\n3. `bot_id`, `status`, `lifecycle_state`, `runtime_health_status`, `dry_run` are non-null for an active bot.\n4. Note: field name conflict V-001 (`runtime_health_status` vs `health_status`) remains formally open — TC uses the UC-stated name `runtime_health_status`; follow-up pass needed only if BA gives a different formal confirmation. (Ref: UC §3 step 7, N-001, V-001.) | P0 |
| TC_051 | Verify status response reflects paused state — status='paused', lifecycle_state='active' [UPDATED — Reason: CH-01 (added Admin actor precondition), CH-02 (endpoint reworded), CH-03 (A10 formalized, E-EXBOT-031 assertion added, V-008 now Resolved)] | 1. The caller has Admin privileges, so the request passes the `isAdmin` guard before any business logic runs (ExBot Lambda step 2b).\n2. Bot has `bots.status='paused'`, `bots.lifecycle_state='active'` in Aurora PostgreSQL (paused from `lifecycle_state='active'` per FR-EXBOT-003). | 1. Call `GET /api/exbot/status/{botId}` for the paused bot. | 1. Response is HTTP 200 with `status='paused'` and `lifecycle_state='active'`.\n2. `lifecycle_state` is NOT changed to 'paused' — FR-EXBOT-003 preserves lifecycle_state at time of pause.\n3. Response body contains message E-EXBOT-031 "Bot Paused. Hedge and LP are maintained. You may still redeem."\n4. Caller can distinguish from running-active bot by checking `status='paused'`.\n5. Note: this case is now covered by the formal Alternate Flow A10 (V-008 Resolved); the Investor variant of A10 remains Blocked by the `isAdmin` guard (V-009) until v1.1. (Ref: UC §4 A10, FR-EXBOT-003, states.md.) | P0 |
| TC_052 | Verify transitional lifecycle_state returns HTTP 200 with the exact state value [UPDATED — Reason: CH-01 (added Admin actor precondition), CH-02 (endpoint reworded)] | 1. The caller has Admin privileges, so the request passes the `isAdmin` guard before any business logic runs (ExBot Lambda step 2b).\n2. Bot has a transitional `lifecycle_state` (e.g., `'lp_opening'`) in Aurora PostgreSQL. | 1. Call `GET /api/exbot/status/{botId}` while bot is in `lifecycle_state='lp_opening'`. | 1. Response is HTTP 200 — transitional states do NOT produce 404 or error.\n2. `lifecycle_state` in response matches Aurora PostgreSQL value exactly.\n3. No lifecycle_state value is silently remapped, defaulted, or suppressed. (Ref: states.md.) | P1 |

### II.2. Integration & State verification — Gap fill

| TC ID | Title | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|
| TC_053 | Verify status response maps all valid lifecycle_state values — each returned as-stored in Aurora PostgreSQL [UPDATED — Reason: CH-01 (added Admin actor precondition), CH-02 (endpoint reworded)] | 1. The caller has Admin privileges, so the request passes the `isAdmin` guard before any business logic runs (ExBot Lambda step 2b).\n2. Aurora PostgreSQL contains bots in each of the 18 canonical `lifecycle_state` values: `active`, `lp_rebalancing`, `lp_closing`, `closed`, `hedge_stopped_cooldown`, `lp_opening`, `safe_mode`, `error`, `closing`, `paused`, `initializing`, `pending_deposit`, `pending_start`, `starting`, `pending_close`, `partial_close`, `pending_recovery`, `recovering`. | 1. For each `lifecycle_state`, call `GET /api/exbot/status/{botId}` and record the returned value.\n2. Compare each returned value against Aurora PostgreSQL-stored value. | 1. For every `lifecycle_state`, the value in response matches exactly the value stored in Aurora PostgreSQL — no state remapped, aliased, or suppressed.\n2. All states return HTTP 200 (closed also HTTP 200 per AC-05; no-record returns 404 per AC-06).\n3. No value outside the 18 canonical states is accepted or returned. (Ref: UC §4 all alternates, states.md.) | P1 |

---

## III. v3 New Test Cases

### III.1. Functional verification — v3 additions

| TC ID | Title | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|
| TC_054 | Verify drift_pct is null when lp_eth_amount is zero — division-by-zero guard [UPDATED — Reason: CH-01 (added Admin actor precondition), CH-02 (endpoint reworded)] | 1. The caller has Admin privileges, so the request passes the `isAdmin` guard before any business logic runs (ExBot Lambda step 2b).\n2. `bot_runtime_state.lp_eth_amount = 0` in Aurora PostgreSQL.\n3. Bot is otherwise running normally. | 1. Call `GET /api/exbot/status/{botId}`. | 1. Response is HTTP 200.\n2. `drift_pct` is `null` (not 0, not Infinity, not NaN).\n3. Lambda does not crash or return 500.\n4. Applies now (Pending field) — `drift_pct` is already `null`; once implemented, null guard for division by zero must remain. (Ref: UC §3 step 6, FR-EXBOT-021, CHG-001, CHG-006, TS_025.) | P0 |
| TC_055 | Verify drift_pct is null when lp_eth_amount is null — null guard [UPDATED — Reason: CH-01 (added Admin actor precondition), CH-02 (endpoint reworded)] | 1. The caller has Admin privileges, so the request passes the `isAdmin` guard before any business logic runs (ExBot Lambda step 2b).\n2. `bot_runtime_state.lp_eth_amount = null` in Aurora PostgreSQL. | 1. Call `GET /api/exbot/status/{botId}`. | 1. Response is HTTP 200.\n2. `drift_pct` is `null` — null guard handles `lp_eth_amount = null` in addition to `= 0`.\n3. Lambda does not crash. (Ref: UC §3 step 6, FR-EXBOT-021, CHG-001, CHG-006, TS_026.) | P0 |
| TC_056 | Verify drift_pct formula uses normalizeTargetRatioBps() with BigDecimal — no float arithmetic [UPDATED — Reason: CH-01 (added Admin actor precondition), CH-02 (endpoint reworded)] | 1. The caller has Admin privileges, so the request passes the `isAdmin` guard before any business logic runs (ExBot Lambda step 2b).\n2. [Applies when Pending fields are implemented.] `lp_eth_amount > 0`; `hedge_legs.target_ratio = "0.70"` (string format in ERD).\n3. `last_known_hl_short_size` set to a value that produces a non-trivial drift percentage. | 1. Call `GET /api/exbot/status/{botId}`.\n2. Compute expected: `target_ratio_bps = normalizeTargetRatioBps("0.70")` → `7000n`; `targetShortEth = lp_eth_amount × 7000n / 10000n` (BigDecimal); `drift_pct = abs(actual - targetShortEth) / targetShortEth × 100`.\n3. Compare returned `drift_pct` against computed value. | 1. `drift_pct` matches BigDecimal formula result with no float precision loss.\n2. Specifically: `"0.70"` must be converted to `7000n` via `normalizeTargetRatioBps()` — using `Number("0.70") × 10000 = 6999.999...` violates NFR-EXBOT-008.\n3. Currently `drift_pct = null` (Pending); assertion activates when implemented. (Ref: FR-EXBOT-021, NFR-EXBOT-008, CHG-001, TS_024.) | P0 |
| TC_057 | Verify margin_status='normal' is returned when margin usage is below the warning threshold — new enum value [UPDATED — Reason: CH-01 (added Admin actor precondition), CH-02 (endpoint reworded)] | 1. The caller has Admin privileges, so the request passes the `isAdmin` guard before any business logic runs (ExBot Lambda step 2b).\n2. [Applies when Pending fields are implemented.] `hedge_legs.margin_status='normal'` in Aurora PostgreSQL (marginUsage < 0.55 at last hedge-sync write).\n3. Note: value name is `normal` (not `ok`) per scenarios v4 TS_017 and CHG-007. | 1. Call `GET /api/exbot/status/{botId}`. | 1. Response returns `margin_status='normal'` (not `'ok'`) — the lowest margin risk band.\n2. Value matches Aurora PostgreSQL — endpoint reads it, does not recompute.\n3. Currently `margin_status = null` (Pending field). (Ref: UC §D row 16, TS_017, CHG-007.) | P1 |
| TC_058 | Verify the full JSON response contains exactly 19 fields — 8 Implemented with values and 11 Pending as null [UPDATED — Reason: CH-01 (added Admin actor precondition), CH-02 (endpoint reworded), CH-04 (firmed up v1-gap null wording)] | 1. The caller has Admin privileges, so the request passes the `isAdmin` guard before any business logic runs (ExBot Lambda step 2b).\n2. Bot running normally (`bots.lifecycle_state='active'`).\n3. All Aurora PostgreSQL tables have fully populated rows. | 1. Call `GET /api/exbot/status/{botId}`.\n2. Count total keys in the JSON response object.\n3. Categorize each key as Implemented (non-null expected) or Pending (null expected). | 1. JSON response contains exactly 19 keys — not 18, not 20.\n2. Of the 8 Implemented fields, `bot_id`, `status`, `lifecycle_state`, `runtime_health_status`, `dry_run` carry real non-null values; `safe_mode_tier`, `last_reconcile_at`, `last_error_code` are present but always `null` in v1 (confirmed v1 gap, not a missing key).\n3. 11 Pending fields present with value `null`: `tick_lower`, `tick_upper`, `current_tick`, `range_state`, `actual_short_eth`, `target_short_eth`, `drift_pct`, `margin_status`, `last_light_check_at`, `safe_mode_reason`, `cooldown_end_at`.\n4. No undocumented extra key present. (Ref: UC §3 step 7, CHG-002, TS_036, AC-ms-01.) | P0 |

### III.2. Integration & State verification — v3 additions

| TC ID | Title | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|
| TC_059 | Verify bot_id in response matches bots.id primary key in Aurora PostgreSQL [UPDATED — Reason: CH-01 (added Admin actor precondition), CH-02 (endpoint reworded)] | 1. The caller has Admin privileges, so the request passes the `isAdmin` guard before any business logic runs (ExBot Lambda step 2b).\n2. Bot exists in Aurora PostgreSQL with a known UUID primary key (`bots.id`).\n3. Bot is running normally. | 1. Call `GET /api/exbot/status/{botId}` and note the returned `bot_id` value.\n2. Query Aurora PostgreSQL `bots` table for the same record and read `id`. | 1. `bot_id` in response equals `bots.id` in Aurora PostgreSQL — `bot_id` is an alias of the UUID primary key.\n2. `bot_id` is not the ID of any other table or a generated/fabricated value. (Ref: UC §D row 1, V-006, CHG-008, TS_030.) | P1 |
| TC_060 | Verify lp_eth_amount and lp_usdc_amount in the response match the positions table values [UPDATED — Reason: CH-01 (added Admin actor precondition), CH-02 (endpoint reworded)] | 1. The caller has Admin privileges, so the request passes the `isAdmin` guard before any business logic runs (ExBot Lambda step 2b).\n2. Bot has active LP position; `positions` table has known values for LP token amounts. | 1. Call `GET /api/exbot/status/{botId}`.\n2. Compare returned LP amount fields against `positions` table rows for the bot's active position. | 1. LP amount values in the response match the `positions` table data for the queried bot.\n2. Values do not come from a stale lifecycle cycle or from a different bot. (Ref: UC §D, TS_031.) | P1 |
| TC_061 | Verify range_state in the response is consistent with currentTick from Pool Slot0 Cache at query time [UPDATED — Reason: CH-01 (added Admin actor precondition), CH-02 (endpoint reworded)] | 1. The caller has Admin privileges, so the request passes the `isAdmin` guard before any business logic runs (ExBot Lambda step 2b).\n2. [Applies when Pending fields are implemented.] Pool Slot0 Cache has a known `currentTick` at the time of the request. | 1. Record `currentTick` in Pool Slot0 Cache before the call.\n2. Call `GET /api/exbot/status/{botId}`.\n3. Verify `range_state` against the formula `tickLower <= currentTick < tickUpper` using the recorded tick. | 1. `range_state` is consistent with the `currentTick` value that was in Pool Slot0 Cache at query time.\n2. If `currentTick` changes between two calls, `range_state` reflects the most recent value.\n3. Currently both fields are `null` (Pending). (Ref: UC §3 step 5, TS_032.) | P1 |
| TC_062 | Verify a status call after a completed rebalance cycle reflects the post-rebalance data [UPDATED — Reason: CH-01 (added Admin actor precondition), CH-02 (endpoint reworded)] | 1. The caller has Admin privileges, so the request passes the `isAdmin` guard before any business logic runs (ExBot Lambda step 2b).\n2. A full rebalance cycle has just completed: `lp_rebalancing` → `active` transition persisted to Aurora PostgreSQL. | 1. Call `GET /api/exbot/status/{botId}` immediately after the bot returns to `active` post-rebalance. | 1. Response reflects post-rebalance data — `lifecycle_state='active'`.\n2. When Pending fields are implemented: `lp_eth_amount`, `lp_usdc_amount`, `drift_pct`, and `range_state` must reflect the updated post-rebalance values, not the pre-rebalance snapshot.\n3. Read-after-write consistency: Aurora PostgreSQL commit from the rebalance flow is immediately visible. (Ref: TS_040.) | P1 |
| TC_063 | Verify safe_mode is not a permanent state — bot can transition back to active after recovery [UPDATED — Reason: CH-01 (added Admin actor precondition), CH-02 (endpoint reworded)] | 1. The caller has Admin privileges, so the request passes the `isAdmin` guard before any business logic runs (ExBot Lambda step 2b).\n2. Bot currently has `lifecycle_state='safe_mode'`, `bots.status='safe_mode'`. | 1. Call `GET /api/exbot/status/{botId}` — confirm `status='safe_mode'`.\n2. Trigger auto-recovery: update Aurora PostgreSQL state to `lifecycle_state='active'`, `bots.status='active'`.\n3. Call `GET /api/exbot/status/{botId}` again. | 1. First call returns `status='safe_mode'`.\n2. Second call returns `status='active'`, `lifecycle_state='active'` — safe_mode is not locked.\n3. BR-EXBOT-007 is satisfied: SAFE_MODE is a recoverable state, not terminal. (Ref: BR-EXBOT-007, TS_027, CHG-009.) | P0 |
| TC_064 | Verify a status call immediately after running→safe_mode transition returns the new state [UPDATED — Reason: CH-01 (added Admin actor precondition), CH-02 (endpoint reworded)] | 1. The caller has Admin privileges, so the request passes the `isAdmin` guard before any business logic runs (ExBot Lambda step 2b).\n2. Bot was running (`lifecycle_state='active'`); transition to `safe_mode` has just been persisted to Aurora PostgreSQL. | 1. Call `GET /api/exbot/status/{botId}` immediately after the transition write. | 1. Response returns `status='safe_mode'`, `lifecycle_state='safe_mode'`.\n2. Must not return the stale `active` state — read-after-write consistency required.\n3. `safe_mode_reason` is `null` (Pending field). (Ref: TS_028, CHG-009.) | P0 |
| TC_065 | Verify a status call immediately after running→error transition returns the new state [UPDATED — Reason: CH-01 (added Admin actor precondition), CH-02 (endpoint reworded)] | 1. The caller has Admin privileges, so the request passes the `isAdmin` guard before any business logic runs (ExBot Lambda step 2b).\n2. Bot was running; transition to `error` has just been persisted to Aurora PostgreSQL. | 1. Call `GET /api/exbot/status/{botId}` immediately after the `error` state write. | 1. Response returns `status='error'`, `lifecycle_state='error'`, and message E-EXBOT-029.\n2. Must not return the stale `active` state — read-after-write consistency. (Ref: TS_029, CHG-009.) | P1 |
| TC_066 | Verify Aurora PostgreSQL cold-start does not return HTTP 200 with empty body — a clear error must be returned [UPDATED — Reason: CH-01 (added Admin actor precondition), CH-02 (endpoint reworded)] | 1. The caller has Admin privileges, so the request passes the `isAdmin` guard before any business logic runs (ExBot Lambda step 2b).\n2. Aurora Serverless v2 is in cold-start (scaling from zero) or times out when ExBot Lambda queries `bots` table. | 1. Simulate Aurora PostgreSQL unavailability / cold-start timeout and call `GET /api/exbot/status/{botId}`. | 1. Lambda does NOT return HTTP 200 with an empty body or partial data.\n2. Response is a clear error (e.g., HTTP 503 or 500) indicating database unavailability.\n3. No internal DB connection details (connection string, ARN, stack trace) exposed in the response body. (Ref: TS_038, CHG-009.) | P0 |

### III.3. Non-functional (logic) verification — v3 additions

| TC ID | Title | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|
| TC_067 | Verify HMAC key rotation — requests signed with the new key are accepted and with the old key are rejected [UPDATED — Reason: CH-02 (endpoint reworded to `/{botId}`)] | 1. HMAC key rotation has been performed: Lambda Authorizer is configured with the new key; Operator Facade holds the new key.\n2. Old key is no longer valid. | 1. Send `GET /api/exbot/status/{botId}` signed with the new HMAC key.\n2. Send a second `GET /api/exbot/status/{botId}` signed with the old HMAC key. | 1. Request signed with the new key: HTTP 200 with full bot status.\n2. Request signed with the old key: HTTP 401 from HMAC Lambda Authorizer — old key rejected.\n3. Key rotation does not break legitimate requests; old signatures cannot bypass it. (Ref: NFR-EXBOT-003, TS_035, CHG-009.) | P1 |
| TC_068 | Verify status response latency meets the acceptable latency threshold under normal load [UPDATED — Reason: CH-01 (added Admin actor precondition), CH-02 (endpoint reworded)] | 1. The caller has Admin privileges, so the request passes the `isAdmin` guard before any business logic runs (ExBot Lambda step 2b).\n2. Bot running normally. Aurora PostgreSQL and Pool Slot0 Cache are operating normally.\n3. Test executed in staging environment with representative load. | 1. Send multiple `GET /api/exbot/status/{botId}` requests and measure end-to-end latency from request sent to response received.\n2. Record P50, P95, P99 latency across a series of calls. | 1. All latency percentiles (P50, P95, P99) fall within the threshold defined by NFR-EXBOT-001.\n2. No request times out under normal operating conditions. (Ref: NFR-EXBOT-001, TS_037, CHG-009.) | P1 |
| TC_069 | Verify that data inconsistency between bots and bot_runtime_state is surfaced clearly, not silently ignored [UPDATED — Reason: CH-01 (added Admin actor precondition), CH-02 (endpoint reworded)] | 1. The caller has Admin privileges, so the request passes the `isAdmin` guard before any business logic runs (ExBot Lambda step 2b).\n2. Data inconsistency injected: `bots.status='active'` but `bot_runtime_state` contains a health indicator pointing to an error condition.\n3. V-001 open: assertion on `runtime_health_status` field name is tentative. | 1. Call `GET /api/exbot/status/{botId}` with the inconsistent Aurora PostgreSQL state. | 1. Response is HTTP 200 with whatever the Lambda reads from each table.\n2. Response does not silently merge conflicting values or suppress any field.\n3. The `runtime_health_status` field (or `health_status` pending V-001 resolution) reflects the value from `bot_runtime_state` as-read, not a derived or defaulted value.\n4. Note: exact assertion blocked partially by V-001 — follow up when BA confirms field name. (Ref: TS_039, V-001, CHG-009.) | P1 |
| TC_070 | Verify five concurrent status requests from the same wallet return consistent data without mixing [UPDATED — Reason: CH-01 (added Admin actor precondition), CH-02 (endpoint reworded)] | 1. The caller has Admin privileges, so the request passes the `isAdmin` guard before any business logic runs (ExBot Lambda step 2b).\n2. Bot running normally. No state-changing operation during the test window. | 1. Send five `GET /api/exbot/status/{botId}` requests concurrently from the same wallet. | 1. All five responses return HTTP 200 with identical field values — no response has missing fields or data from a different bot.\n2. No 500 or timeout under concurrent read load.\n3. Lambda has no connection pool race condition on Aurora PostgreSQL for read-only queries. (Ref: TS_033, CHG-009.) | P1 |
| TC_071 | Verify three sequential status calls with no state change return identical data [UPDATED — Reason: CH-01 (added Admin actor precondition), CH-02 (endpoint reworded)] | 1. The caller has Admin privileges, so the request passes the `isAdmin` guard before any business logic runs (ExBot Lambda step 2b).\n2. Bot running normally. No mutation between calls. | 1. Send `GET /api/exbot/status/{botId}` three times in sequence with no intervening state change. | 1. All three responses return HTTP 200 with identical values for all 19 fields.\n2. No field value drifts between calls.\n3. Read-only endpoint produces no side effects — Aurora PostgreSQL state is unchanged after the calls. (Ref: TS_034, CHG-009.) | P1 |
