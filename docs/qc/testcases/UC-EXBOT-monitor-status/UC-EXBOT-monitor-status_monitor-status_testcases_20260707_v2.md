# Test Cases — UC-EXBOT-monitor-status Monitor Bot Status

**Document title:** Test Cases — UC-EXBOT-monitor-status Monitor Bot Status
**Date created:** 2026-07-07
**Author/Agent:** qc-func-tc-design-exbot
**Version:** v2
**Update (2026-07-10):** TC_ID renumber — sequential 001→051, doc order

**Total test cases:** 51 (FUNC: 36, INTG: 11, NFR: 4)
**Scope:** Logic-only (backend / API / bot — no UI)
**Source UC:** UC-EXBOT-monitor-status_monitor-status_audited_20260706_v4.md
**Source scenarios (if any):** UC-EXBOT-monitor-status_monitor-status_scenarios_20260706_v3.md
**Output language:** English
**Version note:** v2 (updated 2026-07-07: arc-migration applied — D1→Aurora PostgreSQL, ExBot Worker→ExBot Lambda, MarketDataDO→Pool Slot0 Cache, shared-secret→HMAC Lambda Authorizer; 7 new TCs TC_022–TC_035 from audit v4 ACs; TC_020 split; field names resolved per I-006)
**QC Review (2026-07-08):** +12 TCs (TC_040–TC_051) — Pool Slot0 Cache null/stale path, BVA rangeState and margin_status boundaries, blocked-wallet 403, Investor ownership happy path, paused state, transitional state, Implemented fields contract, all-lifecycle-states EP

#### Requirement Traceability Matrix

| AC ID | Acceptance Criteria | Linked Test Cases | Status |
|---|---|---|---|
| AC-01 | Active bot: all required fields present (status, lifecycle_state, LP range, currentTick, rangeState, hedgeSize, targetShortEth, driftPct, marginStatus, last_light_check_at) | TC_001, TC_002, TC_019, TC_047 | Covered [+TC_047] |
| AC-02 | SAFE_MODE: status='safe_mode', safe_mode_reason present; not terminal (BR-EXBOT-007) | TC_006, TC_013, TC_014 | Covered |
| AC-03 | hedge_stopped_cooldown: cooldown_end_at timestamp present | TC_007, TC_015 | Covered |
| AC-04 | lp_closing: HTTP 200 + E-EXBOT-021 "Bot close is in progress. Please wait." | TC_016, TC_022 | Covered |
| AC-05 | closed bot: HTTP 200 + E-EXBOT-022; NOT 404; record retained in Aurora PostgreSQL | TC_020, TC_023 | Covered |
| AC-06 | No bot record: HTTP 404 + E-EXBOT-023 | TC_008 | Covered |
| AC-07 | Pool Slot0 Cache unavailable: current_tick=null, range_state=null; still HTTP 200 | TC_040, TC_041, TC_042 | Covered [NEW — TC_040–TC_042 replace partial TC_029 coverage] |
| AC-08 | lp_rebalancing: HTTP 200 with lifecycle_state='lp_rebalancing' | TC_016 | Covered |
| AC-09 | error state: HTTP 200 with status='error' | TC_019 | Covered |
| AC-10 | Investor wallet mismatch: HTTP 403 | TC_025 | Covered |
| AC-11 | Admin bypass ownership check: HTTP 200 for any bot | TC_026 | Covered |
| AC-12 | Missing X-Wallet-Address: HTTP 401 from Operator Facade | TC_024 | Covered |
| AC-13 | HMAC invalid (Facade→Lambda): HTTP 401 from ExBot Lambda | TC_039 | Covered |
| AC-14 | drift% BigDecimal precision | TC_009, TC_010, TC_011 | Covered (targetShortEth source pending I-003) |
| AC-15 | Pending implementation fields return null (not absent) | TC_035 | Covered |
| FR-EXBOT-090 | ExBot Lambda 403 to direct internet; Facade forwards unchanged | TC_028, TC_036 | Covered |

---

## I. Operation: Monitor Bot Status (GET /api/exbot/status)

### I.1. Functional verification — Operation: Monitor Bot Status (GET /api/exbot/status)

| TC ID | Title | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|
| TC_001 | Verify the full status response is returned with all required fields for a normally running bot | 1. Bot is running normally (`bots.lifecycle_state='active'`, `bots.status='active'`).\n2. LP position is in range (`tickLower <= currentTick < tickUpper`) and margin is healthy (`hedge_legs.margin_status='ok'`).\n3. Pool Slot0 Cache has a valid, fresh tick for the bot's pool.\n4. All Aurora PostgreSQL tables (`bots`, `positions`, `bot_runtime_state`, `hedge_legs`) have fully populated rows. | 1. Call `GET /api/exbot/status` via the Operator Facade for the running bot. | 1. Response is HTTP 200 with a JSON body containing all fields documented in UC §3 step 7: `status='active'`, `lifecycle_state='active'`, `tick_lower`, `tick_upper`, `current_tick`, `range_state='in'`, actual hedge size (from `bot_runtime_state.last_known_hl_short_size`), `target_short_eth` (source pending I-003), `drift_pct`, `margin_status='ok'`, and `last_light_check_at`. No mandatory field is null without justification. (Ref: UC §3 Main Flow, AC-01, FR-EXBOT-090.) | P0 |
| TC_002 | Verify rangeState is 'in' when the current tick equals the lower boundary of the LP range | 1. Bot is running normally (`bots.lifecycle_state='active'`).\n2. Current tick in Pool Slot0 Cache equals `tick_lower` exactly (lower boundary, inclusive). | 1. Call `GET /api/exbot/status` while `current_tick == tick_lower`. | 1. Response returns `range_state='in'` because the lower bound is inclusive in the half-open interval `[tick_lower, tick_upper)`. The `current_tick` value in the response equals `tick_lower`. (Ref: UC §3 step 5, AC-05 [v1]→ rangeState='in', FR-EXBOT-020.) | P0 |
| TC_003 | Verify rangeState is 'out' when the current tick equals the upper boundary of the LP range | 1. Bot is running normally (`bots.lifecycle_state='active'`).\n2. Current tick in Pool Slot0 Cache equals `tick_upper` exactly (upper boundary, exclusive). | 1. Call `GET /api/exbot/status` while `current_tick == tick_upper`. | 1. Response returns `range_state='out'` because `tick_upper` is excluded from the interval `[tick_lower, tick_upper)`. (Example: tick_lower=100, tick_upper=200, current_tick=200 → range_state='out'.) (Ref: UC §3 step 5, FR-EXBOT-020.) | P0 |
| TC_004 | Verify rangeState is 'in' when the current tick is one unit below the upper boundary | 1. Bot is running normally (`bots.lifecycle_state='active'`).\n2. Current tick in Pool Slot0 Cache is exactly one unit below `tick_upper` (`current_tick = tick_upper - 1`), the last in-range tick. | 1. Call `GET /api/exbot/status` while `current_tick == tick_upper - 1`. | 1. Response returns `range_state='in'` — this is the last tick value considered in-range under the half-open interval. (Example: tick_upper=200, current_tick=199 → range_state='in'.) (Ref: UC §3 step 5, FR-EXBOT-020.) | P0 |
| TC_005 | Verify rangeState is 'out' when the current tick is below the lower boundary of the LP range | 1. Bot is running normally (`bots.lifecycle_state='active'`).\n2. Current tick in Pool Slot0 Cache is less than `tick_lower` (below the lower bound). | 1. Call `GET /api/exbot/status` while `current_tick < tick_lower`. (Example: tick_lower=100, current_tick=99.) | 1. Response returns `range_state='out'`. The `current_tick`, `tick_lower`, and `tick_upper` values are all present in the response so the caller can determine the magnitude of the out-of-range move. (Ref: UC §3 step 5.) | P0 |
| TC_006 | Verify the response includes safe_mode_reason when the bot is in SAFE_MODE | 1. Bot has entered SAFE_MODE (`bots.status='safe_mode'`, `bots.lifecycle_state='safe_mode'`).\n2. `safe_mode_reason` is persisted in Aurora PostgreSQL. | 1. Call `GET /api/exbot/status` for the bot in SAFE_MODE. | 1. Response is HTTP 200 with `status='safe_mode'`, `lifecycle_state='safe_mode'`, and the `safe_mode_reason` field containing the cause of SAFE_MODE entry. Response does not contain any value implying the bot is permanently closed — SAFE_MODE is a recoverable state per BR-EXBOT-007. (Ref: UC §4 A1, AC-02, FR-EXBOT-050, BR-EXBOT-007.) | P0 |
| TC_007 | Verify the response includes a cooldown_end_at timestamp when the bot is in hedge_stopped_cooldown state | 1. Bot is in the stop-fired cooldown period (`bots.lifecycle_state='hedge_stopped_cooldown'`, `bots.status='active'`).\n2. `cooldown_end_at` is persisted in Aurora PostgreSQL. | 1. Call `GET /api/exbot/status` for a bot in `hedge_stopped_cooldown`. | 1. Response is HTTP 200 with `lifecycle_state='hedge_stopped_cooldown'` and `cooldown_end_at` containing a timestamp accurate enough for the caller to compute remaining duration. Response also conveys that automatic re-hedging will be attempted after the cooldown ends (FR-EXBOT-034). (Ref: UC §4 A2, AC-03, FR-EXBOT-034.) | P0 |
| TC_008 | Verify the response is HTTP 404 with E-EXBOT-023 when there is no bot record for the user | 1. No bot record exists in Aurora PostgreSQL for the authenticated user, OR the query result for `botId` returns no rows.\n2. This case is distinct from a bot with `lifecycle_state='closed'` — a closed bot record still exists in Aurora PostgreSQL and returns HTTP 200 (AC-05). | 1. Call `GET /api/exbot/status` for a user with no bot record in Aurora PostgreSQL. | 1. Response is HTTP 404 with message body containing E-EXBOT-023 "No active bot found for this account." No bot data fields are present. Response is not HTTP 200 with empty data, and not HTTP 500. (Ref: UC §4 A4, AC-06, E-EXBOT-023.) | P0 |
| TC_009 | Verify drift percentage is computed correctly using the formula when actual and target hedge sizes differ | 1. Bot is running normally (`bots.lifecycle_state='active'`).\n2. Actual hedge size (`bot_runtime_state.last_known_hl_short_size`) differs from the target by a measurable amount. (Example: actual = 1.0 ETH, target = 0.9 ETH, expected drift ≈ 11.11%.) | 1. Call `GET /api/exbot/status` and compare the returned `drift_pct` against the manually computed value: `abs(actual_short_eth - target_short_eth) / target_short_eth × 100`. | 1. `drift_pct` matches the formula result computed via BigDecimal arithmetic — no floating-point rounding error. (Note: targetShortEth source pending I-003 — assertion requires confirmation of whether Lambda reads `bot_runtime_state.target_short_size` or recomputes from `lpEthAmount × hedgeRatio`.) (Ref: UC §3 step 6, AC-14, FR-EXBOT-021, NFR-EXBOT-008.) | P0 |
| TC_010 | Verify drift percentage is exactly zero when the actual hedge size equals the target | 1. Bot is running normally (`bots.lifecycle_state='active'`).\n2. Actual hedge size equals the target exactly (`last_known_hl_short_size == target_short_size`). | 1. Call `GET /api/exbot/status` when the hedge is perfectly balanced. | 1. Response returns `drift_pct = 0` (exactly zero, not a floating-point near-zero). BigDecimal arithmetic must yield precise equality. (Note: targetShortEth source pending I-003.) (Ref: UC §3 step 6, AC-14, NFR-EXBOT-008.) | P0 |
| TC_011 | Verify drift percentage precision is preserved for large hedge sizes with many decimal places | 1. Bot is running normally (`bots.lifecycle_state='active'`).\n2. Both actual and target hedge sizes are large values with many decimal places. (Example: actual = 9999999.123456789 ETH, target = 9999998.987654321 ETH.) | 1. Call `GET /api/exbot/status` with large-valued hedge sizes in Aurora PostgreSQL. | 1. Response `drift_pct` preserves full decimal precision without truncation or floating-point artifacts, as required by NFR-EXBOT-008 (BigDecimal arithmetic). (Note: targetShortEth source pending I-003.) (Ref: AC-14, NFR-EXBOT-008.) | P1 |
| TC_012 | Verify the response returns margin_status 'warning' when the margin is in the warning band | 1. Bot is running normally (`bots.lifecycle_state='active'`).\n2. `hedge_legs.margin_status='warning'` is persisted in Aurora PostgreSQL (marginUsage was in the warning band when hedge-sync last updated it). | 1. Call `GET /api/exbot/status` for a bot with margin in the warning band. | 1. Response returns `margin_status='warning'`. This value is distinct from `ok` and `critical`. (Ref: UC §3 step 3, FR-EXBOT-060.) | P0 |
| TC_013 | Verify the response returns margin_status 'critical' when the margin is in the critical band | 1. Bot is running normally (`bots.lifecycle_state='active'`).\n2. `hedge_legs.margin_status='critical'` is persisted in Aurora PostgreSQL (marginUsage was at or above the critical threshold when hedge-sync last updated it). | 1. Call `GET /api/exbot/status` for a bot with margin in the critical band. | 1. Response returns `margin_status='critical'`. This value is distinct from `ok` and `warning`. Note: reaching critical twice consecutively triggers SAFE_MODE — this case tests read-only reporting of the current critical state, not the SAFE_MODE transition itself. (Ref: UC §3 step 3, FR-EXBOT-060.) | P0 |
| TC_014 | Verify that SAFE_MODE is not presented as a terminal state in the status response | 1. Bot has `lifecycle_state='safe_mode'` and `bots.status='safe_mode'`. | 1. Call `GET /api/exbot/status` for the bot in SAFE_MODE. | 1. Response contains `status='safe_mode'` and does not include any field or value that implies the bot is permanently closed or terminated. SAFE_MODE must remain distinguishable from `lifecycle_state='closed'`. (Ref: BR-EXBOT-007, FR-EXBOT-050, AC-02.) | P0 |
| TC_015 | Verify the coarse status is 'active' while lifecycle_state is hedge_stopped_cooldown | 1. Bot has `lifecycle_state='hedge_stopped_cooldown'` and `bots.status='active'` (per states.md State Registry). | 1. Call `GET /api/exbot/status` while the bot is in `hedge_stopped_cooldown`. | 1. Response returns `status='active'` and `lifecycle_state='hedge_stopped_cooldown'`. The caller can distinguish this from fully active (`lifecycle_state='active'`) using the fine-grained state. Response conveys that hedge-sync is suppressed during this period. (Ref: UC §4 A2, FR-EXBOT-034, states.md State Registry.) | P1 |
| TC_016 | Verify the response correctly reflects the lp_rebalancing transient state | 1. Bot has `lifecycle_state='lp_rebalancing'` and `bots.status='active'` (LP range rebalance is in progress). | 1. Call `GET /api/exbot/status` while the bot is in `lp_rebalancing`. | 1. Response returns `lifecycle_state='lp_rebalancing'` and `status='active'`. State is not presented as stopped, closed, or error. Caller can distinguish this transient state from fully active. (Ref: UC §4 A6, AC-08, states.md State Registry.) | P1 |
| TC_017 | Verify the response is HTTP 503 when the Operator Facade cannot reach ExBot Lambda | 1. Operator Facade is running but ExBot Lambda is unavailable (API Gateway returns 503 due to Lambda cold-start failure, throttling, or Lambda itself returning 5xx). | 1. Simulate ExBot Lambda unavailability and call `GET /api/exbot/status` via the Operator Facade. | 1. Response is HTTP 503. No bot data fields are present. Response does not expose a stack trace or internal error details. 503 is an AWS/infra-level response — no E-EXBOT-* code is expected in the body. (Ref: UC §4 A5, AC-08 [legacy mapping].) | P0 |
| TC_018 | Verify the response reflects margin_status at the exact lower boundary of the warning band | 1. Bot is running normally (`bots.lifecycle_state='active'`).\n2. `hedge_legs.margin_status` was written when marginUsage was exactly at the ok/warning threshold (0.55 per FR-EXBOT-060). | 1. Call `GET /api/exbot/status` with the boundary margin_status value from Aurora PostgreSQL. | 1. Response returns the same `margin_status` value as stored in Aurora PostgreSQL. The boundary assignment (whether 0.55 is 'ok' or 'warning') must be consistent with FR-EXBOT-060. (Ref: FR-EXBOT-060.) | P1 |
| TC_019 | Verify the response reflects the error state and distinguishes it from SAFE_MODE | 1. Bot has `lifecycle_state='error'` and `bots.status='error'` (admin intervention required). | 1. Call `GET /api/exbot/status` for the bot in the error state. | 1. Response returns `status='error'` and `lifecycle_state='error'`. This state is distinct from `safe_mode` — no auto-recovery without admin action. Caller can clearly differentiate `error` from `safe_mode`. Only Close Bot emergency is enabled in this state. (Ref: UC §4 A7, AC-09, states.md State Registry.) | P1 |
| TC_020 | Verify a closed bot returns HTTP 200 (not 404) with the closed lifecycle state | 1. A bot record exists in Aurora PostgreSQL with `lifecycle_state='closed'` and `bots.status='closed'` (bot was previously active, then safely closed).\n2. The record is retained in Aurora PostgreSQL per UC §4 A3b. | 1. Call `GET /api/exbot/status` for the user whose bot has `lifecycle_state='closed'`. | 1. Response is HTTP 200 (NOT 404) with `lifecycle_state='closed'`. This distinguishes a closed bot (record retained) from a no-record case (AC-06, HTTP 404). The HTTP 200 confirms that closed bot records are preserved in Aurora PostgreSQL and not treated as absent. (Ref: UC §4 A3b, AC-05.) | P0 |
| TC_021 | Verify two back-to-back status calls return identical data when no state change occurs between them | 1. Bot is running normally (`bots.lifecycle_state='active'`).\n2. No other operation modifies the bot's Aurora PostgreSQL state or Pool Slot0 Cache between the two calls. | 1. Call `GET /api/exbot/status` twice in quick succession with no intervening state change. | 1. Both responses return HTTP 200 with identical values for all fields. The UC is read-only — it produces no side effects in Aurora PostgreSQL and does not enqueue any message. (Ref: UC §3 Main Flow, NFR-EXBOT-011.) | P1 |
| TC_022 | Verify a bot in lp_closing state returns HTTP 200 with E-EXBOT-021 message | 1. Bot has `lifecycle_state='lp_closing'` in Aurora PostgreSQL (LP close operation is in progress, not yet complete). | 1. Call `GET /api/exbot/status` while the bot is in `lp_closing`. | 1. Response is HTTP 200 with `lifecycle_state='lp_closing'` and message body containing E-EXBOT-021 "Bot close is in progress. Please wait." Response is not HTTP 404 — the bot record exists. (Ref: UC §4 A3a, AC-04.) | P0 |
| TC_023 | Verify a closed bot returns HTTP 200 with E-EXBOT-022 message body | 1. Bot has `lifecycle_state='closed'` in Aurora PostgreSQL (bot safely closed, record retained).\n2. This is the same pre-condition as TC_020 but focuses on the message content rather than the status code. | 1. Call `GET /api/exbot/status` for the user whose bot is `closed`. | 1. Response is HTTP 200 and the response body contains message E-EXBOT-022 "Bot safely closed. Funds have been returned to your wallet." (Ref: UC §4 A3b, AC-05, E-EXBOT-022.) | P0 |
| TC_024 | Verify the response is HTTP 401 when the X-Wallet-Address header is missing | 1. Operator Facade is running and ExBot Lambda is available.\n2. No `X-Wallet-Address` header is included in the request (regardless of actor type). | 1. Call `GET /api/exbot/status` without the `X-Wallet-Address` header. | 1. Response is HTTP 401 from the Operator Facade. No bot data is returned. The Operator Facade rejects the request before it reaches ExBot Lambda because the wallet identity header is absent. (Ref: UC §2 Preconditions, AC-12.) | P0 |
| TC_025 | Verify the response is HTTP 403 when the Investor wallet address does not match the bot's owner | 1. A bot exists with `bot.user_wallet_address = '0xABC...'`.\n2. Authenticated Investor sends `X-Wallet-Address: '0xDEF...'` (a different wallet). | 1. Call `GET /api/exbot/status` with a wallet address that does not match `bot.user_wallet_address` in Aurora PostgreSQL. | 1. Response is HTTP 403 Forbidden. No bot data is returned. This confirms the ownership check: Investor can only query their own bot. (Ref: UC §2 Preconditions, AC-10.) | P0 |
| TC_026 | Verify Admin can query any bot regardless of wallet ownership | 1. A bot exists with `bot.user_wallet_address = '0xABC...'`.\n2. Admin actor sends `X-Wallet-Address` corresponding to a different wallet (not the bot's owner). | 1. Admin calls `GET /api/exbot/status` for a bot owned by a different user. | 1. Response is HTTP 200 with the full bot status. Admin bypasses the ownership check — the `accessControl` middleware grants Admin access to any bot regardless of `bot.user_wallet_address`. Response schema is the same as for Investor. (Ref: UC §2, AC-11.) | P0 |

---

### I.2. Integration & State verification — Operation: Monitor Bot Status (GET /api/exbot/status)

| TC ID | Title | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|
| TC_027 | Verify hedgeSize and marginStatus fields reflect the Aurora PostgreSQL values written by the most recent hedge-sync | 1. Bot is running normally.\n2. A hedge-sync has just completed and written updated `bot_runtime_state.last_known_hl_short_size` and `hedge_legs.margin_status` to Aurora PostgreSQL. | 1. Call `GET /api/exbot/status` immediately after the hedge-sync Aurora PostgreSQL write completes. | 1. Response fields for hedge size and `margin_status` reflect the values written by the most recent hedge-sync. Aurora PostgreSQL (Aurora Serverless v2) provides read consistency — the latest written values must be immediately visible on the next read within the same session. (Ref: UC §3 step 3, FR-EXBOT-025, §F.4.) | P0 |
| TC_028 | Verify the Operator Facade forwards the ExBot Lambda response to the caller without modifying any field | 1. Bot is running normally.\n2. Both Operator Facade and ExBot Lambda are available. | 1. Call `GET /api/exbot/status` via the Operator Facade and compare the response with the JSON ExBot Lambda assembled from Aurora PostgreSQL and Pool Slot0 Cache. | 1. Operator Facade forwards the ExBot Lambda JSON response to the caller unchanged — no field is added, removed, renamed, or modified in value. Operator Facade owns no ExBot business logic and performs no transformation. (Ref: UC §3 steps 2 and 8, FR-EXBOT-090.) | P0 |
| TC_029 | Verify the currentTick in the response is read from Pool Slot0 Cache and not from a direct on-chain RPC call | 1. Bot is running normally.\n2. Pool Slot0 Cache contains a known `current_tick` value.\n3. Lambda execution trace or logs are available to distinguish cache reads from outbound RPC calls. | 1. Call `GET /api/exbot/status` and inspect the Lambda execution trace for outbound on-chain RPC calls during request processing. | 1. `current_tick` in the response matches the cached value in Pool Slot0 Cache (ElastiCache Redis). Lambda trace shows no outbound on-chain RPC call to the Uniswap V3 pool during this request — tick is sourced exclusively from the cache per FR-EXBOT-093 and BR-EXBOT-003. (Ref: UC §3 step 4, FR-EXBOT-093.) | P0 |
| TC_030 | Verify the status response for a bot on Base chain uses the Base pool's Pool Slot0 Cache currentTick | 1. Bot is deployed on Base chain (chainId 8453) and is running normally.\n2. The Base pool's Pool Slot0 Cache slot contains a known `current_tick`. | 1. Call `GET /api/exbot/status` for the Base-chain bot. | 1. `current_tick` in the response matches the Base pool's cached tick, not the Optimism pool's tick. `range_state` reflects the Base pool tick relative to the bot's LP range. (Ref: UC §3 step 4, FR-EXBOT-093, project-context-master §3.3 Dual-chain constraint.) | P0 |
| TC_031 | Verify the status response immediately after a SAFE_MODE transition reflects the new state | 1. Bot has just transitioned from `lifecycle_state='active'` to `lifecycle_state='safe_mode'` due to a SAFE_MODE trigger condition (e.g., two consecutive critical margin readings per FR-EXBOT-050). | 1. Call `GET /api/exbot/status` immediately after the `lifecycle_state` transition is persisted to Aurora PostgreSQL. | 1. Response returns `status='safe_mode'` and `lifecycle_state='safe_mode'` reflecting the current Aurora PostgreSQL state. Aurora PostgreSQL consistency ensures the new state is immediately visible — the response must not return the previous `active` state. (Ref: UC §4 A1, FR-EXBOT-050.) | P0 |
| TC_032 | Verify ten concurrent status requests for the same bot return consistent data and produce no errors | 1. Bot is running normally.\n2. No state-changing operation runs concurrently during the test window. | 1. Send ten `GET /api/exbot/status` requests concurrently (within a 1-second window) for the same bot. | 1. All ten responses return HTTP 200 with consistent field values drawn from the same Aurora PostgreSQL snapshot and the same Pool Slot0 Cache value. No request returns HTTP 500 or times out abnormally. Because this UC is read-only, no locking or mutual exclusion is required and no race condition is expected. (Ref: FR-EXBOT-090, FR-EXBOT-093.) | P1 |
| TC_033 | Verify the full request pipeline from Operator Facade through API Gateway, ExBot Lambda, Aurora PostgreSQL, and Pool Slot0 Cache in a single cycle | 1. Bot is running normally with LP in range and margin ok.\n2. All components (Operator Facade, API Gateway, ExBot Lambda, Aurora PostgreSQL, Pool Slot0 Cache) are available. | 1. Call `GET /api/exbot/status` and verify each stage end-to-end: Operator Facade routes to API Gateway → HMAC Lambda Authorizer validates the request → ExBot Lambda reads all required Aurora PostgreSQL fields → queries `current_tick` from Pool Slot0 Cache → computes `range_state` and `drift_pct` → assembles response → Facade forwards to caller. | 1. Response contains all fields with values consistent with the Aurora PostgreSQL data and the Pool Slot0 Cache value. `range_state` and `drift_pct` values match the expected computation. Round-trip completes without error. (Ref: UC §3 Full Main Flow, FR-EXBOT-090, FR-EXBOT-093.) | P0 |
| TC_034 | Verify the first status call after bot-start completes returns all fields populated and non-null | 1. UC-EXBOT-bot-start has completed successfully and the bot has reached `lifecycle_state='active'`. | 1. Call `GET /api/exbot/status` immediately after bot-start completes. | 1. All response fields (`tick_lower`, `tick_upper`, `last_known_hl_short_size`, `margin_status`, `drift_pct`, `range_state`) are non-null, confirming bot-start has fully initialized all Aurora PostgreSQL tables the status endpoint depends on. (Ref: UC §2 Preconditions, §F.4.) | P0 |
| TC_035 | Verify pending implementation fields return null and are not absent from the JSON response body | 1. Bot is running normally (`bots.lifecycle_state='active'`).\n2. Fields marked "Pending implementation" in UC §3 step 7 schema have not yet been implemented (i.e., no value is persisted for them). | 1. Call `GET /api/exbot/status` and inspect the full JSON response body for the presence and value of all "Pending implementation" fields. | 1. Every "Pending implementation" field is present in the JSON response body with value `null` — it is NOT absent or omitted from the JSON object. This confirms the API contract: pending fields always appear in the schema as `null`, not as missing keys. (Ref: UC §3 step 7 null-handling, AC-15, N-001.) | P0 |

---

### I.3. Non-functional (logic) verification — Operation: Monitor Bot Status (GET /api/exbot/status)

| TC ID | Title | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|
| TC_036 | Verify ExBot Lambda returns HTTP 403 when called directly from the internet without going through API Gateway | 1. ExBot Lambda is deployed and accessible via API Gateway + HMAC Lambda Authorizer internally. | 1. Attempt a direct HTTP request to the ExBot Lambda function URL (bypassing API Gateway), without HMAC authorization. | 1. ExBot Lambda returns HTTP 403 Forbidden. No bot data is returned. ExBot Lambda is not internet-accessible directly — it only accepts requests via API Gateway with valid HMAC authorization. (Ref: FR-EXBOT-090, BR-EXBOT-010.) | P0 |
| TC_037 | Verify HMAC Lambda Authorizer rejects a request from Operator Facade when the HMAC signature is missing or invalid | 1. ExBot Lambda is deployed and reachable via API Gateway.\n2. HMAC Lambda Authorizer is configured with a known signing secret. | 1. Send a request to ExBot Lambda via API Gateway with a missing or invalid HMAC signature in the authorization header. | 1. HMAC Lambda Authorizer rejects the request with HTTP 401. ExBot Lambda is never invoked. No bot data is returned. This ensures that a misconfigured Facade or test harness cannot access ExBot Lambda without the correct HMAC signature. (Ref: UC §2, FR-EXBOT-090, AC-13.) | P0 |
| TC_038 | Verify no sensitive data appears in the status response body or Lambda logs | 1. Bot is running normally.\n2. Lambda execution logs are accessible for inspection. | 1. Call `GET /api/exbot/status` and inspect the full response body and the associated Lambda log output. | 1. Response body contains only the documented status fields. No field contains raw agent key material, raw DEK, HL account credentials, or any other sensitive secret. Lambda logs for this request contain no raw key or credential values at INFO level. (Ref: NFR-EXBOT-010, FR-EXBOT-080.) | P0 |
| TC_039 | Verify HTTP 401 is returned from ExBot Lambda when the HMAC signature forwarded by Operator Facade is invalid | 1. Operator Facade is running.\n2. Operator Facade is misconfigured and sends an invalid HMAC signature to API Gateway when forwarding the status request. | 1. Trigger a status request where the Facade sends a wrong HMAC signature (e.g., signed with an incorrect key or tampered payload). | 1. HMAC Lambda Authorizer returns HTTP 401. ExBot Lambda is not invoked. No bot data is returned. This confirms end-to-end enforcement of the HMAC security layer on the Facade→Lambda path. (Ref: UC §2, AC-13.) | P0 |

---

## II. QC Review — Gap Fill (2026-07-08)

### II.1. Functional verification — Gap fill

| TC ID | Title | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|
| TC_040 | Verify Pool Slot0 Cache unavailable returns HTTP 200 with current_tick=null and range_state=null [NEW — AC-07; TS_UC-EXBOT-monitor-status_011] | 1. Bot is running normally (`bots.lifecycle_state='active'`, `bots.status='active'`).\n2. Pool Slot0 Cache (ElastiCache Redis) is unreachable — Redis connection fails or returns an error.\n3. All Aurora PostgreSQL tables have fully populated rows. | 1. Call `GET /api/exbot/status` while Pool Slot0 Cache is unreachable. | 1. Response is HTTP 200 — the unavailability of Pool Slot0 Cache does NOT produce a 503 or 500.\n2. `current_tick` field is `null` in the response.\n3. `range_state` field is `null` in the response — rangeState computation is skipped when `currentTick` is unavailable.\n4. All Aurora PostgreSQL-sourced fields (`status`, `lifecycle_state`, `margin_status`, `last_light_check_at`, tick range, hedge size) are populated normally.\n5. No retry is made to Pool Slot0 Cache. (Ref: UC §4 A8, FR-EXBOT-093, AC-07.) | P0 |
| TC_041 | Verify Pool Slot0 Cache snapshot stale is treated identically to unavailable — current_tick=null, range_state=null [NEW — AC-07; TS_UC-EXBOT-monitor-status_012] | 1. Bot is running normally.\n2. Pool Slot0 Cache has data but the snapshot is stale (blockNumber has not advanced beyond the threshold defined by OQ-EXBOT-09 — currently tested as 2× the expected refresh interval). | 1. Call `GET /api/exbot/status` while Pool Slot0 Cache has a stale snapshot. | 1. Response is HTTP 200 — stale snapshot is treated identically to unavailable.\n2. `current_tick: null` and `range_state: null` in the response.\n3. All Aurora PostgreSQL-sourced fields are populated normally.\n4. No retry or forced refresh of Pool Slot0 Cache is attempted. (Ref: UC §4 A8, FR-EXBOT-093, AC-07.) | P1 |
| TC_042 | Verify range_state is null and no rangeState computation occurs when current_tick is null [NEW — BVA; TS_UC-EXBOT-monitor-status_017] | 1. Pool Slot0 Cache returns no `currentTick` (key absent or returns null). | 1. Call `GET /api/exbot/status`.\n2. Inspect both `current_tick` and `range_state` in the response. | 1. `current_tick: null` in the response.\n2. `range_state: null` in the response — the rangeState formula `tickLower <= currentTick < tickUpper` is NOT evaluated when `currentTick` is null.\n3. Response does not return `"in"` or `"out"` for `range_state` based on assumption.\n4. Response is HTTP 200 — no error thrown. (Ref: UC §3 step 5, AC-07.) | P0 |
| TC_043 | BVA rangeState — currentTick equals tickLower - 1 (one step below lower bound, must be 'out') [NEW — TS_UC-EXBOT-monitor-status_014] | 1. Bot is running normally.\n2. Pool Slot0 Cache `currentTick = tickLower - 1` — one step below the lower bound of the LP range.\n   (Example: tickLower=100, tickUpper=200, currentTick=99.) | 1. Call `GET /api/exbot/status` while `currentTick = tickLower - 1`. | 1. Response returns `range_state='out'` — `currentTick < tickLower` falls outside the half-open interval `[tickLower, tickUpper)`.\n2. `current_tick` value in the response equals `tickLower - 1`.\n3. Confirms the lower boundary: `tickLower` is 'in' (TC_002), `tickLower - 1` is 'out'. (Ref: UC §3 step 5.) | P0 |
| TC_044 | BVA margin_status — margin_status is 'ok' when marginUsage is below 0.55 [NEW — TS_UC-EXBOT-monitor-status_019] | 1. `hedge_legs.margin_status='ok'` is set in Aurora PostgreSQL (marginUsage < 0.55 when hedge-sync last updated it). | 1. Call `GET /api/exbot/status`. | 1. Response returns `margin_status='ok'` — the lowest margin risk band.\n2. Value matches what is stored in Aurora PostgreSQL — status endpoint reads the value, it does not recompute from HL. (Ref: UC §3 step 3, FR-EXBOT-060.) | P1 |
| TC_045 | BVA margin_status — margin_status is 'critical' when marginUsage is at or above 0.75 [NEW — TS_UC-EXBOT-monitor-status_020] | 1. `hedge_legs.margin_status='critical'` is set in Aurora PostgreSQL (marginUsage >= 0.75 when hedge-sync last updated it). | 1. Call `GET /api/exbot/status`. | 1. Response returns `margin_status='critical'` — the highest margin risk band.\n2. Value matches Aurora PostgreSQL — no recomputation occurs in the status Lambda. (Ref: UC §3 step 3, FR-EXBOT-060.) | P0 |
| TC_046 | Verify HTTP 403 is returned when the caller's wallet address is blocked by Operator Facade [NEW — TS_UC-EXBOT-monitor-status_025] | 1. The caller's wallet address is on the Operator Facade block list.\n2. A valid `X-Wallet-Address` header is present in the request. | 1. Call `GET /api/exbot/status` with a blocked wallet address. | 1. Response is HTTP 403 Forbidden from the Operator Facade — not from ExBot Lambda.\n2. The request is NOT forwarded to ExBot Lambda.\n3. No bot data is returned. (Ref: UC §2 Preconditions.) | P0 |
| TC_047 | Verify all 8 Implemented fields are always present (not absent) in the JSON response when a bot exists [NEW — TS_UC-EXBOT-monitor-status_047] | 1. Bot exists in Aurora PostgreSQL with `lifecycle_state='active'`.\n2. All 8 Implemented fields (`bot_id`, `status`, `lifecycle_state`, `safe_mode_tier`, `runtime_health_status`, `last_reconcile_at`, `last_error_code`, `dry_run`) are resolvable. | 1. Call `GET /api/exbot/status` and inspect the full JSON response keys. | 1. All 8 Implemented fields are present as keys in the JSON response body — none is absent.\n2. Fields that may be null (`safe_mode_tier`, `last_reconcile_at`, `last_error_code`) must appear with value `null`, not omitted from the JSON object.\n3. `bot_id`, `status`, `lifecycle_state`, `dry_run` are non-null for an active bot. (Ref: UC §3 step 7, N-001.) | P0 |
| TC_048 | Verify Investor can query their own bot (ownership match — HTTP 200 returned) [NEW — TS_UC-EXBOT-monitor-status_028] | 1. A bot exists with `bot.user_wallet_address = '0xABC...'`.\n2. Investor sends `X-Wallet-Address: '0xABC...'` — matching the bot's owner wallet. | 1. Call `GET /api/exbot/status` with the matching wallet address. | 1. Response is HTTP 200 with full bot status data.\n2. Ownership check passes — Investor is authorized to view their own bot.\n3. No 403 is returned. (Ref: UC §2, AC-01.) | P0 |
| TC_049 | Verify status response reflects paused state correctly — status='paused', lifecycle_state='active' [NEW — TS_UC-EXBOT-monitor-status_029; FR-EXBOT-003] | 1. Bot has `bots.status='paused'` and `bots.lifecycle_state='active'` in Aurora PostgreSQL (paused from `lifecycle_state='active'` per FR-EXBOT-003 — pause preserves the prior `lifecycle_state`). | 1. Call `GET /api/exbot/status` for the paused bot. | 1. Response is HTTP 200 with `status='paused'` and `lifecycle_state='active'` — both values reflect the Aurora PostgreSQL state accurately.\n2. `lifecycle_state` is NOT changed to 'paused' — FR-EXBOT-003 preserves the lifecycle_state at the time of pause.\n3. Caller can distinguish this from a running-active bot by checking `status='paused'`. (Ref: UC §6.1, FR-EXBOT-003, srs/states.md.) | P0 |
| TC_050 | Verify status response for a bot in a transitional lifecycle_state returns HTTP 200 and reflects the exact state [NEW — TS_UC-EXBOT-monitor-status_030] | 1. Bot has a transitional `lifecycle_state` (e.g., `'lp_opening'`) in Aurora PostgreSQL — a state the bot passes through during initialization. | 1. Call `GET /api/exbot/status` while the bot is in `lifecycle_state='lp_opening'`. | 1. Response is HTTP 200 — transitional states do NOT produce a 404 or error.\n2. `lifecycle_state` in the response matches the exact value in Aurora PostgreSQL (`'lp_opening'` in this case).\n3. No lifecycle_state value is silently remapped, defaulted, or suppressed. (Ref: UC §6.1, srs/states.md.) | P1 |

### II.2. Integration & State verification — Gap fill

| TC ID | Title | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|
| TC_051 | Verify status response correctly maps all valid lifecycle_state values — each state returned as-stored in Aurora PostgreSQL [NEW — TS_UC-EXBOT-monitor-status_049 EP all states] | 1. Aurora PostgreSQL contains bots in each of the 18 canonical `lifecycle_state` values: `active`, `lp_rebalancing`, `lp_closing`, `closed`, `hedge_stopped_cooldown`, `lp_opening`, `safe_mode`, `error`, `closing`, `paused`, `initializing`, `pending_deposit`, `pending_start`, `starting`, `pending_close`, `partial_close`, `pending_recovery`, `recovering`, `terminated`. | 1. For each `lifecycle_state`, call `GET /api/exbot/status` and record the `lifecycle_state` value returned.\n2. Compare each returned value against the Aurora PostgreSQL-stored value. | 1. For every `lifecycle_state`, the value in the response matches exactly the value stored in Aurora PostgreSQL — no state is remapped, aliased, or suppressed.\n2. All states return HTTP 200 (except `closed` which also returns HTTP 200 per AC-05 and no-record which returns 404 per AC-06).\n3. No lifecycle_state outside the 18 canonical values is accepted or returned. (Ref: UC §4 all alternates, srs/states.md, FR-EXBOT-003.) | P1 |
