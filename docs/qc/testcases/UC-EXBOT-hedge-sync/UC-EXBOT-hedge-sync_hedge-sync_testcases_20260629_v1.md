# Test Cases — UC-EXBOT-hedge-sync Execute Delta-Only Hedge Adjustment

**Total test cases:** 70 (FUNC: 36, INTG: 22, NFR: 12)
**Scope:** Logic-only (backend / API / bot — no UI)
**Source UC:** UC-EXBOT-hedge-sync_hedge-sync_audited_20260626_v2.md
**Source scenarios (if any):** UC-EXBOT-hedge-sync_hedge-sync_scenarios_20260629_v1.md
**Output language:** English


#### Requirement Traceability Matrix

| AC ID | Acceptance Criteria | Linked Test Cases | Status |
|---|---|---|---|
| AC-01 | Happy path: delta computed, adjustShortDelta sent, INV-STOP, reconcile confirmed, status='success' | TC_001, TC_002, TC_003, TC_004, TC_005 | Covered |
| AC-02 | stateVersion mismatch → status='skipped', no HL order | TC_006, TC_007 | Covered |
| AC-03 | Lock not acquired → re-queue, no mutation | TC_008, TC_009 | Covered |
| AC-04 | HL rejected → status='failed', failure_count++, notification | TC_014, TC_015, TC_016 | Covered |
| AC-05 | 3 consecutive failures → circuit open, stop monitoring continues | TC_023, TC_024, TC_025 | Covered |
| AC-06 | half_open probe success → circuit closed, failure_count=0 | TC_027, TC_028 | Covered |
| AC-07 | half_open probe failure → circuit open, new reset_at | TC_029, TC_030 | Covered |
| AC-08 | Partial fill → partial_repair enqueued, 3 fails → bot_safe_close | TC_040, TC_041, TC_042 | Covered |
| AC-09 | delta=0 → no HL order, INV-STOP continues, status TBD | TC_017, TC_018 | Covered (partial — status TBD I-01) |
| AC-10 | Duplicate message → idempotency guard exits, no second HL call | TC_010, TC_011 | Covered |


---


## I. Operation: Hedge-sync (delta-only adjustment)


### I.1. Functional verification — Operation: Hedge-sync (delta-only adjustment)

| TC ID | Title | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|
| TC_001 | Verify hedge-sync sends only the difference when the bot needs rebalancing (happy path) | 1. The bot is running with an open hedge (`bots.status='active'`, `lifecycle_state='active'`).\n2. The circuit breaker is closed (`circuit_breakers.state='closed'`).\n3. The `hedge-sync` message `stateVersion` matches `bot_runtime_state.state_version` in D1.\n4. The `queue_idempotency` table has no row for this `message_id`.\n5. The `UserLockDO` lease is available (no other worker holds it).\n6. The actual HL short size differs from the target (delta ≠ 0).\n7. The HL agent key is approved and not expired (`approval_status='approved'`). | 1. Deliver a `hedge-sync` queue message for a bot whose actual HL short differs from the target; observe the HL calls and D1 writes. | 1. The worker inserts a `queue_idempotency` row with `state='started'`, acquires the `UserLockDO` lease, fetches `clearinghouseState`, computes the delta using BigDecimal, and sends exactly one `adjustShortDelta(delta, cloid)` call to HL — it does NOT close the full position and reopen it (BR-EXBOT-004). A `reconcile` message is enqueued. After the Reconcile Worker confirms the size matches `expectedAbsSize`, `rebalance_attempts.status='success'` is written and `queue_idempotency.state='succeeded'`. (Reference: FR-EXBOT-022, BR-EXBOT-004, FR-EXBOT-025, AC-01.) | P0 |
| TC_002 | Verify hedge_legs fields are updated after reconcile confirms the new position | 1. The bot is running (`status='active'`).\n2. A delta adjustment has been sent to HL and the Reconcile Worker has confirmed the size matches `expectedAbsSize`. | 1. Let the Reconcile Worker complete and read the D1 `hedge_legs` row. | 1. `hedge_legs.stop_price`, `entry_price`, `effective_leverage` all reflect the post-reconcile values. `stop_replacing_started_at` is `NULL`. `bot_runtime_state.last_known_hl_short_size` equals the reconciled size. `rebalance_attempts.status='success'` is written ONLY after this confirmation — not before. (Reference: FR-EXBOT-025, FR-EXBOT-035.) | P0 |
| TC_003 | Verify a positive delta (short too small) adjusts upward without a full close-then-open | 1. The bot is running with an active hedge.\n2. The actual HL short size is smaller than `targetShortEth`, so delta is positive (needs to increase the short).\n3. The circuit breaker is closed and the `stateVersion` matches. | 1. Process the `hedge-sync` message and inspect the HL calls made. | 1. The worker sends one `adjustShortDelta` call with a positive delta only. No full-close call is issued and no new-open call is issued. After reconcile, the actual HL short size equals the original actual size plus the delta (within tolerance). (Reference: FR-EXBOT-022, BR-EXBOT-004.) | P0 |
| TC_004 | Verify a negative delta (short too large) reduces the position without a full close-then-open | 1. The bot is running with an active hedge.\n2. The actual HL short size is larger than `targetShortEth`, so delta is negative (needs to reduce the short).\n3. The circuit breaker is closed and the `stateVersion` matches. | 1. Process the `hedge-sync` message and inspect the HL calls made. | 1. The worker sends one `adjustShortDelta` call with a negative delta only. No full-close call is issued. After reconcile, the new short size equals the original actual size minus the absolute delta (within tolerance). (Reference: FR-EXBOT-022, BR-EXBOT-004.) | P0 |
| TC_005 | Verify rebalance_attempts.status is not written as success before reconcile confirms | 1. The bot is running and a delta adjustment has been sent to HL.\n2. The Reconcile Worker has not yet run (the `reconcile` queue message has not been processed). | 1. Read the `rebalance_attempts` table immediately after `adjustShortDelta` returns and before the Reconcile Worker processes the `reconcile` message. | 1. The `rebalance_attempts` row does NOT have `status='success'` at this point. The status must remain at an in-progress value until the Reconcile Worker confirms `size == expectedAbsSize`. (Reference: FR-EXBOT-025 — "only then insert rebalance_attempts.status='success'".) | P0 |
| TC_006 | Verify an out-of-date hedge-sync message is discarded without sending any HL order | 1. The bot is running.\n2. The `hedge-sync` message `stateVersion` is lower than the current `bot_runtime_state.state_version` in D1 (a stale message — e.g., stateVersion = n − 1). | 1. Deliver the out-of-date `hedge-sync` message. | 1. The worker checks `bot_runtime_state.state_version` before acquiring the lock. The stateVersion mismatch is detected immediately. The worker sets `rebalance_attempts.status='skipped'` and exits without acquiring the `UserLockDO`, without calling HL, and without running the INV-STOP protocol. (Reference: FR-EXBOT-027, AC-02.) | P0 |
| TC_007 | Verify a hedge-sync message with stateVersion exactly matching D1 passes the guard | 1. The bot is running.\n2. The `hedge-sync` message `stateVersion` equals `bot_runtime_state.state_version` exactly (boundary: exact match). | 1. Deliver the `hedge-sync` message with a stateVersion equal to the D1 value. | 1. The stateVersion guard passes. The worker proceeds to lock acquisition. No skip or discard occurs. (Reference: FR-EXBOT-027, BVA at the exact-match boundary.) | P0 |
| TC_008 | Verify the worker re-queues with delay when the UserLockDO is held by another worker | 1. The bot is running.\n2. Another worker already holds the `UserLockDO` lease for this bot (`acquired=false` is returned).\n3. The `stateVersion` matches and the idempotency check passes. | 1. Deliver the `hedge-sync` message while another worker holds the lock. | 1. `UserLockDO.acquire` returns `acquired=false`. The worker re-queues the `hedge-sync` message with a backoff delay and exits — no HL call, no delta computation, and no INV-STOP step. No `rebalance_attempts` row with `status='failed'` is written. (Reference: FR-EXBOT-026, AC-03.) | P0 |
| TC_009 | Verify the UserLockDO lease is always released in the finally block even on failure | 1. The bot is running and the `UserLockDO` lease has been acquired.\n2. The HL `adjustShortDelta` call fails partway through the run (mocked error). | 1. Trigger a delta adjustment and inject an HL failure after the lease is acquired. | 1. Regardless of the failure, `UserLockDO.release(holderToken, idempotencyKey, result)` is called in the `finally` block and the lease is freed. No orphaned lease remains in the DO. The next `hedge-sync` message for the same bot can acquire the lock immediately. (Reference: FR-EXBOT-026.) | P0 |
| TC_010 | Verify delivering the same hedge-sync message twice does not act twice | 1. A `hedge-sync` message with a specific `message_id` has already been processed once (the `queue_idempotency` row exists with `state='started'` or `state='succeeded'`). | 1. Re-deliver the same `hedge-sync` message with the identical `message_id`. | 1. The second delivery hits a UNIQUE constraint conflict on `queue_idempotency.message_id` and the worker exits immediately. No second HL call is made and no duplicate `rebalance_attempts` row is written. Exactly one effect from the original delivery. (Reference: FR-EXBOT-011, NFR-EXBOT-007, AC-10.) | P0 |
| TC_011 | Verify the queue_idempotency state transitions from started to succeeded after a successful run | 1. A `hedge-sync` message is delivered for the first time (no existing `queue_idempotency` row for this `message_id`). | 1. Process the `hedge-sync` message through to completion (reconcile confirmed). | 1. On first delivery: the worker inserts a row with `state='started'`. After reconcile confirms, the row is updated to `state='succeeded'`. The lifecycle `started → succeeded` is observed with no skip. (Reference: FR-EXBOT-011.) | P1 |
| TC_012 | Verify delta=0 skips the HL order but continues the INV-STOP protocol | 1. The bot is running and the `stateVersion` matches.\n2. The actual HL short size equals `targetShortEth` exactly, so delta = 0.\n3. The `UserLockDO` lease is acquired. | 1. Process the `hedge-sync` message where delta computes to 0. | 1. No `adjustShortDelta` call is made to HL (delta-only invariant holds even when delta=0). The INV-STOP protocol runs normally: `stop_replacing_started_at` is set, the stop is cancelled then re-placed, and `stop_replacing_started_at` is cleared in `finally`. A `rebalance_attempts` row is written with a reason logged. (Note: the exact `status` value for delta=0 is pending OQ-EXBOT-013 — I-01; assert the no-HL-call behavior and mark the status assertion as TBD.) (Reference: UC §4 A6, FR-EXBOT-035, AC-09.) | P0 |
| TC_013 | Verify the INV-STOP protocol sets and clears stop_replacing_started_at correctly | 1. The `UserLockDO` lease has been acquired and the worker is about to execute the stop replacement.\n2. `hedge_legs.stop_replacing_started_at` is currently `NULL`. | 1. Run the INV-STOP protocol (cancel current stop, place new stop).\n2. Read `hedge_legs.stop_replacing_started_at` immediately after the new stop is confirmed and the `finally` block executes. | 1. Before the critical section, `stop_replacing_started_at` is set to a non-NULL timestamp. After the `finally` block completes (success or failure path), `stop_replacing_started_at` is `NULL`. The field must not remain set after the run exits — it must never be left non-NULL without the `finally` guard. (Reference: FR-EXBOT-035.) | P0 |
| TC_014 | Verify HL rejection for insufficient margin stores E-EXBOT-007 and increments the failure count | 1. The bot is running and the circuit breaker is closed.\n2. HL (mocked) rejects the `adjustShortDelta` call with an insufficient-margin error. | 1. Process a `hedge-sync` message where the HL order is rejected for insufficient margin. | 1. The worker sets `rebalance_attempts.status='failed'` and stores error code `E-EXBOT-007` ("Hedge adjustment rejected: insufficient margin on Hyperliquid. Deposit additional margin."). The `circuit_breakers.failure_count` is incremented by 1. A notification is enqueued. No retry of the HL call within the same worker invocation. (Reference: FR-EXBOT-040, E-EXBOT-007, AC-04.) | P0 |
| TC_015 | Verify HL unreachability stores E-EXBOT-008 and increments the circuit breaker failure count | 1. The bot is running and the circuit breaker is closed.\n2. The HL API is unreachable (mocked timeout / connection refused). | 1. Process a `hedge-sync` message where HL cannot be reached. | 1. The worker sets `rebalance_attempts.status='failed'` and stores error code `E-EXBOT-008` ("Hyperliquid API is currently unreachable. Bot entered Safe Mode. Retrying automatically."). The `circuit_breakers.failure_count` is incremented. If this is the 3rd consecutive failure, the circuit opens. (Reference: FR-EXBOT-040, E-EXBOT-008.) | P0 |
| TC_016 | Verify a full close followed by a re-open during normal hedge-sync is treated as a violation | 1. The bot is running and the circuit breaker is closed.\n2. Delta is non-zero (a real adjustment is needed). | 1. Simulate or detect any code path that would close the full HL short position and immediately re-open it as part of normal hedge-sync. | 1. This behavior is a hard violation of BR-EXBOT-004. Under no normal hedge-sync condition should two calls (full-close + new-open) be observed. Only `adjustShortDelta` with the computed delta is acceptable. (Reference: BR-EXBOT-004, FR-EXBOT-022.) | P0 |
| TC_017 | Verify the stop_trigger_px is computed using BigDecimal and the correct formula | 1. The Reconcile Worker has confirmed the position and `entry_price`, `liquidation_price` (or `effective_leverage`) are available from HL. | 1. Trigger the `stop_trigger_px` recomputation and read the resulting `hedge_legs.stop_price`. | 1. `stop_trigger_px = entry_price × (1 + liq_distance_pct × stopSafetyFactor)` where `liq_distance_pct = (liq_price − entry_price) / entry_price` and `stopSafetyFactor = 0.70` (Phase A). The result is computed entirely in BigDecimal — no `Number()` arithmetic is used. The computed value is stored in `hedge_legs.stop_price` and placed on HL. (Reference: FR-EXBOT-030, NFR-EXBOT-008.) | P0 |
| TC_018 | Verify BigDecimal is used for delta computation — Number() arithmetic is rejected | 1. The worker is computing `delta = targetShortEth − actualShortEth` where the values have sub-cent fractional precision. | 1. Provide `targetShortEth` and `actualShortEth` values that would produce a different result under IEEE 754 float arithmetic vs BigDecimal (fractional values that expose rounding loss). | 1. The delta is computed using BigDecimal only. The result is exact with no rounding loss. Any code path that uses `Number()` arithmetic for this calculation violates NFR-EXBOT-008 and must not exist. (Reference: NFR-EXBOT-008, FR-EXBOT-021.) | P0 |
| TC_019 | Verify only canonical RebalanceReason values are accepted in the hedge-sync message | 1. The worker receives a `hedge-sync` message payload. | 1. Send a `hedge-sync` message with a `reasons` field containing a non-canonical value (not in `RebalanceReason[]`).\n2. Send a message with a valid canonical reason (e.g., `drift_threshold`). | 1. The message with the non-canonical reason is rejected with a clear error. No HL order is placed. The `rebalance_attempts.reason` column only ever contains values from the canonical enum: `drift_threshold`, `drift_relative`, `range_out`, `range_boundary_near`, `margin_warning`, `funding_alert`, `time_fallback`, `manual_admin`, `recovery_reconcile`. (Reference: FR-EXBOT-023.)\n2. The message with the valid reason is processed normally. | P1 |
| TC_020 | Verify the cloid is the same for the same botId + attemptId + stage + version | 1. A specific combination of `botId`, `attemptId`, `stage`, `version` is used as input. | 1. Compute the cloid twice independently with the same four-field input and compare. | 1. Both computations produce the same hex string: `first128BitsHex(keccak256("bnza:{botId}:{attemptId}:{stage}:{version}"))`. The determinism invariant holds: same input → same cloid, always. (Reference: FR-EXBOT-024.) | P0 |
| TC_021 | Verify the cloid changes when the version component is incremented for a new attempt | 1. A prior attempt used `version=1` for a specific `botId+attemptId+stage`, producing cloid A. | 1. Compute the cloid with the same `botId+attemptId+stage` but `version=2`. | 1. The new cloid (version=2) is different from the cloid at version=1. The two hex strings differ. (Reference: FR-EXBOT-024.) | P1 |
| TC_022 | Verify the worker reconciles before retrying when the same cloid is reused | 1. A previous `adjustShortDelta` attempt with a specific `cloid` did not receive a clear success confirmation. | 1. Trigger a retry that would reuse the same `botId+attemptId+stage+version` (same cloid). | 1. Before any retry submission, the worker checks the reconcile state to determine whether the original order was partially or fully filled. It does NOT blindly resubmit with the same cloid. Only if reconcile shows the original did not execute does the worker proceed with a new order. (Reference: FR-EXBOT-024.) | P0 |

### I.2. Integration & State verification — Operation: Hedge-sync (delta-only adjustment)

| TC ID | Title | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|
| TC_023 | Verify the circuit breaker opens after three consecutive HL failures within 24 hours | 1. The circuit breaker is closed (`circuit_breakers.state='closed'`).\n2. HL (mocked) is configured to fail the `adjustShortDelta` call.\n3. Two prior consecutive failures have already been recorded in the 24-hour rolling window (so this run is the third). | 1. Process a `hedge-sync` message that results in a third consecutive HL failure within the 24-hour window. | 1. The `circuit_breakers.state` transitions from `closed` to `open`. `reset_at` is set to `now + 1h`. The state is persisted atomically to D1. Subsequent `hedge-sync` messages for the same bot are blocked (hedge suppressed). Stop monitoring continues — a bot near its stop price still receives a `price-near-stop-audit` message. (Reference: FR-EXBOT-040, AC-05, US-008 AC-1.) | P0 |
| TC_024 | Verify two failures in 24 hours do not open the circuit breaker | 1. The circuit breaker is closed.\n2. Two consecutive HL failures have been recorded but there has been no third failure yet (failure_count = 2; BVA: limit − 1). | 1. Read the circuit breaker state after the second consecutive failure. | 1. `circuit_breakers.state` remains `closed`. A third failure is still required to trigger the open state. Hedge-sync calls continue to be allowed. (Reference: FR-EXBOT-040, BVA.) | P0 |
| TC_025 | Verify an intervening success resets the consecutive failure count (no carry-over) | 1. The circuit breaker has recorded 2 consecutive failures in the current 24-hour window.\n2. A successful `hedge-sync` run occurs next (failure_count would be reset to 0). | 1. Process a successful `hedge-sync` after 2 failures.\n2. Then process two more failures. | 1. After the successful run, `circuit_breakers.failure_count` is reset to 0. The subsequent 2 failures start a new consecutive count — they do NOT combine with the prior 2 to make 4. The circuit does not open after the 2 new failures. The circuit requires exactly 3 consecutive failures with no intervening success to open. (Reference: FR-EXBOT-040, UC §6 BR: "consecutive".) | P0 |
| TC_026 | Verify the circuit breaker stays open and suppresses hedge after reset_at is not yet reached | 1. The circuit breaker is open (`circuit_breakers.state='open'`, `reset_at` is in the future). | 1. Deliver a `hedge-sync` message while the circuit is open. | 1. The hedge mutation is suppressed — no `adjustShortDelta` call is made. Stop monitoring remains active (the INV-STOP / stop-check path continues). No `rebalance_attempts.status='failed'` is written for the suppressed attempt. (Reference: FR-EXBOT-014, AC-05.) | P0 |
| TC_027 | Verify the circuit transitions to half_open and allows exactly one probe after reset_at passes | 1. The circuit breaker is open and `reset_at` has now passed (the time threshold is crossed). | 1. Deliver a `hedge-sync` message after `reset_at` elapses. | 1. The circuit transitions to `half_open`. The `half_open_probe_used` flag is atomically set from 0 to 1. The probe HL call is sent. No second probe is allowed (any concurrent worker that also reads `half_open_probe_used=0` before the atomic claim must be blocked). (Reference: FR-EXBOT-040.) | P0 |
| TC_028 | Verify a successful probe in half_open closes the circuit and resets the failure count | 1. The circuit breaker is `half_open` and the probe HL call succeeds. | 1. Let the probe `hedge-sync` run and confirm the HL call succeeds. | 1. `circuit_breakers.state` transitions to `closed`. `failure_count` is reset to 0. `reset_at` is cleared. Subsequent `hedge-sync` runs are allowed to call HL without restriction. (Reference: FR-EXBOT-040, AC-06, US-008 AC-3.) | P0 |
| TC_029 | Verify a failed probe in half_open re-opens the circuit with a new reset_at | 1. The circuit breaker is `half_open` and the probe HL call fails. | 1. Let the probe `hedge-sync` run and observe the HL call failing. | 1. `circuit_breakers.state` transitions back to `open`. `reset_at` is set to `now + 1h`. The `half_open_probe_used` flag is reset so the next probe cycle can begin after `reset_at` elapses again. Subsequent `hedge-sync` runs are blocked. (Reference: FR-EXBOT-040, AC-07, US-008 AC-4.) | P0 |
| TC_030 | Verify a second concurrent probe attempt is blocked when half_open_probe_used is already 1 | 1. The circuit breaker is `half_open` and the first probe has already claimed `half_open_probe_used=1` (the probe HL call is in flight).\n2. A second concurrent worker also tries to send a probe. | 1. Simulate two concurrent workers racing to claim the probe slot when `half_open_probe_used=0`. | 1. Only one worker atomically increments `half_open_probe_used` from 0 to 1 and proceeds. The second worker sees `half_open_probe_used=1` and is blocked — it does not send an HL call and does not modify the circuit breaker state. (Reference: FR-EXBOT-040.) | P0 |
| TC_031 | Verify the reconcile worker detects a partial fill and enqueues partial_repair | 1. `adjustShortDelta` was sent to HL and the `reconcile` message is now being processed.\n2. The Reconcile Worker fetches the actual HL position and finds the size does not match `expectedAbsSize` (partial fill). | 1. Let the Reconcile Worker process the `reconcile` message with a mocked HL response showing a partial fill. | 1. The Reconcile Worker detects `\|actual − expected\| > tolerance`. It enqueues a `partial_repair` message with `reason='partial_fill'`. `rebalance_attempts.status='partial'` is written. E-EXBOT-011 ("Hedge position mismatch detected. Bot entered Safe Mode pending reconciliation.") is stored internally. (Reference: FR-EXBOT-036, E-EXBOT-011, AC-08.) | P0 |
| TC_032 | Verify bot_safe_close is triggered after three consecutive partial_repair failures | 1. A partial fill has been detected and `partial_repair` messages have been enqueued.\n2. Three consecutive `partial_repair` attempts have all failed to bring the actual HL position to `expectedAbsSize`. | 1. Simulate three sequential `partial_repair` failures. | 1. After the third failure, `bot_safe_close` is triggered. The bot transitions to `safe_mode`. No further automatic repair attempts are made. (Reference: FR-EXBOT-036, AC-08.) | P0 |
| TC_033 | Verify the UserLockDO heartbeat is sent when the hedge run exceeds 30 seconds | 1. The `UserLockDO` lease has been acquired (TTL=90s).\n2. The HL API processing within the locked run takes longer than 30 seconds (simulated delay). | 1. Trigger a hedge-sync run with an intentional delay beyond 30 seconds within the locked section. | 1. The worker sends a heartbeat to the `UserLockDO` before the 90s TTL would expire, extending the lease. The lock is not auto-released mid-operation. At the end of the run, the lock is released in `finally`. (Reference: FR-EXBOT-026, FR-EXBOT-092.) | P1 |
| TC_034 | Verify the UserLockDO auto-releases after 90s without a heartbeat (TTL expiry) | 1. A worker holds the `UserLockDO` lease but stops sending heartbeats (e.g., the worker hangs or crashes after acquiring). | 1. Wait for the TTL (90s) to expire without any heartbeat.\n2. Deliver a new `hedge-sync` message for the same bot. | 1. After 90 seconds, the DO auto-releases the lease. The new `hedge-sync` message is able to acquire the lock and proceed normally. No manual intervention is required. (Reference: FR-EXBOT-092.) | P1 |
| TC_035 | Verify two concurrent hedge-sync workers for the same bot result in only one mutation | 1. Two `hedge-sync` messages for the same bot are being processed concurrently by two worker instances.\n2. The `UserLockDO` lease is available at the start. | 1. Race two workers to acquire the `UserLockDO` lease. | 1. Only the first worker to atomically acquire the lock proceeds to the HL mutation. The second receives `acquired=false` and re-queues with delay. No double `adjustShortDelta` is sent to HL. (Reference: FR-EXBOT-026, FR-EXBOT-092, TS_UC-EXBOT-hedge-sync_029.) | P0 |
| TC_036 | Verify bot_runtime_state.last_known_hl_short_size is updated after reconcile | 1. A `reconcile` message has been processed and the Reconcile Worker has confirmed the actual HL position. | 1. Read `bot_runtime_state.last_known_hl_short_size` after the reconcile completes. | 1. The value equals the reconciled actual HL short size. If this field is not updated, the next light-check will compute a wrong `deltaErrorUsd` based on a stale value — so this update is a data consistency requirement. (Reference: FR-EXBOT-025, §F.4.) | P1 |
| TC_037 | Verify hedge-sync is skipped entirely when lifecycle_state is lp_rebalancing | 1. The bot's `lifecycle_state` is `lp_rebalancing` (not `active`).\n2. A `hedge-sync` message is delivered (from a prior enqueue before the state changed). | 1. Process the `hedge-sync` message when `lifecycle_state='lp_rebalancing'`. | 1. The worker detects the non-permitted lifecycle state and skips the hedge-sync without acquiring the lock or calling HL. No partial side effect is produced. (Reference: states.md — hedge-sync skip rule for `lp_rebalancing`.) | P1 |
| TC_038 | Verify hedge-sync is suppressed and stop monitoring continues when circuit is open | 1. The circuit breaker is open (`state='open'`).\n2. A `hedge-sync` message is delivered. | 1. Process the `hedge-sync` message while the circuit is open.\n2. Observe whether stop monitoring events are still generated for the bot. | 1. No `adjustShortDelta` HL call is sent. Stop monitoring remains active — if the bot's `markPrice` is near `stop_price`, a `price-near-stop-audit` message is still enqueued. The hedge suppression does not disable the stop monitor. (Reference: FR-EXBOT-014, AC-05.) | P0 |
| TC_039 | Verify hedge_legs and rebalance_attempts are updated atomically — no partial DB state | 1. The Reconcile Worker is about to write `hedge_legs` updates and `rebalance_attempts.status='success'` atomically. | 1. Inject a failure mid-way through the atomic write (e.g., after `hedge_legs` update but before `rebalance_attempts` insert).\n2. Read both tables. | 1. Neither table reflects a partial update — both writes are committed or both are rolled back. No state where `hedge_legs` shows updated values but `rebalance_attempts` is still absent (or vice versa). (Reference: FR-EXBOT-025.) | P0 |
| TC_040 | Verify the HL agent key decrypt failure mid-hedge-sync routes to SAFE_MODE without leaking key material | 1. The worker is about to call HL (needing to sign with the agent key).\n2. The agent key decryption fails (e.g., the key has been revoked mid-flight per FR-EXBOT-082). | 1. Inject a decryption failure after the worker has acquired the lock but before the HL call. | 1. The worker does not proceed with an HL call using an invalid or missing key. The bot transitions to `safe_mode`. The raw agent key and raw DEK do NOT appear in any logs or error outputs. (Note: this scenario is partially blocked by I-06 — the exact flow position of the decrypt step is not in the UC main flow; assert the failure path and flag the pre-condition dependency on BA confirmation.) (Reference: FR-EXBOT-082, NFR-EXBOT-006.) | P0 |
| TC_041 | Verify the reconcile confirms full fill and sets status='success' with stop updated | 1. `adjustShortDelta` was sent to HL and the `reconcile` message is being processed.\n2. The Reconcile Worker fetches actual HL position: `size == expectedAbsSize` (full fill). | 1. Let the Reconcile Worker run with the actual HL size exactly matching `expectedAbsSize`. | 1. `rebalance_attempts.status='success'` is written. `hedge_legs.stop_price`, `entry_price`, `effective_leverage` are updated. `stop_replacing_started_at` is `NULL`. `bot_runtime_state.last_known_hl_short_size` is updated to the confirmed size. (Reference: FR-EXBOT-025.) | P0 |
| TC_042 | Verify hedge-sync completes the full happy path end-to-end in the correct sequence | 1. All preconditions for the happy path are met: `status='active'`, circuit closed, stateVersion match, no prior `message_id`, lock available, HL healthy. | 1. Deliver a `hedge-sync` message and trace the full execution from message receipt to reconcile confirmation. | 1. The operations execute in this exact sequence with no step skipped: (1) `queue_idempotency` insert; (2) stateVersion check pass; (3) `UserLockDO.acquire` → `{acquired: true}`; (4) fetch `clearinghouseState`; (5) compute delta (BigDecimal); (6) `adjustShortDelta(delta, cloid)`; (7) set `stop_replacing_started_at`; (8) cancel stop → place new stop; (9) clear `stop_replacing_started_at` in `finally`; (10) enqueue `reconcile`; (11) `UserLockDO.release`; (12) Reconcile Worker confirms size; (13) update `hedge_legs` + insert `rebalance_attempts.status='success'` + update `queue_idempotency.state='succeeded'`. (Reference: FR-EXBOT-025, FR-EXBOT-035, UC §3 Main Flow.) | P0 |

### I.3. Non-functional (logic) verification — Operation: Hedge-sync (delta-only adjustment)

| TC ID | Title | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|
| TC_043 | Verify the raw agent key and DEK are never written to logs during hedge-sync signing | 1. The bot is running and a hedge-sync run requires HL signing (the agent key must be decrypted to sign the order).\n2. The agent key is approved and valid. | 1. Run a successful hedge-sync that requires agent key decryption for signing.\n2. Read all worker logs and any error output generated during the run. | 1. No raw agent key value and no raw DEK (data-encryption key) appear in any log line or error output. The key is used only within the function scope and is not written to any persistent or observable medium. A dump of the `hl_agent_keys` D1 table shows only encrypted blobs (`encrypted_secret`, `wrapped_dek`, etc.). (Reference: NFR-EXBOT-006, FR-EXBOT-080.) | P0 |
| TC_044 | Verify hedge-sync completes within 30 seconds under normal conditions | 1. The bot is running with normal HL API response times (no artificial delays).\n2. All happy path preconditions are met. | 1. Process a `hedge-sync` message and measure the time from message receipt to `rebalance_attempts.status='success'` being written. | 1. The end-to-end time (from message deliver to reconcile confirmed) is ≤ 30 seconds. (Reference: NFR-EXBOT-002.) | P1 |
| TC_045 | Verify the worker does not exceed 6 simultaneous outbound HL connections per invocation | 1. A hedge-sync run is in progress involving multiple HL API calls (fetch + adjustShortDelta + stop operations). | 1. Monitor the number of concurrent outbound connections opened to HL during a single Worker invocation. | 1. No more than 6 simultaneous outbound connections are opened at any point in the invocation. (Reference: NFR-EXBOT-011.) | P1 |
| TC_046 | Verify the worker re-queues with delay and does not proceed when HLRateLimitDO returns allowed=false | 1. The `HLRateLimitDO` is at its rate limit and returns `{allowed: false, retryAfterMs: N}`.\n2. The worker is about to make an HL API call. | 1. Trigger a hedge-sync run when the `HLRateLimitDO` is at the rate limit. | 1. The worker does NOT proceed with the HL call. It re-queues with the specified `retryAfterMs` delay. The HL rate budget remains within ≤800 weight/min. (Note: detailed HLRateLimitDO + UserLockDO interaction is deferred per I-05 / OQ-EXBOT-015 — verify the basic re-queue behavior.) (Reference: FR-EXBOT-091, NFR-EXBOT-004.) | P1 |
| TC_047 | Verify every successful hedge-sync run produces an audit-quality rebalance_attempts row | 1. A hedge-sync run has completed successfully (reconcile confirmed). | 1. Read the `rebalance_attempts` row for this run. | 1. The row contains: `status='success'`, the `reason` from the canonical `RebalanceReason[]` enum used in the message, `adjust_cloid`, `reconciled_short_size`, and the timestamps needed to reconstruct the timeline. No PII or secrets appear in the row. (Reference: FR-EXBOT-025, NFR-EXBOT-006.) | P1 |
| TC_048 | Verify every hedge-sync run produces a rebalance_attempts row (failed runs included) | 1. A hedge-sync run has failed (HL rejection). | 1. Read the `rebalance_attempts` row for the failed run. | 1. The row exists with `status='failed'` and the relevant error code (`E-EXBOT-007` or `E-EXBOT-008`). The failure is auditable. (Reference: FR-EXBOT-025, FR-EXBOT-040.) | P1 |

## II. Operation: Circuit Breaker State Machine


### II.1. Functional verification — Operation: Circuit Breaker State Machine

| TC ID | Title | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|
| TC_049 | Verify the circuit breaker state is read from D1 column — not recomputed from history | 1. The `circuit_breakers.state` column is `open`.\n2. The failure history in the rolling window would also logically compute `open`. | 1. Read `circuit_breakers.state` directly and compare it to the value a dynamic recompute from the failure history would produce. | 1. The authoritative source is the `circuit_breakers.state` column. Logic must not recompute the state from history at query time — the column is the canonical truth. (Reference: FR-EXBOT-040, US-008 Notes.) | P1 |
| TC_050 | Verify the circuit breaker reset to closed happens only on probe success — not on expiry alone | 1. The circuit breaker is `open`. `reset_at` elapses and the state transitions to `half_open`.\n2. No probe is run yet (the `half_open_probe_used` is 0). | 1. Allow `reset_at` to elapse without a probe run. Read `circuit_breakers.state`. | 1. The state is `half_open` — not `closed`. The circuit is NOT automatically closed by time expiry alone. It requires an explicit successful probe. (Reference: FR-EXBOT-040.) | P0 |

### II.2. Integration & State verification — Operation: Circuit Breaker State Machine

| TC ID | Title | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|
| TC_051 | Verify circuit breaker open triggers admin notification | 1. The circuit transitions from `closed` to `open` after 3 consecutive failures. | 1. Observe the notification queue after the circuit opens. | 1. A notification message for the admin is enqueued. The bot owner is also alerted. (Reference: §F.2, FR-EXBOT-040.) | P1 |
| TC_052 | Verify half_open probe success clears the half_open_probe_used flag atomically | 1. The circuit is `half_open`, `half_open_probe_used=1`, and the probe succeeds. | 1. Let the probe complete successfully and read `circuit_breakers` columns. | 1. `state='closed'`, `failure_count=0`, and `half_open_probe_used` is reset to 0 (ready for the next cycle if the circuit opens again). The transition is atomic — no intermediate state where `state='closed'` but `failure_count` is still non-zero. (Reference: FR-EXBOT-040.) | P0 |

### II.3. Non-functional (logic) verification — Operation: Circuit Breaker State Machine

| TC ID | Title | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|
| TC_053 | Verify the circuit breaker state column is updated atomically — no race condition between workers | 1. Two hedge-sync failure results arrive concurrently for the same bot, both attempting to increment `failure_count`. | 1. Simulate two concurrent `incrementCircuitBreaker` calls for the same `hedge_leg_id`. | 1. `failure_count` increments exactly twice (no lost update, no double increment). The final value reflects both increments. The D1 update uses optimistic locking or an atomic increment to prevent a race. (Reference: FR-EXBOT-040.) | P0 |

## III. Operation: Post-Order Reconcile


### III.1. Functional verification — Operation: Post-Order Reconcile

| TC ID | Title | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|
| TC_054 | Verify the Reconcile Worker only marks success after confirming size matches expectedAbsSize | 1. The `reconcile` queue message is processed. The actual HL position `size` equals `expectedAbsSize` exactly. | 1. Let the Reconcile Worker process the message with a matching actual HL size. | 1. `rebalance_attempts.status='success'` is written. `hedge_legs` is updated. `queue_idempotency.state='succeeded'`. (Reference: FR-EXBOT-025.) | P0 |
| TC_055 | Verify the Reconcile Worker enqueues partial_repair on a size mismatch | 1. The `reconcile` queue message is processed. The actual HL `size` does not match `expectedAbsSize` (partial fill). | 1. Process the `reconcile` message with a mocked HL response showing a partial fill. | 1. A `partial_repair` message is enqueued. `rebalance_attempts.status='partial'`. E-EXBOT-011 stored internally. No success is written before repair. (Reference: FR-EXBOT-036, AC-08.) | P0 |

### III.2. Integration & State verification — Operation: Post-Order Reconcile

| TC ID | Title | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|
| TC_056 | Verify partial_repair worker resubmits with a new cloid after a partial fill | 1. A partial fill has been detected and a `partial_repair` message is enqueued. | 1. Let the Partial Repair Worker process the message. | 1. The worker fetches the actual HL position, computes the remaining delta, and resubmits `adjustShortDelta` with a new deterministic `cloid` (incremented version). It does NOT reuse the original cloid. The repair attempts at most 3 times. (Reference: FR-EXBOT-036, FR-EXBOT-024.) | P1 |

### III.3. Non-functional (logic) verification — Operation: Post-Order Reconcile

| TC ID | Title | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|
| TC_057 | Verify the Reconcile Worker result is recorded for audit — success and failure both produce rows | 1. A reconcile run has completed (either success or partial/failure). | 1. Read the `rebalance_attempts` table after the reconcile completes. | 1. A row with `status` equal to the correct final outcome exists. The row contains `reconciled_short_size`, timestamps, and the `adjust_cloid`. Reconcile outcomes are never silently discarded. (Reference: FR-EXBOT-025.) | P1 |

## IV. Operation: INV-STOP Protocol


### IV.1. Functional verification — Operation: INV-STOP Protocol

| TC ID | Title | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|
| TC_058 | Verify INV-STOP runs after a successful delta adjustment (step 8 in main flow) | 1. `adjustShortDelta` has returned a fill confirmation from HL.\n2. `stop_replacing_started_at` is `NULL` before the INV-STOP step. | 1. Let the worker execute the INV-STOP protocol: cancel current stop → place new stop. | 1. `stop_replacing_started_at` is set to a non-NULL timestamp before the cancel call. The existing stop is cancelled. A new stop is placed with the recomputed `stop_trigger_px`. After the `finally` block, `stop_replacing_started_at` is `NULL`. `hedge_legs.stop_cloid` and `stop_price` are updated. (Reference: FR-EXBOT-035.) | P0 |
| TC_059 | Verify INV-STOP also runs when delta=0 (stop replacement is not skipped) | 1. Delta was computed as 0 (no HL order sent). | 1. After skipping the HL delta order, observe whether the INV-STOP protocol runs. | 1. The INV-STOP protocol runs normally even when no delta adjustment was made. The stop is updated with the current recomputed `stop_trigger_px`. (Reference: UC §4 A6, FR-EXBOT-035, AC-09.) | P0 |
| TC_060 | Verify a direct cancelStop then placeStop without the INV-STOP guard is blocked | 1. Any code path that attempts to cancel a stop and place a new stop without setting `stop_replacing_started_at` first. | 1. Review that any stop replacement goes through the INV-STOP protocol (no raw cancel → place without the guard). | 1. Stop replacement without setting `stop_replacing_started_at` first is a protocol violation. The correct flow must always set `stop_replacing_started_at`, then cancel, then place, then clear in `finally`. (Reference: FR-EXBOT-035.) | P1 |

### IV.2. Integration & State verification — Operation: INV-STOP Protocol

| TC ID | Title | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|
| TC_061 | Verify the stop price stored in hedge_legs matches the value placed on HL | 1. The INV-STOP protocol has completed and a new stop has been placed on HL. | 1. Read `hedge_legs.stop_price` and compare it to the stop order placed on HL (observable from the fill/confirmation). | 1. The `hedge_legs.stop_price` matches the `stop_trigger_px` that was placed on HL. There is no discrepancy between the off-chain stored value and the on-chain/HL order. (Reference: FR-EXBOT-030, FR-EXBOT-035.) | P0 |

### IV.3. Non-functional (logic) verification — Operation: INV-STOP Protocol

| TC ID | Title | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|
| TC_062 | Verify stop_replacing_started_at being stuck >60s triggers SAFE_MODE via light-check | 1. `hedge_legs.stop_replacing_started_at` is set to a timestamp more than 60 seconds ago (worker crashed after setting it, before the `finally` block).\n2. The Light-Check Worker runs (≤5 min cadence). | 1. Run the Light-Check Worker while `stop_replacing_started_at` has been set for more than 60 seconds. | 1. The Light-Check Worker detects the stuck state, enqueues a `partial_repair(reason='stop_replacing_overrun')`, and triggers `SAFE_MODE` for the bot. (Reference: FR-EXBOT-033 primary; FR-EXBOT-035.) | P0 |

## V. Operation: stateVersion Guard & Queue Idempotency


### V.1. Functional verification — Operation: stateVersion Guard & Queue Idempotency

| TC ID | Title | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|
| TC_063 | Verify the stateVersion guard runs before lock acquisition | 1. The `hedge-sync` message carries a `stateVersion` lower than D1 (stale). | 1. Deliver the stale message and trace the execution order. | 1. The stateVersion mismatch is detected after the idempotency insert but BEFORE `UserLockDO.acquire` is called. No lock is acquired for a stale message. (Reference: FR-EXBOT-027, UC §3 step 2 before step 3.) | P0 |
| TC_064 | Verify the queue_idempotency insert runs before the stateVersion check | 1. A fresh `hedge-sync` message arrives (new `message_id`). | 1. Deliver the message and trace the order of the first two operations. | 1. The `queue_idempotency` insert (step 1) occurs before the stateVersion check (step 2). This ordering ensures that even a stale-version message is recorded in the idempotency ledger, preventing any redelivery from re-running the staleVersion check on an already-seen message. (Reference: FR-EXBOT-011, UC §3 Main Flow steps 1–2.) | P1 |

### V.2. Integration & State verification — Operation: stateVersion Guard & Queue Idempotency

| TC ID | Title | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|
| TC_065 | Verify a stateVersion equal to D1 value passes and a value below it is discarded | 1. Bot with a known `state_version = N` in D1. | 1. Deliver a message with `stateVersion = N` (exact match).\n2. Deliver a message with `stateVersion = N − 1` (one below — BVA). | 1. The message with `stateVersion = N` passes the guard and processing continues to lock acquisition.\n2. The message with `stateVersion = N − 1` is discarded: `rebalance_attempts.status='skipped'`, no lock acquired, no HL call. (Reference: FR-EXBOT-027, BVA.) | P0 |

### V.3. Non-functional (logic) verification — Operation: stateVersion Guard & Queue Idempotency

| TC ID | Title | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|
| TC_066 | Verify the queue_idempotency message_id UNIQUE constraint prevents double execution | 1. A `hedge-sync` message with a specific `message_id` is already recorded in `queue_idempotency`. | 1. Attempt to insert a new row with the same `message_id`. | 1. The D1 UNIQUE constraint fires. The insert is rejected. The worker exits without any side effect. The existing row is not modified. (Reference: FR-EXBOT-011, NFR-EXBOT-007.) | P0 |

## VI. Operation: Agent Key & Security


### VI.1. Functional verification — Operation: Agent Key & Security

| TC ID | Title | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|
| TC_067 | Verify a revoked agent key mid-flight causes a safe failure and routes to SAFE_MODE | 1. The worker has acquired the `UserLockDO` lease and is about to sign an HL call.\n2. The agent key row has `approval_status='revoked'` (revoked between acquire and signing). | 1. Attempt to decrypt the revoked agent key during the HL signing step. | 1. The decryption fails. The bot transitions to `SAFE_MODE`. The raw key material does not appear in any log or error message. A new valid key must be provisioned before recovery. (Reference: FR-EXBOT-082, NFR-EXBOT-006.) | P0 |
| TC_068 | Verify a pending or expired agent key is rejected before any HL call is made | 1. The `hedge-sync` worker reaches the signing step.\n2. The agent key has `approval_status='pending'` OR `expires_at < now`. | 1. Process a hedge-sync run where the key is either pending or expired. | 1. The HL call is blocked. The bot cannot sign orders with a non-approved or expired key. (Reference: FR-EXBOT-081, FR-EXBOT-083, E-EXBOT-003, E-EXBOT-004.) | P0 |

### VI.2. Integration & State verification — Operation: Agent Key & Security

| TC ID | Title | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|
| TC_069 | Verify the hl_agent_keys D1 table contains only encrypted blobs — no plaintext | 1. An agent key has been provisioned and approved. | 1. Dump the `hl_agent_keys` D1 table and inspect every row. | 1. The `encrypted_secret`, `wrapped_dek`, `secret_iv`, `secret_auth_tag`, and `dek_iv` columns contain encrypted binary / hex blobs only. No column contains a plaintext private key or plaintext DEK. (Reference: FR-EXBOT-080, NFR-EXBOT-006.) | P0 |

### VI.3. Non-functional (logic) verification — Operation: Agent Key & Security

| TC ID | Title | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|
| TC_070 | Verify a revoked agent key row is retained for audit — no DELETE allowed | 1. An agent key has been revoked (`approval_status='revoked'`, `revoked_at` set). | 1. Attempt to DELETE the revoked row from `hl_agent_keys`. | 1. The deletion is blocked (application-level guard or constraint). The row remains for audit purposes. Key revocation is non-destructive — old rows are marked `revoked` and retained, never deleted. (Reference: FR-EXBOT-082.) | P1 |
