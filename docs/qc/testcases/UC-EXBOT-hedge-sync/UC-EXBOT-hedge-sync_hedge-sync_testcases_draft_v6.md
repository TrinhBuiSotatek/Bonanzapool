# Test Cases — UC-EXBOT-hedge-sync Execute Delta-Only Hedge Adjustment (v6)

**Total test cases:** 76 (Delta vs v5: +8 new TC_069–TC_076, 2 updated TC_015/TC_032, -0 deleted)
**Scope:** Logic-only (backend / API / bot — no UI)
**Update trigger:** REQUIREMENT_DELTA
**Source UC:** UC-EXBOT-hedge-sync_hedge-sync_audited_20260724_v5.md
**Source scenarios (if any):** UC-EXBOT-hedge-sync_hedge-sync_scenarios_20260724_v5.md
**Output language:** English

#### Requirement Traceability Matrix

| AC ID | Acceptance Criteria | Linked Test Cases | Status |
|---|---|---|---|
| AC-01 | Happy path — delta adjustment + reconcile end-to-end | TC_013, TC_034, TC_035, TC_036, TC_064 | Covered |
| AC-02 | stateVersion mismatch — discard, no HL order | TC_004, TC_010 | Covered |
| AC-03 | Lock held — re-queue, no HL order | TC_006, TC_011 | Covered |
| AC-03b | Circuit open at execution time — discard, no HL order | TC_058 | Covered |
| AC-04 | HL order rejection — circuit increment | TC_019, TC_043 | Covered |
| AC-04b | Partial fill does NOT increment failure_count | TC_059 | Covered |
| AC-05 | Circuit opens after 3 consecutive failures | TC_043, TC_044 | Covered |
| AC-06 | Circuit transitions to half_open at reset_at | TC_046 | Covered |
| AC-07 | Half_open probe success — circuit closes | TC_047 | Covered |
| AC-08 | Half_open probe failure — circuit reopens | TC_048 | Covered |
| AC-08b | Partial fill routes to partial_repair — separate from circuit failure path | TC_037, TC_059 | Covered |
| AC-09 | allowHedgeSync atomic — exactly 1 half_open probe | TC_049, TC_062, TC_063 | Covered |
| AC-10 | INV-STOP happy path — place → verifyStopPlaced → cancel; no 0-stop window | TC_072 | Covered [NEW] |
| AC-11 | INV-STOP place failure — old stop stays active; partial_repair enqueued; no SAFE_MODE | TC_073 | Covered [NEW] |
| AC-12 | delta=0 (abs < 0.000001) — no_op_dust: no HL, no reconcile, no stop replacement, all DB fields unchanged | TC_015, TC_069, TC_070, TC_071 | Covered [UPDATED] |
| AC-12b | rebalance_attempts.reason = original RebalanceReason[] from payload (all outcomes) | TC_060, TC_061, TC_064 | Covered |
| AC-13 | marginSummary fetched AFTER lock; margin_status updated before mutation; extend() if processing approaches TTL=90s | TC_065, TC_066 | Covered |
| AC-14 | stop_replacing_started_at overrun — SAFE_MODE entry | TC_031 | Covered |
| AC-15 | drift_threshold = max($25, lpValueUsd x 3%); lpValueUsd = (lpEthAmount x uniPoolPrice) + lpUsdcAmount (principal only, no tokensOwed) | TC_074, TC_075, TC_076 | Covered [NEW] |
| AC-16 | queue_idempotency.state='succeeded' set only after full pipeline | TC_041 | Covered |
| AC-17 | Circuit breaker rechecked at execution time (step 2b — after stateVersion step 2a) | TC_058, TC_067 | Covered |

---

## I. Operation: Hedge-sync preflight (idempotency, stateVersion, lock)

### I.1. Functional verification — Operation: Hedge-sync preflight (idempotency, stateVersion, lock)

| TC ID | Title | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|
| TC_001 | Verify a new hedge-sync message is accepted when its message_id has not been seen before | 1. Bot running (`bots.status='active'`, `lifecycle_state='active'`).\n2. `queue_idempotency` has no row for the incoming `message_id`. | 1. Deliver a `hedge-sync` message whose `message_id` does not exist in `queue_idempotency`. | 1. A new row is inserted in `queue_idempotency` with `state='started'`; worker proceeds past the dedup check to the stateVersion check. No early exit. (Ref: FR-EXBOT-011.) | P0 |
| TC_002 | Verify delivering the same hedge-sync message twice causes the second delivery to exit immediately | 1. A `hedge-sync` message was already processed; its `message_id` exists in `queue_idempotency`. | 1. Deliver the exact same `hedge-sync` message a second time with the same `message_id`. | 1. Second delivery hits UNIQUE constraint conflict on `queue_idempotency.message_id` and returns immediately. No HL order placed, no new `rebalance_attempts` row created. Existing row unchanged. (Ref: FR-EXBOT-011, NFR-EXBOT-007.) | P0 |
| TC_003 | Verify the worker proceeds when the message stateVersion matches the current bot state | 1. Bot running.\n2. `bot_runtime_state.state_version` in Aurora PostgreSQL matches `stateVersion` in the incoming message. | 1. Deliver a `hedge-sync` message whose `stateVersion` equals current `bot_runtime_state.state_version`. | 1. stateVersion check passes; worker proceeds to acquire User Lock (Redis Redlock via ElastiCache). No discard or skip. (Ref: FR-EXBOT-027.) | P0 |
| TC_004 | Verify a stale hedge-sync message is discarded immediately at step 2a — stateVersion check short-circuits before circuit recheck executes [UPDATED — Reason: N4-03/F-02 ordering + AC-02 + AC-12b] | 1. Bot running.\n2. Queued `hedge-sync` message has `stateVersion` lower than current `bot_runtime_state.state_version`.\n3. Message payload contains a known `RebalanceReason[]` value (e.g., `['lp_drift']`).\n4. Circuit breaker may be in any state. | 1. Deliver the out-of-date `hedge-sync` message. | 1. Worker discards the message at the stateVersion check (step 2a — first in F-02 ordering); no circuit recheck DB read (`circuit_breakers` table) is executed. `rebalance_attempts.status='skipped'` recorded; no HL order sent; no lock acquired. `rebalance_attempts.reason` equals the original `RebalanceReason[]` from the message payload. Note: F-02 separates step 2 into (a) stateVersion check first and (b) circuit recheck second. A stale-message discard never reaches (b). (Ref: FR-EXBOT-027, FR-EXBOT-040, AC-02, AC-12b, flows.md F-02, N4-03.) | P0 |
| TC_005 | Verify the worker acquires the user lock and proceeds when the lock is free | 1. Bot running; stateVersion and idempotency checks pass.\n2. No other worker holds the User Lock (Redis Redlock via ElastiCache) lease for this user. | 1. Process the `hedge-sync` message when the lock is available. | 1. `User Lock (Redis Redlock via ElastiCache).acquire(holderToken, ttl=90s, idempotencyKey='hedge-sync:{botId}:{stateVersion}')` returns `acquired=true`; worker proceeds to fetch actual HL position. (Ref: FR-EXBOT-026, FR-EXBOT-092.) | P0 |
| TC_006 | Verify the worker re-queues with a delay and takes no action when the lock is held by another worker | 1. Bot running; stateVersion and idempotency checks pass.\n2. Another worker holds the User Lock (Redis Redlock via ElastiCache) lease. | 1. Process the `hedge-sync` message while lock is held by another worker. | 1. `User Lock (Redis Redlock via ElastiCache).acquire` returns `acquired=false` with `currentHolderExpiresAt`; worker re-queues with a delay and exits. No HL order placed. No `rebalance_attempts` row for this attempt. (Ref: FR-EXBOT-026, AC-03.) | P0 |
| TC_007 | Verify a size-increase delta adjustment is blocked when the margin is in warning state — margin_status determined by marginSummary fetched AFTER lock [UPDATED — Reason: RD-v4-01 Q3] | 1. Bot running with lock acquired (User Lock TTL=90s).\n2. Worker fetches HL marginSummary AFTER acquiring lock; marginSummary response yields `marginUsage` in warning band (0.55 ≤ marginUsage < 0.75).\n3. `hedge_legs.margin_status` updated to `warning` by this preflight step.\n4. Computed delta is positive (size increase). | 1. Process the `hedge-sync` message when margin is warning and delta is positive. | 1. Worker blocks the size-increase order; only reduce-only adjustments allowed. Attempt recorded as blocked/skipped with margin reason. No size-increase order sent to Hyperliquid. Note: margin_status is always derived from marginSummary fetched in the same invocation after lock acquisition — never from a prior invocation's cached value. (Ref: FR-EXBOT-060, FR-EXBOT-021, FR-EXBOT-092, Q3/OQ-EXBOT-014.) | P1 |
| TC_008 | Verify both increase and reduce adjustments are allowed when the margin is in OK state — margin_status determined by marginSummary fetched AFTER lock [UPDATED — Reason: RD-v4-01 Q3] | 1. Bot running with lock acquired.\n2. Worker fetches HL marginSummary AFTER acquiring lock; marginSummary response yields `marginUsage` < 0.55.\n3. `hedge_legs.margin_status` updated to `ok` by this preflight step. | 1. Process a `hedge-sync` message with a positive delta (size increase) when margin status is OK. | 1. Size-increase delta order is not blocked by margin check; worker proceeds to call `adjustShortDelta`. Note: margin_status reflects the freshly fetched marginSummary from this invocation, ensuring no race with concurrent workers. (Ref: FR-EXBOT-060, FR-EXBOT-092, Q3/OQ-EXBOT-014.) | P1 |
| TC_058 | Verify the worker discards a hedge-sync message when the circuit breaker is open at execution time, even when it was enqueued while the circuit was still closed [NEW — AC-03b] | 1. Light-check enqueued a `hedge-sync` message when `circuit_breakers.state='closed'`.\n2. Before the Worker processes the message, circuit breaker transitions to `open` (a concurrent failure opened it).\n3. Worker begins processing the queued message. | 1. Worker reaches step 2b (circuit recheck — after stateVersion check passes in step 2a): read `circuit_breakers.state` from Aurora PostgreSQL.\n2. State is now `open`. | 1. Worker discards the message with `rebalance_attempts.status='skipped'`. No `adjustShortDelta` call made to Hyperliquid. Lock released in `finally` block. This execution-time recheck (step 2b) runs only after step 2a (stateVersion check) passes — it guards against the race condition where circuit state changes between enqueue and execute. (Ref: FR-EXBOT-040, AC-03b, flows.md F-02.) | P0 |


### I.2. Integration & State verification — Operation: Hedge-sync preflight (idempotency, stateVersion, lock)

| TC ID | Title | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|
| TC_009 | Verify that two concurrent deliveries of the same message result in exactly one execution | 1. Bot running.\n2. Two workers receive the same `hedge-sync` message (same `message_id`) at the same time. | 1. Deliver the same `hedge-sync` message to two workers simultaneously. | 1. UNIQUE constraint on `queue_idempotency.message_id` ensures exactly one worker proceeds; the other gets conflict and exits. Only one HL order placed and one `rebalance_attempts` row exists. (Ref: FR-EXBOT-011.) | P0 |
| TC_010 | Verify the exact stateVersion boundary — a message matching the current version proceeds; one lower is discarded | 1. Bot running with known current `bot_runtime_state.state_version`. | 1. Deliver a `hedge-sync` message with `stateVersion` equal to current Aurora PostgreSQL value (exact match).\n2. Deliver a second message with `stateVersion` one less than current. | 1. Step 1: message proceeds.\n2. Step 2: message discarded (`rebalance_attempts.status='skipped'`), no HL order. Lower-than-current means discard. (Ref: FR-EXBOT-027, AC-02.) | P0 |
| TC_011 | Verify the User Lock (Redis Redlock via ElastiCache) lease auto-releases if the worker holds it past TTL — and that extend() is required for long-running invocations including slow marginSummary fetch [UPDATED — Reason: Q3 + AC-13 updated] | 1. Worker acquired User Lock (Redis Redlock via ElastiCache) lease (TTL=90s).\n2. HL marginSummary fetch is slow (total processing including marginSummary fetch approaches 90s).\n3. Worker does NOT call `extend()` before TTL expires. | 1. Scenario A — No extend: let TTL expire without extend(); second worker tries to acquire the lock.\n2. Scenario B — With extend: worker calls `extend(holderToken, ttl=90s)` before TTL expires during slow marginSummary fetch. | 1. Scenario A: after TTL expiry, lease auto-released; second worker acquires it (`acquired=true`). Second worker reconciles actual HL state before any new order. Risk: first worker may still be in critical section — highlights the need for extend().\n2. Scenario B: `extend()` refreshes TTL; first worker holds lock through marginSummary fetch completion. Second worker cannot acquire until first releases or TTL re-expires. (Ref: FR-EXBOT-026, FR-EXBOT-092, AC-13 updated, Q3/OQ-EXBOT-014.) | P1 |
| TC_012 | Verify the User Lock (Redis Redlock via ElastiCache) returns a cached result for the same idempotency key within the same state version | 1. Worker already processed hedge-sync for `{botId, stateVersion}` and released lock.\n2. Same `idempotencyKey='hedge-sync:{botId}:{stateVersion}'` as prior run. | 1. Second worker attempts to acquire the lock with the same `idempotencyKey`. | 1. Redlock returns cached result from previous run without re-running mutation; second worker does not re-submit HL order. (Ref: FR-EXBOT-026, FR-EXBOT-092.) | P1 |
| TC_060 | Verify rebalance_attempts.reason equals the original payload RebalanceReason for a stateVersion mismatch discard (A2 path) [NEW — AC-12b] | 1. Bot running.\n2. Incoming `hedge-sync` message has `stateVersion` lower than current `bot_runtime_state.state_version`.\n3. Message payload `reason` field contains a specific `RebalanceReason[]` value (Example: `['lp_drift']`). | 1. Deliver the stale `hedge-sync` message.\n2. Read the newly created `rebalance_attempts` row for this attempt. | 1. `rebalance_attempts.status='skipped'`; `rebalance_attempts.reason` equals the original `RebalanceReason[]` from the message payload — not `'stale_message'`, not `null`, not any synthetic value. Row written before worker exits. (Ref: FR-EXBOT-023, AC-12b, F1-18.) | P1 |

---

## II. Operation: Hedge-sync delta computation and HL order

### II.1. Functional verification — Operation: Hedge-sync delta computation and HL order

| TC ID | Title | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|
| TC_013 | Verify hedge-sync only sends the difference when the short position needs to increase | 1. Bot running with lock acquired.\n2. Target short size is larger than actual short size (positive delta, at or above drift threshold). | 1. Process `hedge-sync` message with a positive computed delta. | 1. Only one `adjustShortDelta(delta, cloid)` call with the positive difference — bot does NOT close full position and reopen. Reconcile confirms new actual size matches target. `rebalance_attempts.status='success'` written only after reconcile confirms. (Ref: FR-EXBOT-022, FR-EXBOT-020, FR-EXBOT-021, BR-EXBOT-004, AC-01.) | P0 |
| TC_014 | Verify hedge-sync sends a reduce-only adjustment when the short position needs to decrease | 1. Bot running with lock acquired.\n2. Target short size is smaller than actual (negative delta, reduce-only). | 1. Process `hedge-sync` message with a negative computed delta. | 1. `adjustShortDelta` called with negative delta (reduce-only); order reduces short by exact difference. Reconcile confirms new size. `rebalance_attempts.status='success'` recorded. (Ref: FR-EXBOT-022, FR-EXBOT-025.) | P0 |
| TC_015 | Verify no HL order is placed when the computed delta is zero (no_op_dust) — Worker returns immediately with no downstream effects [UPDATED — RD-05-01; OQ-EXBOT-013 Closed; AC-12] | 1. Bot running with lock acquired.\n2. Target short size equals actual short size; BigDecimal delta computation yields abs(delta) < 0.000001 ETH (no_op_dust boundary). | 1. Process `hedge-sync` message when abs(delta) < 0.000001. | 1. Worker returns `{status: 'no_op_dust'}` immediately after dust check at `rebalance.ts` line 83–84. No `adjustShortDelta` call made to Hyperliquid. No reconcile enqueued. No INV-STOP stop-replacement executed. `entry_price`, `liq_price`, `stop_trigger_px` in `hedge_legs` unchanged. `queue_idempotency.state='succeeded'`. `rebalance_attempts` row written with `reason` equal to the original `RebalanceReason[]` from the message payload. (Ref: FR-EXBOT-022, FR-EXBOT-023, AC-12, OQ-EXBOT-013 Closed, UC §A6.) | P1 |
| TC_069 | Verify delta=0 (no_op_dust) — Worker returns immediately with no HL call, no reconcile enqueued, no stop replacement, and all DB fields unchanged [NEW — RD-05-01; AC-12; OQ-EXBOT-013 Closed; TS_006] | 1. Bot running with lock acquired.\n2. `targetShortEth` equals `actualShortEth` exactly; BigDecimal delta = 0 (abs < 0.000001). | 1. Process `hedge-sync` message with delta = 0.\n2. Read `hedge_legs` for `entry_price`, `liq_price`, `stop_trigger_px` immediately after invocation.\n3. Check `partial_repair` SQS queue for any new messages. | 1. No `adjustShortDelta` call made to HL. `partial_repair` queue has no new message. INV-STOP protocol (place/verifyStopPlaced/cancel) not executed. `hedge_legs.entry_price`, `liq_price`, `stop_trigger_px` unchanged from pre-invocation values. `queue_idempotency.state='succeeded'`. Worker exits with `{status: 'no_op_dust'}`. (Ref: AC-12, OQ-EXBOT-013 Closed, UC §A6, rebalance.ts line 83–84.) | P0 |
| TC_070 | BVA: delta exactly 0.000001 ETH (at no_op_dust boundary) — Worker proceeds to adjustShortDelta, not short-circuit [NEW — RD-05-01; AC-12; TS_007] | 1. Bot running with lock acquired.\n2. `targetShortEth - actualShortEth` = +0.000001 ETH exactly (positive delta at the exclusive lower bound). | 1. Process `hedge-sync` message where abs(delta) = 0.000001 exactly. | 1. Worker does NOT short-circuit as no_op_dust; `adjustShortDelta(0.000001, cloid)` called on Hyperliquid. Reconcile proceeds normally. `rebalance_attempts.status='success'` written after reconcile confirms. This confirms the threshold is exclusive: abs < 0.000001 → no_op_dust; abs = 0.000001 → proceed. (Ref: AC-12, OQ-EXBOT-013 Closed, rebalance.ts line 83–84.) | P0 |
| TC_071 | BVA: delta 0.0000009 ETH (just below no_op_dust boundary) — Worker returns no_op_dust, does NOT submit HL order [NEW — RD-05-01; AC-12; TS_008] | 1. Bot running with lock acquired.\n2. abs(`targetShortEth - actualShortEth`) = 0.0000009 ETH (strictly less than 0.000001). | 1. Process `hedge-sync` message where abs(delta) = 0.0000009 ETH. | 1. Worker returns `{status: 'no_op_dust'}` — no `adjustShortDelta` call to HL. No INV-STOP. No reconcile. `hedge_legs` fields unchanged. This confirms "just below" boundary is treated as no_op_dust. (Ref: AC-12, OQ-EXBOT-013 Closed, rebalance.ts line 83–84.) | P0 |
| TC_016 | Verify the target ratio normalizer converts a decimal string to the correct BigInt basis points | 1. Hedge ratio stored as decimal string (e.g., `"0.70"`) in bot config. | 1. Call `normalizeTargetRatioBps("0.70")` during delta computation. | 1. Returns `7000n` (BigInt), not `7000` (Number) or `6999.9999…` from float rounding. No `Number()` intermediate used. (Ref: FR-EXBOT-021, NFR-EXBOT-008.) | P0 |
| TC_017 | Verify the delta calculation uses BigDecimal throughout with no float arithmetic | 1. Bot running with `targetShortEth` and `actualShortEth` stored as TEXT BigDecimal strings in Aurora PostgreSQL. | 1. Compute `delta = targetShortEth - actualShortEth` using the delta computation function. | 1. Result is a BigDecimal value with full precision. No `Number()` cast or JS float arithmetic at any step. (Ref: NFR-EXBOT-008, FR-EXBOT-021.) | P0 |
| TC_018 | Verify the order id for the same attempt is always the same regardless of how many times it is computed | 1. Specific set of `{botId, attemptId, stage, version}` inputs known. | 1. Compute `cloid` for the same `{botId, attemptId, stage, version}` twice in separate calls. | 1. Both calls return the same `cloid` value (`first128BitsHex(keccak256('bnza:{botId}:{attemptId}:{stage}:{version}'))`). Changing any input field produces a different `cloid`. (Ref: FR-EXBOT-024.) | P0 |
| TC_019 | Verify an HL order rejection increments the circuit breaker failure count — partial fill does NOT call incrementCircuitBreaker | 1. Bot running with lock acquired, positive delta computed.\n2. Hyperliquid (mocked) rejects the `adjustShortDelta` call with `status='failed'` (e.g., insufficient margin). | 1. Process `hedge-sync` when HL rejects the order with a full failure (not a partial fill). | 1. `rebalance_attempts.status='failed'` recorded. `incrementCircuitBreaker` called exactly once. Error `E-EXBOT-007` returned. Lock released in `finally` block. Note: partial fill (`status='partial'`) does NOT call `incrementCircuitBreaker` — routes to `partial_repair` queue instead (see TC_037, TC_059). (Ref: FR-EXBOT-040, E-EXBOT-007, AC-04.) | P0 |
| TC_020 | Verify the delta is computed and the order is placed correctly at the exact drift threshold boundary | 1. Bot running with lock acquired.\n2. Difference between target and actual short size is exactly at the drift threshold. | 1. Process `hedge-sync` when delta is at exact drift threshold. | 1. Order placed for the exact threshold amount (not rounded down, not skipped). Reconcile confirms new actual size matches expected. (Ref: FR-EXBOT-022, FR-EXBOT-025.) | P1 |


### II.2. Integration & State verification — Operation: Hedge-sync delta computation and HL order

| TC ID | Title | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|
| TC_021 | Verify the worker checks with the HL rate limiter before calling Hyperliquid, and waits when told to | 1. Bot running with lock acquired. | 1. Process `hedge-sync` when ElastiCache Redis rate limiter (HLRateLimit) returns `{allowed: true}` (normal case).\n2. In separate test: process when ElastiCache Redis rate limiter (HLRateLimit) returns `{allowed: false, retryAfterMs: <delay>}`. | 1. Step 1: worker calls Hyperliquid normally.\n2. Step 2: worker does NOT call Hyperliquid; re-queues with specified delay and exits. (Ref: FR-EXBOT-091, NFR-EXBOT-004.) | P0 |
| TC_022 | Verify the bot enters safe mode when Hyperliquid is unreachable for longer than the grace period | 1. Bot running with lock acquired.\n2. Hyperliquid (mocked) returns connection errors for all calls. | 1. Process `hedge-sync` when HL has been unreachable past the grace period. | 1. Bot enters safe mode (`lifecycle_state='safe_mode'`, `bots.status='safe_mode'`). Error `E-EXBOT-008` logged. No HL order placed. (Grace period approx. 5 minutes.) (Ref: FR-EXBOT-050, E-EXBOT-008.) | P0 |
| TC_023 | Verify the bot enters safe mode when the signing service fails to sign the HL order | 1. Bot running with lock acquired, delta ready to send.\n2. KMS Signing Lambda set to fail during signing step. | 1. Process `hedge-sync` when signing service returns error. | 1. No HL order submitted. Bot enters safe mode (`lifecycle_state='safe_mode'`). Admin notified. No raw key material in any log. (Ref: FR-EXBOT-080.) | P0 |
| TC_024 | Verify a duplicate cloid is detected and reconciled before any retry, not blindly resubmitted | 1. A `hedge-sync` attempt already sent an order with specific `cloid`; outcome unknown (timeout or partial response). | 1. Retry the same `hedge-sync` attempt (same `{botId, attemptId, stage, version}`), producing the same `cloid`. | 1. Worker detects duplicate `cloid`, fetches actual HL position to reconcile, proceeds based on confirmed actual state — does NOT blindly submit a second order with same `cloid`. (Ref: FR-EXBOT-024.) | P0 |
| TC_061 | Verify rebalance_attempts.reason equals the original payload RebalanceReason for a delta=0 no_op_dust return [UPDATED — RD-05-01; AC-12] | 1. Bot running with lock acquired.\n2. Delta computes to abs < 0.000001 (no_op_dust).\n3. Message payload `reason` field contains a specific `RebalanceReason[]` value (Example: `['lp_drift']`). | 1. Process `hedge-sync` message when delta is no_op_dust.\n2. Read the `rebalance_attempts` row written for this attempt. | 1. No `adjustShortDelta` call made. No INV-STOP executed. `rebalance_attempts.reason` equals the original `RebalanceReason[]` from the message payload — NOT `'delta_zero'`, NOT `null`. `queue_idempotency.state='succeeded'`. (Ref: FR-EXBOT-022, FR-EXBOT-023, AC-12, F1-18.) | P1 |

### II.3. Non-functional (logic) verification — Operation: Hedge-sync delta computation and HL order

| TC ID | Title | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|
| TC_025 | Verify the stop trigger price is computed with full BigDecimal precision at large decimal values | 1. `entry_price`, `liq_price`, and `stopSafetyFactor` available as BigDecimal strings. | 1. Compute `stop_trigger_px = entry_price x (1 + liq_distance_pct x stopSafetyFactor)` using the stop-price function. | 1. Result has full decimal precision — no intermediate `Number()` cast, no rounding loss. Formula output matches expected BigDecimal value. (Ref: NFR-EXBOT-008, FR-EXBOT-030.) | P0 |
| TC_026 | Verify hedge-sync does not open more than six outbound connections in a single worker invocation | 1. Bot running; full hedge-sync cycle about to execute (HL fetch, order, reconcile, ElastiCache calls). | 1. Monitor outbound connection count during one full hedge-sync invocation. | 1. Total simultaneous outbound connections from worker is at most 6. (Ref: NFR-EXBOT-011.) | P2 |

---

## III. Operation: INV-STOP protocol (stop replacement)

### III.1. Functional verification — Operation: INV-STOP protocol (stop replacement)

| TC ID | Title | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|
| TC_027 | Verify stop_replacing_started_at is set in Aurora PostgreSQL before the critical stop-replacement section begins | 1. Bot running; hedge adjustment completed (delta ≠ 0, adjustShortDelta succeeded and reconcile confirmed).\n2. `hedge_legs.stop_replacing_started_at` is NULL. | 1. Begin the INV-STOP protocol to replace the stop on Hyperliquid. | 1. Before any cancel or place call to HL, `hedge_legs.stop_replacing_started_at` is written to the current timestamp in Aurora PostgreSQL. Field is not NULL during the critical section. (Ref: FR-EXBOT-035.) | P0 |
| TC_028 | Verify stop_replacing_started_at is cleared after the stop replacement succeeds | 1. INV-STOP protocol in progress; `stop_replacing_started_at` is set.\n2. Both verifyStopPlaced and cancel HL calls succeed. | 1. Complete the INV-STOP stop-replacement sequence successfully. | 1. After stop is placed, verifyStopPlaced confirms, and old stop cancelled, `hedge_legs.stop_replacing_started_at` set to NULL in `finally` block. Field must be NULL regardless of whether hedge-sync overall succeeds. (Ref: FR-EXBOT-035.) | P0 |
| TC_029 | Verify stop_replacing_started_at is cleared even when stop replacement fails midway | 1. INV-STOP protocol in progress; `stop_replacing_started_at` is set.\n2. HL place or verifyStopPlaced call fails (mocked to return error). | 1. Let INV-STOP stop-replacement sequence fail at the HL call step. | 1. Even though stop-replacement failed, `hedge_legs.stop_replacing_started_at` cleared to NULL in `finally` block in Aurora PostgreSQL. No half-completed state left. (Ref: FR-EXBOT-035.) | P0 |
| TC_030 | Verify the new stop is placed with the correct trigger price and as reduce-only | 1. INV-STOP protocol: new stop is ready to be placed (place step, before verifyStopPlaced).\n2. New `stop_trigger_px` computed from latest `entry_price`, `liq_price`, and `stopSafetyFactor`. | 1. Execute `placeReduceOnlyStopMarket` call as part of INV-STOP. | 1. New stop placed on HL as reduce-only stop-market order with BigDecimal `stop_trigger_px`. New `stop_cloid` and `stop_price` written to `hedge_legs` after verifyStopPlaced confirms. (Ref: FR-EXBOT-035, FR-EXBOT-030.) | P0 |
| TC_031 | Verify the bot enters safe mode when stop_replacing_started_at is stuck for too long | 1. Previous hedge-sync run set `hedge_legs.stop_replacing_started_at` but crashed before clearing it.\n2. Field has remained set longer than the stuck threshold. | 1. Light-check worker runs while `stop_replacing_started_at` is stuck beyond the threshold. | 1. Light-check detects stuck flag and triggers bot to enter safe mode (`lifecycle_state='safe_mode'`). (Threshold approx. 60 seconds.) (Ref: FR-EXBOT-033, FR-EXBOT-035.) | P0 |
| TC_072 | Verify INV-STOP happy path: place → verifyStopPlaced → cancel; no 0-stop window at any point in the sequence [NEW — RD-05-02; AC-10; OQ-EXBOT-002 Closed; TS_012] | 1. Bot running; delta adjustment completed (delta ≠ 0); reconcile confirmed new position.\n2. Old stop exists on HL (`hedge_legs.stop_cloid` set).\n3. INV-STOP protocol about to begin; `stop_replacing_started_at` set. | 1. Observe HL call sequence during stop replacement.\n2. Record timestamps of: placeReduceOnlyStopMarket, verifyStopPlaced response, cancelOrder. | 1. HL call order is strictly: (1) `placeReduceOnlyStopMarket` → (2) `verifyStopPlaced` confirms new stop is live on HL → (3) `cancelOrder` removes old stop. At no point in the sequence are both old and new stops absent simultaneously (no 0-stop window). New `stop_cloid` written to `hedge_legs` after step 1. Old stop cancelled only after step 2 confirms. `stop_replacing_started_at` cleared to NULL in `finally` after step 3. (Ref: FR-EXBOT-035, AC-10, OQ-EXBOT-002 Closed.) | P0 |
| TC_073 | Verify INV-STOP place failure: new stop cannot be placed on HL — old stop remains active, partial_repair enqueued, Worker does NOT enter SAFE_MODE [NEW — RD-05-02; AC-11; OQ-EXBOT-002 Closed; TS_013] | 1. Bot running; delta adjustment completed; reconcile confirmed.\n2. Old stop active on HL.\n3. `placeReduceOnlyStopMarket` call to HL fails (mocked error — e.g., rate limit or HL rejection). | 1. Process hedge-sync through to INV-STOP place step.\n2. Let `placeReduceOnlyStopMarket` fail.\n3. Read HL active orders and `partial_repair` SQS queue after invocation. | 1. Old stop remains active on HL — `cancelOrder` is NOT called (cancel-before-place is forbidden; INV-STOP is place-before-cancel). `partial_repair` message enqueued with `{botId, attemptId, reason='stop_place_failed'}`. `rebalance_attempts.status='partial'` recorded. Bot does NOT enter `safe_mode` for this failure alone. `stop_replacing_started_at` cleared to NULL in `finally`. (Ref: FR-EXBOT-035, AC-11, OQ-EXBOT-002 Closed.) | P0 |

### III.2. Integration & State verification — Operation: INV-STOP protocol (stop replacement)

| TC ID | Title | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|
| TC_032 | Verify that stop replacement always uses the INV-STOP place-before-cancel protocol and never direct cancel-then-place [UPDATED — RD-05-02; OQ-EXBOT-002 Closed; AC-10] | 1. Hedge-sync run needs to replace stop after delta adjustment (delta ≠ 0). | 1. Observe exact sequence of HL calls made during stop-replacement step. | 1. Stop replacement follows confirmed INV-STOP sequence (OQ-EXBOT-002 Closed; AC-10; FR-EXBOT-035): (1) Place new stop → (2) `verifyStopPlaced` confirms new stop is live → (3) Cancel old stop ONLY after step 2 succeeds. Direct `cancelOrder` followed immediately by `placeStop` outside this protected protocol is forbidden. `stop_replacing_started_at` set before any HL call and cleared to NULL in `finally` after completion. (Ref: FR-EXBOT-035, AC-10, OQ-EXBOT-002 Closed.) | P0 |
| TC_033 | Verify the new stop trigger price is recomputed from the latest entry and liquidation prices after reconcile | 1. Reconcile step has returned latest `entry_price`, `liq_price`, and `effective_leverage` from HL. | 1. Compute new `stop_trigger_px` as part of INV-STOP stop-placement step. | 1. `stop_trigger_px = entry_price x (1 + liq_distance_pct x stopSafetyFactor)` computed with BigDecimal, where `liq_distance_pct = (liq_price - entry_price) / entry_price` using HL-provided `liq_price` (fallback: `1/effective_leverage` only when `liq_price` absent). No float intermediate. (Ref: FR-EXBOT-030, NFR-EXBOT-008.) | P0 |

---

## IV. Operation: Reconcile and Aurora PostgreSQL update

### IV.1. Functional verification — Operation: Reconcile and Aurora PostgreSQL update

| TC ID | Title | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|
| TC_034 | Verify that a successful reconcile updates all hedge leg fields and marks the attempt as succeeded | 1. Hedge-sync delta order submitted; reconcile worker fetched actual HL position.\n2. Actual short size matches expected size within tolerance. | 1. Let reconcile worker run and confirm position matches expected size. | 1. `hedge_legs` updated with new `entry_price`, `liq_price`, `effective_leverage`, `stop_price`, `stop_cloid`. `bot_runtime_state.last_known_hl_short_size` set to reconciled size. `rebalance_attempts` row inserted with `status='success'`. `queue_idempotency.state='succeeded'`. (Ref: FR-EXBOT-025, AC-01.) | P0 |
| TC_035 | Verify the attempt is only marked successful after reconcile confirms the position, not before | 1. Hedge-sync delta order has been placed.\n2. Reconcile step has not yet run. | 1. Check `rebalance_attempts` state immediately after order is placed (before reconcile confirms). | 1. No `rebalance_attempts` row with `status='success'` exists before reconcile confirms. Success row written only after actual HL position verified. (Ref: FR-EXBOT-025.) | P0 |
| TC_036 | Verify the bot's last known HL short size is updated to the value confirmed by reconcile | 1. Reconcile completed successfully and confirmed a new short size. | 1. Read `bot_runtime_state.last_known_hl_short_size` after successful reconcile. | 1. Value equals reconciled actual short size from HL `clearinghouseState` response — not the expected size, not a locally computed value. (Ref: FR-EXBOT-025.) | P0 |
| TC_037 | Verify a partial fill detection causes a repair message to be queued, the attempt marked partial, and the circuit breaker failure count unchanged | 1. Reconcile worker fetched actual HL position.\n2. Actual short size differs from expected by more than drift threshold (partial fill — HL returned `status='partial'`). | 1. Let reconcile worker detect a partial fill mismatch. | 1. `partial_repair` message enqueued with `{botId, attemptId, reason='partial_fill'}`. `rebalance_attempts.status='partial'` recorded. No `success` row written. `circuit_breakers.failure_count` is NOT incremented — partial fill is not treated as a circuit failure. Only `status='failed'` (HL order rejection) increments `failure_count`. (Ref: FR-EXBOT-036, FR-EXBOT-040, AC-08b.) | P0 |
| TC_038 | Verify three consecutive partial repair failures trigger a bot safe close | 1. `partial_repair` queue has processed and failed for the same bot three consecutive times. | 1. Let the third partial repair attempt fail. | 1. `close_operations` row created with `trigger_reason='partial_repair_exhausted'`; UNIQUE constraint prevents double-creation. Bot transitions to close flow (`lifecycle_state='lp_closing'` or similar). (Ref: FR-EXBOT-036, FR-EXBOT-072.) | P0 |
| TC_039 | Verify the reconcile handles a zero actual position without crashing or writing incorrect state | 1. Reconcile worker fetched actual HL position and found it is zero (unexpected). | 1. Run reconcile step when HL position returns size=0. | 1. Mismatch between expected non-zero size and zero actual size detected. `partial_repair` message enqueued or bot enters safe recovery path. `rebalance_attempts.status` set to non-success value. No success row written. (Ref: FR-EXBOT-025, FR-EXBOT-036.) | P1 |

### IV.2. Integration & State verification — Operation: Reconcile and Aurora PostgreSQL update

| TC ID | Title | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|
| TC_040 | Verify all hedge leg fields are written atomically in a single Aurora PostgreSQL operation after reconcile | 1. Successful reconcile has returned new position data from HL. | 1. Observe Aurora PostgreSQL write for `hedge_legs` after successful reconcile. | 1. All fields (`entry_price`, `liq_price`, `effective_leverage`, `stop_price`, `stop_cloid`) written in a single Aurora PostgreSQL update — not separate individual writes. No partial update state visible. (Ref: FR-EXBOT-025, ERD.) | P0 |
| TC_041 | Verify queue_idempotency state is updated to succeeded only after the full pipeline finishes | 1. Hedge-sync message fully processed through pipeline (preflight, order, reconcile, Aurora PostgreSQL update). | 1. Check `queue_idempotency.state` at the very end of the pipeline. | 1. `queue_idempotency.state='succeeded'` written only after all Aurora PostgreSQL updates (`hedge_legs`, `bot_runtime_state`, `rebalance_attempts`) completed. Not set prematurely. (Ref: FR-EXBOT-011, AC-16.) | P0 |
| TC_042 | Verify the bot state version is incremented after a successful rebalance, causing any older messages to be discarded | 1. Hedge-sync completed successfully; `bot_runtime_state.state_version` incremented.\n2. Older `hedge-sync` message with previous `stateVersion` still in queue. | 1. Deliver the older message after the state version has incremented. | 1. Older message discarded (stateVersion mismatch), no HL order placed. Aurora PostgreSQL state version reflects only the latest completed rebalance. (Ref: FR-EXBOT-027.) | P0 |
| TC_064 | Verify rebalance_attempts.reason equals the original payload RebalanceReason for a happy-path success | 1. Bot running; full hedge-sync pipeline completes successfully (delta computed, HL order accepted, reconcile confirms).\n2. Message payload `reason` field contains a specific `RebalanceReason[]` value (Example: `['lp_drift']`). | 1. Process `hedge-sync` message through the full happy path.\n2. Read the `rebalance_attempts` row written for this attempt after reconcile confirms. | 1. `rebalance_attempts.status='success'`. `rebalance_attempts.reason` equals the original `RebalanceReason[]` from the message payload — not `null`, not a synthetic value. (Ref: FR-EXBOT-023, AC-01, AC-12b, F1-18.) | P1 |


---

## V. Operation: Circuit breaker management

### V.1. Functional verification — Operation: Circuit breaker management

| TC ID | Title | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|
| TC_043 | Verify the circuit breaker opens after three consecutive HL order failures — partial fill does not count toward failure_count | 1. Circuit breaker closed (`circuit_breakers.state='closed'`).\n2. Two previous `status='failed'` (HL rejection) failures already recorded within last 24 hours (`failure_count=2`).\n3. No partial fills in the previous two failures — each was a full HL rejection. | 1. Process a `hedge-sync` message that fails at HL order step with `status='failed'` (third HL rejection in 24h window). | 1. `circuit_breakers.state` changes from `closed` to `open`, `reset_at = now + 1h`. Light-check no longer enqueues `hedge-sync` for this bot. Stop monitoring continues. Note: partial fills (`status='partial'`) do NOT increment `failure_count` and do NOT contribute to opening the circuit. (Ref: FR-EXBOT-040, AC-05.) | P0 |
| TC_044 | Verify the circuit does not open when failures are not consecutive within the rolling 24h window | 1. Circuit breaker closed.\n2. Two previous failures in 24h window, but a success occurred between them (resetting consecutive count). | 1. Process a `hedge-sync` message that fails. | 1. `circuit_breakers.state` stays `closed`; `failure_count` increments but does not reach open threshold because intervening success broke the consecutive run. (Ref: FR-EXBOT-040, AC-05.) | P1 |
| TC_045 | Verify stop monitoring continues when the circuit is open but hedge-sync is suppressed | 1. Circuit breaker open (`circuit_breakers.state='open'`).\n2. Bot's mark price is at or above stop price (stop-crossing condition). | 1. Run light-check for this bot while circuit is open. | 1. Light-check does NOT enqueue `hedge-sync` message (suppressed by open circuit). Light-check DOES enqueue `price-near-stop-audit` because stop monitoring is never suppressed. (Ref: FR-EXBOT-012, FR-EXBOT-014.) | P0 |
| TC_046 | Verify the circuit transitions to half_open when the reset timer expires — allowHedgeSync called atomically | 1. Circuit breaker open (`circuit_breakers.state='open'`).\n2. `reset_at` time reached.\n3. `half_open_probe_used=0`. | 1. Run light-check after `reset_at` has passed. | 1. Light-check calls `allowHedgeSync` atomically to claim the half_open probe slot (`half_open_probe_used` transitions 0 to 1). Exactly one `hedge-sync` probe message enqueued; circuit transitions to `half_open`. No more than one probe allowed. `allowHedgeSync` returns `true` only once per half_open window. (Ref: FR-EXBOT-040, AC-06, AC-09, F1-25.) | P0 |
| TC_047 | Verify a successful half_open probe closes the circuit and resets the failure count | 1. Circuit breaker in `half_open` state.\n2. One probe `hedge-sync` message in flight. | 1. Process probe hedge-sync message and let it succeed (HL order succeeds, reconcile confirms). | 1. `circuit_breakers.state` changes from `half_open` to `closed`, `failure_count=0`. Normal hedge-sync resumes at next light-check cycle. (Ref: FR-EXBOT-040, AC-07.) | P0 |
| TC_048 | Verify a failed half_open probe reopens the circuit with a new reset timer | 1. Circuit breaker in `half_open` state.\n2. One probe `hedge-sync` message in flight. | 1. Process probe hedge-sync message and let it fail (HL order fails). | 1. `circuit_breakers.state` changes from `half_open` back to `open`, `reset_at` extended by 1 hour. Admin notification sent. (Ref: FR-EXBOT-040, AC-08.) | P0 |
| TC_059 | Verify a partial fill does NOT increment the circuit breaker failure_count | 1. Circuit breaker closed (`circuit_breakers.state='closed'`, `failure_count=1`).\n2. HL returns a partial fill for a `hedge-sync` order (`status='partial'`). | 1. Process `hedge-sync` when HL executes only part of the order (partial fill).\n2. Read `circuit_breakers.failure_count` after the attempt. | 1. `rebalance_attempts.status='partial'`. `circuit_breakers.failure_count` remains unchanged (still 1). `partial_repair` message enqueued. `circuit_breakers.state` remains `closed`. `incrementCircuitBreaker` is NOT called for partial fill. (Ref: FR-EXBOT-040, FR-EXBOT-036, AC-04b, AC-08b.) | P0 |
| TC_062 | Verify allowHedgeSync returns true only once when the circuit transitions to half_open and rejects subsequent calls | 1. Circuit breaker open (`circuit_breakers.state='open'`), `reset_at` reached.\n2. `half_open_probe_used=0`. | 1. Call `allowHedgeSync` for this bot (first call).\n2. Call `allowHedgeSync` again for the same bot without any state change (second concurrent call). | 1. Step 1: `allowHedgeSync` returns `true`; `half_open_probe_used` atomically transitions 0 to 1; circuit state transitions to `half_open`.\n2. Step 2: `allowHedgeSync` returns `false`; `half_open_probe_used` remains 1. (Ref: FR-EXBOT-040, AC-09, F1-25.) | P0 |

### V.2. Integration & State verification — Operation: Circuit breaker management

| TC ID | Title | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|
| TC_049 | Verify a second probe attempt while in half_open is blocked atomically | 1. Circuit breaker in `half_open` state; one probe already claimed (`half_open_probe_used=1`). | 1. Attempt to enqueue a second `hedge-sync` probe for the same bot. | 1. Second probe blocked because `half_open_probe_used` is already 1. Atomic 0 to 1 claim ensures exactly one probe. (Ref: FR-EXBOT-040.) | P0 |
| TC_050 | Verify the circuit breaker state change is visible to light-check at the next tick without cache delay | 1. Hedge-sync failure just opened circuit breaker (`state='open'`). | 1. Run the next light-check tick for the same bot immediately after circuit opens. | 1. Light-check reads updated `circuit_breakers.state='open'` from Aurora PostgreSQL and does not enqueue new `hedge-sync`. State change not served from stale cache. (Ref: FR-EXBOT-012, FR-EXBOT-040.) | P0 |
| TC_051 | Verify the failure count is not double-incremented when the same failed message is redelivered | 1. A `hedge-sync` message processed and failed, incrementing `failure_count` once. | 1. Redeliver the same failed `hedge-sync` message (same `message_id`). | 1. Redelivery caught by idempotency check (`queue_idempotency` UNIQUE conflict) before `incrementCircuitBreaker` called; `failure_count` not incremented a second time. (Ref: FR-EXBOT-011, FR-EXBOT-040.) | P0 |
| TC_063 | Verify two simultaneous light-check invocations during half_open result in exactly one probe being enqueued | 1. Circuit breaker in `open` state; `reset_at` has just passed.\n2. `half_open_probe_used=0`.\n3. Two light-check workers start simultaneously for the same bot. | 1. Both light-check workers concurrently call `allowHedgeSync`. | 1. Exactly one gets `allowHedgeSync` returning `true` and enqueues one `hedge-sync` probe. The other gets `false`. Total probes in queue: exactly 1. `half_open_probe_used` transitions from 0 to 1 exactly once. (Ref: FR-EXBOT-040, AC-09.) | P0 |

---

## VI. Operation: SAFE_MODE entry and recovery from hedge-sync

### VI.1. Functional verification — Operation: SAFE_MODE entry and recovery from hedge-sync

| TC ID | Title | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|
| TC_052 | Verify the bot enters safe mode when Hyperliquid remains unreachable past the grace period | 1. Bot running (`lifecycle_state='active'`).\n2. Hyperliquid unreachable (all HL API calls fail) for longer than grace period. | 1. Attempt hedge-sync while HL remains unreachable past the grace period. | 1. `lifecycle_state` changes to `safe_mode`, `bots.status='safe_mode'`. Error `E-EXBOT-008` recorded. Lock released in `finally`. (Grace period approx. 5 minutes.) (Ref: FR-EXBOT-050, E-EXBOT-008.) | P0 |
| TC_053 | Verify the bot enters safe mode when the reconcile detects a size mismatch it cannot explain | 1. Bot running.\n2. Hedge-sync order submitted; reconcile finds actual HL position does not match expected by more than allowed tolerance. | 1. Let reconcile worker detect an unexplained size mismatch after an order. | 1. `lifecycle_state` changes to `safe_mode`. Error `E-EXBOT-011` recorded. Mismatch routed to repair or admin alert path. (Ref: FR-EXBOT-050, E-EXBOT-011.) | P0 |
| TC_054 | Verify the bot enters safe mode after two consecutive critical margin readings — critical margin derived from marginSummary fetched AFTER lock [UPDATED — Reason: RD-v4-01 Q3] | 1. Bot running.\n2. First hedge-sync run: worker acquired lock, fetched marginSummary AFTER lock, `marginUsage >= 0.75` — `margin_status` updated to `critical`; `consecutive_critical_count` now 1.\n3. Bot remains active after first critical reading (single critical does not trigger SAFE_MODE). | 1. Run second hedge-sync preflight: worker acquires lock, fetches marginSummary from HL AFTER acquiring lock, marginSummary again yields `marginUsage >= 0.75`.\n2. Observe SAFE_MODE trigger. | 1. After second consecutive `critical` margin reading (both derived from marginSummary fetched after lock acquisition), bot enters safe mode (`lifecycle_state='safe_mode'`). Investor and admin alerts sent. Note: both critical readings must be independently fetched after lock — a stale marginSummary from before lock does not count. (Ref: FR-EXBOT-050, FR-EXBOT-060, FR-EXBOT-092, AC-13 updated, Q3/OQ-EXBOT-014.) | P0 |
| TC_055 | Verify all mutations are blocked when the bot is in safe mode | 1. Bot in safe mode (`lifecycle_state='safe_mode'`). | 1. Attempt each while bot is in safe mode: (a) place hedge order, (b) cancel stop, (c) rebalance hedge, (d) increase leverage. | 1. All mutation attempts (a)-(d) rejected with no side effect. Read operations and retry connectivity checks are allowed. (Ref: FR-EXBOT-050, BR-EXBOT-007.) | P0 |

### VI.2. Integration & State verification — Operation: SAFE_MODE entry and recovery from hedge-sync

| TC ID | Title | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|
| TC_056 | Verify the bot exits safe mode automatically when recovery conditions are met | 1. Bot in safe mode.\n2. HL becomes reachable again.\n3. Three consecutive reconcile attempts succeed with `margin_status` OK. | 1. Let auto-recovery check run three times, all successful. | 1. After third consecutive successful reconcile with OK margin, bot transitions from `safe_mode` to `active` (`lifecycle_state='active'`). (Ref: FR-EXBOT-050, BR-EXBOT-007.) | P0 |
| TC_057 | Verify safe mode is never a dead end — it always leads to recovery or safe close | 1. Bot in safe mode. | 1. Verify the possible paths out of `safe_mode` as documented. | 1. Safe mode must exit via: (a) auto-recovery to `active` when HL back and 3 consecutive reconciles succeed with OK margin, or (b) `bot_safe_close` when recovery impossible. No path leaves bot in `safe_mode` permanently. (Ref: BR-EXBOT-007, FR-EXBOT-050.) | P0 |

---

## VII. New test cases — v5 additions

### VII.1. Functional verification — marginSummary ordering post-lock (AC-13 updated)

| TC ID | Title | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|
| TC_065 | Verify marginSummary is fetched AFTER Redlock acquire and margin_status is updated before the hedge mutation executes [NEW — AC-13 updated, Q3] | 1. Bot running; stateVersion check passed; circuit recheck passed.\n2. Worker is about to call `User Lock (Redis Redlock via ElastiCache).acquire()`.\n3. HL marginSummary API is responsive (normal latency). | 1. Observe the full step-4 execution sequence: acquire lock → fetch HL `clearinghouseState` and `marginSummary` → update `margin_status` → execute hedge mutation → release lock.\n2. Confirm that `marginSummary` fetch occurs AFTER lock acquired, not before. | 1. Lock acquired (step 3) before any HL API call that reads marginSummary. `margin_status` in `hedge_legs` updated to reflect the freshly fetched marginSummary value before `adjustShortDelta` is called. If marginUsage in marginSummary response is within `ok` band (< 0.55), hedge mutation proceeds. No HL mutation called before `margin_status` is written. Lock released in `finally` block after mutation completes. (Ref: FR-EXBOT-060, FR-EXBOT-092, AC-13 updated, Q3/OQ-EXBOT-014, flows.md F-02.) | P0 |
| TC_066 | Verify extend() is called before TTL=90s expiry when HL marginSummary fetch is slow (>~80s) [NEW — AC-13 updated, Q3] | 1. Bot running; lock acquired (TTL=90s, `idempotencyKey='hedge-sync:{botId}:{stateVersion}'`).\n2. HL marginSummary API is slow — simulated response latency of 85s (approaching TTL=90s boundary).\n3. Worker has not yet received marginSummary response at t=80s. | 1. Start hedge-sync invocation with slow marginSummary fetch (85s).\n2. At t=~80s (before TTL=90s expires), observe whether `extend(holderToken, ttl=90s)` is called.\n3. After extend(), HL marginSummary returns at t=85s.\n4. Worker continues to update margin_status and execute mutation. | 1. `extend(holderToken, ttl=90s)` is called before t=90s to refresh the lock TTL; lock remains held. After marginSummary returns, `margin_status` updated and mutation proceeds normally. Lock NOT auto-released mid-invocation. Second worker cannot acquire lock during this period. Lock released in `finally` after full completion. Note: if extend() is NOT called and TTL expires at t=90s, the lock releases while the first worker is still in the critical section — a second worker could then acquire and cause concurrent mutation. This TC verifies the heartbeat mechanism prevents that race. (Ref: FR-EXBOT-092, AC-13 updated, Q3/OQ-EXBOT-014.) | P1 |

### VII.2. Functional verification — stateVersion ordering (N4-03/F-02)

| TC ID | Title | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|
| TC_067 | Verify stateVersion mismatch causes immediate discard at step 2a without executing the circuit breaker recheck DB read at step 2b [NEW — N4-03, flows.md F-02] | 1. Bot running; circuit breaker is `closed`.\n2. Incoming `hedge-sync` message has `stateVersion` lower than current `bot_runtime_state.state_version` (stale message).\n3. Circuit breaker DB read (`circuit_breakers` table) would be the next operation if step 2a were not a short-circuit. | 1. Deliver the stale `hedge-sync` message.\n2. Monitor Aurora PostgreSQL query log (or equivalent observability) for any SELECT against `circuit_breakers` table during this invocation. | 1. `rebalance_attempts.status='skipped'` recorded with staleVersion reason. NO SELECT query executed against `circuit_breakers` table — stateVersion mismatch short-circuits before step 2b. Lock NOT acquired. No HL call made. This verifies F-02 ordering: (a) stateVersion first → (b) circuit recheck second. A stale message never triggers circuit-breaker DB access. (Ref: FR-EXBOT-027, FR-EXBOT-040, N4-03, flows.md F-02.) | P1 |

### VII.3. Functional verification — F-01 light-check suppress behavior

| TC ID | Title | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|
| TC_068 | Verify light-check suppress (from F-01 step 11) applies to this tick only — next tick resumes normal hedge-sync evaluation [NEW — flows.md F-01 updated] | 1. Bot is active; light-check tick N triggers a condition that causes hedge-sync to be suppressed for this tick (e.g., `lifecycle_state='lp_rebalancing'`, rate limit, or other suppress condition per F-01 step 11).\n2. The suppress condition resolves before tick N+1 (e.g., rebalancing completes). | 1. Tick N: light-check runs; observe that no `hedge-sync` message is enqueued for this bot due to suppress condition.\n2. Wait for next light-check cycle (tick N+1); suppress condition has cleared.\n3. Tick N+1: light-check runs again for same bot. | 1. Tick N: no `hedge-sync` message enqueued; suppress is scoped to this tick only — no persistent flag set on the bot that would block future ticks.\n2. Tick N+1: suppress condition cleared; light-check evaluates hedge-sync need normally and enqueues `hedge-sync` if warranted. Bot is NOT left in a permanently suppressed state. Note: F-01 clarified "freeze" behavior as "suppress routine hedge-sync (this tick only)" — distinguishing it from circuit-open suppression which persists until reset_at. (Ref: flows.md F-01 step 11, FR-EXBOT-012.) | P1 |

---

## VIII. New test cases — v6 additions

### VIII.1. Functional verification — drift_threshold and lpValueUsd formula (RD-05-03; AC-15; OQ-EXBOT-011 Closed)

*For no_op_dust BVA TCs: see TC_069–TC_071 in Section II.1. For INV-STOP confirmed sequence TCs: see TC_072–TC_073 in Section III.1.*

| TC ID | Title | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|
| TC_074 | BVA: deltaErrorUsd exactly at drift_threshold = max($25, lpValueUsd x 3%) — hedge-sync enqueued when just above, NOT enqueued at or below threshold [NEW — RD-05-03; AC-15; OQ-EXBOT-011 Closed; TS_024] | 1. Light-check worker running for an active bot.\n2. `lpValueUsd` known (e.g., `lpValueUsd = $1000`); `drift_threshold = max($25, $1000 x 3%) = max($25, $30) = $30`.\n3. Scenario A: `deltaErrorUsd = $30.01` (just above threshold).\n4. Scenario B: `deltaErrorUsd = $30.00` (exactly at threshold).\n5. Scenario C: `deltaErrorUsd = $29.99` (just below threshold). | 1. Run light-check for each scenario A, B, C.\n2. Check SQS hedge-sync queue for new messages after each run. | 1. Scenario A ($30.01): `hedge-sync` message enqueued — drift exceeds threshold.\n2. Scenario B ($30.00): `hedge-sync` message NOT enqueued — at-threshold is not above.\n3. Scenario C ($29.99): `hedge-sync` message NOT enqueued — below threshold. Confirms drift trigger uses strict `>` (not `>=`) against `max($25, lpValueUsd x 3%)`. (Ref: FR-EXBOT-025, AC-15, OQ-EXBOT-011 Closed.) | P1 |
| TC_075 | Verify drift_threshold uses $25 floor when lpValueUsd x 3% < $25 — $25.01 enqueues hedge-sync; $24.99 does not [NEW — RD-05-03; AC-15; OQ-EXBOT-011 Closed; TS_025] | 1. Light-check worker running.\n2. `lpValueUsd` is small enough that `lpValueUsd x 3% < $25` (e.g., `lpValueUsd = $500`, `3% = $15`); `drift_threshold = max($25, $15) = $25`.\n3. Scenario A: `deltaErrorUsd = $25.01`.\n4. Scenario B: `deltaErrorUsd = $24.99`. | 1. Run light-check for each scenario A, B. | 1. Scenario A ($25.01): `hedge-sync` enqueued — drift exceeds $25 floor.\n2. Scenario B ($24.99): `hedge-sync` NOT enqueued — below $25 floor. Confirms `max($25, lpValueUsd x 3%)` with the $25 floor branch active when percentage result < $25. (Ref: FR-EXBOT-025, AC-15, OQ-EXBOT-011 Closed.) | P1 |
| TC_076 | Verify lpValueUsd formula excludes tokensOwed — only principal (lpEthAmount x uniPoolPrice) + lpUsdcAmount counts toward drift_threshold computation [NEW — RD-05-03; AC-15; OQ-EXBOT-011 Closed; TS_026] | 1. Light-check worker running.\n2. Bot LP position has: `lpEthAmount=1.0`, `uniPoolPrice=$2000`, `lpUsdcAmount=$500`, `tokensOwed_ETH=0.1`, `tokensOwed_USDC=$50`.\n3. Correct `lpValueUsd = (1.0 x $2000) + $500 = $2500` (tokensOwed excluded).\n4. Incorrect `lpValueUsd = (1.1 x $2000) + $550 = $2750` (tokensOwed incorrectly included). | 1. Trigger drift_threshold computation for this bot.\n2. Confirm which `lpValueUsd` is used: `$2500` or `$2750`. | 1. `drift_threshold` computed using `lpValueUsd = $2500` (principal only). `tokensOwed_ETH` and `tokensOwed_USDC` are excluded. `drift_threshold = max($25, $2500 x 3%) = max($25, $75) = $75`. If `lpValueUsd` incorrectly included `tokensOwed`, the threshold would be `max($25, $2750 x 3%) = $82.50` — a different value. (Ref: FR-EXBOT-025, AC-15, OQ-EXBOT-011 Closed.) | P1 |

