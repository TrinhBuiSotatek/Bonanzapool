# Test Cases — UC-EXBOT-user-redeem User-Initiated LP-First Redemption

**Total test cases:** 50 (FUNC: 32, INTG: 14, NFR: 4)
**Scope:** Logic-only (backend / API / bot — no UI)
**Source UC:** UC-EXBOT-user-redeem_user-redeem_audited_20260701_v1.md
**Source scenarios:** UC-EXBOT-user-redeem_user-redeem_scenarios_20260701_v1.md
**Output language:** English

#### Requirement Traceability Matrix

| AC ID | Acceptance Criteria | Linked Test Cases | Status |
|---|---|---|---|
| AC-01 | Happy path — hedge close within SLA, all postconditions met | TC_001, TC_004, TC_017, TC_027, TC_032, TC_036, TC_038, TC_039 | Covered |
| AC-02 | SLA breach → admin alert E-EXBOT-010, hedge close continues | TC_041, TC_042, TC_043, TC_044, TC_045 | Covered |
| AC-03 | Hedge close failure → residual_hl_liability, LP-portion not reversed | TC_020, TC_021, TC_022, TC_023 | Covered (TC_022 Expected Result partial — I-05 blocked) |
| AC-04 | Duplicate message → graceful no-op | TC_014, TC_015, TC_016, TC_037, TC_046 | Covered |
| AC-05 | Redeem on already-closed bot → E-EXBOT-012 | TC_010, TC_011 | Covered |
| AC-06 | LP-portion guaranteed when HL unavailable | TC_023 | Covered |
| AC-07 | light-check suppressed while lp_closing | TC_012, TC_013 | Covered |
| AC-F5-01 | Happy path full sequence | TC_001, TC_017, TC_032, TC_034, TC_038 | Covered |
| AC-F5-02 | SLA breach alert and continued close | TC_041, TC_042 | Covered |
| AC-F5-03 | Hedge close fails, LP-portion protected | TC_020, TC_021 | Covered |
| AC-F5-04 | Duplicate delivery → no-op | TC_014, TC_015 | Covered |
| AC-F5-05 | Already-closed bot → E-EXBOT-012 | TC_010 | Covered |
| AC-F5-06 | light-check suppressed during lp_closing | TC_012 | Covered |
| AC-F5-07 | INV-STOP: stop cancelled via replaceStopProtected | TC_029, TC_030 | Covered |

---

## I. Operation: On-chain LP liquidation and event detection

### I.1. Functional verification — Operation: On-chain LP liquidation and event detection

| TC ID | Title | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|
| TC_001 | Verify LP-portion USDC is returned to investor in the same on-chain transaction as the redeem call | 1. The bot is in `active` state with an open LP position.\n2. The investor owns the `tokenId` being redeemed.\n3. The BnzaExVault contract is deployed and ABI-integrated. | 1. Call `BnzaExVault.redeem(tokenId)` on-chain from the investor's wallet address. | 1. On-chain: the LP position is liquidated and LP-portion USDC appears at the investor's wallet address within the same transaction — not in a subsequent block. Off-chain: no Worker execution is required for the LP-portion delivery; the transfer is an on-chain guarantee. (Reference: FR-EXBOT-070(A), BR-EXBOT-006.) | P0 |
| TC_002 | Verify RedemptionEvent is emitted with correct parameters after a valid redeem call | 1. The bot is `active` and the investor owns the `tokenId`.\n2. The BnzaExVault contract is configured correctly. | 1. Call `BnzaExVault.redeem(tokenId)` on-chain successfully. | 1. On-chain: `RedemptionEvent(botId, redeemTxHash, userAddress)` is emitted in the same transaction. Each parameter (`botId`, `redeemTxHash`, `userAddress`) matches the caller's wallet and the bot's registered data. Off-chain: the event watcher receives this event. (Reference: UC §3 step 3, flows.md F-04.) | P0 |
| TC_003 | Verify on-chain redeem is reverted when the caller does not own the tokenId | 1. A wallet address that does not own the specified `tokenId` is used as the caller. | 1. Attempt to call `BnzaExVault.redeem(tokenId)` from a wallet that does not own that `tokenId`. | 1. On-chain: the transaction is reverted by the BnzaExVault contract. No `RedemptionEvent` is emitted. Off-chain: no message is enqueued in the `user_redeem` queue; no `close_operations` row is created. (Reference: UC §2 precondition 3, UC §3 step 1.) | P0 |
| TC_004 | Verify event watcher enqueues user_redeem message with highest priority after RedemptionEvent | 1. The event watcher is running and subscribed to `RedemptionEvent` from BnzaExVault.\n2. A valid `RedemptionEvent(botId, redeemTxHash, userAddress)` has been emitted on-chain. | 1. Let the event watcher process the on-chain `RedemptionEvent`. | 1. A message `{botId, redeemTxHash, userAddress}` is inserted into the `user_redeem` queue with the highest priority setting, before any Worker begins processing. No other queue receives this message first. (Reference: UC §3 step 4, flows.md F-04.) | P0 |

### I.2. Integration & State verification — Operation: On-chain LP liquidation and event detection

| TC ID | Title | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|
| TC_005 | Verify close_operations record is created with kind=user_redeem before any HL mutation | 1. The Worker has dequeued a `user_redeem` message for the first time.\n2. No `close_operations` row exists for this `botId`/`idempotency_key`. | 1. Let the Worker begin processing the `user_redeem` message up to (but not past) the first HL API call. | 1. A row is inserted into `close_operations` with `kind='user_redeem'`, a unique `idempotency_key`, and `bot_id` matching the message. This insert occurs before any call to Hyperliquid. (Reference: UC §3 step 7, erd.md, FR-EXBOT-070.) | P0 |
| TC_006 | Verify bots.lifecycle_state transitions from active to lp_closing atomically with close_operations creation | 1. The bot's `lifecycle_state` is `active`.\n2. No `close_operations` row exists for this bot. | 1. Trigger `close_operations` row insertion for the bot. | 1. `bots.lifecycle_state` changes from `active` to `lp_closing` in the same atomic operation as the `close_operations` insert. There is no observable window where `close_operations` exists but `lifecycle_state` is still `active`. (Reference: states.md, erd.md, §F.2 item 2.) | P0 |
| TC_007 | Verify close_operations record contains the correct redeemTxHash from the on-chain event | 1. A valid `RedemptionEvent` has been detected with a known `redeemTxHash`. | 1. Allow the Worker to create the `close_operations` record. | 1. The `close_operations` row's reference to `redeemTxHash` matches the on-chain transaction hash from the `RedemptionEvent` exactly. No fabricated or stale hash is stored. (Reference: UC §3 step 7, §F.4.) | P1 |

## II. Operation: Worker state check and idempotency guard

### II.1. Functional verification — Operation: Worker state check and idempotency guard

| TC ID | Title | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|
| TC_008 | Verify Worker checks bot state before any mutation and proceeds when state is valid | 1. The bot's `lifecycle_state` is `active` and `bots.status` is not `closing` or `closed`. | 1. Deliver a `user_redeem` message for the bot and observe the state check step. | 1. The Worker reads `bots.lifecycle_state` and `bots.status` before acquiring any lock, before creating `close_operations`, and before calling Hyperliquid. Because the state is valid, processing continues to the next step with no rejection. (Reference: UC §3 step 6, states.md.) | P0 |
| TC_009 | Verify Worker inserts queue_idempotency record at start and exits on duplicate message_id | 1. The Worker is processing a `user_redeem` message.\n2. `queue_idempotency` does not yet have a row for this `message_id`. | 1. Let the Worker insert the `message_id` into `queue_idempotency` at the very start of processing. | 1. The row is inserted with `state='started'`. The Worker continues to the next step. (Reference: FR-EXBOT-010, erd.md.) | P0 |
| TC_010 | Verify Worker returns E-EXBOT-012 when the bot is already closed | 1. The bot's `lifecycle_state` is `closed`.\n2. A `user_redeem` message has been delivered for this bot. | 1. Let the Worker process the `user_redeem` message for the closed bot. | 1. The Worker detects `lifecycle_state='closed'` before any lock or HL call. It returns `E-EXBOT-012` (HTTP 409): "Bot is already closed. No action needed." No `close_operations` row is created, no HL mutation is made, and no state changes. (Reference: E-EXBOT-012, AC-05.) | P0 |
| TC_011 | Verify Worker returns E-EXBOT-012 when bot is already in lp_closing state | 1. The bot's `lifecycle_state` is `lp_closing` (a close is already in progress).\n2. A second `user_redeem` message arrives. | 1. Deliver a second `user_redeem` message for the bot already in `lp_closing`. | 1. The Worker detects the `lp_closing` state early in processing and either returns `E-EXBOT-012` or detects a duplicate via `close_operations.idempotency_key` UNIQUE constraint. In either case, no second HL close is initiated and no second `close_operations` row is created. (Reference: E-EXBOT-012, states.md.) | P0 |

### II.2. Integration & State verification — Operation: Worker state check and idempotency guard

| TC ID | Title | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|
| TC_012 | Verify light-check worker skips the bot entirely while lifecycle_state is lp_closing | 1. The bot's `lifecycle_state` is `lp_closing` (close in progress). | 1. Trigger a light-check evaluation cycle for the same bot while it is in `lp_closing`. | 1. The light-check worker skips this bot completely. No `hedge-sync` message is enqueued for this bot. Stop monitoring is also suppressed for this bot during `lp_closing`. (Reference: states.md, AC-07.) | P0 |
| TC_013 | Verify hedge-sync message already in queue is dropped or returns early when bot is in lp_closing | 1. A `hedge-sync` message for the bot was already queued before the close began.\n2. The bot's `lifecycle_state` is now `lp_closing`. | 1. Let the `hedge-sync` Worker process the queued message while the bot is in `lp_closing`. | 1. The `hedge-sync` Worker detects `lifecycle_state='lp_closing'` and exits early without submitting any HL order or updating any state. No hedge mutation occurs during the close window. (Reference: states.md, §F.4.) | P0 |
| TC_014 | Verify duplicate user_redeem message detected via queue_idempotency message_id UNIQUE constraint | 1. A `user_redeem` message with a specific `message_id` has already been fully processed.\n2. `queue_idempotency` has a row for this `message_id`. | 1. Redeliver the same `user_redeem` message (same `message_id`). | 1. The Worker hits the UNIQUE constraint on `queue_idempotency.message_id` at the very first insert step and returns immediately. No HL mutation, no new `close_operations` row, and no state change occur. (Reference: FR-EXBOT-010, AC-04.) | P0 |
| TC_015 | Verify duplicate user_redeem message detected via close_operations idempotency_key UNIQUE constraint | 1. A `close_operations` row with the same `idempotency_key` as the incoming message already exists. | 1. Deliver a `user_redeem` message whose derived `idempotency_key` matches an existing `close_operations` row. | 1. The Worker detects the UNIQUE constraint violation on `close_operations.idempotency_key` and exits gracefully. No HL mutation, no duplicate row created. (Reference: erd.md, AC-04.) | P0 |
| TC_016 | Verify two concurrent deliveries of the same user_redeem message result in exactly one hedge close | 1. Two Worker instances receive the same `user_redeem` message at the same time. | 1. Simulate concurrent delivery of the identical message to two Workers simultaneously. | 1. Exactly one Worker proceeds to execute the hedge close. The other is blocked by either the `queue_idempotency.message_id` or `close_operations.idempotency_key` UNIQUE constraint. Only one `close_operations` row exists; only one `closeShortReduceOnlyIoc` call is made to Hyperliquid. (Reference: FR-EXBOT-010, erd.md.) | P0 |

## III. Operation: HL hedge close and stop cancel (closeShortReduceOnlyIoc + replaceStopProtected)

### III.1. Functional verification — Operation: HL hedge close and stop cancel

| TC ID | Title | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|
| TC_017 | Verify Worker calls closeShortReduceOnlyIoc with full short size, not a partial delta | 1. The bot is active with an open HL short position.\n2. The `close_operations` record has been created and `lifecycle_state='lp_closing'`. | 1. Let the Worker execute the HL hedge close step. | 1. The Worker calls `closeShortReduceOnlyIoc` with the full short size (target size = 0 — the entire position is closed, not a partial delta adjustment). This is the permitted full-close exception per FR-EXBOT-020. A deterministic `cloid` is used. (Reference: FR-EXBOT-020 exception #1, UC §3 step 8.) | P0 |
| TC_018 | Verify stop order is cancelled via replaceStopProtected(size=0) and not by direct cancelOrder | 1. An active stop order exists on Hyperliquid for the bot's hedge leg.\n2. `hedge_legs.stop_order_id` and `stop_cloid` are populated. | 1. Let the Worker execute the stop cancel step as part of the close flow. | 1. The Worker calls `replaceStopProtected(size=0)` — the INV-STOP protocol. No direct `cancelOrder` call is made outside this protocol. After the call, `hedge_legs.stop_order_id` and `hedge_legs.stop_cloid` are cleared or marked cancelled in D1. (Reference: FR-EXBOT-032, UC §3 step 10.) | P0 |
| TC_019 | Verify closeShortReduceOnlyIoc uses a deterministic cloid that is the same on retry | 1. The bot has an open HL short position.\n2. The Worker is about to attempt the hedge close. | 1. Invoke the hedge close step twice for the same logical operation (simulate a retry of the same close attempt).\n2. Compare the `cloid` used in both calls. | 1. Both attempts produce the same `cloid` computed from the same fixed inputs (e.g., `botId`, operation type, attempt context). The `cloid` is deterministic — the same inputs always yield the same value. A different bot's close attempt produces a different `cloid`. (Reference: FR-EXBOT-022, UC §3 step 8.) | P0 |
| TC_020 | Verify LP-portion is not reversed when hedge close fails after LP liquidation | 1. The LP position has already been liquidated on-chain and LP-portion USDC has been delivered to the investor.\n2. `close_operations.state` is past the initial state (LP already returned).\n3. The HL `closeShortReduceOnlyIoc` call (mocked) is set to fail with an unrecoverable error. | 1. Let the Worker attempt the HL hedge close while the mock HL call returns an unrecoverable failure. | 1. The LP-portion USDC already delivered to the investor on-chain is NOT reversed, blocked, or withheld. The Worker sets `close_operations.state='residual_hl_liability'` and enqueues an admin notification. The on-chain LP-portion transfer is permanent and irreversible. BR-EXBOT-006: "user_redeem LP-portion repayment is unconditional. Hedge close failure must never block or reverse the LP-portion return to the user." (Reference: BR-EXBOT-006, AC-03.) | P0 |
| TC_021 | Verify LP-portion guarantee holds when HL API is completely unreachable during hedge close | 1. LP-portion has been returned to the investor on-chain.\n2. All Hyperliquid API calls (mocked) return connection errors. | 1. Let the Worker attempt the HL hedge close while HL is fully unreachable. | 1. LP-portion USDC already returned on-chain is not affected. `close_operations.state` transitions to `residual_hl_liability`. HL unreachability is treated as a hedge failure — it must never block or reverse LP repayment. (Reference: BR-EXBOT-006, AC-06.) | P0 |
| TC_022 | Verify admin notification is enqueued when hedge close fails and residual_hl_liability is set | 1. `close_operations.state` has transitioned to `residual_hl_liability`.\n2. `close_operations.residual_amount` is populated with the outstanding HL liability. | 1. Observe the Worker's actions after setting `close_operations.state='residual_hl_liability'`. | 1. The Worker enqueues an admin notification containing the outstanding liability amount from `close_operations.residual_amount`. The notification is enqueued regardless of whether the SLA has been breached. (Note: exact message content pending I-05 BA confirmation — notification body expected result is left blank until I-05 resolved.) (Reference: UC §4 A2, §F.2 item 8.) | P0 |
| TC_023 | Verify close_operations.state transitions to residual_hl_liability when hedge close fails | 1. The LP-portion has been returned on-chain.\n2. `closeShortReduceOnlyIoc` fails after all retries. | 1. Allow the Worker to exhaust all hedge close attempts and fail. | 1. `close_operations.state` transitions to `residual_hl_liability`. `close_operations.residual_amount` is populated as a BigDecimal TEXT string representing the outstanding HL liability. No `done` state is set. `bots.lifecycle_state` after A2 is left blank pending I-06 BA confirmation. (Reference: UC §4 A2, erd.md, AC-03.) | P0 |
| TC_024 | Verify retry of closeShortReduceOnlyIoc with same cloid does not double-close the HL position | 1. `closeShortReduceOnlyIoc` was submitted but the response timed out or was ambiguous.\n2. The HL position may or may not be closed. | 1. Retry the hedge close with the same deterministic `cloid`. | 1. Hyperliquid recognises the duplicate `cloid` and does not execute a second close. After the retry, reconcile confirms HL position = 0 with only one close execution in history. (Reference: FR-EXBOT-022, UC §3 step 8.) | P0 |

### III.2. Integration & State verification — Operation: HL hedge close and stop cancel

| TC ID | Title | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|
| TC_025 | Verify HLRateLimitDO is consulted before closeShortReduceOnlyIoc is called | 1. `HLRateLimitDO` is running and tracking HL weight.\n2. The Worker is about to call `closeShortReduceOnlyIoc`. | 1. Let the Worker reach the HL close step while `HLRateLimitDO` is active. | 1. The Worker queries `HLRateLimitDO` for permission before calling Hyperliquid. If `{allowed: false, retryAfterMs: N}` is returned, the Worker waits the specified delay before retrying — it does not call Hyperliquid immediately when told to wait. (Reference: FR-EXBOT-070, NFR-EXBOT-004.) | P1 |
| TC_026 | Verify hedge_legs stop fields are cleared in D1 after replaceStopProtected(size=0) succeeds | 1. `hedge_legs.stop_order_id` and `stop_cloid` are populated with the active stop order's data. | 1. Let the Worker execute `replaceStopProtected(size=0)` successfully. | 1. `hedge_legs.stop_order_id` and `hedge_legs.stop_cloid` are cleared (null or empty) in D1 after the call succeeds. No stale stop reference remains in `hedge_legs`. (Reference: FR-EXBOT-032, UC §3 step 10, §F.1 item 5.) | P0 |

## IV. Operation: Post-close reconcile

### IV.1. Functional verification — Operation: Post-close reconcile

| TC ID | Title | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|
| TC_027 | Verify reconcile confirms HL position equals zero before advancing close_operations to hedge_closed | 1. `closeShortReduceOnlyIoc` has been submitted.\n2. The Worker is about to fetch `clearinghouseState` from Hyperliquid. | 1. Let the Worker run the reconcile step — fetch `clearinghouseState` and compare to expected size = 0. | 1. The Worker fetches `clearinghouseState` from Hyperliquid and confirms the actual short position size = 0. Only after this confirmation does `close_operations.state` advance to `hedge_closed`. If the actual size is not 0, the state does not advance. (Reference: UC §3 step 11, flows.md F-04, FR-EXBOT-023.) | P0 |
| TC_028 | Verify close_operations.state does not advance to done before reconcile confirms position closed | 1. `closeShortReduceOnlyIoc` has been submitted but reconcile has not yet run. | 1. Check `close_operations.state` immediately after the close order is submitted and before reconcile completes. | 1. `close_operations.state` remains at the pre-reconcile state. It does not jump to `hedge_closed` or `done` optimistically before the reconcile fetch confirms size = 0. (Reference: UC §3 step 11, FR-EXBOT-025.) | P0 |
| TC_029 | Verify reconcile failure behavior in user_redeem context when HL position is not zero after close | 1. `closeShortReduceOnlyIoc` has been submitted.\n2. Reconcile fetches `clearinghouseState` and finds the actual HL position size ≠ 0. | 1. Simulate a reconcile mismatch — the HL position is not confirmed as zero after the close order. | 1. (Expected result left blank pending I-08 BA confirmation — behavior when reconcile mismatch occurs in user_redeem context is not documented: does it trigger SAFE_MODE, residual_hl_liability, or a retry?) (Reference: UC §3 step 11, I-08.) | P0 |

### IV.2. Integration & State verification — Operation: Post-close reconcile

| TC ID | Title | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|
| TC_030 | Verify close_operations state machine transitions through the correct full sequence to done | 1. A full user_redeem flow has started from event detection through to HL-portion transfer.\n2. All steps succeed. | 1. Observe `close_operations.state` at each key milestone during the full happy path. | 1. `close_operations.state` progresses in sequence: initial creation state → `hedge_closed` (after reconcile confirms HL position = 0) → `done` (after HL-portion USDC transfer completes). Each state is persisted to D1 before the next step begins; no state is skipped. (Note: initial state pending I-04 — leaving this field as "initial creation state" until BA confirms whether it starts as `lp_closed`, `funds_returned`, or `requested`.) (Reference: states.md, erd.md, UC §3 steps 7–15.) | P0 |

## V. Operation: HL-portion USDC transfer and bot close finalization

### V.1. Functional verification — Operation: HL-portion USDC transfer and bot close finalization

| TC ID | Title | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|
| TC_031 | Verify HL-portion USDC transfer to investor does not occur before reconcile confirms close | 1. `closeShortReduceOnlyIoc` has been submitted but reconcile has not yet confirmed size = 0. | 1. Check whether any HL-portion USDC transfer is initiated before reconcile confirms HL position = 0. | 1. No HL-portion USDC transfer is initiated before reconcile confirms the short position is fully closed. The transfer step is gated by `close_operations.state='hedge_closed'`. (Reference: UC §3 step 12, flows.md F-04.) | P0 |
| TC_032 | Verify HL-portion USDC is transferred via RedemptionQueue after hedge_closed is confirmed | 1. Reconcile has confirmed HL position = 0.\n2. `close_operations.state='hedge_closed'`. | 1. Let the Worker execute the HL-portion transfer step. | 1. The Worker initiates the HL-portion USDC transfer to the investor via the RedemptionQueue ledger. `close_operations.usdc_amount` is populated with the transferred amount as a BigDecimal TEXT string. (Reference: UC §3 steps 12–13, erd.md.) | P0 |
| TC_033 | Verify bots.lifecycle_state transitions to closed only after HL-portion transfer is recorded | 1. The HL-portion USDC transfer has been recorded in the RedemptionQueue ledger.\n2. `close_operations.state='done'`. | 1. Check `bots.lifecycle_state` and `bots.status` after the HL-portion transfer step. | 1. `bots.lifecycle_state` transitions to `closed` and `bots.status` to `closed` only after the HL-portion transfer is confirmed and `close_operations.state='done'`. Neither field is set prematurely during any intermediate step. (Reference: UC §3 steps 14–15, states.md.) | P0 |
| TC_034 | Verify close_operations.usdc_amount stores HL-portion using BigDecimal TEXT, not a float | 1. A successful user_redeem has completed and HL-portion USDC has been transferred. | 1. Read `close_operations.usdc_amount` from D1 after the transfer step. | 1. `close_operations.usdc_amount` is stored as a BigDecimal string (TEXT field), not as a JavaScript Number or floating-point value. No floating-point rounding artefacts appear in the stored value. All intermediate computation uses BigDecimal arithmetic. (Reference: NFR-EXBOT-008, erd.md.) | P0 |

### V.2. Integration & State verification — Operation: HL-portion USDC transfer and bot close finalization

| TC ID | Title | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|
| TC_035 | Verify no duplicate HL-portion transfer on user_redeem message redelivery after close_operations=done | 1. The full user_redeem pipeline has already completed (`close_operations.state='done'`, HL-portion already sent).\n2. The `user_redeem` message is redelivered by the queue infrastructure. | 1. Redeliver the same `user_redeem` message after completion. | 1. The Worker detects the already-processed state via `queue_idempotency` or `close_operations.idempotency_key` and exits without re-executing the HL-portion transfer. No second RedemptionQueue entry is created for the same investor. The total USDC received equals LP-portion + HL-portion (not LP-portion + 2×HL-portion). (Reference: UC §4 A3, erd.md.) | P0 |
| TC_036 | Verify complete happy path state sequence from event detection to bot=closed | 1. All preconditions are met: bot `active`, investor owns `tokenId`, HL agent key approved, event watcher running. | 1. Execute the full user_redeem flow end-to-end starting from on-chain `BnzaExVault.redeem(tokenId)`. | 1. The full sequence completes correctly: (a) On-chain: LP liquidated, LP-portion USDC returned to investor in same tx. (b) Event watcher: `RedemptionEvent` detected, message enqueued. (c) Worker: `close_operations` created, `lifecycle_state='lp_closing'`, `closeShortReduceOnlyIoc` called, `replaceStopProtected(size=0)` called, reconcile confirms HL position = 0, `close_operations.state='hedge_closed'`, HL-portion transferred, `close_operations.state='done'`, `bots.lifecycle_state='closed'`, `bots.status='closed'`. All state transitions are persisted to D1 before each subsequent step. (Reference: AC-01, flows.md F-04, FR-EXBOT-070.) | P0 |

## VI. Operation: SLA monitoring (NFR-EXBOT-003)

### VI.1. Functional verification — Operation: SLA monitoring

| TC ID | Title | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|
| TC_037 | Verify no SLA alert is sent when hedge close completes within 5 minutes | 1. A `user_redeem` message has been picked up and `RedemptionEvent` detection timestamp is recorded.\n2. Mocked HL responses are fast. | 1. Run the full hedge close flow within the 5-minute window from event detection. | 1. No `E-EXBOT-010` admin alert is enqueued. `close_operations.state` reaches `hedge_closed` within the SLA window without triggering any admin notification. (Reference: NFR-EXBOT-003, AC-01.) | P0 |
| TC_038 | Verify hedge close completing at exactly the 5-minute boundary does not trigger SLA alert | 1. The hedge close SLA timer starts at event detection.\n2. The hedge close is simulated to complete at exactly 5 minutes (300 seconds) from event detection. | 1. Simulate hedge close completion at exactly the SLA boundary. | 1. No `E-EXBOT-010` alert is sent when the hedge close completes at exactly 5 minutes. The alert is triggered only when elapsed time strictly exceeds 5 minutes (or confirm the exact boundary semantics — ≥ vs > — when I-05 is resolved). (Reference: NFR-EXBOT-003, UC §4 A1.) | P1 |
| TC_039 | Verify SLA alert E-EXBOT-010 is sent when hedge close exceeds 5 minutes | 1. A `user_redeem` message has been picked up.\n2. HL close is mocked to be slow and will not complete within 5 minutes. | 1. Let the hedge close run beyond 5 minutes from event detection. | 1. Admin alert `E-EXBOT-010` is enqueued with message: "Hedge close for redemption exceeded 5-minute SLA. Escalated to admin. LP funds already returned." The alert contains accurate context about the LP funds status. (Reference: NFR-EXBOT-003, E-EXBOT-010, AC-02.) | P0 |
| TC_040 | Verify hedge close continues and does not abort after SLA alert is sent | 1. The SLA breach alert (`E-EXBOT-010`) has been sent.\n2. The hedge close is still in progress. | 1. Observe Worker behavior after `E-EXBOT-010` is enqueued. | 1. The Worker does not abort, reset, or interrupt the hedge close after the SLA alert. The HL position close, stop cancel, reconcile, and HL-portion transfer all continue to completion. `close_operations.state` eventually reaches `done`. The SLA alert is a notification-only event with no control flow side effects. (Reference: UC §4 A1, NFR-EXBOT-003.) | P0 |
| TC_041 | Verify SLA alert is enqueued exactly once per redeem operation even if hedge close is delayed for an extended period | 1. A user_redeem flow has been running past 5 minutes.\n2. The hedge close is still in progress with multiple retry loops. | 1. Let the hedge close run for significantly more than 5 minutes without completing. | 1. `E-EXBOT-010` is enqueued exactly once for this `close_operations` record — not sent again on subsequent retry loops or checks. The admin notification queue does not accumulate duplicate SLA alerts for the same close operation. (Reference: E-EXBOT-010, UC §4 A1.) | P0 |
| TC_042 | Verify SLA breach boundary — hedge close 1 second past 5 minutes triggers alert | 1. The hedge close SLA timer starts at event detection.\n2. The hedge close is simulated to complete at 5 minutes + 1 second from event detection. | 1. Simulate hedge close completion at one second past the SLA boundary. | 1. `E-EXBOT-010` admin alert is enqueued. This confirms the boundary is at 300 seconds — a close completing at 301 seconds must trigger the alert. (Reference: NFR-EXBOT-003, E-EXBOT-010.) | P0 |

### VI.2. Integration & State verification — Operation: SLA monitoring

| TC ID | Title | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|
| TC_043 | Verify SLA timer is started at event detection time, not at Worker dequeue time | 1. A `RedemptionEvent` has been detected and a `user_redeem` message has been enqueued.\n2. There is a deliberate delay between event detection and Worker pickup (queue backlog). | 1. Introduce a delay between event detection and Worker processing, then measure SLA from event detection timestamp. | 1. The 5-minute SLA window starts from the moment `RedemptionEvent` was detected (the timestamp captured by the event watcher), not from the moment the Worker dequeued the message. If total elapsed time from detection to `hedge_closed` exceeds 5 minutes, `E-EXBOT-010` is triggered. (Reference: NFR-EXBOT-003, UC §3 step 5.) | P1 |

## VII. Operation: Acceptance criteria verification

### VII.1. Functional verification — Operation: Acceptance criteria verification

| TC ID | Title | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|
| TC_044 | Verify AC-01: all happy path postconditions are met simultaneously within SLA | 1. Bot is `active`; investor owns a valid `tokenId`; HL short open; stop order placed; agent key approved; event watcher running.\n2. All HL mocked responses succeed quickly. | 1. Execute the full user_redeem E2E flow starting from `BnzaExVault.redeem(tokenId)`. | 1. All five AC-01 conditions are true simultaneously at completion: (a) LP-portion USDC is in the investor's wallet from the same on-chain tx; (b) `close_operations.state='done'` reached within ≤ 5 minutes of event detection; (c) `bots.lifecycle_state='closed'`; (d) HL position = 0 confirmed by reconcile; (e) HL-portion USDC sent via RedemptionQueue. (Reference: AC-01, us-004.md AC-004-1, NFR-EXBOT-003.) | P0 |
| TC_045 | Verify AC-02: SLA breach sends correct admin alert text and hedge close still completes | 1. Bot is `active`; HL mocked to be slow (will exceed 5-minute SLA).\n2. LP-portion has been returned on-chain. | 1. Run user_redeem with delayed HL close. | 1. `E-EXBOT-010` alert is sent to admin with exact message: "Hedge close for redemption exceeded 5-minute SLA. Escalated to admin. LP funds already returned." Hedge close continues without abort. `close_operations.state` eventually reaches `done`. LP-portion is not reversed. (Reference: AC-02, us-004.md AC-004-2, E-EXBOT-010.) | P0 |
| TC_046 | Verify AC-04: duplicate user_redeem message results in a complete graceful no-op | 1. A `user_redeem` flow has already fully completed (`close_operations.state='done'`). | 1. Redeliver the same `user_redeem` message a second time. | 1. The Worker performs no HL mutation, creates no additional D1 rows, and exits without error or state change. The previously completed `close_operations` record remains unchanged. (Reference: AC-04, UC §4 A3.) | P0 |

### VII.3. Non-functional (logic) verification — Operation: Acceptance criteria verification

| TC ID | Title | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|
| TC_047 | Verify HL agent key plaintext is never written to logs or error output during hedge close signing | 1. The HL agent key is approved and not expired.\n2. The Worker is decrypting the key for use in `closeShortReduceOnlyIoc`. | 1. Execute the hedge close step and read all Worker logs and error output afterward. | 1. The plain agent key and plain data-encryption key (DEK) do not appear anywhere in the logs or error output. They are used only inside the signing function and dropped immediately after use. A dump of the `hl_agent_keys` table shows encrypted blobs only. (Reference: FR-EXBOT-080, NFR-EXBOT-006.) | P0 |
| TC_048 | Verify SAFE_MODE state machine has no dead-end path for bot stuck in lp_closing on HL failure | 1. The bot is in `lp_closing` (`lifecycle_state='lp_closing'`) and hedge close has failed.\n2. `close_operations.state='residual_hl_liability'`. | 1. Verify there is a documented path for admin resolution or auto-recovery from `residual_hl_liability`. | 1. (Expected result left blank pending I-06 BA confirmation — `bots.lifecycle_state` after A2 is not documented; tester cannot verify this is not a terminal dead-end state. BA must confirm postcondition.) (Reference: BR-EXBOT-007, I-06.) | P1 |
| TC_049 | Verify close_operations idempotency_key UNIQUE constraint prevents double settlement under any trigger combination | 1. Two separate triggering paths for the same redeem arrive — e.g., a queue redelivery and a manual retry. | 1. Simulate both triggers attempting to insert a `close_operations` row with the same `idempotency_key` simultaneously. | 1. Only one `close_operations` row exists after both attempts. The UNIQUE constraint blocks the second insert. Only one HL hedge close is executed. No double settlement results. (Reference: FR-EXBOT-070, erd.md.) | P0 |
| TC_050 | Verify HL-portion USDC calculation uses BigDecimal arithmetic with no floating-point loss | 1. A user_redeem has completed.\n2. The HL-portion amount calculation involved multiple arithmetic operations. | 1. Inspect the computation path and stored value for `close_operations.usdc_amount`. | 1. All arithmetic in the HL-portion computation uses BigDecimal operations throughout — no intermediate `Number(...)` or float multiplication. The stored value in `close_operations.usdc_amount` is a precise TEXT BigDecimal string with no rounding artifacts. (Reference: NFR-EXBOT-008, §9 NFR.) | P0 |
