# Test Cases — UC-EXBOT-light-check Execute Periodic Light-Check (v5)

**Total test cases:** 106 (Delta vs v4: +0 new, ~1 updated [TC_036], -2 deleted [TC_050, TC_104])
**Scope:** Logic-only (backend / API / bot — no UI)
**Update trigger:** USER_FEEDBACK_ONLY (2026-07-15 — dedup cleanup: retired TC_050 [dup TC_087], TC_104 [dup TC_039+TC_040]; removed 4 stale struck-through rows; updated TC_036 note) | PRIORITY_REVIEW (2026-07-20 — reclassified 25 TCs from P1 → P2: BVA secondary [TC_007, TC_008, TC_029, TC_030, TC_038, TC_040, TC_083, TC_084, TC_088, TC_089, TC_092, TC_102], infra/timing [TC_015, TC_031, TC_034, TC_085], maintenance [TC_020], supplement [TC_075, TC_077, TC_094, TC_096, TC_097, TC_099, TC_101, TC_106])
**Source UC:** UC-EXBOT-light-check_light-check_audited_20260714_v4.md
**Source scenarios (if any):** UC-EXBOT-light-check_light-check_scenarios_20260715_v4.md
**Output language:** English

#### Requirement Traceability Matrix

| AC ID | Acceptance Criteria | Linked Test Cases | Status |
|---|---|---|---|
| AC-LC-01 | Bot selected by Scan Worker when status=active and next_light_check_at <= now | TC_001, TC_002, TC_013, TC_014 | Covered |
| AC-LC-02 | lifecycle_state=lp_rebalancing — LCW skips rebalance evaluation; next_light_check_at still updated | TC_003, TC_096 | Covered |
| AC-LC-03 | Pool Slot0 Cache stale > 5min → LCW skip tick entirely, no mutation | TC_028, TC_033, TC_029, TC_030 | Covered |
| AC-LC-04 | Zero HL API calls throughout light-check pipeline | TC_001, TC_002, TC_013, TC_023, TC_024, TC_010 | Covered |
| AC-LC-05 | next_light_check_at batch UPDATE (1 stmt/shard) for ALL eligible bots including skipped | TC_006, TC_007, TC_008 | Covered |
| AC-LC-06 | Idempotency: UNIQUE conflict on message_id → skip, no mutation | TC_016, TC_017, TC_018 | Covered |
| AC-LC-07 | drift_threshold = max($25, lpValueUsd × 3%) with lpValueUsd = max(0, lpEthAmount × currentPriceUsd) | TC_035, TC_036, TC_037, TC_038, TC_094 | Covered |
| AC-LC-08 | range_boundary_near fires when min(distToLower,distToUpper)/halfRange <= 0.9 (price-based USD) | TC_046, TC_102 | Covered |
| AC-LC-09 | funding_alert fires when 7d APR < -15%; primary: fundingApr7dPct from funding_rolling_metrics | TC_087, TC_088, TC_089, TC_090, TC_091, TC_092 | Covered [BLOCKED v1 primary path — fallback testable] |
| AC-LC-10 | Strategy engine gathers ALL fired reasons into decision.reason[]; enqueues 1 hedge-sync | TC_047, TC_093 | Covered |
| AC-LC-11 | range_out enqueued to hedge-sync; LCW does NOT set lifecycle_state=lp_rebalancing | TC_043, TC_093, TC_108 | Covered |
| AC-LC-12 | Circuit breaker open → hedge-sync suppressed; stop monitoring continues | TC_052, TC_064 | Covered |
| AC-LC-13 | markPrice >= stop_price → stop_trigger_crossed_at set (write-once); price-near-stop-audit enqueued | TC_062, TC_079, TC_105 | Covered |
| AC-LC-14 | HL Mark Price Cache stale > 120s → fallback eth_price_usd; widen band 2%→4%; freeze hedge-sync | TC_080, TC_081, TC_082, TC_083, TC_084, TC_085 | Covered |
| AC-LC-15 | stop_replacing_started_at overrun > 60s → SAFE_MODE atomic UPDATE (status + lifecycle_state) + partial_repair | TC_069, TC_072, TC_098, TC_074 | Covered |
| AC-LC-16 | Circuit breaker half_open → 1 probe hedge-sync (atomic claim half_open_probe_used) | TC_053, TC_058 | Covered |
| AC-LC-17 | LIMIT 500 + jitter: steady state no overflow; post-downtime oldest-first catch-up | TC_011, TC_099, TC_100 | Covered |
| AC-LC-18 | queue_idempotency.expires_at always set = now+1min; hourly EventBridge cron deletes expired rows | TC_016, TC_019, TC_020 | Covered |

---


## I. Operation: Cron fan-out + Scan Worker


### I.1. Functional verification — Operation: Cron fan-out + Scan Worker

| TC ID | Title | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|
| TC_001 | Verify EventBridge Scheduler sends bot-scan messages via chunkSendBatch, not direct sendBatch [UPDATED — D-03: EventBridge Scheduler replaces Cron Worker] | 1. The EventBridge Scheduler fires on its 1-minute schedule. | 1. Trigger one EventBridge-initiated Scan Worker execution and capture all outbound queue calls.\n2. Verify that all bot-scan SQS messages are sent using the `chunkSendBatch()` helper. | 1. No direct `queue.sendBatch()` call is made without the chunking helper.\n2. All bot-scan queue calls go through `chunkSendBatch()`, with each chunk limited to the platform maximum per call. HL API call count remains 0 throughout the Scan Worker path. (Reference: UC §3 step 1; SRS FR-EXBOT-010; BR-EXBOT-003; AC-LC-04.) | P0 |
| TC_002 | Verify Scan Worker query selects only active bots whose check time is due [UPDATED — D-04: Aurora PostgreSQL replaces D1] | 1. Aurora PostgreSQL contains bots in mixed states: active bots with `next_light_check_at` in the past (eligible), active bots with `next_light_check_at` in the future (not yet due), paused bots, and closed bots. | 1. Trigger one Scan Worker execution on the shard.\n2. Inspect which botIds appear in the light-check SQS queue. | 1. Only bots with `status='active'` AND `next_light_check_at <= now` appear in the light-check queue.\n2. Bots that are paused, closed, or not yet due are not enqueued. HL API call count = 0. (Reference: UC §3 step 2; SRS FR-EXBOT-012; AC-LC-01; AC-LC-04.) | P0 |
| TC_003 | Verify Scan Worker filters out bots in lp_rebalancing lifecycle before enqueue but still reschedules them [UPDATED — D-04: Aurora PostgreSQL replaces D1] | 1. Aurora PostgreSQL contains an active bot with `lifecycle_state='lp_rebalancing'` and `next_light_check_at <= now`. | 1. Trigger one Scan Worker execution.\n2. Inspect the light-check queue for that bot's message.\n3. Read `next_light_check_at` for the bot after the Scan Worker completes. | 1. The bot is NOT enqueued in the light-check queue — no light-check message is sent for it.\n2. The Scan Worker excludes bots with `lifecycle_state IN ('lp_rebalancing', 'lp_closing')` at query time.\n3. `next_light_check_at` IS updated to `now + 5min + jitter(±45s)` for the bot — it is rescheduled even though it was not enqueued, so it does not flood the queue on recovery. (Reference: UC §2; SRS FR-EXBOT-012; AC-LC-02.) | P0 |
| TC_004 | Verify Scan Worker filters out bots in lp_closing lifecycle but still reschedules them [UPDATED — D-04: Aurora PostgreSQL replaces D1] | 1. Aurora PostgreSQL contains a bot with `lifecycle_state='lp_closing'` and `next_light_check_at <= now`. | 1. Trigger one Scan Worker execution.\n2. Check whether a light-check message is enqueued for that bot.\n3. Read `next_light_check_at` for the bot after the Scan Worker completes. | 1. No light-check message is enqueued for the bot in `lp_closing` state.\n2. `next_light_check_at` IS updated for the bot — rescheduled despite being skipped, preventing flood on recovery. (Reference: UC §2; SRS FR-EXBOT-012; AC-LC-02.) | P1 |
| TC_005 | Verify Scan Worker sends per-bot messages to the light-check queue via chunkSendBatch | 1. The Scan Worker has queried a set of eligible bots from Aurora PostgreSQL. | 1. Let the Scan Worker enqueue the per-bot light-check messages.\n2. Capture all queue calls made. | 1. All per-bot light-check messages are enqueued through `chunkSendBatch()`, not via raw `sendBatch()`.\n2. Each bot gets exactly one light-check message. (Reference: UC §3 step 3; SRS FR-EXBOT-010.) | P1 |
| TC_006 | Verify next_light_check_at is batch-updated with jitter as one statement per shard for ALL eligible bots including skipped bots [UPDATED — D-04: Aurora PostgreSQL replaces D1] | 1. The Scan Worker has queried eligible bots on a shard, including bots with `lifecycle_state='lp_rebalancing'` or `status='paused'` that were not enqueued for light-check. | 1. After enqueue, capture the Aurora PostgreSQL write statements for `next_light_check_at`.\n2. Read back `next_light_check_at` for each eligible bot — including those not enqueued. | 1. Exactly one Aurora PostgreSQL UPDATE statement is issued per shard — not one per bot.\n2. All eligible bots — including `lp_rebalancing`, `lp_closing`, and `paused` bots — have their `next_light_check_at` updated to a value in the range `now + 5min ± 45s`. (Example: a bot processed at T=0 gets `next_light_check_at` between T+255s and T+345s.)\n3. No bot eligible at query time is skipped in the batch-update. (Reference: UC §3 step 4; SRS FR-EXBOT-013; AC-LC-05.) | P0 |
| TC_007 | Verify next_light_check_at lower boundary — earliest possible value is not below the minimum | 1. The Scan Worker batch-updates `next_light_check_at` with jitter applied. | 1. Run multiple Scan Worker cycles and record the minimum observed `next_light_check_at` offset from `now`.\n2. Verify the minimum offset is at or above the expected lower bound. | 1. No bot's `next_light_check_at` is set to less than `now + 4m15s` (lower BVA: `5min - 45s`). (Reference: SRS FR-EXBOT-013; AC-LC-05.) | P2 |
| TC_008 | Verify next_light_check_at upper boundary — latest possible value does not exceed the maximum | 1. The Scan Worker batch-updates `next_light_check_at` with jitter applied. | 1. Run multiple Scan Worker cycles and record the maximum observed `next_light_check_at` offset from `now`.\n2. Verify the maximum offset does not exceed the expected upper bound. | 1. No bot's `next_light_check_at` exceeds `now + 5m45s` (upper BVA: `5min + 45s`). (Reference: SRS FR-EXBOT-013; AC-LC-05.) | P2 |
| TC_009 | Verify Scan Worker does not enqueue paused bots but still reschedules them [UPDATED — D-04: Aurora PostgreSQL replaces D1] | 1. Aurora PostgreSQL contains a bot with `status='paused'` and `next_light_check_at <= now`. | 1. Trigger one Scan Worker execution.\n2. Check whether a light-check message is enqueued for the paused bot.\n3. Read `next_light_check_at` for the bot after the Scan Worker completes. | 1. No light-check message is enqueued for the paused bot.\n2. `next_light_check_at` IS updated for the bot — it is rescheduled even though it was not enqueued, preventing flood on resume. (Reference: UC §2; SRS FR-EXBOT-003; AC-LC-01 precondition.) | P0 |
| TC_010 | Verify Scan Worker makes zero HL API calls during query and enqueue phase [NEW — AC-LC-04] | 1. HL API call monitoring is active.\n2. The Scan Worker has eligible bots in Aurora PostgreSQL and processes a full scan cycle. | 1. Trigger one full Scan Worker execution (query → enqueue → batch UPDATE).\n2. Check HL API call count after the scan cycle completes. | 1. HL API call count = 0 throughout the entire Scan Worker execution — no calls to Hyperliquid are made at any step.\n2. All data is sourced from Aurora PostgreSQL only. (Reference: UC §3 steps 1–4; SRS BR-EXBOT-003; AC-LC-04.) | P0 |

### I.2. Integration & State verification — Operation: Cron fan-out + Scan Worker

| TC ID | Title | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|
| TC_011 | Verify Scan Worker applies the LIMIT per shard and defers remaining bots to the next cycle (post-downtime catch-up scenario) [UPDATED — D-04: Aurora PostgreSQL replaces D1] | 1. An Aurora PostgreSQL shard contains more eligible bots than the per-shard query limit — simulating a post-downtime backlog where many bots became eligible simultaneously.\n2. In steady state this condition does not occur because the jitter hash distributes bots evenly; this test targets the post-downtime exception. | 1. Trigger one Scan Worker cycle for the over-limit shard.\n2. Count how many bots are enqueued for light-check and how many remain eligible. | 1. Exactly the per-shard query limit of bots is enqueued (not more). (Example: if LIMIT=500, exactly 500 bots are enqueued.)\n2. The remaining bots' `next_light_check_at` stays unchanged — they remain eligible for the next EventBridge tick (1 minute later), oldest first.\n3. No bot is permanently lost — all bots are eventually processed without pagination logic. (Reference: UC §3 step 2; AC-LC-17.) | P1 |
| TC_012 | Verify a concurrent EventBridge overlap does not double-process bots through idempotency at the light-check level [UPDATED — D-03, D-04: EventBridge Scheduler; Aurora PostgreSQL] | 1. A second EventBridge Scheduler fires while the first Scan Worker Lambda invocation is still running.\n2. Both Scan Workers query the same shard for eligible bots. | 1. Let both Scan Worker Lambda instances enqueue messages for overlapping bot sets.\n2. Observe the light-check SQS queue and idempotency table after both complete. | 1. Even if duplicate light-check messages are enqueued, the Light-Check Worker processes each bot exactly once per cycle — the UNIQUE constraint on `queue_idempotency.message_id` prevents double-execution. (Reference: UC §3 step 5; SRS FR-EXBOT-011; AC-LC-06.) | P1 |
| TC_013 | Verify the end-to-end EventBridge → Scan → light-check fan-out for one active bot [UPDATED — D-03, D-04: EventBridge Scheduler; Aurora PostgreSQL] | 1. One active bot with `next_light_check_at <= now` exists in Aurora PostgreSQL, with `RebalanceReason[]` evaluating to empty.\n2. Pool Slot0 Cache (ElastiCache Redis) is healthy with valid `sqrtPriceX96` and `currentTick`. | 1. Trigger the EventBridge Scheduler and let the pipeline run from EventBridge to Light-Check Worker completion.\n2. Check the resulting Aurora PostgreSQL state and SQS queue state. | 1. EventBridge enqueues a bot-scan message; the Scan Worker enqueues one light-check message for the bot; the Light-Check Worker processes it, updates `queue_idempotency.state='succeeded'`, and updates `next_light_check_at`. HL API call count throughout the entire chain = 0. (Reference: UC §3 steps 1–13; SRS BR-EXBOT-003; AC-LC-01; AC-LC-04.) | P0 |
| TC_014 | Verify hedge_stopped_cooldown bot is included in the Scan Worker query and enqueued [UPDATED — D-04: Aurora PostgreSQL replaces D1] | 1. A bot with `lifecycle_state='hedge_stopped_cooldown'` and `status='active'` has `next_light_check_at <= now`. | 1. Trigger one Scan Worker execution.\n2. Check whether a light-check message is enqueued for the `hedge_stopped_cooldown` bot. | 1. The bot IS included in the light-check queue — `hedge_stopped_cooldown` is not a skip condition at the Scan Worker level.\n2. The Light-Check Worker will process it normally but suppress hedge-sync. (Reference: UC §2 note; SRS states.md; AC-LC-01.) | P0 |
| TC_015 | Verify EventBridge Scheduler fires at 1-minute interval and bot-scan message is visible in SQS within acceptable latency [NEW — D-03: EventBridge Scheduler] | 1. EventBridge Scheduler is configured with a 1-minute rate expression.\n2. The target SQS queue is accessible and the Scan Worker Lambda is deployed. | 1. Record the EventBridge scheduled time.\n2. Poll the bot-scan SQS queue and measure the elapsed time from scheduled fire to message visibility. | 1. The bot-scan SQS message becomes visible within 5 seconds of the scheduled fire time.\n2. No message is dropped or delivered more than once in steady state. (Reference: UC §3 step 1; SRS FR-EXBOT-010; AC-LC-01.) | P2 |



## II. Operation: Light-Check Worker — Idempotency


### II.1. Functional verification — Operation: Light-Check Worker — Idempotency

| TC ID | Title | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|
| TC_016 | Verify a new message_id is accepted and the idempotency row is created with state started [UPDATED — I-10: expires_at always set; hourly cron confirmed] | 1. The `queue_idempotency` table has no row for the incoming `message_id`. | 1. Deliver a light-check message with a fresh `message_id`.\n2. After processing completes, check the `queue_idempotency` row. | 1. The Light-Check Worker inserts a row with `state='started'` and `expires_at=now+1min` at the beginning, processes the bot, then updates the row to `state='succeeded'` at the end. No downstream fan-out message is suppressed due to idempotency.\n2. `expires_at` is always set to `now+1min` — NEVER NULL. Expired rows are purged by a separate hourly EventBridge cron job (`DELETE FROM queue_idempotency WHERE expires_at < now`) — see TC_020. (Reference: UC §3 steps 5, 13; SRS FR-EXBOT-011; AC-LC-06; AC-LC-18.) | P0 |
| TC_017 | Verify a duplicate light-check message for the same message_id exits immediately without any mutation | 1. A `queue_idempotency` row already exists for the `message_id` (with `state='started'` from a prior delivery). | 1. Deliver the same light-check message again with the same `message_id`.\n2. Check Aurora PostgreSQL state and SQS queue state after the second delivery. | 1. The Light-Check Worker detects the UNIQUE constraint conflict on insert and exits immediately.\n2. No downstream message (hedge-sync, price-near-stop-audit, partial_repair) is enqueued from the second delivery.\n3. No Aurora PostgreSQL bot state field is mutated by the second delivery. (Reference: UC §3 step 5; SRS FR-EXBOT-011; AC-LC-06.) | P0 |
| TC_018 | Verify a duplicate message with state succeeded also exits without mutation | 1. A `queue_idempotency` row exists for the `message_id` with `state='succeeded'` (prior run completed fully). | 1. Deliver the same light-check message again.\n2. Observe worker behavior and Aurora PostgreSQL/SQS state. | 1. The UNIQUE constraint conflict is detected on insert attempt; the worker exits immediately without enqueuing any message or mutating any bot state. (Reference: SRS FR-EXBOT-011; AC-LC-06.) | P1 |
| TC_019 | Verify idempotency row is finalized to succeeded after all fan-out logic completes [UPDATED — I-10: hourly cron confirmed] | 1. The Light-Check Worker has started processing a fresh message and inserted `queue_idempotency` row with `state='started'`.\n2. The bot has a rebalance reason that triggers hedge-sync enqueue. | 1. Let the Light-Check Worker run to completion.\n2. Read the `queue_idempotency` row for the message. | 1. After all fan-out operations complete (hedge-sync enqueued, stop check done), the `queue_idempotency.state` is updated to `'succeeded'` — not before the fan-out, not left as `'started'`.\n2. `expires_at` was set to `now+1min` at insert time and is not modified at finalize.\n3. Expired rows (expires_at < now) are cleaned up by a separate hourly EventBridge cron — not by the Light-Check Worker itself. (Reference: UC §3 step 13; SRS FR-EXBOT-011 AC; AC-LC-06; AC-LC-18.) | P0 |
| TC_020 | Verify hourly EventBridge cron deletes expired queue_idempotency rows where expires_at < now [NEW — I-10: hourly cron confirmed in FR-EXBOT-011 AC] | 1. The `queue_idempotency` table contains rows with `expires_at` in the past (already expired) and rows with `expires_at` in the future (still valid).\n2. The hourly EventBridge cron job is scheduled. | 1. Trigger or simulate the hourly EventBridge cron job execution.\n2. Read the `queue_idempotency` table after the cron completes. | 1. All rows with `expires_at < now` are deleted from `queue_idempotency`.\n2. Rows with `expires_at >= now` are NOT deleted — only expired rows are purged.\n3. No bot state fields (`bots`, `hedge_legs`, `bot_runtime_state`) are touched by the cron job. (Reference: SRS FR-EXBOT-011 AC; UC §3 step 5 note; AC-LC-18.) | P2 |

### II.2. Integration & State verification — Operation: Light-Check Worker — Idempotency

| TC ID | Title | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|
| TC_021 | Verify two concurrent Lambda instances racing on the same message_id result in exactly one execution [UPDATED — D-04: Aurora PostgreSQL; Lambda concurrency] | 1. Two Light-Check Worker Lambda instances receive the same light-check message simultaneously (concurrent redelivery — possible with Lambda).\n2. Both attempt to insert into `queue_idempotency` for the same `message_id` at the same time. | 1. Simulate concurrent delivery of the same message to two Lambda instances.\n2. Observe Aurora PostgreSQL `queue_idempotency` state and downstream SQS after both workers complete. | 1. Exactly one Lambda instance wins the UNIQUE constraint race and proceeds with processing.\n2. The losing instance exits immediately on the conflict.\n3. Exactly one set of fan-out messages is enqueued — no duplicates in the hedge-sync or other downstream queues. (Reference: UC §3 step 5; SRS FR-EXBOT-011; AC-LC-06.) | P0 |
| TC_022 | Verify Aurora PostgreSQL read failure after idempotency insert leaves the row in started state for retry [UPDATED — D-04: Aurora PostgreSQL replaces D1] | 1. The Light-Check Worker has successfully inserted the `queue_idempotency` row with `state='started'`.\n2. The next Aurora PostgreSQL read (bot state) is configured to fail. | 1. Let the worker fail on the bot state read after the idempotency insert.\n2. Re-deliver the same message on the next queue retry. | 1. The `queue_idempotency` row remains in `state='started'` — it is NOT updated to `succeeded` because the worker did not complete.\n2. On the next delivery of the same `message_id`, the UNIQUE constraint conflict is detected and the worker exits (treating the in-progress state as a duplicate guard). (Reference: UC §3 steps 5–6; SRS FR-EXBOT-011; AC-LC-06.) | P1 |



## III. Operation: Light-Check Worker — State read and lpEthAmount computation


### III.1. Functional verification — Operation: Light-Check Worker — State read and lpEthAmount computation

| TC ID | Title | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|
| TC_023 | Verify Light-Check Worker reads bot state from Aurora PostgreSQL without any Hyperliquid API call [UPDATED — D-04: Aurora PostgreSQL; v4-N-02: circuit_breakers.state canonical source] | 1. The bot is active with a valid `bot_runtime_state`, `hedge_legs`, and `lifecycle_state` in Aurora PostgreSQL.\n2. HL API call monitoring is active. | 1. Deliver a light-check message and let the worker read bot state.\n2. Check HL API call count after the read. | 1. `bot_runtime_state.last_known_hl_short_size`, `bots.lifecycle_state`, `hedge_legs.stop_price`, `hedge_legs.margin_status`, `hedge_legs.stop_replacing_started_at` are read from Aurora PostgreSQL.\n2. Circuit breaker state is read from the `circuit_breakers.state` column in the **`circuit_breakers`** table — NOT from a `circuit_state` field in `hedge_legs` (per SRS erd.md; v4-N-02).\n3. HL API call count = 0 during the state read phase. (Reference: UC §3 step 6; SRS BR-EXBOT-003; SRS erd.md; AC-LC-04.) | P0 |
| TC_024 | Verify Light-Check Worker reads sqrtPriceX96 and currentTick from Pool Slot0 Cache (ElastiCache Redis), not via direct RPC [UPDATED — D-01: Pool Slot0 Cache replaces MarketDataDO] | 1. Pool Slot0 Cache (ElastiCache Redis) has valid and fresh `sqrtPriceX96` and `currentTick` values.\n2. Direct on-chain RPC calls are blocked/monitored. | 1. Deliver a light-check message and observe data source for pool price data.\n2. Verify the source of `sqrtPriceX96` and `currentTick`. | 1. `sqrtPriceX96` and `currentTick` are sourced from Pool Slot0 Cache (ElastiCache Redis).\n2. No direct RPC call to the chain is made for pool slot0. HL API call count = 0. (Reference: UC §3 step 7; SRS FR-EXBOT-093; BR-EXBOT-003; AC-LC-03; AC-LC-04.) | P0 |
| TC_025 | Verify lpEthAmount is computed using Uniswap V3 TickMath + LiquidityAmounts (BigDecimal) [UPDATED — D-04: Aurora PostgreSQL replaces D1] | 1. The bot has valid `positions.liquidity`, `positions.tick_lower`, `positions.tick_upper`, `positions.weth_index` in Aurora PostgreSQL.\n2. Pool Slot0 Cache provides `sqrtPriceX96` and `currentTick`. | 1. Deliver a light-check message and observe the `lpEthAmount` computation method.\n2. Verify the arithmetic uses BigDecimal throughout, not JavaScript float. | 1. `lpEthAmount` is calculated via the Uniswap V3 AMM formula (TickMath + LiquidityAmounts) — NOT via `depositedToken − withdrawnToken + collectedFees`.\n2. All intermediate and final values use BigDecimal arithmetic — no `Number(...)` conversion used for financial amounts. (Reference: UC §3 step 8; SRS FR-EXBOT-020; FR-EXBOT-021; NFR-EXBOT-008.) | P0 |
| TC_026 | Verify wethIndex is read from Aurora PostgreSQL positions table, not hardcoded [UPDATED — D-04: Aurora PostgreSQL replaces D1] | 1. Two bots exist: one on Base chain with `positions.weth_index=0` (WETH is token0), one on Optimism with `positions.weth_index=1` (WETH is token1). | 1. Deliver light-check messages for both bots.\n2. Observe which token leg is used as the ETH leg in `lpEthAmount` for each bot. | 1. The Base chain bot uses token0 as the WETH leg when computing `lpEthAmount`.\n2. The Optimism chain bot uses token1 as the WETH leg.\n3. Neither uses a hardcoded index — the value comes from `positions.weth_index` in Aurora PostgreSQL. (Reference: SRS FR-EXBOT-021; NFR-EXBOT-009.) | P1 |
| TC_027 | Verify deltaErrorUsd for drift_threshold uses uniPoolPrice (from Pool Slot0 Cache), not hlMarkPrice [UPDATED — D-01: Pool Slot0 Cache reference] | 1. The bot has different values for `uniPoolPrice` (derived from `sqrtPriceX96` in Pool Slot0 Cache) and a hypothetical `hlMarkPrice`.\n2. Both prices differ enough that the drift decision changes depending on which is used. | 1. Deliver a light-check message where `deltaErrorUsd` computed with `uniPoolPrice` exceeds the threshold but computed with `hlMarkPrice` would not (or vice versa).\n2. Observe which price source the worker uses for `drift_threshold` evaluation. | 1. `deltaErrorUsd` is computed using `uniPoolPrice` (derived from `sqrtPriceX96` via Pool Slot0 Cache).\n2. `hlMarkPrice` and `hlOraclePrice` are not used for drift computation (3-way price split invariant). The drift decision matches the `uniPoolPrice`-based calculation. (Reference: SRS FR-EXBOT-012; AC-LC-07.) | P0 |
| TC_028 | Verify Pool Slot0 Cache stale > 5min triggers fail-fast throw and tick skip [UPDATED — D-01, D-15: Pool Slot0 Cache (ElastiCache Redis) replaces MarketDataDO] | 1. Pool Slot0 Cache (ElastiCache Redis) `updatedAt` is more than 5 minutes ago (stale). | 1. Deliver a light-check message while the cache is in a stale state.\n2. Observe whether the worker throws, skips, or attempts a forced refresh. | 1. The Light-Check Worker throws fail-fast immediately at step 7 — it does NOT attempt a forced refresh and does NOT use the stale data.\n2. The tick is skipped entirely — no trigger is evaluated, no hedge-sync or partial_repair is enqueued.\n3. `queue_idempotency.state` remains `'started'` (not advanced to `'succeeded'`), because the worker did not complete normally.\n4. No retry is emitted — the skip is clean and unconditional. (Reference: UC §3 step 7; SRS FR-EXBOT-093; AC-LC-03.) | P1 |
| TC_029 | BVA — Pool Slot0 Cache stale at exactly 5 minutes (not stale — primary source active) [NEW — AC-LC-03 BVA] | 1. Pool Slot0 Cache (ElastiCache Redis) `updatedAt` is exactly 5 minutes ago (i.e., age = 300s). | 1. Deliver a light-check message.\n2. Observe whether the worker proceeds normally or skips the tick. | 1. The worker does NOT trigger fail-fast — exactly 5 minutes is NOT stale (threshold is strictly > 5min).\n2. `sqrtPriceX96` and `currentTick` are read from Pool Slot0 Cache normally.\n3. Light-check evaluation proceeds. (Reference: UC §3 step 7; SRS FR-EXBOT-093; AC-LC-03.) | P2 |
| TC_030 | BVA — Pool Slot0 Cache stale at 5min + 1s (just past threshold — fail-fast) [NEW — AC-LC-03 BVA] | 1. Pool Slot0 Cache (ElastiCache Redis) `updatedAt` is exactly 5 minutes and 1 second ago (age = 301s). | 1. Deliver a light-check message.\n2. Observe worker behavior. | 1. The worker triggers fail-fast at step 7 — 301s exceeds the > 5min threshold.\n2. Tick is skipped entirely; no triggers evaluated; no Aurora PostgreSQL mutation; `queue_idempotency.state` remains `started`. (Reference: UC §3 step 7; SRS FR-EXBOT-093; AC-LC-03.) | P2 |

### III.2. Integration & State verification — Operation: Light-Check Worker — State read and lpEthAmount computation

| TC ID | Title | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|
| TC_031 | Verify Pool Slot0 Cache blockNumber monotonically increases and a regression triggers re-fetch [UPDATED — D-01: Pool Slot0 Cache replaces MarketDataDO] | 1. Pool Slot0 Cache (ElastiCache Redis) returns a `blockNumber` lower than the previously observed value (cache regression scenario). | 1. Simulate a Pool Slot0 Cache response where `blockNumber` is lower than the last seen value.\n2. Observe worker behavior. | 1. The Light-Check Worker detects the `blockNumber` regression.\n2. A re-fetch of the cache is triggered — the worker does not proceed with regressed data. (Reference: SRS FR-EXBOT-093.) | P2 |
| TC_032 | Verify the worker handles a missing bot in Aurora PostgreSQL gracefully without panic [UPDATED — D-04: Aurora PostgreSQL replaces D1] | 1. A light-check message is delivered for a `botId` that does not exist in Aurora PostgreSQL (bot was deleted or the message is stale). | 1. Deliver the light-check message for the non-existent botId.\n2. Observe worker error handling. | 1. The worker does not panic or throw an unhandled exception.\n2. The worker logs the not-found condition and exits cleanly (with idempotency row updated appropriately).\n3. No downstream messages are enqueued for a non-existent bot. (Reference: UC §3 step 6.) | P1 |
| TC_033 | Verify Pool Slot0 Cache (ElastiCache Redis) unreachable triggers same fail-fast behavior as stale data [UPDATED — D-01, D-15: Pool Slot0 Cache replaces MarketDataDO] | 1. Pool Slot0 Cache (ElastiCache Redis) is unreachable — Redis connection times out or returns an error. | 1. Deliver a light-check message while Pool Slot0 Cache is unreachable.\n2. Observe worker behavior. | 1. The worker throws fail-fast at step 7 — identical behavior to stale data.\n2. Tick is skipped entirely — no triggers evaluated, no Aurora PostgreSQL mutation.\n3. `queue_idempotency.state` remains `'started'`.\n4. HL API call count = 0. (Reference: AC-LC-03; SRS FR-EXBOT-093.) | P1 |
| TC_034 | Verify Lambda cold start does not cause Pool Slot0 Cache stale false-positive [NEW — Lambda cold start] | 1. The Light-Check Worker Lambda has not been invoked recently (cold start scenario — JVM/Node process initialization takes up to 2s).\n2. Pool Slot0 Cache `updatedAt` is 4m50s ago (10s before the 5min threshold). | 1. Trigger a light-check message delivery that forces a Lambda cold start.\n2. Measure the elapsed time from Lambda init to Pool Slot0 Cache read.\n3. Observe whether the worker triggers fail-fast due to apparent stale data. | 1. The worker evaluates staleness based on `updatedAt` relative to the time of the Redis read — not relative to Lambda init time.\n2. If `updatedAt` is 4m50s ago at time-of-read, no fail-fast is triggered.\n3. Cold start latency does not artificially inflate the apparent staleness of the cache. (Reference: UC §3 step 7; SRS FR-EXBOT-093; AC-LC-03.) | P2 |



## IV. Operation: Light-Check Worker — RebalanceReason[] evaluation


### IV.1. Functional verification — Operation: Light-Check Worker — RebalanceReason[] evaluation

| TC ID | Title | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|
| TC_035 | Verify drift_threshold reason is triggered when deltaErrorUsd exceeds the $25 floor | 1. The bot is small enough that `lpValueUsd × 3% < $25`, making the $25 floor the active threshold. (`lpValueUsd = max(0, lpEthAmount × currentPriceUsd)`.)\n2. `deltaErrorUsd` (computed as `|targetShortEth - actualShortEth| × uniPoolPrice`) is just above $25. (Example: deltaErrorUsd = $25.01.) | 1. Deliver a light-check message for this bot.\n2. Inspect `RebalanceReason[]` after evaluation. | 1. `drift_threshold` is included in `RebalanceReason[]`.\n2. A hedge-sync message is enqueued with `reasons` containing `drift_threshold`. (Reference: SRS FR-EXBOT-012; AC-LC-07.) | P0 |
| TC_036 | Verify drift_threshold reason is NOT triggered when deltaErrorUsd equals exactly $25 (boundary — strict greater-than) [UPDATED — v5: removed stale TS_LC_007 conflict note] | 1. The bot is small with active $25 floor threshold.\n2. `deltaErrorUsd` is exactly equal to $25.00 — at the boundary, not above it. | 1. Deliver a light-check message for this bot.\n2. Inspect `RebalanceReason[]`. | 1. `drift_threshold` is NOT in `RebalanceReason[]` — the condition is `deltaErrorUsd > max(...)`, strict greater-than, so exactly $25.00 does NOT fire.\n2. No hedge-sync is enqueued for drift_threshold specifically. (Reference: SRS FR-EXBOT-012; AC-LC-07.) | P0 |
| TC_037 | Verify drift_threshold uses the 3% of lpValueUsd ceiling when it is larger than $25 | 1. The bot is large enough that `lpValueUsd × 3% > $25`, making the percentage ceiling the active threshold. (`lpValueUsd = max(0, lpEthAmount × currentPriceUsd)`.)\n2. `deltaErrorUsd` is just above `lpValueUsd × 3%`. (Example: lpValueUsd = $2000, threshold = $60, deltaErrorUsd = $60.01.) | 1. Deliver a light-check message.\n2. Inspect `RebalanceReason[]`. | 1. `drift_threshold` is included in `RebalanceReason[]` and hedge-sync is enqueued. (Reference: SRS FR-EXBOT-012; AC-LC-07.) | P0 |
| TC_038 | Verify drift_threshold is NOT triggered at exactly 3% of lpValueUsd (large bot boundary) | 1. The bot is large with `lpValueUsd × 3% > $25`.\n2. `deltaErrorUsd` equals exactly `lpValueUsd × 3%` — at the boundary, not above. | 1. Deliver a light-check message and inspect `RebalanceReason[]`. | 1. `drift_threshold` is NOT in `RebalanceReason[]` — equal is not greater than. (Reference: SRS FR-EXBOT-012; AC-LC-07.) | P2 |
| TC_039 | Verify drift_relative reason is triggered when the relative drift exceeds 15% | 1. `|targetShortEth - actualShortEth| / targetShortEth` is just above 0.15. (Example: ratio = 0.151.)\n2. `drift_threshold` condition is NOT triggered (to isolate drift_relative). | 1. Deliver a light-check message and inspect `RebalanceReason[]`. | 1. `drift_relative` is in `RebalanceReason[]` and a hedge-sync message is enqueued with `reasons` containing `drift_relative`. (Reference: SRS FR-EXBOT-012.) | P0 |
| TC_040 | Verify drift_relative reason is NOT triggered at exactly 0.15 relative drift (boundary) | 1. `|targetShortEth - actualShortEth| / targetShortEth` equals exactly 0.15. | 1. Deliver a light-check message and inspect `RebalanceReason[]`. | 1. `drift_relative` is NOT in `RebalanceReason[]`. The condition is `> 0.15`, not `>= 0.15`. (Reference: SRS FR-EXBOT-012.) | P2 |
| TC_041 | Verify margin_warning reason is triggered when hedge_legs.margin_status is warning [UPDATED — D-04: Aurora PostgreSQL replaces D1] | 1. `hedge_legs.margin_status='warning'` is set in Aurora PostgreSQL.\n2. No HL API call is required to read margin status (it is already stored in Aurora PostgreSQL). | 1. Deliver a light-check message and inspect `RebalanceReason[]`.\n2. Verify HL API call count remains 0. | 1. `margin_warning` is in `RebalanceReason[]`.\n2. A hedge-sync message is enqueued.\n3. HL API call count = 0 — margin status is read from Aurora PostgreSQL, not fetched live from HL. (Reference: UC §3 step 9; SRS FR-EXBOT-012; FR-EXBOT-060; AC-LC-04.) | P0 |
| TC_042 | Verify time_fallback reason is triggered when the bot has not been adjusted for more than 4 hours [UPDATED — D-04: Aurora PostgreSQL replaces D1] | 1. `bot_runtime_state.last_hl_reconcile_at` is more than 4 hours in the past (stored in Aurora PostgreSQL).\n2. No other rebalance condition is triggered. | 1. Deliver a light-check message and inspect `RebalanceReason[]`. | 1. `time_fallback` is in `RebalanceReason[]` and a hedge-sync message is enqueued. (Reference: SRS FR-EXBOT-012.) | P1 |
| TC_043 | Verify range_out reason is detected and hedge-sync is enqueued with range_out in reasons; lifecycle_state NOT set by Light-Check Worker [UPDATED — D-01, D-04: Pool Slot0 Cache; Aurora PostgreSQL] | 1. `currentTick` from Pool Slot0 Cache (ElastiCache Redis) is outside the range `[positions.tick_lower, positions.tick_upper]` so that `rangeState != 'in'`.\n2. The bot's `lifecycle_state` is `active` (stored in Aurora PostgreSQL). | 1. Deliver a light-check message and inspect which SQS queue receives a message.\n2. Read `bots.lifecycle_state` from Aurora PostgreSQL after processing. | 1. `range_out` is included in `RebalanceReason[]`.\n2. One hedge-sync message is enqueued with `range_out` in `reasons`.\n3. `bots.lifecycle_state` is NOT mutated by the Light-Check Worker — it remains `active` after the light-check completes.\n4. No separate `lp_rebalancing` queue message is enqueued directly by the Light-Check Worker. (Reference: SRS FR-EXBOT-012; FR-EXBOT-015; AC-LC-11.) | P0 |
| TC_044 | Verify RebalanceReason[] uses only canonical enum values (no variant spellings) [UPDATED — v4-N-01: FR-EXBOT-023 has 9 canonical values; UC step 9 lists 7] | 1. The bot has conditions that trigger multiple reasons simultaneously. | 1. Deliver a light-check message and capture the raw `RebalanceReason[]` values.\n2. Check each value against the canonical enum. | 1. All values in `RebalanceReason[]` are from the canonical set per FR-EXBOT-023 (9 values total): `drift_threshold`, `drift_relative`, `range_out`, `range_boundary_near`, `margin_warning`, `funding_alert`, `time_fallback`, `manual_admin`, `recovery_reconcile`.\n2. `stop_trigger_crossed` is NOT in `RebalanceReason[]` — it is not a rebalance reason.\n3. Note (v4-N-01 open): UC step 9 only documents 7 of these values. It is currently unconfirmed whether the Light-Check Worker can emit `manual_admin` or `recovery_reconcile`. If either appears in output, flag as unexpected and raise to BA for clarification. (Reference: SRS FR-EXBOT-023; UC §4 A4.) | P0 |
| TC_045 | Verify RebalanceReason[] is empty when no rebalance condition is met | 1. The bot is in range, delta is within threshold, margin is ok, funding is fine, not time_fallback triggered. | 1. Deliver a light-check message and inspect `RebalanceReason[]`. | 1. `RebalanceReason[]` is empty (no reason added).\n2. No hedge-sync message is enqueued.\n3. `queue_idempotency.state` is updated to `succeeded`. (Reference: UC §3 step 9, UC §5.) | P0 |
| TC_046 | Verify range_boundary_near reason routes to hedge-sync, not to price-near-stop-audit | 1. The price-based `range_boundary_near` condition is met: `nearestFraction = min(distToLower, distToUpper) / halfRange <= 0.9`. (Example: range $3,000–$3,400, `halfRange = $200`; price = $3,020 → `distToLower = $20`, `nearestFraction = 20/200 = 0.1 <= 0.9` → fires.)\n2. Stop trigger is NOT crossed (`markPrice < stop_price`). | 1. Deliver a light-check message and inspect which queue receives a message. | 1. `range_boundary_near` is in `RebalanceReason[]`.\n2. A hedge-sync message is enqueued — not `price-near-stop-audit`.\n3. No `lp_rebalancing` lifecycle transition occurs from the Light-Check Worker (`range_boundary_near` is not a range_out condition). (Reference: UC §4 A4; AC-LC-08.) | P0 |
| TC_047 | Verify both drift_threshold and drift_relative triggered simultaneously produce one hedge-sync with both reasons | 1. Both `drift_threshold` and `drift_relative` conditions are simultaneously true for the bot. | 1. Deliver a light-check message.\n2. Inspect the hedge-sync message payload enqueued. | 1. Exactly one hedge-sync message is enqueued — not two separate messages.\n2. The `reasons` field in the message contains both `drift_threshold` and `drift_relative`. (Reference: UC §3 step 10; SRS FR-EXBOT-023; AC-LC-10.) | P1 |

### IV.2. Integration & State verification — Operation: Light-Check Worker — RebalanceReason[] evaluation

| TC ID | Title | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|
| TC_048 | Verify BigDecimal is used throughout drift and lpEthAmount computations, not JavaScript float | 1. The bot has a `deltaErrorUsd` value near a known precision-sensitive boundary. (Example: near the $25 floor where a floating-point representation might round differently.) | 1. Deliver a light-check message with precision-sensitive input values.\n2. Inspect the computation internals for any use of JavaScript `Number` arithmetic. | 1. `lpEthAmount`, `deltaErrorUsd`, and all drift comparisons use BigDecimal arithmetic throughout — no `Number(...)` conversion for financial amounts.\n2. The comparison result matches the exact BigDecimal-based calculation, not a floating-point approximation. (Reference: SRS NFR-EXBOT-008.) | P0 |
| TC_049 | Verify drift_threshold decision is consistent with the 3-way price split across multiple cycles [UPDATED — D-01: Pool Slot0 Cache reference] | 1. The bot has different `uniPoolPrice` (from Pool Slot0 Cache) vs hypothetical `hlMarkPrice` values across multiple light-check cycles. | 1. Run three consecutive light-check cycles with varying `uniPoolPrice` but stable `hlMarkPrice`.\n2. Verify each cycle's `drift_threshold` decision. | 1. Each cycle's `drift_threshold` decision is based solely on `uniPoolPrice` sourced from Pool Slot0 Cache — decisions vary with `uniPoolPrice` changes, not with `hlMarkPrice` changes.\n2. The 3-way price split invariant is maintained across all cycles. (Reference: SRS FR-EXBOT-012; AC-LC-07.) | P1 |



## V. Operation: Light-Check Worker — Fan-out (hedge-sync / lp_rebalancing)


### V.1. Functional verification — Operation: Light-Check Worker — Fan-out (hedge-sync / lp_rebalancing)

| TC ID | Title | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|
| TC_051 | Verify a rebalance reason triggers hedge-sync enqueue with correct payload when circuit is closed [UPDATED — D-04: Aurora PostgreSQL replaces D1] | 1. The bot has `circuit_breakers.state='closed'` in Aurora PostgreSQL.\n2. At least one non-range_out reason is in `RebalanceReason[]` (e.g. `drift_threshold`). | 1. Deliver a light-check message and inspect the hedge-sync SQS queue. | 1. A hedge-sync message is enqueued with payload `{botId, reasons, stateVersion}`.\n2. `stateVersion` in the message matches `bot_runtime_state.state_version` read at the time of light-check.\n3. HL API call count = 0. (Reference: UC §3 step 10; SRS FR-EXBOT-012; FR-EXBOT-027; AC-LC-04.) | P0 |
| TC_052 | Verify circuit open suppresses hedge-sync but does not suppress price-near-stop-audit | 1. `circuit_breakers.state='open'`.\n2. The bot has rebalance reasons that would normally trigger hedge-sync.\n3. `markPrice >= stop_price` (stop trigger also applies). | 1. Deliver a light-check message and inspect both the hedge-sync and price-near-stop-audit SQS queues. | 1. No hedge-sync message is enqueued (circuit open suppresses all hedge mutations).\n2. `stop_trigger_crossed_at` is set (if NULL) and `price-near-stop-audit` IS enqueued — stop monitoring continues even with circuit open. (Reference: UC §4 A2; SRS FR-EXBOT-014; AC-LC-12; AC-LC-13.) | P0 |
| TC_053 | Verify circuit half_open allows exactly one probe hedge-sync and atomically claims half_open_probe_used [UPDATED — D-04: Aurora PostgreSQL] | 1. `circuit_breakers.state='half_open'` and `circuit_breakers.half_open_probe_used=0` in Aurora PostgreSQL.\n2. The bot has a rebalance reason present. | 1. Deliver a light-check message.\n2. Inspect the hedge-sync SQS queue and `circuit_breakers` row in Aurora PostgreSQL. | 1. Exactly one hedge-sync probe message is enqueued.\n2. `circuit_breakers.half_open_probe_used` is atomically set to 1.\n3. The claim is performed as a single atomic operation — no window where probe_used=0 and hedge-sync is not yet enqueued. (Reference: UC §4 A3; SRS FR-EXBOT-040; AC-LC-16.) | P0 |
| TC_054 | Verify circuit half_open with probe already claimed suppresses hedge-sync like circuit open [UPDATED — D-04: Aurora PostgreSQL] | 1. `circuit_breakers.state='half_open'` and `circuit_breakers.half_open_probe_used=1` (already claimed) in Aurora PostgreSQL.\n2. The bot has a rebalance reason. | 1. Deliver a light-check message.\n2. Inspect the hedge-sync SQS queue. | 1. No hedge-sync message is enqueued — the worker treats this identically to circuit open (probe already used). (Reference: UC §4 A3; SRS FR-EXBOT-040; AC-LC-16.) | P0 |
| TC_055 | Verify hedge_stopped_cooldown bot does not enqueue hedge-sync even with rebalance reasons | 1. `bots.lifecycle_state='hedge_stopped_cooldown'` (status='active').\n2. The bot has a non-range_out rebalance reason present (e.g. `drift_threshold`). | 1. Deliver a light-check message and inspect the hedge-sync SQS queue. | 1. No hedge-sync message is enqueued — `hedge_stopped_cooldown` suppresses hedge-sync.\n2. Stop monitoring (price-near-stop-audit check) still runs.\n3. `queue_idempotency.state` is updated to `succeeded`. (Reference: UC §2 note; SRS states.md.) | P0 |
| TC_056 | Verify hedge-sync stateVersion matches the Aurora PostgreSQL state_version read at light-check time [UPDATED — D-04: Aurora PostgreSQL replaces D1] | 1. The bot has a known `bot_runtime_state.state_version` value in Aurora PostgreSQL.\n2. A rebalance reason triggers hedge-sync. | 1. Deliver a light-check message and inspect the enqueued hedge-sync message payload. | 1. The hedge-sync message's `stateVersion` field matches the `bot_runtime_state.state_version` value that was in Aurora PostgreSQL when the light-check read occurred.\n2. A stale or mismatched stateVersion in the hedge-sync message would cause the hedge-sync worker to discard it. (Reference: UC §3 step 10; SRS FR-EXBOT-027.) | P0 |
| TC_057 | Verify circuit breaker suppression reads circuit_breakers.state from the circuit_breakers table (canonical ERD source), not from a hedge_legs field [NEW — v4-N-02] | 1. `circuit_breakers.state='open'` for the bot in Aurora PostgreSQL (the `circuit_breakers` table).\n2. No `circuit_state` field exists in `hedge_legs` for this bot, or it is set to a different value.\n3. The bot has a rebalance reason that would normally trigger hedge-sync. | 1. Deliver a light-check message.\n2. Inspect the hedge-sync SQS queue.\n3. Read `circuit_breakers.state` and any `hedge_legs` circuit field. | 1. Hedge-sync is NOT enqueued — the circuit suppression is driven by `circuit_breakers.state='open'` from the `circuit_breakers` table (canonical source per SRS erd.md).\n2. The worker does not read a non-existent `hedge_legs.circuit_state` field — the correct join is to the `circuit_breakers` table.\n3. Stop monitoring continues as normal. (Reference: UC §3 step 6; SRS erd.md; v4-N-02.) | P0 |

### V.2. Integration & State verification — Operation: Light-Check Worker — Fan-out (hedge-sync / lp_rebalancing)

| TC ID | Title | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|
| TC_058 | Verify two concurrent half_open probes for the same bot result in exactly one probe hedge-sync [UPDATED — D-04: Aurora PostgreSQL] | 1. `circuit_breakers.state='half_open'` and `half_open_probe_used=0` in Aurora PostgreSQL.\n2. Two Light-Check Worker Lambda instances concurrently attempt to claim the probe for the same bot. | 1. Simulate two Lambda workers simultaneously attempting the atomic claim on `half_open_probe_used`.\n2. Inspect the hedge-sync SQS queue and the `circuit_breakers` row after both complete. | 1. Exactly one probe hedge-sync message is enqueued — the first Lambda wins the atomic CAS write.\n2. The second Lambda's claim fails and it suppresses hedge-sync (treating it as already-claimed = open state).\n3. `half_open_probe_used` is 1 in Aurora PostgreSQL. (Reference: UC §4 A3; SRS FR-EXBOT-040; AC-LC-16.) | P0 |
| TC_059 | Verify lp_rebalancing lifecycle suppresses subsequent Scan Worker queries for the bot [UPDATED — D-04: Aurora PostgreSQL] | 1. A light-check pass has just caused `bots.lifecycle_state='lp_rebalancing'` for a bot (set by the downstream hedge-sync handler). | 1. Run the next Scan Worker cycle.\n2. Check whether a new light-check message is enqueued for the bot. | 1. The bot is excluded from the next Scan Worker query — `lifecycle_state='lp_rebalancing'` is a skip condition.\n2. No new light-check message is enqueued for the bot until its lifecycle returns to `active`. (Reference: UC §2; SRS FR-EXBOT-012; AC-LC-02.) | P0 |
| TC_060 | Verify circuit closed transitions to open after three consecutive hedge-sync failures within 24h [UPDATED — D-04: Aurora PostgreSQL] | 1. `circuit_breakers.state='closed'` for the bot in Aurora PostgreSQL.\n2. Three consecutive hedge-sync failures are configured within a 24h window. | 1. Trigger three consecutive hedge-sync failures for the bot.\n2. On the next light-check cycle, inspect the circuit state and fan-out behavior. | 1. `circuit_breakers.state` transitions to `open` after the third failure, with `reset_at = now + 1h`.\n2. In the next light-check cycle, hedge-sync is suppressed.\n3. Stop monitoring continues — price-near-stop-audit is still triggered if stop conditions are met. (Reference: SRS FR-EXBOT-040.) | P1 |
| TC_061 | Verify circuit open transitions to half_open when reset_at is elapsed [UPDATED — D-04: Aurora PostgreSQL] | 1. `circuit_breakers.state='open'` with `reset_at` in the past (in Aurora PostgreSQL). | 1. Deliver a light-check message after `reset_at` has passed.\n2. Inspect circuit state and fan-out behavior. | 1. `circuit_breakers.state` transitions from `open` to `half_open`.\n2. One probe hedge-sync is enqueued and `half_open_probe_used` is set to 1. (Reference: SRS FR-EXBOT-040; AC-LC-16.) | P1 |



## VI. Operation: Light-Check Worker — Stop trigger detection


### VI.1. Functional verification — Operation: Light-Check Worker — Stop trigger detection

| TC ID | Title | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|
| TC_062 | Verify stop_trigger_crossed_at is set when markPrice from HL Mark Price Cache crosses stop_price and field is currently NULL [UPDATED — D-02: HL Mark Price Cache replaces HlMarkDO] | 1. `hedge_legs.stop_trigger_crossed_at` is NULL in Aurora PostgreSQL.\n2. `HL Mark Price Cache (ElastiCache Redis).markPriceUsd >= stop_price` (stop trigger condition met with the primary source).\n3. HL Mark Price Cache is fresh (not stale). | 1. Deliver a light-check message.\n2. Read `hedge_legs.stop_trigger_crossed_at` after processing. | 1. The worker reads `markPrice` from HL Mark Price Cache (ElastiCache Redis) `markPriceUsd` (primary source).\n2. `hedge_legs.stop_trigger_crossed_at` is set to a non-null timestamp (write-once guarded write).\n3. A `price-near-stop-audit` message is enqueued.\n4. No hedge-sync message is enqueued for the stop trigger itself.\n5. HL API call count = 0. (Reference: UC §3 step 11; SRS FR-EXBOT-032; BR-EXBOT-005; AC-LC-13.) | P0 |
| TC_063 | Verify stop_trigger_crossed_at is NOT overwritten when already set (write-once invariant) | 1. `hedge_legs.stop_trigger_crossed_at` is already set to a non-null timestamp in Aurora PostgreSQL.\n2. `markPrice >= stop_price` (stop trigger condition still met). | 1. Deliver another light-check message.\n2. Read `hedge_legs.stop_trigger_crossed_at` after processing. | 1. The original `stop_trigger_crossed_at` value is NOT overwritten.\n2. A `price-near-stop-audit` message is still enqueued (stop monitoring continues regardless).\n3. Write-once invariant holds — the field is set exactly once per stop event. (Reference: UC §4 A4; SRS FR-EXBOT-032; BR-EXBOT-005; AC-LC-13.) | P0 |
| TC_064 | Verify price-near-stop-audit is enqueued even when circuit breaker is open | 1. `circuit_breakers.state='open'`.\n2. `markPrice >= stop_price` and `stop_trigger_crossed_at` is NULL. | 1. Deliver a light-check message.\n2. Inspect both hedge-sync and price-near-stop-audit SQS queues. | 1. No hedge-sync is enqueued (circuit open suppresses it).\n2. `stop_trigger_crossed_at` is set and `price-near-stop-audit` IS enqueued — stop monitoring is never suppressed by the circuit breaker. (Reference: UC §4 A2; SRS FR-EXBOT-014; AC-LC-12; AC-LC-13.) | P0 |
| TC_065 | Verify price-near-stop-audit is NOT enqueued when markPrice is below stop_price | 1. `markPrice < stop_price` (stop trigger not crossed).\n2. `stop_trigger_crossed_at` is NULL. | 1. Deliver a light-check message.\n2. Check the price-near-stop-audit SQS queue. | 1. No `price-near-stop-audit` message is enqueued.\n2. `stop_trigger_crossed_at` remains NULL. (Reference: UC §3 step 11; SRS FR-EXBOT-032; AC-LC-13.) | P0 |
| TC_066 | Verify stop monitoring runs normally for a hedge_stopped_cooldown bot | 1. `bots.lifecycle_state='hedge_stopped_cooldown'` (status='active').\n2. `markPrice >= stop_price` and `stop_trigger_crossed_at` is NULL. | 1. Deliver a light-check message.\n2. Inspect Aurora PostgreSQL `stop_trigger_crossed_at` and price-near-stop-audit SQS queue. | 1. `stop_trigger_crossed_at` is set and `price-near-stop-audit` is enqueued — stop monitoring runs normally even in `hedge_stopped_cooldown`.\n2. No hedge-sync is enqueued (suppressed by `hedge_stopped_cooldown`). (Reference: UC §2 note; UC §3 step 11; AC-LC-13.) | P0 |

### VI.2. Integration & State verification — Operation: Light-Check Worker — Stop trigger detection

| TC ID | Title | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|
| TC_067 | Verify stop_trigger_crossed_at is written atomically (single Aurora PostgreSQL write) [UPDATED — D-04: Aurora PostgreSQL replaces D1] | 1. `stop_trigger_crossed_at` is NULL and stop trigger condition is met.\n2. Simulate an interruption between the NULL check and the write. | 1. Deliver a light-check message where stop is triggered.\n2. Verify the guarded write is atomic in Aurora PostgreSQL. | 1. The check-then-set operation is atomic — if two concurrent Lambda workers both see NULL, only one should succeed in setting the value.\n2. The value is set exactly once — no double-write or inconsistent state from concurrent light-checks. (Reference: UC §3 step 11; SRS FR-EXBOT-032; BR-EXBOT-005; AC-LC-13.) | P0 |
| TC_068 | Verify stop_trigger_crossed_at is checked on every light-check pass (stop monitoring always active) | 1. The bot is in `active` lifecycle_state with circuit closed.\n2. `stop_trigger_crossed_at` is NULL. | 1. Deliver three consecutive light-check messages where `markPrice < stop_price`, then one where `markPrice >= stop_price`.\n2. Observe when `price-near-stop-audit` is first enqueued. | 1. For the first three passes, no `price-near-stop-audit` is enqueued.\n2. On the fourth pass (when `markPrice >= stop_price`), `stop_trigger_crossed_at` is set and `price-near-stop-audit` is enqueued.\n3. Stop monitoring is evaluated every light-check pass — not conditionally. (Reference: UC §3 step 11; AC-LC-13.) | P1 |



## VII. Operation: Light-Check Worker — Stop replace overrun detection


### VII.1. Functional verification — Operation: Light-Check Worker — Stop replace overrun detection

| TC ID | Title | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|
| TC_069 | Verify stop replace overrun is detected when stop_replacing_started_at age exceeds 60 seconds [UPDATED — D-04: Aurora PostgreSQL replaces D1] | 1. `hedge_legs.stop_replacing_started_at` is set to a timestamp more than 60 seconds in the past (NOT NULL) in Aurora PostgreSQL.\n2. The bot is in `active` lifecycle state. | 1. Deliver a light-check message.\n2. Inspect Aurora PostgreSQL `bots.lifecycle_state` and the partial_repair SQS queue. | 1. A `partial_repair` message with `reason='stop_replacing_overrun'` is enqueued.\n2. `bots.lifecycle_state` transitions to `safe_mode`.\n3. Primary detection occurs within one 5-minute light-check window — not deferred. (Reference: UC §3 step 12; SRS FR-EXBOT-033; AC-LC-15.) | P0 |
| TC_070 | Verify stop replace overrun is NOT triggered when stop_replacing_started_at is NULL [UPDATED — D-04: Aurora PostgreSQL] | 1. `hedge_legs.stop_replacing_started_at` is NULL (no stop replace in progress) in Aurora PostgreSQL. | 1. Deliver a light-check message.\n2. Check the partial_repair SQS queue for overrun messages. | 1. No `partial_repair(reason='stop_replacing_overrun')` message is enqueued.\n2. `bots.lifecycle_state` remains unchanged. (Reference: UC §3 step 12; SRS FR-EXBOT-033; AC-LC-15.) | P0 |
| TC_071 | Verify stop replace overrun is NOT triggered at exactly 60 seconds (boundary — strict greater than) | 1. `hedge_legs.stop_replacing_started_at` is set to exactly 60 seconds ago. | 1. Deliver a light-check message at exactly t=60s from `stop_replacing_started_at`.\n2. Check overrun detection. | 1. The overrun condition `age > 60s` is NOT satisfied at exactly 60s — equal is not greater than.\n2. No `partial_repair(stop_replacing_overrun)` is enqueued and `lifecycle_state` remains unchanged. (Reference: UC §3 step 12; SRS FR-EXBOT-033; AC-LC-15.) | P0 |
| TC_072 | Verify stop replace overrun IS triggered at 61 seconds (just above boundary) | 1. `hedge_legs.stop_replacing_started_at` is set to exactly 61 seconds ago. | 1. Deliver a light-check message at t=61s from `stop_replacing_started_at`. | 1. The overrun condition `age > 60s` IS satisfied.\n2. `partial_repair(reason='stop_replacing_overrun')` is enqueued and `bots.lifecycle_state='safe_mode'` is set in Aurora PostgreSQL. (Reference: UC §3 step 12; SRS FR-EXBOT-033; AC-LC-15.) | P0 |
| TC_073 | Verify SAFE_MODE entry blocks all hedge mutations for the bot [UPDATED — D-04: Aurora PostgreSQL] | 1. A stop replace overrun has triggered `lifecycle_state='safe_mode'` for the bot in Aurora PostgreSQL. | 1. Attempt to enqueue a hedge-sync message for the bot in SAFE_MODE.\n2. Observe worker behavior on the next light-check for this bot. | 1. No hedge-sync is processed for the bot while in `safe_mode` — all hedge mutations are blocked.\n2. SAFE_MODE is not terminal — a recovery path exists (auto-recovery or bot_safe_close). (Reference: SRS FR-EXBOT-050; BR-EXBOT-007.) | P0 |

### VII.2. Integration & State verification — Operation: Light-Check Worker — Stop replace overrun detection

| TC ID | Title | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|
| TC_074 | Verify bots.status and bots.lifecycle_state are both set atomically in one Aurora PostgreSQL UPDATE before partial_repair is enqueued on SAFE_MODE entry [UPDATED — D-04: Aurora PostgreSQL replaces D1] | 1. The stop replace overrun condition is met: `stop_replacing_started_at` IS NOT NULL and age > 60s.\n2. `bots.lifecycle_state='active'` and `bots.status='active'` in Aurora PostgreSQL before the light-check. | 1. Deliver the triggering light-check message.\n2. Read both `bots.lifecycle_state` and `bots.status` after processing.\n3. Inspect the Aurora PostgreSQL write sequence and SQS enqueue ordering. | 1. Both `bots.lifecycle_state='safe_mode'` and `bots.status='safe_mode'` are set in a single atomic Aurora PostgreSQL UPDATE statement — not in two separate writes.\n2. There is no observable window where only one field is `safe_mode`.\n3. The Aurora PostgreSQL UPDATE completes BEFORE the `partial_repair` SQS message is enqueued — no state where the SQS message exists but the Aurora PostgreSQL write has not committed.\n4. `partial_repair(reason='stop_replacing_overrun')` is present in the partial_repair SQS queue after the UPDATE. (Reference: UC §3 step 12; SRS FR-EXBOT-050; FR-EXBOT-033; AC-LC-15.) | P0 |
| TC_075 | Verify deep-audit acts as a backstop when light-check misses the stop_replacing_started_at overrun | 1. `hedge_legs.stop_replacing_started_at` has been set for more than 60 seconds, but the light-check cycle that would have detected it was delayed or missed.\n2. Deep-audit is scheduled to run (normally at 6h cadence, reduced to 1h in high-risk mode). | 1. Skip or simulate a missed light-check cycle.\n2. Trigger a deep-audit cycle.\n3. Observe whether the overrun is detected. | 1. Deep-audit detects `stop_replacing_started_at` stuck > 60s and routes to SAFE_MODE / repair as a secondary backstop.\n2. The primary detection (light-check, within ≤5 min) is preferred, but deep-audit ensures no overrun is permanently missed. (Reference: UC §3 step 12; SRS FR-EXBOT-033.) | P2 |

### VII.3. Non-functional (logic) verification — Operation: Light-Check Worker — Stop replace overrun detection

| TC ID | Title | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|
| TC_076 | Verify SAFE_MODE is not a terminal state and a recovery path exists | 1. The bot has been placed in `lifecycle_state='safe_mode'` by a stop replace overrun.\n2. HL is responsive and margin_status recovers to `ok`. | 1. Simulate three consecutive successful reconcile operations for the bot in SAFE_MODE.\n2. Observe whether the bot transitions out of SAFE_MODE. | 1. After three consecutive reconcile successes AND `margin_status='ok'`, the bot transitions from `safe_mode` back to `active` (auto-recovery path).\n2. If auto-recovery is not possible, `bot_safe_close` is the fallback — SAFE_MODE is never a permanent dead-end. (Reference: SRS FR-EXBOT-050; BR-EXBOT-007.) | P0 |
| TC_077 | Verify Light-Check Worker Lambda does not exhaust Aurora PostgreSQL connection pool under normal operation [UPDATED — D-06: NFR-EXBOT-011 updated for AWS Lambda] | 1. The Light-Check Worker Lambda is processing a bot and makes connections to Aurora PostgreSQL and ElastiCache Redis.\n2. Aurora PostgreSQL has a configured `max_connections` limit (potentially enforced via RDS Proxy). | 1. Deliver a light-check message and observe the number of Aurora PostgreSQL connections opened during the Lambda invocation.\n2. Monitor for any connection pool exhaustion errors. | 1. The Lambda invocation completes without any connection pool exhaustion error.\n2. The number of Aurora PostgreSQL connections used by the Lambda does not exceed the RDS Proxy connection limit.\n3. Connections are released or returned to the pool before Lambda function handler exits. (Reference: SRS NFR-EXBOT-011; arc-migration 2026-07-04.) | P2 |
| TC_078 | Verify Aurora PostgreSQL connection pool exhaustion under shard burst does not cause light-check tick loss [NEW — NFR-EXBOT-011; Aurora PostgreSQL connection limit] | 1. Multiple Lambda instances are concurrently processing light-check messages for the same shard (burst scenario).\n2. Aurora PostgreSQL RDS Proxy has a configured max connection limit. | 1. Simulate a burst of concurrent Lambda invocations exceeding the RDS Proxy connection limit.\n2. Observe whether any light-check messages are silently dropped or processed with no Aurora PostgreSQL write (tick loss). | 1. Lambda invocations that cannot acquire a connection receive an explicit error and the SQS message becomes visible again for retry — no silent tick loss.\n2. After the burst subsides, all ticks are processed successfully.\n3. `queue_idempotency.state` for affected ticks remains `started` until the tick is retried and completed. (Reference: SRS NFR-EXBOT-011; AC-LC-15.) | P1 |



## VIII. Operation: Light-Check Worker — HL Mark Price Cache staleness handling

### VIII.1. Functional verification — Operation: Light-Check Worker — HL Mark Price Cache staleness handling

| TC ID | Title | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|
| TC_079 | Verify Light-Check Worker reads hlMarkPrice from HL Mark Price Cache (ElastiCache Redis) as primary source for stop trigger [UPDATED — D-02: HL Mark Price Cache replaces HlMarkDO] | 1. HL Mark Price Cache (ElastiCache Redis) `markPriceUsd` is fresh (last updated less than 120 seconds ago).\n2. `markPriceUsd >= stop_price` so stop trigger condition is met.\n3. `stop_trigger_crossed_at` is NULL. | 1. Deliver a light-check message.\n2. Confirm which source provided the `markPrice` used for stop trigger evaluation.\n3. Read `stop_trigger_crossed_at` after processing. | 1. `markPrice` is sourced from HL Mark Price Cache (ElastiCache Redis) `markPriceUsd` — the primary source.\n2. `stop_trigger_crossed_at` is set (write-once).\n3. `price-near-stop-audit` is enqueued.\n4. HL API call count = 0. (Reference: UC §3 step 11; AC-LC-13; AC-LC-14.) | P0 |
| TC_080 | Verify HL Mark Price Cache stale > 120s triggers fallback to eth_price_usd for stop trigger evaluation [UPDATED — D-02: HL Mark Price Cache replaces HlMarkDO] | 1. HL Mark Price Cache (ElastiCache Redis) `markPriceUsd` last updated more than 120 seconds ago (stale).\n2. `bot_runtime_state.eth_price_usd` is available and is the fallback (stored in Aurora PostgreSQL). | 1. Deliver a light-check message.\n2. Observe which price source the worker uses for stop trigger evaluation. | 1. The worker falls back to `bot_runtime_state.eth_price_usd` from Aurora PostgreSQL — the HL Mark Price Cache primary source is not used.\n2. Stop trigger evaluation proceeds with the fallback price.\n3. HL API call count = 0. (Reference: UC §3 step 11; AC-LC-14.) | P0 |
| TC_081 | Verify HL Mark Price Cache stale > 120s widens near-stop band from 2% to 4% [UPDATED — D-02: HL Mark Price Cache replaces HlMarkDO] | 1. HL Mark Price Cache (ElastiCache Redis) `markPriceUsd` is stale > 120s.\n2. The bot has a near-stop band configured at the default 2%. | 1. Deliver a light-check message.\n2. Observe the near-stop band width used for stop trigger evaluation. | 1. The near-stop band is widened from 2% to 4% — stop trigger is evaluated with the wider band.\n2. A stop that would not fire at 2% band may fire at 4% band with the fallback price. (Reference: AC-LC-14.) | P0 |
| TC_082 | Verify HL Mark Price Cache stale > 120s freezes hedge-sync even when rebalance reasons are present [UPDATED — D-02: HL Mark Price Cache replaces HlMarkDO] | 1. HL Mark Price Cache (ElastiCache Redis) `markPriceUsd` is stale > 120s.\n2. The bot has at least one active rebalance reason (e.g., `drift_threshold`) that would normally trigger hedge-sync. | 1. Deliver a light-check message.\n2. Inspect the hedge-sync SQS queue after processing. | 1. No hedge-sync message is enqueued — hedge-sync is frozen due to HL Mark Price Cache staleness.\n2. The rebalance reasons were detected but suppressed.\n3. Stop monitoring (step 11) still runs using the fallback price. (Reference: AC-LC-14.) | P0 |
| TC_083 | BVA — HL Mark Price Cache stale at exactly 120s (not stale — primary source active, 2% band, no freeze) [UPDATED — D-02: HL Mark Price Cache replaces HlMarkDO] | 1. HL Mark Price Cache (ElastiCache Redis) `markPriceUsd` last updated exactly 120 seconds ago. | 1. Deliver a light-check message.\n2. Observe which price source is used and whether hedge-sync is frozen. | 1. The worker uses HL Mark Price Cache `markPriceUsd` as primary source — the staleness threshold is strictly > 120s, so exactly 120s is NOT stale.\n2. Near-stop band remains at 2% (not widened).\n3. Hedge-sync is NOT frozen. (Reference: AC-LC-14.) | P2 |
| TC_084 | BVA — HL Mark Price Cache stale at 121s (just past threshold — fallback active, 4% band, freeze) [UPDATED — D-02: HL Mark Price Cache replaces HlMarkDO] | 1. HL Mark Price Cache (ElastiCache Redis) `markPriceUsd` last updated exactly 121 seconds ago. | 1. Deliver a light-check message.\n2. Observe price source, band width, and hedge-sync behavior. | 1. The worker falls back to `bot_runtime_state.eth_price_usd`.\n2. Near-stop band widens to 4%.\n3. Hedge-sync is frozen. (Reference: AC-LC-14.) | P2 |
| TC_085 | Verify HL Mark Price Cache Redis key TTL aligns with 120s staleness policy [NEW — D-02: ElastiCache Redis TTL for HL Mark Price Cache] | 1. HL Mark Price Cache is stored as an ElastiCache Redis key.\n2. The Redis key has a TTL configured. | 1. Inspect the Redis key TTL for the HL Mark Price Cache key.\n2. Simulate TTL expiry before the Fargate HL WS Poller refreshes the key. | 1. The Redis key TTL is NOT less than the 120s staleness threshold — if TTL < 120s, the key may expire and be absent before the staleness check fires, causing an unexpected key-not-found condition.\n2. The worker handles a missing (evicted) HL Mark Price Cache key identically to a stale key: fallback to `eth_price_usd`, widen band to 4%, freeze hedge-sync. (Reference: AC-LC-14; UC §3 step 11.) | P2 |
| TC_086 | Verify HL Mark Price Cache stale hedge-sync freeze is scoped to the current tick only — next tick resumes normally [NEW — v4-N-04] | 1. In tick N: HL Mark Price Cache `updatedAt > 120s` (stale). The bot has a rebalance reason (e.g. `drift_threshold`) that would normally enqueue hedge-sync.\n2. In tick N+1: HL Mark Price Cache `updatedAt <= 120s` (fresh). Same bot, same rebalance reason. | 1. Deliver light-check message for tick N and observe hedge-sync queue.\n2. Deliver light-check message for tick N+1 and observe hedge-sync queue. | 1. Tick N: Hedge-sync is NOT enqueued — frozen due to HL Mark Price Cache staleness. Stop monitoring runs with fallback `eth_price_usd` and 4% band.\n2. Tick N+1: Hedge-sync IS enqueued normally — the freeze does not persist into subsequent ticks. No manual intervention is required to unfreeze.\n3. There is no persistent freeze flag in Aurora PostgreSQL set by tick N that would carry over to tick N+1. (Reference: UC §3 step 11; SRS flows.md F-01; v4-N-04.) | P0 |



## IX. Operation: Light-Check Worker — funding_alert evaluation

### IX.1. Functional verification — Operation: Light-Check Worker — funding_alert evaluation

| TC ID | Title | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|
| TC_087 | Verify funding_alert fires when fundingApr7dPct is below -15% threshold (primary source) [UPDATED — v3-I-03 partial: funding_rolling_metrics in ERD; DEFERRED to v2+ — bnza-market-cron not deployed in v1] | 1. `funding_rolling_metrics.fundingApr7dPct` is set to a value well below -15% (e.g., -20%).\n2. No other rebalance reason is triggered.\n3. bnza-market-cron worker is deployed (v2+ scope — skip in v1). | 1. Deliver a light-check message.\n2. Inspect `RebalanceReason[]`. | 1. `funding_alert` is in `RebalanceReason[]`.\n2. The worker reads `fundingApr7dPct` directly from `funding_rolling_metrics` in Aurora PostgreSQL (primary path).\n3. Hedge-sync is enqueued. HL API call count = 0. (Reference: AC-LC-09.) | P0 |
| TC_088 | BVA — funding_alert at exactly -15% is NOT triggered (strict less-than) [UPDATED — v3-I-03 partial: applies to both primary (fundingApr7dPct, v2+) and fallback (fundingRate × 8760, v1)] | 1. `funding_rolling_metrics.fundingApr7dPct = -15.0` (exact threshold). | 1. Deliver a light-check message.\n2. Inspect `RebalanceReason[]`. | 1. `funding_alert` is NOT in `RebalanceReason[]` — the condition is `< -15%`, not `<= -15%`. Equal to -15% does not satisfy strict less-than.\n2. No hedge-sync enqueued for funding_alert. (Reference: AC-LC-09.) | P2 |
| TC_089 | BVA — funding_alert at -15.01% IS triggered (just past threshold) [UPDATED — v3-I-03 partial: applies to both primary (fundingApr7dPct, v2+) and fallback (fundingRate × 8760, v1)] | 1. `funding_rolling_metrics.fundingApr7dPct = -15.01`. | 1. Deliver a light-check message.\n2. Inspect `RebalanceReason[]`. | 1. `funding_alert` IS in `RebalanceReason[]` — -15.01% is strictly less than -15%.\n2. Hedge-sync is enqueued. (Reference: AC-LC-09.) | P2 |
| TC_090 | Verify funding_alert fallback — uses fundingRate × 8760 when fundingApr7dPct is unavailable [UPDATED — v3-I-03 partial: fallback path testable in v1; primary deferred to v2+] | 1. `funding_rolling_metrics` row for the bot has `fundingApr7dPct = NULL` or the row is missing.\n2. `fundingRate` is set such that `fundingRate × 8760 < -0.15` (annualized per-hour rate triggers threshold). | 1. Deliver a light-check message.\n2. Inspect `RebalanceReason[]` and the data source used. | 1. The worker falls back to the annualization formula `fundingRate × 8760`.\n2. `funding_alert` fires and is in `RebalanceReason[]`.\n3. Hedge-sync is enqueued. (Reference: AC-LC-09.) | P1 |
| TC_091 | Verify funding_alert fires via fallback path (fundingRate × 8760) when funding_rolling_metrics is empty (v1 scope) [NEW — TS_LC_046; v3-I-03 partial] | 1. `funding_rolling_metrics` table is empty (bnza-market-cron not deployed in v1).\n2. `bot_runtime_state.last_funding_rate` (or equivalent Aurora PostgreSQL cached field) is set such that `fundingRate × 8760 < -0.15` (annualized rate below -15% APR). Example: `fundingRate = -0.00002` → `fundingRate × 8760 = -0.1752 = -17.52%`.\n3. No other rebalance condition is triggered (to isolate funding_alert). | 1. Deliver a light-check message.\n2. Inspect `RebalanceReason[]` and confirm data source used. | 1. `funding_alert` is in `RebalanceReason[]` — the fallback formula `fundingRate × 8760` computed a value below -15%.\n2. The worker did NOT read `fundingApr7dPct` from `funding_rolling_metrics` (table is empty) — fallback path executed.\n3. Hedge-sync is enqueued with `reasons=['funding_alert']`.\n4. HL API call count = 0 — `fundingRate` is sourced from Aurora PostgreSQL (cached field), NOT from a live HL API call. (Note v4-N-03: BA to confirm exact field name; assumption is Aurora source.) (Reference: UC §3 step 9; SRS FR-EXBOT-023; TS_LC_046.) | P1 |
| TC_092 | BVA — funding_alert fallback: fundingRate × 8760 at exactly -15% APR does NOT fire; at -15.01% it fires [NEW — TS_LC_047; BVA] | 1. `funding_rolling_metrics` table is empty (v1 scope — fallback path).\n2. For Step 1: `fundingRate` such that `fundingRate × 8760 = -0.15` (exactly -15.00% APR). Example: `fundingRate ≈ -0.00001712`.\n3. For Step 3: `fundingRate` such that `fundingRate × 8760 = -0.1501` (-15.01% APR). | 1. Deliver a light-check message with `fundingRate × 8760 = -15.00%`.\n2. Verify `funding_alert` is NOT in `RebalanceReason[]`.\n3. Deliver a light-check message with `fundingRate × 8760 = -15.01%`.\n4. Verify `funding_alert` IS in `RebalanceReason[]`. | 1. At -15.00% (exactly at threshold): `funding_alert` is NOT in `RebalanceReason[]` — condition is strictly less-than (`< -15%`), equal does not satisfy it. No hedge-sync enqueued for funding_alert.\n2. At -15.01% (just past threshold): `funding_alert` IS in `RebalanceReason[]`. Hedge-sync is enqueued. HL API call count = 0 in both cases. (Reference: UC §3 step 9; SRS FR-EXBOT-023; TS_LC_047.) | P2 |



## X. Operation: Light-Check Worker — Additional coverage

### X.1. Functional verification — Operation: Light-Check Worker — Additional coverage

| TC ID | Title | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|
| TC_093 | Verify mixed reasons range_out + drift_threshold produce single hedge-sync; lifecycle_state NOT set by Light-Check Worker [UPDATED — D-01, D-04: Pool Slot0 Cache; Aurora PostgreSQL] | 1. Bot simultaneously triggers `range_out` (tick outside range per Pool Slot0 Cache) AND `drift_threshold` (delta error above threshold) in the same light-check pass.\n2. Circuit breaker is closed. | 1. Deliver a light-check message.\n2. Inspect hedge-sync SQS queue and Aurora PostgreSQL `bots.lifecycle_state`. | 1. Engine gathers both `range_out` and `drift_threshold` into `decision.reason[]`.\n2. Exactly one hedge-sync message is enqueued with both reasons present (e.g., `reasons: ['range_out', 'drift_threshold']`).\n3. `bots.lifecycle_state` is NOT mutated by the Light-Check Worker — it remains at its current value after the light-check.\n4. No `partial_repair` or `lp_rebalancing` message is enqueued directly by Light-Check Worker. (Reference: AC-LC-10; AC-LC-11.) | P0 |
| TC_094 | Verify lpValueUsd = 0 edge case — drift_threshold floor is $25 | 1. The bot's `lpEthAmount = 0` (position fully out of range, both tokens at boundary).\n2. `currentPriceUsd > 0`. | 1. Deliver a light-check message.\n2. Observe the drift_threshold value used for evaluation. | 1. `lpValueUsd = max(0, 0 × currentPriceUsd) = $0`.\n2. `drift_threshold = max($25, $0 × 3%) = $25` — the $25 floor applies.\n3. Subsequent drift_threshold evaluation uses $25, not $0 or a negative value. (Reference: AC-LC-07.) | P2 |
| TC_095 | Verify bot in safe_mode lifecycle — Light-Check Worker skips all evaluation and produces zero downstream messages [NEW — TS_LC_051] | 1. Bot has `bots.lifecycle_state='safe_mode'` and `bots.status='safe_mode'` in Aurora PostgreSQL (entered via prior stop_replacing_overrun).\n2. A light-check SQS message has been delivered for this bot (Scan Worker may still enqueue if status filtering doesn't exclude safe_mode at query time). | 1. Deliver a light-check message for the safe_mode bot.\n2. Inspect Aurora PostgreSQL state and all downstream SQS queues. | 1. The Light-Check Worker exits without performing any trigger evaluation — `safe_mode` is not a valid precondition state for trigger evaluation (per UC §2).\n2. No hedge-sync, price-near-stop-audit, or partial_repair message is enqueued.\n3. No Aurora PostgreSQL fields are mutated by this light-check pass.\n4. `queue_idempotency.state` is updated to `'succeeded'` (message processed cleanly — skip is intentional, not an error). (Reference: UC §2; SRS states.md; TS_LC_051.) | P0 |

### X.2. Integration & State verification — Operation: Light-Check Worker — Additional coverage

| TC ID | Title | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|
| TC_096 | Verify next_light_check_at is updated for lp_rebalancing bot even though it is not enqueued for light-check [UPDATED — D-04: Aurora PostgreSQL replaces D1] | 1. Bot has `lifecycle_state='lp_rebalancing'` and `next_light_check_at <= now` in Aurora PostgreSQL. | 1. Run the Scan Worker.\n2. Check the light-check SQS queue for this bot.\n3. Read `next_light_check_at` for the bot after the Scan Worker completes. | 1. The bot is NOT enqueued in the light-check queue.\n2. `next_light_check_at` IS updated to `now + 5min + jitter(±45s)` in the Aurora PostgreSQL batch UPDATE.\n3. The bot will resume normal light-check scheduling after its lifecycle returns to `active`, without falling behind. (Reference: AC-LC-02; AC-LC-05.) | P2 |
| TC_097 | Verify next_light_check_at is updated for paused bot even though it is not enqueued for light-check [UPDATED — D-04: Aurora PostgreSQL replaces D1] | 1. Bot has `status='paused'` and `next_light_check_at <= now` in Aurora PostgreSQL. | 1. Run the Scan Worker.\n2. Check the light-check SQS queue for this bot.\n3. Read `next_light_check_at` for the bot after the Scan Worker completes. | 1. The bot is NOT enqueued in the light-check queue.\n2. `next_light_check_at` IS updated to `now + 5min + jitter(±45s)` in Aurora PostgreSQL.\n3. On resume, the bot will not flood the light-check queue. (Reference: AC-LC-05.) | P2 |
| TC_098 | Verify SAFE_MODE atomic write — both bots.status and bots.lifecycle_state set in single Aurora PostgreSQL UPDATE, UPDATE completes before partial_repair enqueued [UPDATED — D-04: Aurora PostgreSQL replaces D1] | 1. `hedge_legs.stop_replacing_started_at` IS NOT NULL and age > 60s in Aurora PostgreSQL.\n2. Bot is in `lifecycle_state='active'`, `status='active'`. | 1. Trigger the stop_replacing_overrun condition.\n2. Inspect the Aurora PostgreSQL write sequence and SQS enqueue ordering. | 1. `bots.status='safe_mode'` AND `bots.lifecycle_state='safe_mode'` are set in a single atomic `UPDATE bots SET status='safe_mode', lifecycle_state='safe_mode' WHERE id=?` in Aurora PostgreSQL.\n2. No observable window exists where only one field is `safe_mode`.\n3. The `partial_repair(reason='stop_replacing_overrun')` SQS message is enqueued AFTER the Aurora PostgreSQL UPDATE commits — no state where SQS message exists without both Aurora PostgreSQL fields being `safe_mode`. (Reference: AC-LC-15; SRS FR-EXBOT-033; FR-EXBOT-050.) | P0 |
| TC_099 | Verify LIMIT 500 steady-state design — jitter hash distribution prevents shard overflow [UPDATED — D-04: Aurora PostgreSQL replaces D1] | 1. An Aurora PostgreSQL shard has approximately 10,000 active bots with `next_light_check_at` values uniformly distributed across the 5-minute window via the deterministic `hash(botId)` jitter.\n2. This represents steady-state operation (no recent downtime or mass activation). | 1. Observe one full Scan Worker cycle for the shard.\n2. Count eligible bots (those with `next_light_check_at <= now`) at cycle start. | 1. The number of eligible bots at any given scan cycle is at or below the LIMIT (e.g., 500) — the jitter hash distributes bots evenly across the 5-minute window.\n2. No overflow condition is encountered in steady state.\n3. All eligible bots are enqueued in one cycle without deferral. (Reference: AC-LC-17.) | P2 |
| TC_100 | Verify post-downtime catch-up — bots over LIMIT 500 processed oldest-first across next EventBridge ticks, no bot lost [UPDATED — D-03, D-04: EventBridge; Aurora PostgreSQL] | 1. An Aurora PostgreSQL shard has many more than 500 bots with `next_light_check_at` overdue — simulating recovery after a downtime period.\n2. Bots have varied `next_light_check_at` values, some significantly overdue. | 1. Run multiple consecutive Scan Worker cycles (1 per EventBridge tick = 1 minute).\n2. Track which bots are processed and in what order across cycles. | 1. Each Scan Worker cycle processes up to LIMIT bots (oldest `next_light_check_at` first).\n2. All overdue bots are eventually processed — no bot is permanently skipped or lost.\n3. Each processed bot has its `next_light_check_at` updated after the cycle, removing it from the overdue pool.\n4. No pagination logic is required — the catch-up happens naturally across successive EventBridge ticks. (Reference: AC-LC-17.) | P1 |
| TC_101 | Verify Aurora PostgreSQL batch UPDATE uses parameterized query (no SQL injection surface) [NEW — NFR; Aurora PostgreSQL replaces D1] | 1. The Scan Worker is about to batch UPDATE `next_light_check_at` for a set of bot IDs.\n2. One of the bot IDs in the batch contains SQL special characters (e.g., `'; DROP TABLE bots; --`). | 1. Trigger a Scan Worker execution with a malformed `botId` containing SQL injection payload.\n2. Observe whether the Aurora PostgreSQL batch UPDATE executes safely. | 1. The batch UPDATE uses parameterized query / prepared statement — the malformed `botId` is treated as a literal string value, not executable SQL.\n2. No Aurora PostgreSQL table is dropped or modified beyond the intended batch UPDATE.\n3. The malformed `botId` row receives a normal UPDATE (if it matches) or no UPDATE (if no match), without error. (Reference: SRS NFR; OWASP Top 10 A03:2021 Injection; arc-migration.) | P2 |



## XI. QC Review — Gap Fill (2026-07-08)


### XI.1. Functional verification — QC Review gap fill

| TC ID | Title | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|
| TC_102 | Verify range_boundary_near does NOT fire when nearestFraction is just above 0.9 threshold [NEW — TS_LC_006 negative BVA] | 1. `nearestFraction = min(distToLower, distToUpper) / halfRange = 0.91` — just above the 0.9 threshold.\n2. No other rebalance condition is triggered. | 1. Deliver a light-check message for this bot.\n2. Inspect `RebalanceReason[]`. | 1. `range_boundary_near` is NOT in `RebalanceReason[]` — `nearestFraction = 0.91 > 0.9` does not satisfy the `<= 0.9` condition.\n2. No hedge-sync is enqueued for range_boundary_near.\n3. Confirms the boundary pair: exactly 0.9 fires (TC_051); 0.91 does not. (Reference: UC §3 step 9; AC-LC-08.) | P2 |
| TC_103 | Verify Light-Check Worker does NOT mutate circuit_breakers.state during a light-check pass [NEW — TS_LC_036 state boundary] | 1. `circuit_breakers.state='closed'` in Aurora PostgreSQL before the light-check.\n2. A rebalance reason is present and hedge-sync is enqueued. | 1. Deliver a light-check message.\n2. Read `circuit_breakers.state` from Aurora PostgreSQL after the Light-Check Worker completes. | 1. `circuit_breakers.state` remains `'closed'` — the Light-Check Worker never writes to `circuit_breakers.state`.\n2. Circuit state transitions (`closed → open` after 3 failures) are driven by the hedge-sync handler downstream, not by the Light-Check Worker.\n3. Invariant holds regardless of how many rebalance reasons are triggered. (Reference: UC §3 step 10; SRS FR-EXBOT-040.) | P0 |

### XI.2. Integration & State verification — QC Review gap fill

| TC ID | Title | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|
| TC_105 | Verify price-near-stop-audit SQS message payload contains at minimum botId and is separate from hedge-sync queue [NEW — TS_LC_040 payload verification] | 1. `markPriceUsd >= hedge_legs.stop_price` and `stop_trigger_crossed_at` is NULL.\n2. HL Mark Price Cache is fresh.\n3. Bot also has a rebalance reason (e.g., `drift_threshold`) to verify both queues receive separate messages. | 1. Deliver a light-check message.\n2. Inspect the `price-near-stop-audit` SQS queue and the `hedge-sync` SQS queue after processing. | 1. The `price-near-stop-audit` SQS message is enqueued in its own queue and contains at minimum `botId`.\n2. The `hedge-sync` message (for the rebalance reason) is a separate message in a separate queue — stop trigger and rebalance fan-out do NOT share the same SQS message or queue.\n3. `stop_trigger_crossed_at` is set in Aurora PostgreSQL.\n4. HL API call count = 0. (Reference: UC §3 step 11; SRS FR-EXBOT-032; AC-LC-13.) | P0 |
| TC_106 | Verify jitter offset for next_light_check_at is deterministic per botId across Scan Worker cycles [NEW — TS_LC_029 determinism] | 1. A bot has a known `botId`.\n2. The Scan Worker runs two consecutive cycles that both update `next_light_check_at` for that bot. | 1. Record the jitter offset (offset = new_next_light_check_at - cycle_time) after the first Scan Worker cycle.\n2. Record the jitter offset after the second Scan Worker cycle.\n3. Compare the two offsets for the same botId. | 1. Both offsets are identical — the offset is derived from `hash(botId) mod jitter_range`, not from a random number generator.\n2. The offset falls within ±45s of the 5-minute base interval.\n3. A different botId produces a different offset (hash distributes evenly), but the same botId always produces the same offset. (Reference: UC §3 step 4; SRS FR-EXBOT-013.) | P2 |
| TC_107 | E2E acceptance — 3-bot EventBridge tick: drift bot enqueues hedge-sync, stop bot sets stop_trigger_crossed_at, no-action bot finishes clean [NEW — TS_LC_042] | 1. Three active bots in Aurora PostgreSQL, all with `next_light_check_at <= now`: Bot A has `drift_threshold` condition true; Bot B has `markPriceUsd >= stop_price` and `stop_trigger_crossed_at IS NULL`; Bot C has all conditions within safe bounds.\n2. Both HL Mark Price Cache and Pool Slot0 Cache are fresh.\n3. HL API call monitoring is active. | 1. Trigger one EventBridge tick and let the full pipeline (EventBridge → Scan Worker → 3× Light-Check Worker) complete.\n2. After all three workers complete, read Aurora PostgreSQL state and inspect SQS queues. | 1. Bot A: One `hedge-sync` message enqueued with `reasons=['drift_threshold']`; `queue_idempotency.state='succeeded'`.\n2. Bot B: `hedge_legs.stop_trigger_crossed_at` set (non-null); one `price-near-stop-audit` enqueued; no hedge-sync for stop trigger; `queue_idempotency.state='succeeded'`.\n3. Bot C: No `hedge-sync`, no `price-near-stop-audit`; `queue_idempotency.state='succeeded'`.\n4. `next_light_check_at` advanced for all three bots to `now + 5min ± 45s`.\n5. HL API call count = 0 across all three bots and all pipeline stages. (Reference: UC §3 step 1–13; SRS BR-EXBOT-003; AC-LC-04.) | P0 |



## XII. Operation: Acceptance — Light-Check Worker integration

### XII.1. Acceptance verification

| TC ID | Title | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|
| TC_108 | Acceptance: range_out triggers hedge-sync; Light-Check Worker does NOT set lifecycle_state='lp_rebalancing' [NEW — TS_LC_052; v3-I-02 resolved; AC-LC-14] | 1. Bot has `bots.lifecycle_state='active'` in Aurora PostgreSQL.\n2. `currentTick` from Pool Slot0 Cache (ElastiCache Redis) is outside `[positions.tick_lower, positions.tick_upper]` — range_out condition is true.\n3. Circuit breaker is `closed`. | 1. Deliver a light-check message and let the worker complete.\n2. Read `bots.lifecycle_state` from Aurora PostgreSQL after processing.\n3. Inspect the hedge-sync SQS queue. | 1. `range_out` is in `decision.reason[]` and exactly one hedge-sync message is enqueued with `reasons=['range_out']` (or with other co-triggered reasons if present).\n2. `bots.lifecycle_state` in Aurora PostgreSQL is NOT changed by the Light-Check Worker — it remains `'active'` after the light-check completes.\n3. No separate `lp_rebalancing` queue message is enqueued by the Light-Check Worker.\n4. The `lifecycle_state='lp_rebalancing'` transition belongs exclusively to the downstream hedge-sync worker (per US-EXBOT-007 AC-007-1, corrected via v3-I-02). HL API call count = 0. (Reference: UC §3 step 10; US-EXBOT-007 AC-007-4; AC-LC-14; v3-I-02 resolved 2026-07-14.) | P0 |

