# Test Cases — UC-EXBOT-light-check Execute Periodic Light-Check (v2)

**Total test cases:** 88 (Delta vs v1: +18 new, ~14 updated, -2 retired) (FUNC: 61, INTG: 25, NFR: 2)
**Scope:** Logic-only (backend / API / bot — no UI)
**Update trigger:** REQUIREMENT_DELTA
**Source UC:** UC-EXBOT-light-check_light-check_audited_20260703_v2.md
**Source scenarios (if any):** UC-EXBOT-light-check_light-check_scenarios_20260703_v2.md
**Output language:** English

#### Requirement Traceability Matrix

| AC ID | Acceptance Criteria | Linked Test Cases | Status |
|---|---|---|---|
| AC-LC-01 | Happy path — zero HL calls invariant | TC_001, TC_002, TC_012, TC_030, TC_031 | Covered |
| AC-LC-02 | Idempotency — duplicate message exits immediately, no mutation | TC_020, TC_021, TC_022 | Covered |
| AC-LC-03 | Skip — lp_rebalancing not enqueued; next_light_check_at still updated | TC_003, TC_101 | Covered [UPDATE] |
| AC-LC-04 | Skip — paused not enqueued; next_light_check_at still updated | TC_009, TC_102 | Covered [UPDATE] |
| AC-LC-05 | hedge_stopped_cooldown runs normally, hedge-sync suppressed | TC_066, TC_076 | Covered |
| AC-LC-06 | Stop trigger detected — HlMarkDO primary source | TC_072, TC_089 | Covered [UPDATE] |
| AC-LC-07 | Stop trigger already set — no overwrite, price-near-stop-audit still enqueued | TC_073 | Covered |
| AC-LC-08 | Circuit open — suppress hedge-sync, stop monitoring continues | TC_061, TC_074 | Covered |
| AC-LC-09 | Circuit half_open — 1 probe only, half_open_probe_used set atomically | TC_062, TC_068 | Covered |
| AC-LC-10 | Mixed reasons including range_out → 1 hedge-sync; lifecycle_state set downstream | TC_048, TC_099 | Covered [NEW] |
| AC-LC-11 | range_boundary_near → hedge-sync, not price-near-stop-audit | TC_051 | Covered [UPDATE] |
| AC-LC-12 | Stop replace overrun → SAFE_MODE atomic + partial_repair | TC_080, TC_083, TC_103 | Covered [UPDATE] |
| AC-LC-13 | next_light_check_at jitter range + 1 stmt/shard for all eligible bots | TC_006, TC_007, TC_008 | Covered [UPDATE] |
| AC-LC-14 | drift_threshold uses sqrtPriceX96 (uniPoolPrice) not hlMarkPrice | TC_034, TC_054 | Covered |
| AC-LC-15 | MarketDataDO stale > 5min → fail-fast, skip tick entirely | TC_035, TC_104 | Covered [UPDATE] |
| AC-LC-16 | HlMarkDO stale > 120s → fallback + widen band 2%→4% + freeze hedge-sync | TC_090, TC_091, TC_092, TC_093, TC_094 | Covered [NEW] |
| AC-LC-17 | funding_alert fires when fundingApr7dPct < -15% | TC_095, TC_096, TC_097 | Covered [NEW] |
| AC-LC-18 | LIMIT 500 + jitter hash — steady state no overflow | TC_010, TC_105, TC_106 | Covered [NEW] |

---


## I. Operation: Cron fan-out + Scan Worker


### I.1. Functional verification — Operation: Cron fan-out + Scan Worker

| TC ID | Title | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|
| TC_001 | Verify Cron Worker sends bot-scan messages via chunkSendBatch, not direct sendBatch | 1. The Cron Worker fires on its 1-minute schedule. | 1. Trigger one Cron Worker execution and capture all outbound queue calls.\n2. Verify that all bot-scan queue messages are sent using the `chunkSendBatch()` helper. | 1. No direct `queue.sendBatch()` call is made without the chunking helper.\n2. All bot-scan queue calls go through `chunkSendBatch()`, with each chunk limited to the platform maximum per call. HL API call count remains 0 throughout the Cron path. (Reference: UC §3 step 1; SRS FR-EXBOT-010; BR-EXBOT-003.) | P0 |
| TC_002 | Verify Scan Worker query selects only active bots whose check time is due | 1. D1 contains bots in mixed states: active bots with `next_light_check_at` in the past (eligible), active bots with `next_light_check_at` in the future (not yet due), paused bots, and closed bots. | 1. Trigger one Scan Worker execution on the shard.\n2. Inspect which botIds appear in the light-check queue. | 1. Only bots with `status='active'` AND `next_light_check_at <= now` appear in the light-check queue.\n2. Bots that are paused, closed, or not yet due are not enqueued. (Reference: UC §3 step 2; SRS FR-EXBOT-012.) | P0 |
| TC_003 | Verify Scan Worker filters out bots in lp_rebalancing lifecycle before enqueue but still reschedules them [UPDATED — AC-LC-03 modified] | 1. D1 contains an active bot with `lifecycle_state='lp_rebalancing'` and `next_light_check_at <= now`. | 1. Trigger one Scan Worker execution.\n2. Inspect the light-check queue for that bot's message.\n3. Read `next_light_check_at` for the bot after the Scan Worker completes. | 1. The bot is NOT enqueued in the light-check queue — no light-check message is sent for it.\n2. The Scan Worker excludes bots with `lifecycle_state IN ('lp_rebalancing', 'lp_closing')` at query time.\n3. `next_light_check_at` IS updated to `now + 5min + jitter(±45s)` for the bot — it is rescheduled even though it was not enqueued, so it does not flood the queue on recovery. (Reference: UC §2; SRS FR-EXBOT-012; AC-LC-03; confirmed code.) | P0 |
| TC_004 | Verify Scan Worker filters out bots in lp_closing lifecycle but still reschedules them [UPDATED — AC-LC-03 modified] | 1. D1 contains a bot with `lifecycle_state='lp_closing'` and `next_light_check_at <= now`. | 1. Trigger one Scan Worker execution.\n2. Check whether a light-check message is enqueued for that bot.\n3. Read `next_light_check_at` for the bot after the Scan Worker completes. | 1. No light-check message is enqueued for the bot in `lp_closing` state.\n2. `next_light_check_at` IS updated for the bot — rescheduled despite being skipped, preventing flood on recovery. (Reference: UC §2; SRS FR-EXBOT-012; AC-LC-03; confirmed code.) | P1 |
| TC_005 | Verify Scan Worker sends per-bot messages to the light-check queue via chunkSendBatch | 1. The Scan Worker has queried a set of eligible bots from D1. | 1. Let the Scan Worker enqueue the per-bot light-check messages.\n2. Capture all queue calls made. | 1. All per-bot light-check messages are enqueued through `chunkSendBatch()`, not via raw `sendBatch()`.\n2. Each bot gets exactly one light-check message. (Reference: UC §3 step 3; SRS FR-EXBOT-010.) | P1 |
| TC_006 | Verify next_light_check_at is batch-updated with jitter as one statement per shard for ALL eligible bots including skipped bots [UPDATED — AC-LC-13 modified] | 1. The Scan Worker has queried eligible bots on a shard, including bots with `lifecycle_state='lp_rebalancing'` or `status='paused'` that were not enqueued for light-check. | 1. After enqueue, capture the D1 write statements for `next_light_check_at`.\n2. Read back `next_light_check_at` for each eligible bot — including those not enqueued. | 1. Exactly one D1 UPDATE statement is issued per shard — not one per bot.\n2. All eligible bots — including `lp_rebalancing`, `lp_closing`, and `paused` bots — have their `next_light_check_at` updated to a value in the range `now + 5min ± 45s`. (Example: a bot processed at T=0 gets `next_light_check_at` between T+255s and T+345s.)\n3. No bot eligible at query time is skipped in the batch-update. (Reference: UC §3 step 4; SRS FR-EXBOT-013; AC-LC-13; confirmed code.) | P0 |
| TC_007 | Verify next_light_check_at lower boundary — earliest possible value is not below the minimum | 1. The Scan Worker batch-updates `next_light_check_at` with jitter applied. | 1. Run multiple Scan Worker cycles and record the minimum observed `next_light_check_at` offset from `now`.\n2. Verify the minimum offset is at or above the expected lower bound. | 1. No bot's `next_light_check_at` is set to less than `now + 4m15s` (lower BVA: `5min - 45s`). (Reference: SRS FR-EXBOT-013.) | P1 |
| TC_008 | Verify next_light_check_at upper boundary — latest possible value does not exceed the maximum | 1. The Scan Worker batch-updates `next_light_check_at` with jitter applied. | 1. Run multiple Scan Worker cycles and record the maximum observed `next_light_check_at` offset from `now`.\n2. Verify the maximum offset does not exceed the expected upper bound. | 1. No bot's `next_light_check_at` exceeds `now + 5m45s` (upper BVA: `5min + 45s`). (Reference: SRS FR-EXBOT-013.) | P1 |
| TC_009 | Verify Scan Worker does not enqueue paused bots but still reschedules them [UPDATED — AC-LC-04 modified] | 1. D1 contains a bot with `status='paused'` and `next_light_check_at <= now`. | 1. Trigger one Scan Worker execution.\n2. Check whether a light-check message is enqueued for the paused bot.\n3. Read `next_light_check_at` for the bot after the Scan Worker completes. | 1. No light-check message is enqueued for the paused bot.\n2. `next_light_check_at` IS updated for the bot — it is rescheduled even though it was not enqueued, preventing flood on resume. (Reference: UC §2; SRS FR-EXBOT-003; AC-LC-04; confirmed code.) | P0 |

### I.2. Integration & State verification — Operation: Cron fan-out + Scan Worker

| TC ID | Title | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|
| TC_010 | Verify Scan Worker applies the LIMIT per shard and defers remaining bots to the next cycle (post-downtime catch-up scenario) [UPDATED — AC-LC-18 modified] | 1. A D1 shard contains more eligible bots than the per-shard query limit — simulating a post-downtime backlog where many bots became eligible simultaneously.\n2. In steady state this condition does not occur because the jitter hash distributes bots evenly; this test targets the post-downtime exception. | 1. Trigger one Scan Worker cycle for the over-limit shard.\n2. Count how many bots are enqueued for light-check and how many remain eligible. | 1. Exactly the per-shard query limit of bots is enqueued (not more). (Example: if LIMIT=500, exactly 500 bots are enqueued.)\n2. The remaining bots' `next_light_check_at` stays unchanged — they remain eligible for the next Cron tick (1 minute later), oldest first.\n3. No bot is permanently lost — all bots are eventually processed without pagination logic. (Reference: UC §3 step 2; AC-LC-18; confirmed code.) | P1 |
| TC_011 | Verify a concurrent Cron overlap does not double-process bots through idempotency at the light-check level | 1. A second Cron Worker fires while the first Scan Worker invocation is still running.\n2. Both Scan Workers query the same shard for eligible bots. | 1. Let both Scan Workers enqueue messages for overlapping bot sets.\n2. Observe the light-check queue and idempotency table after both complete. | 1. Even if duplicate light-check messages are enqueued, the Light-Check Worker processes each bot exactly once per cycle — the UNIQUE constraint on `queue_idempotency.message_id` prevents double-execution. (Reference: UC §3 step 5; SRS FR-EXBOT-011.) | P1 |
| TC_012 | Verify the end-to-end Cron → Scan → light-check fan-out for one active bot | 1. One active bot with `next_light_check_at <= now` exists in D1, with `RebalanceReason[]` evaluating to empty.\n2. `MarketDataDO` is healthy with valid `sqrtPriceX96` and `currentTick`. | 1. Trigger the Cron Worker and let the pipeline run from Cron to Light-Check Worker completion.\n2. Check the resulting D1 state and queue state. | 1. The Cron enqueues a bot-scan message; the Scan Worker enqueues one light-check message for the bot; the Light-Check Worker processes it, updates `queue_idempotency.state='succeeded'`, and updates `next_light_check_at`. HL API call count throughout the entire chain = 0. (Reference: UC §3 steps 1–13; SRS BR-EXBOT-003; AC-LC-01.) | P0 |
| TC_013 | Verify hedge_stopped_cooldown bot is included in the Scan Worker query and enqueued | 1. A bot with `lifecycle_state='hedge_stopped_cooldown'` and `status='active'` has `next_light_check_at <= now`. | 1. Trigger one Scan Worker execution.\n2. Check whether a light-check message is enqueued for the `hedge_stopped_cooldown` bot. | 1. The bot IS included in the light-check queue — `hedge_stopped_cooldown` is not a skip condition at the Scan Worker level.\n2. The Light-Check Worker will process it normally but suppress hedge-sync. (Reference: UC §2 note; SRS states.md; AC-LC-05.) | P0 |

## II. Operation: Light-Check Worker — Idempotency


### II.1. Functional verification — Operation: Light-Check Worker — Idempotency

| TC ID | Title | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|
| TC_020 | Verify a new message_id is accepted and the idempotency row is created with state started | 1. The `queue_idempotency` table has no row for the incoming `message_id`. | 1. Deliver a light-check message with a fresh `message_id`.\n2. After processing completes, check the `queue_idempotency` row. | 1. The Light-Check Worker inserts a row with `state='started'` at the beginning, processes the bot, then updates the row to `state='succeeded'` at the end. No downstream fan-out message is suppressed due to idempotency. (Reference: UC §3 steps 5, 13; SRS FR-EXBOT-011.) | P0 |
| TC_021 | Verify a duplicate light-check message for the same message_id exits immediately without any mutation | 1. A `queue_idempotency` row already exists for the `message_id` (with `state='started'` from a prior delivery). | 1. Deliver the same light-check message again with the same `message_id`.\n2. Check D1 state and queue state after the second delivery. | 1. The Light-Check Worker detects the UNIQUE constraint conflict on insert and exits immediately.\n2. No downstream message (hedge-sync, price-near-stop-audit, partial_repair) is enqueued from the second delivery.\n3. No D1 bot state field is mutated by the second delivery. (Reference: UC §3 step 5; SRS FR-EXBOT-011; AC-LC-02.) | P0 |
| TC_022 | Verify a duplicate message with state succeeded also exits without mutation | 1. A `queue_idempotency` row exists for the `message_id` with `state='succeeded'` (prior run completed fully). | 1. Deliver the same light-check message again.\n2. Observe worker behavior and D1/queue state. | 1. The UNIQUE constraint conflict is detected on insert attempt; the worker exits immediately without enqueuing any message or mutating any bot state. (Reference: SRS FR-EXBOT-011.) | P1 |
| TC_023 | Verify idempotency row is finalized to succeeded after all fan-out logic completes | 1. The Light-Check Worker has started processing a fresh message and inserted `queue_idempotency` row with `state='started'`.\n2. The bot has a rebalance reason that triggers hedge-sync enqueue. | 1. Let the Light-Check Worker run to completion.\n2. Read the `queue_idempotency` row for the message. | 1. After all fan-out operations complete (hedge-sync enqueued, stop check done), the `queue_idempotency.state` is updated to `'succeeded'` — not before the fan-out, not left as `'started'`. (Reference: UC §3 step 13; SRS FR-EXBOT-011.) | P0 |

### II.2. Integration & State verification — Operation: Light-Check Worker — Idempotency

| TC ID | Title | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|
| TC_024 | Verify two concurrent workers racing on the same message_id result in exactly one execution | 1. Two Light-Check Worker instances receive the same light-check message simultaneously (concurrent redelivery).\n2. Both attempt to insert into `queue_idempotency` for the same `message_id` at the same time. | 1. Simulate concurrent delivery of the same message to two worker instances.\n2. Observe D1 queue_idempotency state and downstream queue after both workers complete. | 1. Exactly one worker wins the UNIQUE constraint race and proceeds with processing.\n2. The losing worker exits immediately on the conflict.\n3. Exactly one set of fan-out messages is enqueued — no duplicates in the hedge-sync or other downstream queues. (Reference: UC §3 step 5; SRS FR-EXBOT-011; TS_LC_048.) | P0 |
| TC_025 | Verify D1 read failure after idempotency insert leaves the row in started state for retry | 1. The Light-Check Worker has successfully inserted the `queue_idempotency` row with `state='started'`.\n2. The next D1 read (bot state) is configured to fail. | 1. Let the worker fail on the bot state read after the idempotency insert.\n2. Re-deliver the same message on the next queue retry. | 1. The `queue_idempotency` row remains in `state='started'` — it is NOT updated to `succeeded` because the worker did not complete.\n2. On the next delivery of the same message_id, the UNIQUE constraint conflict is detected and the worker exits (treating the in-progress state as a duplicate guard). The behavior when `state='started'` already exists (retry vs skip) is observable and consistent with the idempotency policy. (Reference: UC §3 steps 5–6; SRS FR-EXBOT-011; TS_LC_057.) | P1 |

## III. Operation: Light-Check Worker — State read and lpEthAmount computation


### III.1. Functional verification — Operation: Light-Check Worker — State read and lpEthAmount computation

| TC ID | Title | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|
| TC_030 | Verify Light-Check Worker reads bot state from D1 without any Hyperliquid API call | 1. The bot is active with a valid `bot_runtime_state`, `hedge_legs`, and `lifecycle_state` in D1.\n2. HL API call monitoring is active. | 1. Deliver a light-check message and let the worker read bot state.\n2. Check HL API call count after the read. | 1. `bot_runtime_state.last_known_hl_short_size`, `bots.lifecycle_state`, `hedge_legs.stop_price`, `hedge_legs.margin_status`, `hedge_legs.circuit_state`, and `hedge_legs.stop_replacing_started_at` are all read from D1.\n2. HL API call count = 0 during the state read phase. (Reference: UC §3 step 6; SRS BR-EXBOT-003.) | P0 |
| TC_031 | Verify Light-Check Worker reads sqrtPriceX96 and currentTick from MarketDataDO, not via direct RPC | 1. `MarketDataDO` has valid and fresh `sqrtPriceX96` and `currentTick` values.\n2. Direct on-chain RPC calls are blocked/monitored. | 1. Deliver a light-check message and observe data source for pool price data.\n2. Verify the source of `sqrtPriceX96` and `currentTick`. | 1. `sqrtPriceX96` and `currentTick` are sourced from `MarketDataDO` (Cloudflare Durable Object cache).\n2. No direct RPC call to the chain is made for pool slot0. HL API call count = 0. (Reference: UC §3 step 7; SRS FR-EXBOT-093; BR-EXBOT-003; AC-LC-01.) | P0 |
| TC_032 | Verify lpEthAmount is computed using Uniswap V3 TickMath + LiquidityAmounts (BigDecimal) | 1. The bot has valid `positions.liquidity`, `positions.tick_lower`, `positions.tick_upper`, `positions.weth_index` in D1.\n2. `MarketDataDO` provides `sqrtPriceX96` and `currentTick`. | 1. Deliver a light-check message and observe the `lpEthAmount` computation method.\n2. Verify the arithmetic uses BigDecimal throughout, not JavaScript float. | 1. `lpEthAmount` is calculated via the Uniswap V3 AMM formula (TickMath + LiquidityAmounts) — NOT via `depositedToken − withdrawnToken + collectedFees`.\n2. All intermediate and final values use BigDecimal arithmetic — no `Number(...)` conversion used for financial amounts. (Reference: UC §3 step 8; SRS FR-EXBOT-020; FR-EXBOT-021; NFR-EXBOT-008.) | P0 |
| TC_033 | Verify wethIndex is read from D1 positions table, not hardcoded | 1. Two bots exist: one on Base chain with `positions.weth_index=0` (WETH is token0), one on Optimism with `positions.weth_index=1` (WETH is token1). | 1. Deliver light-check messages for both bots.\n2. Observe which token leg is used as the ETH leg in `lpEthAmount` for each bot. | 1. The Base chain bot uses token0 as the WETH leg when computing `lpEthAmount`.\n2. The Optimism chain bot uses token1 as the WETH leg.\n3. Neither uses a hardcoded index — the value comes from `positions.weth_index`. (Reference: SRS FR-EXBOT-021; NFR-EXBOT-009; TS_LC_037, TS_LC_038.) | P1 |
| TC_034 | Verify deltaErrorUsd for drift_threshold uses uniPoolPrice, not hlMarkPrice or hlOraclePrice | 1. The bot has different values for `uniPoolPrice` (derived from `sqrtPriceX96`) and a hypothetical `hlMarkPrice`.\n2. Both prices differ enough that the drift decision changes depending on which is used. | 1. Deliver a light-check message where `deltaErrorUsd` computed with `uniPoolPrice` exceeds the threshold but computed with `hlMarkPrice` would not (or vice versa).\n2. Observe which price source the worker uses for `drift_threshold` evaluation. | 1. `deltaErrorUsd` is computed using `uniPoolPrice` (derived from `sqrtPriceX96` via MarketDataDO).\n2. `hlMarkPrice` and `hlOraclePrice` are not used for drift computation (3-way price split invariant). The drift decision matches the `uniPoolPrice`-based calculation. (Reference: SRS FR-EXBOT-012; AC-LC-14; TS_LC_035.) | P0 |
| TC_035 | Verify MarketDataDO stale cache triggers fail-fast throw and tick skip — no forced refresh [UPDATED — AC-LC-15 modified] | 1. `MarketDataDO` cache is stale — last updated more than 5 minutes ago (the fail-fast threshold). | 1. Deliver a light-check message while the cache is in a stale state.\n2. Observe whether the worker throws, skips, or attempts a forced refresh. | 1. The Light-Check Worker throws fail-fast immediately at step 7 — it does NOT attempt a forced refresh and does NOT use the stale data.\n2. The tick is skipped entirely — no trigger is evaluated, no hedge-sync or partial_repair is enqueued.\n3. `queue_idempotency.state` remains `'started'` (not advanced to `'succeeded'`), because the worker did not complete normally.\n4. No warning log or retry is emitted — the skip is clean and unconditional. (Reference: UC §3 step 7; SRS FR-EXBOT-093; AC-LC-15; confirmed code.) | P1 |

### III.2. Integration & State verification — Operation: Light-Check Worker — State read and lpEthAmount computation

| TC ID | Title | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|
| TC_036 | Verify MarketDataDO blockNumber monotonically increases and a regression triggers re-fetch | 1. `MarketDataDO` returns a `blockNumber` lower than the previously observed value (cache regression scenario). | 1. Simulate a MarketDataDO response where `blockNumber` is lower than the last seen value.\n2. Observe worker behavior. | 1. The Light-Check Worker detects the `blockNumber` regression.\n2. A re-fetch of the cache is triggered — the worker does not proceed with regressed data. (Reference: SRS FR-EXBOT-093.) | P1 |
| TC_037 | Verify the worker handles a missing bot in D1 gracefully without panic | 1. A light-check message is delivered for a `botId` that does not exist in D1 (bot was deleted or the message is stale). | 1. Deliver the light-check message for the non-existent botId.\n2. Observe worker error handling. | 1. The worker does not panic or throw an unhandled exception.\n2. The worker logs the not-found condition and exits cleanly (with idempotency row updated appropriately).\n3. No downstream messages are enqueued for a non-existent bot. (Reference: UC §3 step 6; TS_LC_056.) | P1 |

## IV. Operation: Light-Check Worker — RebalanceReason[] evaluation


### IV.1. Functional verification — Operation: Light-Check Worker — RebalanceReason[] evaluation

| TC ID | Title | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|
| TC_040 | Verify drift_threshold reason is triggered when deltaErrorUsd exceeds the $25 floor [UPDATED — formula confirmed] | 1. The bot is small enough that `lpValueUsd × 3% < $25`, making the $25 floor the active threshold. (`lpValueUsd = max(0, lpEthAmount × currentPriceUsd)`.)\n2. `deltaErrorUsd` (computed as `|targetShortEth - actualShortEth| × uniPoolPrice`) is just above $25. (Example: deltaErrorUsd = $25.01.) | 1. Deliver a light-check message for this bot.\n2. Inspect `RebalanceReason[]` after evaluation. | 1. `drift_threshold` is included in `RebalanceReason[]`.\n2. A hedge-sync message is enqueued with `reasons` containing `drift_threshold`. (Reference: SRS FR-EXBOT-012; confirmed code.) | P0 |
| TC_041 | Verify drift_threshold reason is NOT triggered when deltaErrorUsd equals exactly $25 (boundary — not greater than) | 1. The bot is small with active $25 floor threshold.\n2. `deltaErrorUsd` is exactly equal to $25.00 — at the boundary, not above it. | 1. Deliver a light-check message for this bot.\n2. Inspect `RebalanceReason[]`. | 1. `drift_threshold` is NOT in `RebalanceReason[]` — the condition is `deltaErrorUsd > max(...)`, not `>=`.\n2. No hedge-sync is enqueued for drift_threshold specifically. (Reference: SRS FR-EXBOT-012; TS_LC_025.) | P0 |
| TC_042 | Verify drift_threshold uses the 3% of lpValueUsd ceiling when it is larger than $25 [UPDATED — formula confirmed] | 1. The bot is large enough that `lpValueUsd × 3% > $25`, making the percentage ceiling the active threshold. (`lpValueUsd = max(0, lpEthAmount × currentPriceUsd)`.)\n2. `deltaErrorUsd` is just above `lpValueUsd × 3%`. (Example: lpValueUsd = $2000, threshold = $60, deltaErrorUsd = $60.01.) | 1. Deliver a light-check message.\n2. Inspect `RebalanceReason[]`. | 1. `drift_threshold` is included in `RebalanceReason[]` and hedge-sync is enqueued. (Reference: SRS FR-EXBOT-012; confirmed code.) | P0 |
| TC_043 | Verify drift_threshold is NOT triggered at exactly 3% of lpValueUsd (large bot boundary) | 1. The bot is large with `lpValueUsd × 3% > $25`.\n2. `deltaErrorUsd` equals exactly `lpValueUsd × 3%` — at the boundary, not above. | 1. Deliver a light-check message and inspect `RebalanceReason[]`. | 1. `drift_threshold` is NOT in `RebalanceReason[]` — equal is not greater than. (Reference: SRS FR-EXBOT-012; TS_LC_027.) | P1 |
| TC_044 | Verify drift_relative reason is triggered when the relative drift exceeds 15% | 1. `\|targetShortEth - actualShortEth\| / targetShortEth` is just above 0.15. (Example: ratio = 0.151.)\n2. `drift_threshold` condition is NOT triggered (to isolate drift_relative). | 1. Deliver a light-check message and inspect `RebalanceReason[]`. | 1. `drift_relative` is in `RebalanceReason[]` and a hedge-sync message is enqueued with `reasons` containing `drift_relative`. (Reference: SRS FR-EXBOT-012; TS_LC_030.) | P0 |
| TC_045 | Verify drift_relative reason is NOT triggered at exactly 0.15 relative drift (boundary) | 1. `\|targetShortEth - actualShortEth\| / targetShortEth` equals exactly 0.15. | 1. Deliver a light-check message and inspect `RebalanceReason[]`. | 1. `drift_relative` is NOT in `RebalanceReason[]`. The condition is `> 0.15`, not `>= 0.15`. (Reference: SRS FR-EXBOT-012; TS_LC_029.) | P1 |
| TC_046 | Verify margin_warning reason is triggered when hedge_legs.margin_status is warning | 1. `hedge_legs.margin_status='warning'` is set in D1.\n2. No HL API call is required to read margin status (it is already stored in D1). | 1. Deliver a light-check message and inspect `RebalanceReason[]`.\n2. Verify HL API call count remains 0. | 1. `margin_warning` is in `RebalanceReason[]`.\n2. A hedge-sync message is enqueued.\n3. HL API call count = 0 — margin status is read from D1, not fetched live from HL. (Reference: UC §3 step 9; SRS FR-EXBOT-012; FR-EXBOT-060; TS_LC_023.) | P0 |
| TC_047 | Verify time_fallback reason is triggered when the bot has not been adjusted for more than 4 hours | 1. `bot_runtime_state.last_hl_reconcile_at` is more than 4 hours in the past.\n2. No other rebalance condition is triggered. | 1. Deliver a light-check message and inspect `RebalanceReason[]`. | 1. `time_fallback` is in `RebalanceReason[]` and a hedge-sync message is enqueued. (Reference: SRS FR-EXBOT-012; TS_LC_024.) | P1 |
| TC_048 | Verify range_out reason is detected and hedge-sync is enqueued with range_out in reasons; lifecycle_state NOT set by Light-Check Worker [UPDATED — AC-LC-10 modified] | 1. `currentTick` from MarketDataDO is outside the range `[positions.tick_lower, positions.tick_upper]` so that `rangeState != 'in'`.\n2. The bot's `lifecycle_state` is `active`. | 1. Deliver a light-check message and inspect which queue receives a message.\n2. Read `bots.lifecycle_state` from D1 after processing. | 1. `range_out` is included in `RebalanceReason[]`.\n2. One hedge-sync message is enqueued with `range_out` in `reasons`.\n3. `bots.lifecycle_state` is NOT mutated by the Light-Check Worker — it remains `active`. The `lifecycle_state='lp_rebalancing'` transition is the downstream hedge-sync handler's responsibility.\n4. No separate `lp_rebalancing` queue message is enqueued directly by the Light-Check Worker. (Reference: SRS FR-EXBOT-012; FR-EXBOT-015; AC-LC-10; confirmed code.) | P0 |
| TC_049 | Verify RebalanceReason[] uses only canonical enum values (no variant spellings) | 1. The bot has conditions that trigger multiple reasons simultaneously. | 1. Deliver a light-check message and capture the raw `RebalanceReason[]` values.\n2. Check each value against the canonical enum. | 1. All values in `RebalanceReason[]` are from the canonical set: `drift_threshold`, `drift_relative`, `range_out`, `range_boundary_near`, `margin_warning`, `funding_alert`, `time_fallback`.\n2. `stop_trigger_crossed` is NOT in `RebalanceReason[]` — it is not a rebalance reason. (Reference: SRS FR-EXBOT-023; UC §4 A4.) | P0 |
| TC_050 | Verify RebalanceReason[] is empty when no rebalance condition is met | 1. The bot is in range, delta is within threshold, margin is ok, funding is fine, not time_fallback triggered. | 1. Deliver a light-check message and inspect `RebalanceReason[]`. | 1. `RebalanceReason[]` is empty (no reason added).\n2. No hedge-sync message is enqueued.\n3. `queue_idempotency.state` is updated to `succeeded`. (Reference: UC §3 step 9, UC §5; TS_LC_002.) | P0 |
| TC_051 | Verify range_boundary_near reason routes to hedge-sync, not to price-near-stop-audit [UPDATED — formula reference added] | 1. The price-based `range_boundary_near` condition is met: `nearestFraction = min(distToLower, distToUpper) / halfRange <= 0.9`. (Example: range $3,000–$3,400, `halfRange = $200`; price = $3,020 → `distToLower = $20`, `nearestFraction = 20/200 = 0.1 <= 0.9` → fires.)\n2. Stop trigger is NOT crossed (`markPrice < stop_price`). | 1. Deliver a light-check message and inspect which queue receives a message. | 1. `range_boundary_near` is in `RebalanceReason[]`.\n2. A hedge-sync message is enqueued — not `price-near-stop-audit`.\n3. No `lp_rebalancing` lifecycle transition occurs from the Light-Check Worker (`range_boundary_near` is not a range_out condition). (Reference: UC §4 A4; AC-LC-11; confirmed code.) | P0 |
| TC_052 | Verify both drift_threshold and drift_relative triggered simultaneously produce one hedge-sync with both reasons | 1. Both `drift_threshold` and `drift_relative` conditions are simultaneously true for the bot. | 1. Deliver a light-check message.\n2. Inspect the hedge-sync message payload enqueued. | 1. Exactly one hedge-sync message is enqueued — not two separate messages.\n2. The `reasons` field in the message contains both `drift_threshold` and `drift_relative`. (Reference: UC §3 step 10; SRS FR-EXBOT-023; TS_LC_031.) | P1 |

### IV.2. Integration & State verification — Operation: Light-Check Worker — RebalanceReason[] evaluation

| TC ID | Title | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|
| TC_053 | Verify BigDecimal is used throughout drift and lpEthAmount computations, not JavaScript float | 1. The bot has a `deltaErrorUsd` value near a known precision-sensitive boundary. (Example: near the $25 floor where a floating-point representation might round differently.) | 1. Deliver a light-check message with precision-sensitive input values.\n2. Inspect the computation internals for any use of JavaScript `Number` arithmetic. | 1. `lpEthAmount`, `deltaErrorUsd`, and all drift comparisons use BigDecimal arithmetic throughout — no `Number(...)` conversion for financial amounts.\n2. The comparison result matches the exact BigDecimal-based calculation, not a floating-point approximation. (Reference: SRS NFR-EXBOT-008; TS_LC_060.) | P0 |
| TC_054 | Verify drift_threshold decision is consistent with the 3-way price split across multiple cycles | 1. The bot has different `uniPoolPrice` (from MarketDataDO) vs hypothetical `hlMarkPrice` values across multiple light-check cycles. | 1. Run three consecutive light-check cycles with varying `uniPoolPrice` but stable `hlMarkPrice`.\n2. Verify each cycle's `drift_threshold` decision. | 1. Each cycle's `drift_threshold` decision is based solely on `uniPoolPrice` — decisions vary with `uniPoolPrice` changes, not with `hlMarkPrice` changes.\n2. The 3-way price split invariant (v5.2.6 X-5) is maintained across all cycles. (Reference: SRS FR-EXBOT-012; AC-LC-14.) | P1 |
| TC_055 | Verify funding_alert reason fires when fundingApr7dPct is below the -15% threshold [UPDATED — OQ-EXBOT-012 resolved] | 1. `funding_rolling_metrics.fundingApr7dPct` is set to a value below -15% (e.g., -16%). (Example: -16% is well below the -15% threshold.)\n2. No other rebalance condition is triggered (to isolate funding_alert). | 1. Deliver a light-check message when the 7-day funding APR is below the threshold.\n2. Inspect `RebalanceReason[]`. | 1. `funding_alert` appears in `RebalanceReason[]`.\n2. A hedge-sync message is enqueued with `reasons` containing `funding_alert`.\n3. HL API call count = 0 — funding data is read from D1 `funding_rolling_metrics`, not fetched from HL. (Reference: UC §3 step 9; AC-LC-17; confirmed code.) | P1 |

## V. Operation: Light-Check Worker — Fan-out (hedge-sync / lp_rebalancing)


### V.1. Functional verification — Operation: Light-Check Worker — Fan-out (hedge-sync / lp_rebalancing)

| TC ID | Title | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|
| TC_060 | Verify a rebalance reason triggers hedge-sync enqueue with correct payload when circuit is closed | 1. The bot has `circuit_breakers.state='closed'`.\n2. At least one non-range_out reason is in `RebalanceReason[]` (e.g. `drift_threshold`). | 1. Deliver a light-check message and inspect the hedge-sync queue. | 1. A hedge-sync message is enqueued with payload `{botId, reasons, stateVersion}`.\n2. `stateVersion` in the message matches `bot_runtime_state.state_version` read at the time of light-check.\n3. HL API call count = 0. (Reference: UC §3 step 10; SRS FR-EXBOT-012; FR-EXBOT-027.) | P0 |
| TC_061 | Verify circuit open suppresses hedge-sync but does not suppress price-near-stop-audit | 1. `circuit_breakers.state='open'`.\n2. The bot has rebalance reasons that would normally trigger hedge-sync.\n3. `markPrice >= stop_price` (stop trigger also applies). | 1. Deliver a light-check message and inspect both the hedge-sync and price-near-stop-audit queues. | 1. No hedge-sync message is enqueued (circuit open suppresses all hedge mutations).\n2. `stop_trigger_crossed_at` is set (if NULL) and `price-near-stop-audit` IS enqueued — stop monitoring continues even with circuit open. (Reference: UC §4 A1; SRS FR-EXBOT-014; AC-LC-08; TS_LC_012.) | P0 |
| TC_062 | Verify circuit half_open allows exactly one probe hedge-sync and atomically claims half_open_probe_used | 1. `circuit_breakers.state='half_open'` and `circuit_breakers.half_open_probe_used=0`.\n2. The bot has a rebalance reason present. | 1. Deliver a light-check message.\n2. Inspect the hedge-sync queue and `circuit_breakers` row. | 1. Exactly one hedge-sync probe message is enqueued.\n2. `circuit_breakers.half_open_probe_used` is atomically set to 1.\n3. The claim is performed as a single atomic operation — no window where probe_used=0 and hedge-sync is not yet enqueued. (Reference: UC §4 A2; SRS FR-EXBOT-040; AC-LC-09; TS_LC_013.) | P0 |
| TC_063 | Verify circuit half_open with probe already claimed suppresses hedge-sync like circuit open | 1. `circuit_breakers.state='half_open'` and `circuit_breakers.half_open_probe_used=1` (already claimed).\n2. The bot has a rebalance reason. | 1. Deliver a light-check message.\n2. Inspect the hedge-sync queue. | 1. No hedge-sync message is enqueued — the worker treats this identically to circuit open (probe already used). (Reference: UC §4 A2; SRS FR-EXBOT-040; TS_LC_014.) | P0 |
| TC_064 | Verify range_out enqueues hedge-sync with range_out in reasons; lifecycle_state transition is downstream hedge-sync handler's responsibility [UPDATED — AC-LC-10 modified] | 1. `currentTick` is outside `[positions.tick_lower, positions.tick_upper]` so `range_out` is triggered.\n2. The bot's `lifecycle_state` is `active`. | 1. Deliver a light-check message and inspect D1 state and downstream queue. | 1. `range_out` is in `RebalanceReason[]` and one hedge-sync message is enqueued.\n2. `bots.lifecycle_state` in D1 is NOT changed by the Light-Check Worker — it remains `active` after the light-check completes.\n3. The hedge-sync handler downstream is responsible for the `active → lp_rebalancing` lifecycle transition.\n4. Subsequent light-check cycles for this bot will be suppressed at the Scan Worker level only after the lifecycle transition has occurred (downstream). (Reference: UC §3 step 10; SRS FR-EXBOT-015; AC-LC-10; confirmed code.) | P0 |
| ~~TC_065~~ | ~~Verify lp_rebalancing lifecycle set is committed before partial_repair is enqueued (ordering)~~ | ~~RETIRED — Light-Check Worker does not set `lifecycle_state='lp_rebalancing'` at all (confirmed code). This behavior occurs at the hedge-sync handler layer, not the light-check layer. Replaced by TC_103 (SAFE_MODE atomic write ordering).~~ | — | — | Retired |
| TC_066 | Verify hedge_stopped_cooldown bot does not enqueue hedge-sync even with rebalance reasons | 1. `bots.lifecycle_state='hedge_stopped_cooldown'` (status='active').\n2. The bot has a non-range_out rebalance reason present (e.g. `drift_threshold`). | 1. Deliver a light-check message and inspect the hedge-sync queue. | 1. No hedge-sync message is enqueued — `hedge_stopped_cooldown` suppresses hedge-sync.\n2. Stop monitoring (price-near-stop-audit check) still runs.\n3. `queue_idempotency.state` is updated to `succeeded`. (Reference: UC §2 note; SRS states.md; AC-LC-05; TS_LC_011.) | P0 |
| TC_067 | Verify hedge-sync stateVersion matches the D1 state_version read at light-check time | 1. The bot has a known `bot_runtime_state.state_version` value in D1.\n2. A rebalance reason triggers hedge-sync. | 1. Deliver a light-check message and inspect the enqueued hedge-sync message payload. | 1. The hedge-sync message's `stateVersion` field matches the `bot_runtime_state.state_version` value that was in D1 when the light-check read occurred.\n2. A stale or mismatched stateVersion in the hedge-sync message would cause the hedge-sync worker to discard it. (Reference: UC §3 step 10; SRS FR-EXBOT-027; TS_LC_044.) | P0 |

### V.2. Integration & State verification — Operation: Light-Check Worker — Fan-out (hedge-sync / lp_rebalancing)

| TC ID | Title | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|
| TC_068 | Verify two concurrent half_open probes for the same bot result in exactly one probe hedge-sync | 1. `circuit_breakers.state='half_open'` and `half_open_probe_used=0`.\n2. Two Light-Check Worker instances concurrently attempt to claim the probe for the same bot. | 1. Simulate two workers simultaneously attempting the atomic claim on `half_open_probe_used`.\n2. Inspect the hedge-sync queue and the `circuit_breakers` row after both complete. | 1. Exactly one probe hedge-sync message is enqueued — the first worker wins the atomic CAS write.\n2. The second worker's claim fails and it suppresses hedge-sync (treating it as already-claimed = open state).\n3. `half_open_probe_used` is 1 in D1. (Reference: UC §4 A2; SRS FR-EXBOT-040; TS_LC_049.) | P0 |
| TC_069 | Verify lp_rebalancing lifecycle set suppresses subsequent Scan Worker queries for the bot | 1. A light-check pass has just set `bots.lifecycle_state='lp_rebalancing'` for a bot. | 1. Run the next Scan Worker cycle.\n2. Check whether a new light-check message is enqueued for the bot. | 1. The bot is excluded from the next Scan Worker query — `lifecycle_state='lp_rebalancing'` is a skip condition.\n2. No new light-check message is enqueued for the bot until its lifecycle returns to `active`. (Reference: UC §2; SRS FR-EXBOT-012; TS_LC_039.) | P0 |
| TC_070 | Verify circuit closed transitions to open after three consecutive hedge-sync failures within 24h | 1. `circuit_breakers.state='closed'` for the bot.\n2. Three consecutive hedge-sync failures are configured (mocked) within a 24h window. | 1. Trigger three consecutive hedge-sync failures for the bot.\n2. On the next light-check cycle, inspect the circuit state and fan-out behavior. | 1. `circuit_breakers.state` transitions to `open` after the third failure, with `reset_at = now + 1h`.\n2. In the next light-check cycle, hedge-sync is suppressed.\n3. Stop monitoring continues — price-near-stop-audit is still triggered if stop conditions are met. (Reference: SRS FR-EXBOT-040; TS_LC_041.) | P1 |
| TC_071 | Verify circuit open transitions to half_open when reset_at is elapsed | 1. `circuit_breakers.state='open'` with `reset_at` in the past. | 1. Deliver a light-check message after `reset_at` has passed.\n2. Inspect circuit state and fan-out behavior. | 1. `circuit_breakers.state` transitions from `open` to `half_open`.\n2. One probe hedge-sync is enqueued and `half_open_probe_used` is set to 1. (Reference: SRS FR-EXBOT-040; TS_LC_042.) | P1 |

## VI. Operation: Light-Check Worker — Stop trigger detection


### VI.1. Functional verification — Operation: Light-Check Worker — Stop trigger detection

| TC ID | Title | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|
| TC_072 | Verify stop_trigger_crossed_at is set when markPrice from HlMarkDO crosses stop_price and field is currently NULL [UPDATED — AC-LC-06 modified] | 1. `hedge_legs.stop_trigger_crossed_at` is NULL.\n2. `HlMarkDO.markPriceUsd >= stop_price` (stop trigger condition is met with the primary source).\n3. `HlMarkDO` is fresh (not stale). | 1. Deliver a light-check message.\n2. Read `hedge_legs.stop_trigger_crossed_at` after processing. | 1. The worker reads `markPrice` from `HlMarkDO.markPriceUsd` (primary source).\n2. `hedge_legs.stop_trigger_crossed_at` is set to a non-null timestamp (write-once guarded write).\n3. A `price-near-stop-audit` message is enqueued.\n4. No hedge-sync message is enqueued for the stop trigger itself.\n5. HL API call count = 0. (Reference: UC §3 step 11; SRS FR-EXBOT-032; BR-EXBOT-005; AC-LC-06; confirmed Tech Lead.) | P0 |
| TC_073 | Verify stop_trigger_crossed_at is NOT overwritten when already set (write-once invariant) | 1. `hedge_legs.stop_trigger_crossed_at` is already set to a non-null timestamp.\n2. `markPrice >= stop_price` (stop trigger condition still met). | 1. Deliver another light-check message.\n2. Read `hedge_legs.stop_trigger_crossed_at` after processing. | 1. The original `stop_trigger_crossed_at` value is NOT overwritten.\n2. A `price-near-stop-audit` message is still enqueued (stop monitoring continues regardless).\n3. Write-once invariant holds — the field is set exactly once per stop event. (Reference: UC §4 A3; SRS FR-EXBOT-032; BR-EXBOT-005; AC-LC-07; TS_LC_016.) | P0 |
| TC_074 | Verify price-near-stop-audit is enqueued even when circuit breaker is open | 1. `circuit_breakers.state='open'`.\n2. `markPrice >= stop_price` and `stop_trigger_crossed_at` is NULL. | 1. Deliver a light-check message.\n2. Inspect both hedge-sync and price-near-stop-audit queues. | 1. No hedge-sync is enqueued (circuit open suppresses it).\n2. `stop_trigger_crossed_at` is set and `price-near-stop-audit` IS enqueued — stop monitoring is never suppressed by the circuit breaker. (Reference: UC §4 A1; SRS FR-EXBOT-014; AC-LC-08.) | P0 |
| TC_075 | Verify price-near-stop-audit is NOT enqueued when markPrice is below stop_price | 1. `markPrice < stop_price` (stop trigger not crossed).\n2. `stop_trigger_crossed_at` is NULL. | 1. Deliver a light-check message.\n2. Check the price-near-stop-audit queue. | 1. No `price-near-stop-audit` message is enqueued.\n2. `stop_trigger_crossed_at` remains NULL. (Reference: UC §3 step 11; SRS FR-EXBOT-032.) | P0 |
| TC_076 | Verify stop monitoring runs normally for a hedge_stopped_cooldown bot | 1. `bots.lifecycle_state='hedge_stopped_cooldown'` (status='active').\n2. `markPrice >= stop_price` and `stop_trigger_crossed_at` is NULL. | 1. Deliver a light-check message.\n2. Inspect D1 `stop_trigger_crossed_at` and price-near-stop-audit queue. | 1. `stop_trigger_crossed_at` is set and `price-near-stop-audit` is enqueued — stop monitoring runs normally even in `hedge_stopped_cooldown`.\n2. No hedge-sync is enqueued (suppressed by `hedge_stopped_cooldown`). (Reference: UC §2 note; UC §3 step 11; TS_LC_011.) | P0 |
| ~~TC_077~~ | ~~Verify hlMarkPrice source — BLOCKED by OQ-EXBOT-017~~ | ~~RETIRED — OQ-EXBOT-017 resolved. HlMarkDO confirmed as primary source; staleness policy confirmed. Replaced by TC_089–TC_094.~~ | — | — | Retired |

### VI.2. Integration & State verification — Operation: Light-Check Worker — Stop trigger detection

| TC ID | Title | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|
| TC_078 | Verify stop_trigger_crossed_at is written atomically (single D1 write, not split across two operations) | 1. `stop_trigger_crossed_at` is NULL and stop trigger condition is met.\n2. Simulate an interruption between the NULL check and the write. | 1. Deliver a light-check message where stop is triggered.\n2. Verify the guarded write is atomic. | 1. The check-then-set operation is atomic — if two concurrent workers both see NULL, only one should succeed in setting the value.\n2. The value is set exactly once — no double-write or inconsistent state from concurrent light-checks. (Reference: UC §3 step 11; SRS FR-EXBOT-032; BR-EXBOT-005.) | P0 |
| TC_079 | Verify stop_trigger_crossed_at is checked on every light-check pass (stop monitoring always active) | 1. The bot is in `active` lifecycle_state with circuit closed.\n2. `stop_trigger_crossed_at` is NULL. | 1. Deliver three consecutive light-check messages where `markPrice < stop_price`, then one where `markPrice >= stop_price`.\n2. Observe when `price-near-stop-audit` is first enqueued. | 1. For the first three passes, no `price-near-stop-audit` is enqueued.\n2. On the fourth pass (when `markPrice >= stop_price`), `stop_trigger_crossed_at` is set and `price-near-stop-audit` is enqueued.\n3. Stop monitoring is evaluated every light-check pass — not conditionally. (Reference: UC §3 step 11.) | P1 |

## VII. Operation: Light-Check Worker — Stop replace overrun detection


### VII.1. Functional verification — Operation: Light-Check Worker — Stop replace overrun detection

| TC ID | Title | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|
| TC_080 | Verify stop replace overrun is detected when stop_replacing_started_at age exceeds 60 seconds | 1. `hedge_legs.stop_replacing_started_at` is set to a timestamp more than 60 seconds in the past (NOT NULL).\n2. The bot is in `active` lifecycle state. | 1. Deliver a light-check message.\n2. Inspect D1 `bots.lifecycle_state` and the partial_repair queue. | 1. A `partial_repair` message with `reason='stop_replacing_overrun'` is enqueued.\n2. `bots.lifecycle_state` transitions to `safe_mode`.\n3. Primary detection occurs within one 5-minute light-check window — not deferred. (Reference: UC §3 step 12; SRS FR-EXBOT-033; AC-LC-12; TS_LC_017.) | P0 |
| TC_081 | Verify stop replace overrun is NOT triggered when stop_replacing_started_at is NULL | 1. `hedge_legs.stop_replacing_started_at` is NULL (no stop replace in progress). | 1. Deliver a light-check message.\n2. Check the partial_repair queue for overrun messages. | 1. No `partial_repair(reason='stop_replacing_overrun')` message is enqueued.\n2. `bots.lifecycle_state` remains unchanged. (Reference: UC §3 step 12; SRS FR-EXBOT-033; TS_LC_058.) | P0 |
| TC_082 | Verify stop replace overrun is NOT triggered at exactly 60 seconds (boundary — strict greater than) | 1. `hedge_legs.stop_replacing_started_at` is set to exactly 60 seconds ago. | 1. Deliver a light-check message at exactly t=60s from `stop_replacing_started_at`.\n2. Check overrun detection. | 1. The overrun condition `age > 60s` is NOT satisfied at exactly 60s — equal is not greater than.\n2. No `partial_repair(stop_replacing_overrun)` is enqueued and `lifecycle_state` remains unchanged. (Reference: UC §3 step 12; SRS FR-EXBOT-033; TS_LC_018.) | P0 |
| TC_083 | Verify stop replace overrun IS triggered at 61 seconds (just above boundary) | 1. `hedge_legs.stop_replacing_started_at` is set to exactly 61 seconds ago. | 1. Deliver a light-check message at t=61s from `stop_replacing_started_at`. | 1. The overrun condition `age > 60s` IS satisfied.\n2. `partial_repair(reason='stop_replacing_overrun')` is enqueued and `bots.lifecycle_state='safe_mode'` is set. (Reference: UC §3 step 12; SRS FR-EXBOT-033; TS_LC_019.) | P0 |
| TC_084 | Verify SAFE_MODE entry blocks all hedge mutations for the bot | 1. A stop replace overrun has triggered `lifecycle_state='safe_mode'` for the bot. | 1. Attempt to enqueue a hedge-sync message for the bot in SAFE_MODE.\n2. Observe worker behavior on the next light-check for this bot. | 1. No hedge-sync is processed for the bot while in `safe_mode` — all hedge mutations are blocked.\n2. SAFE_MODE is not terminal — a recovery path exists (auto-recovery or bot_safe_close). (Reference: SRS FR-EXBOT-050; BR-EXBOT-007; TS_LC_040.) | P0 |

### VII.2. Integration & State verification — Operation: Light-Check Worker — Stop replace overrun detection

| TC ID | Title | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|
| TC_085 | Verify bots.status and bots.lifecycle_state are both set atomically in one D1 UPDATE before partial_repair is enqueued on SAFE_MODE entry [UPDATED — AC-LC-12 modified] | 1. The stop replace overrun condition is met: `stop_replacing_started_at` IS NOT NULL and age > 60s.\n2. `bots.lifecycle_state='active'` and `bots.status='active'` before the light-check. | 1. Deliver the triggering light-check message.\n2. Read both `bots.lifecycle_state` and `bots.status` after processing.\n3. Inspect the D1 write sequence and queue enqueue ordering. | 1. Both `bots.lifecycle_state='safe_mode'` and `bots.status='safe_mode'` are set in a single atomic UPDATE statement — not in two separate writes.\n2. There is no observable window where only one field is `safe_mode`.\n3. The D1 UPDATE completes BEFORE the `partial_repair` queue message is enqueued — there is no state where the queue message exists but the D1 write has not completed.\n4. `partial_repair(reason='stop_replacing_overrun')` is present in the partial_repair queue after the UPDATE. (Reference: UC §3 step 12; SRS FR-EXBOT-050; FR-EXBOT-033; AC-LC-12; confirmed code.) | P0 |
| TC_086 | Verify deep-audit acts as a backstop when light-check misses the stop_replacing_started_at overrun | 1. `hedge_legs.stop_replacing_started_at` has been set for more than 60 seconds, but the light-check cycle that would have detected it was delayed or missed.\n2. Deep-audit is scheduled to run (normally at 6h cadence, reduced to 1h in high-risk mode). | 1. Skip or simulate a missed light-check cycle.\n2. Trigger a deep-audit cycle.\n3. Observe whether the overrun is detected. | 1. Deep-audit detects `stop_replacing_started_at` stuck > 60s and routes to SAFE_MODE / repair as a secondary backstop.\n2. The primary detection (light-check, within ≤5 min) is preferred, but deep-audit ensures no overrun is permanently missed. (Reference: UC §3 step 12; SRS FR-EXBOT-033; Audited report §7.) | P1 |

### VII.3. Non-functional (logic) verification — Operation: Light-Check Worker — Stop replace overrun detection

| TC ID | Title | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|
| TC_087 | Verify SAFE_MODE is not a terminal state and a recovery path exists | 1. The bot has been placed in `lifecycle_state='safe_mode'` by a stop replace overrun.\n2. HL is responsive and margin_status recovers to `ok`. | 1. Simulate three consecutive successful reconcile operations for the bot in SAFE_MODE.\n2. Observe whether the bot transitions out of SAFE_MODE. | 1. After three consecutive reconcile successes AND `margin_status='ok'`, the bot transitions from `safe_mode` back to `active` (auto-recovery path).\n2. If auto-recovery is not possible, `bot_safe_close` is the fallback — SAFE_MODE is never a permanent dead-end. (Reference: SRS FR-EXBOT-050; BR-EXBOT-007.) | P0 |
| TC_088 | Verify Light-Check Worker concurrency limit is respected when reading D1 and MarketDataDO | 1. The Light-Check Worker is processing a bot and makes concurrent reads to D1 and MarketDataDO. | 1. Deliver a light-check message and observe the number of simultaneous outbound connections made. | 1. The total number of simultaneous outbound connections from the Worker invocation does not exceed the per-invocation limit. (Example: limit = 6 simultaneous connections.)\n2. No connection limit error is thrown during normal light-check processing. (Reference: SRS NFR-EXBOT-011.) | P1 |

## VIII. Operation: Light-Check Worker — HlMarkDO staleness handling

### VIII.1. Functional verification — Operation: Light-Check Worker — HlMarkDO staleness handling

| TC ID | Title | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|
| TC_089 | Verify Light-Check Worker reads hlMarkPrice from HlMarkDO.markPriceUsd as primary source for stop trigger [NEW — AC-LC-06] | 1. `HlMarkDO.markPriceUsd` is fresh (last updated less than 120 seconds ago).\n2. `HlMarkDO.markPriceUsd >= stop_price` so stop trigger condition is met.\n3. `stop_trigger_crossed_at` is NULL. | 1. Deliver a light-check message.\n2. Confirm which source provided the `markPrice` used for stop trigger evaluation.\n3. Read `stop_trigger_crossed_at` after processing. | 1. `markPrice` is sourced from `HlMarkDO.markPriceUsd` — the primary source.\n2. `stop_trigger_crossed_at` is set (write-once).\n3. `price-near-stop-audit` is enqueued.\n4. HL API call count = 0. (Reference: UC §3 step 11; AC-LC-06; confirmed Tech Lead.) | P0 |
| TC_090 | Verify HlMarkDO stale > 120s triggers fallback to eth_price_usd for stop trigger evaluation [NEW — AC-LC-16] | 1. `HlMarkDO.markPriceUsd` last updated more than 120 seconds ago (stale).\n2. `bot_runtime_state.eth_price_usd` is available and is the fallback. | 1. Deliver a light-check message.\n2. Observe which price source the worker uses for stop trigger evaluation. | 1. The worker falls back to `bot_runtime_state.eth_price_usd` — the HlMarkDO primary source is not used.\n2. Stop trigger evaluation proceeds with the fallback price.\n3. HL API call count = 0. (Reference: UC §3 step 11; AC-LC-16; confirmed Tech Lead.) | P0 |
| TC_091 | Verify HlMarkDO stale > 120s widens near-stop band from 2% to 4% [NEW — AC-LC-16] | 1. `HlMarkDO.markPriceUsd` is stale > 120s.\n2. The bot has a near-stop band configured at the default 2%. | 1. Deliver a light-check message.\n2. Observe the near-stop band width used for stop trigger evaluation. | 1. The near-stop band is widened from 2% to 4% — stop trigger is evaluated with the wider band.\n2. A stop that would not fire at 2% band may fire at 4% band with the fallback price. (Reference: AC-LC-16; confirmed Tech Lead.) | P0 |
| TC_092 | Verify HlMarkDO stale > 120s freezes hedge-sync even when rebalance reasons are present [NEW — AC-LC-16] | 1. `HlMarkDO.markPriceUsd` is stale > 120s.\n2. The bot has at least one active rebalance reason (e.g., `drift_threshold`) that would normally trigger hedge-sync. | 1. Deliver a light-check message.\n2. Inspect the hedge-sync queue after processing. | 1. No hedge-sync message is enqueued — hedge-sync is frozen due to HlMarkDO staleness.\n2. The rebalance reasons were detected but suppressed.\n3. Stop monitoring (step 11) still runs using the fallback price. (Reference: AC-LC-16; confirmed Tech Lead.) | P0 |
| TC_093 | BVA — HlMarkDO stale at exactly 120s (not stale — primary source active, 2% band, no freeze) [NEW — AC-LC-16] | 1. `HlMarkDO.markPriceUsd` last updated exactly 120 seconds ago. | 1. Deliver a light-check message.\n2. Observe which price source is used and whether hedge-sync is frozen. | 1. The worker uses `HlMarkDO.markPriceUsd` as primary source — the staleness threshold is strictly > 120s, so exactly 120s is NOT stale.\n2. Near-stop band remains at 2% (not widened).\n3. Hedge-sync is NOT frozen. (Reference: AC-LC-16; confirmed Tech Lead.) | P1 |
| TC_094 | BVA — HlMarkDO stale at 121s (just past threshold — fallback active, 4% band, freeze) [NEW — AC-LC-16] | 1. `HlMarkDO.markPriceUsd` last updated exactly 121 seconds ago. | 1. Deliver a light-check message.\n2. Observe price source, band width, and hedge-sync behavior. | 1. The worker falls back to `bot_runtime_state.eth_price_usd`.\n2. Near-stop band widens to 4%.\n3. Hedge-sync is frozen. (Reference: AC-LC-16; confirmed Tech Lead.) | P1 |

## IX. Operation: Light-Check Worker — funding_alert evaluation

### IX.1. Functional verification — Operation: Light-Check Worker — funding_alert evaluation

| TC ID | Title | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|
| TC_095 | Verify funding_alert fires when fundingApr7dPct is below -15% threshold (primary source) [NEW — AC-LC-17] | 1. `funding_rolling_metrics.fundingApr7dPct` is set to a value well below -15% (e.g., -20%).\n2. No other rebalance reason is triggered (to isolate funding_alert). | 1. Deliver a light-check message.\n2. Inspect `RebalanceReason[]`. | 1. `funding_alert` is in `RebalanceReason[]`.\n2. The worker reads `fundingApr7dPct` directly from `funding_rolling_metrics` (primary path).\n3. Hedge-sync is enqueued. HL API call count = 0. (Reference: AC-LC-17; confirmed code.) | P0 |
| TC_096 | BVA — funding_alert at exactly -15% is NOT triggered (strict less-than) [NEW — AC-LC-17] | 1. `funding_rolling_metrics.fundingApr7dPct = -15.0` (exact threshold). | 1. Deliver a light-check message.\n2. Inspect `RebalanceReason[]`. | 1. `funding_alert` is NOT in `RebalanceReason[]` — the condition is `< -15%`, not `<= -15%`. Equal to -15% does not satisfy strict less-than.\n2. No hedge-sync enqueued for funding_alert. (Reference: AC-LC-17; confirmed code.) | P1 |
| TC_097 | BVA — funding_alert at -15.01% IS triggered (just past threshold) [NEW — AC-LC-17] | 1. `funding_rolling_metrics.fundingApr7dPct = -15.01`. | 1. Deliver a light-check message.\n2. Inspect `RebalanceReason[]`. | 1. `funding_alert` IS in `RebalanceReason[]` — -15.01% is strictly less than -15%.\n2. Hedge-sync is enqueued. (Reference: AC-LC-17; confirmed code.) | P1 |
| TC_098 | Verify funding_alert fallback — uses fundingRate × 8760 when fundingApr7dPct is unavailable [NEW — AC-LC-17] | 1. `funding_rolling_metrics` row for the bot has `fundingApr7dPct = NULL` or the row is missing.\n2. `fundingRate` is set such that `fundingRate × 8760 < -0.15` (annualized per-hour rate triggers threshold). | 1. Deliver a light-check message.\n2. Inspect `RebalanceReason[]` and the data source used. | 1. The worker falls back to the annualization formula `fundingRate × 8760`.\n2. `funding_alert` fires and is in `RebalanceReason[]`.\n3. Hedge-sync is enqueued. (Reference: confirmed code.) | P1 |

## X. Operation: Light-Check Worker — Additional coverage

### X.1. Functional verification — Operation: Light-Check Worker — Additional coverage

| TC ID | Title | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|
| TC_099 | Verify mixed reasons range_out + drift_threshold produce single hedge-sync; lifecycle_state NOT set by Light-Check Worker [NEW — AC-LC-10] | 1. Bot simultaneously triggers `range_out` (tick outside range) AND `drift_threshold` (delta error above threshold) in the same light-check pass.\n2. Circuit breaker is closed. | 1. Deliver a light-check message.\n2. Inspect hedge-sync queue and D1 `bots.lifecycle_state`. | 1. Engine gathers both `range_out` and `drift_threshold` into `decision.reason[]`.\n2. Exactly one hedge-sync message is enqueued with both reasons present (e.g., `reasons: ['range_out', 'drift_threshold']`).\n3. `bots.lifecycle_state` is NOT mutated by the Light-Check Worker — it remains at its current value after the light-check.\n4. No `partial_repair` or `lp_rebalancing` message is enqueued directly by Light-Check Worker. (Reference: AC-LC-10; confirmed code.) | P0 |
| TC_100 | Verify lpValueUsd = 0 edge case — drift_threshold floor is $25 [NEW] | 1. The bot's `lpEthAmount = 0` (position fully out of range, both tokens at boundary).\n2. `currentPriceUsd > 0`. | 1. Deliver a light-check message.\n2. Observe the drift_threshold value used for evaluation. | 1. `lpValueUsd = max(0, 0 × currentPriceUsd) = $0`.\n2. `drift_threshold = max($25, $0 × 3%) = $25` — the $25 floor applies.\n3. Subsequent drift_threshold evaluation uses $25, not $0 or a negative value. (Reference: confirmed code.) | P1 |

### X.2. Integration & State verification — Operation: Light-Check Worker — Additional coverage

| TC ID | Title | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|
| TC_101 | Verify next_light_check_at is updated for lp_rebalancing bot even though it is not enqueued for light-check [NEW — AC-LC-03] | 1. Bot has `lifecycle_state='lp_rebalancing'` and `next_light_check_at <= now`. | 1. Run the Scan Worker.\n2. Check the light-check queue for this bot.\n3. Read `next_light_check_at` for the bot after the Scan Worker completes. | 1. The bot is NOT enqueued in the light-check queue.\n2. `next_light_check_at` IS updated to `now + 5min + jitter(±45s)` in the batch UPDATE.\n3. The bot will resume normal light-check scheduling after its lifecycle returns to `active`, without falling behind. (Reference: AC-LC-03; confirmed code.) | P1 |
| TC_102 | Verify next_light_check_at is updated for paused bot even though it is not enqueued for light-check [NEW — AC-LC-04] | 1. Bot has `status='paused'` and `next_light_check_at <= now`. | 1. Run the Scan Worker.\n2. Check the light-check queue for this bot.\n3. Read `next_light_check_at` for the bot after the Scan Worker completes. | 1. The bot is NOT enqueued in the light-check queue.\n2. `next_light_check_at` IS updated to `now + 5min + jitter(±45s)`.\n3. On resume, the bot will not flood the light-check queue. (Reference: AC-LC-04; confirmed code.) | P1 |
| TC_103 | Verify SAFE_MODE atomic write — both bots.status and bots.lifecycle_state set in single D1 UPDATE, UPDATE completes before partial_repair enqueued [NEW — AC-LC-12] | 1. `hedge_legs.stop_replacing_started_at` IS NOT NULL and age > 60s.\n2. Bot is in `lifecycle_state='active'`, `status='active'`. | 1. Trigger the stop_replacing_overrun condition.\n2. Inspect the D1 write sequence and queue enqueue ordering. | 1. `bots.status='safe_mode'` AND `bots.lifecycle_state='safe_mode'` are set in a single atomic `UPDATE bots SET status='safe_mode', lifecycle_state='safe_mode' WHERE id=?`.\n2. No observable window exists where only one field is `safe_mode`.\n3. The `partial_repair(reason='stop_replacing_overrun')` message is enqueued AFTER the D1 UPDATE completes — no state where queue message exists without both D1 fields being `safe_mode`.\n4. This replaces the ordering guarantee previously implied by TC_065 (retired). (Reference: AC-LC-12; SRS FR-EXBOT-033; FR-EXBOT-050; confirmed code.) | P0 |
| TC_104 | Verify MarketDataDO unreachable (DO timeout) triggers same fail-fast behavior as stale data [NEW — AC-LC-15] | 1. `MarketDataDO` is unreachable — request to the DO times out or returns an error. | 1. Deliver a light-check message while MarketDataDO is unreachable.\n2. Observe worker behavior. | 1. The worker throws fail-fast at step 7 — identical behavior to stale data.\n2. Tick is skipped entirely — no triggers evaluated, no D1 mutation.\n3. `queue_idempotency.state` remains `'started'`.\n4. HL API call count = 0. (Reference: AC-LC-15; SRS FR-EXBOT-093; confirmed code.) | P1 |
| TC_105 | Verify LIMIT 500 steady-state design — jitter hash distribution prevents shard overflow [NEW — AC-LC-18] | 1. A D1 shard has approximately 10,000 active bots with `next_light_check_at` values uniformly distributed across the 5-minute window via the deterministic `hash(botId)` jitter.\n2. This represents steady-state operation (no recent downtime or mass activation). | 1. Observe one full Scan Worker cycle for the shard.\n2. Count eligible bots (those with `next_light_check_at <= now`) at cycle start. | 1. The number of eligible bots at any given scan cycle is at or below the LIMIT (e.g., 500) — the jitter hash distributes bots evenly across the 5-minute window.\n2. No overflow condition is encountered in steady state.\n3. All eligible bots are enqueued in one cycle without deferral. (Reference: AC-LC-18; confirmed code.) | P1 |
| TC_106 | Verify post-downtime catch-up — bots over LIMIT 500 processed oldest-first across next cron ticks, no bot lost [NEW — AC-LC-18] | 1. A shard has many more than 500 bots with `next_light_check_at` overdue — simulating recovery after a downtime period.\n2. Bots have varied `next_light_check_at` values, some significantly overdue. | 1. Run multiple consecutive Scan Worker cycles (1 per minute).\n2. Track which bots are processed and in what order across cycles. | 1. Each Scan Worker cycle processes up to LIMIT bots (oldest `next_light_check_at` first).\n2. All overdue bots are eventually processed — no bot is permanently skipped or lost.\n3. Each processed bot has its `next_light_check_at` updated after the cycle, removing it from the overdue pool.\n4. No pagination logic is required — the catch-up happens naturally across successive Cron ticks. (Reference: AC-LC-18; confirmed code.) | P1 |
